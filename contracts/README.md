# Contracts — Upgrade/migration atomicity & recoverability

This mission is an internal CLI reliability remediation (upgrade recovery, migration
atomicity, frontmatter guard/repair, honest dry-run). It introduces **no external
API contracts** (no HTTP/SDK/wire surface). The behavioral contracts are expressed
as the acceptance tests under `tests/` referenced by each WP.

Placeholder to satisfy the software-dev mission path convention.
