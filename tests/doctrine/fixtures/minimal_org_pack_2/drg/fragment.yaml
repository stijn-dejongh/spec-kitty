# Second minimal organisation-tier DRG fixture (mission
# doctrine-drg-silent-drop-boundary-01M0PE7E, WP05 / T021).
#
# Purpose: the CLASS-A (multi-org-pack fold) proof. A single org pack
# (built-in + one pack) exercises only class-b; to pin that fragments PAST
# THE FIRST are folded (finding F10; merge_three_layers iterates all
# fragments at merge.py:1251) a chain needs a SECOND org pack whose own
# distinctive node/edge is asserted present in the merged graph.
#
# G10 guard (deliberate): this fixture is fragment/refines-only. It carries
# NO governance-profile.yaml and no `selected_*` selection, so WP03's
# org-tier governance-scope guard has nothing to resolve — this fixture
# asserts DELIVERY, not governance fail-loud (that is WP03's territory,
# which is why WP05 depends only on WP04).
#
# Canonical org shape: plural node kinds (`directives`), and a fully
# qualified singular `<kind>:<id>` edge URN for the cross-pack target.
pack_name: minimal-org-2
source_kind: local_path
source_ref: tests/doctrine/fixtures/minimal_org_pack_2
layer_index: 1
provenance_marker: org
nodes:
  - id: MINIMAL_ORG_2_MARKER
    kind: directives
    title: "Minimal Org Pack 2 distinctive marker (class-a multi-fold proof)"
edges:
  # Refines a REAL built-in target so the fragment merges clean (no dangling
  # endpoint). The distinctive source node is what the class-a test asserts
  # reached the merged graph from the SECOND pack in the chain.
  - source: MINIMAL_ORG_2_MARKER
    target: "procedure:red-main-release-discipline"
    relation: refines
    reason: "Marker refines a shipped built-in procedure so pack #2 folds clean"
