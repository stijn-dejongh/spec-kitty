---
packaged: true
audiences: [software_engineer, process_architect, automation_agent]
note: Schema definitions and examples.
---

# Schemas

Purpose: Schema definitions and examples for structured data used in the framework.

What belongs: YAML/JSON schema samples, validation notes.  
What doesn’t: Live data or environment-specific configs (use `validation/` or `work/`).
