# Template Sets

`templates/sets` groups related template bundles that should be installed or
applied together.

Use this directory to define named sets that compose architecture templates,
command templates, and mission-specific scaffolding into coherent packs.
