"""Worktree management utilities for spec-kitty feature development.

This module provides functions for creating and managing workspaces (git worktrees
or jj workspaces) for parallel feature development. Uses the VCS abstraction layer
to support both git and jujutsu backends.

All functions are location-aware and work correctly whether called from main
repository or existing worktree/workspace.
"""

from __future__ import annotations

import platform
import shutil
import subprocess
import warnings
from pathlib import Path
from typing import Optional, Tuple

from .constants import KITTIFY_DIR, KITTY_SPECS_DIR, WORKTREES_DIR
from .vcs import get_vcs
from typing import Annotated
from typing import Callable
from typing import ClassVar

MutantDict = Annotated[dict[str, Callable], "Mutant"] # type: ignore


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None): # type: ignore
    """Forward call to original or mutated function, depending on the environment"""
    import os # type: ignore
    mutant_under_test = os.environ['MUTANT_UNDER_TEST'] # type: ignore
    if mutant_under_test == 'fail': # type: ignore
        from mutmut.__main__ import MutmutProgrammaticFailException # type: ignore
        raise MutmutProgrammaticFailException('Failed programmatically')       # type: ignore
    elif mutant_under_test == 'stats': # type: ignore
        from mutmut.__main__ import record_trampoline_hit # type: ignore
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__) # type: ignore
        # (for class methods, orig is bound and thus does not need the explicit self argument)
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_' # type: ignore
    if not mutant_under_test.startswith(prefix): # type: ignore
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    mutant_name = mutant_under_test.rpartition('.')[-1] # type: ignore
    if self_arg is not None: # type: ignore
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs) # type: ignore
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs) # type: ignore
    return result # type: ignore


def _exclude_from_git(worktree_path: Path, patterns: list[str]) -> None:
    args = [worktree_path, patterns]# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x__exclude_from_git__mutmut_orig, x__exclude_from_git__mutmut_mutants, args, kwargs, None)


def x__exclude_from_git__mutmut_orig(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_1(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = None
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_2(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path * ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_3(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / "XX.gitXX"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_4(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".GIT"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_5(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_6(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = None
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_7(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith(None):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_8(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("XXgitdir:XX"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_9(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("GITDIR:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_10(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = None
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_11(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(None)
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_12(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[8:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_13(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = None
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_14(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" * "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_15(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir * "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_16(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "XXinfoXX" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_17(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "INFO" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_18(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "XXexcludeXX"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_19(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "EXCLUDE"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_20(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = None

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_21(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" * "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_22(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path * "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_23(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "XXinfoXX" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_24(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "INFO" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_25(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "XXexcludeXX"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_26(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "EXCLUDE"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_27(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=None, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_28(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=None)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_29(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_30(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, )

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_31(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=False, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_32(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=False)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_33(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = None
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_34(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = None
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_35(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(None)
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_36(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = None
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_37(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_38(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open(None) as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_39(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("XXaXX") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_40(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("A") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_41(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = None
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_42(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "XX# Added by spec-kitty (worktree symlinks)XX"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_43(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_44(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# ADDED BY SPEC-KITTY (WORKTREE SYMLINKS)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_45(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_46(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(None)
                for pattern in new_patterns:
                    f.write(f"{pattern}\n")
        except OSError:
            # If we can't write, just skip - not critical
            pass


def x__exclude_from_git__mutmut_47(worktree_path: Path, patterns: list[str]) -> None:
    """Add patterns to worktree's .git/info/exclude to prevent committing.

    This prevents symlinks created in worktrees from being committed and
    overwriting real files in main on merge (fixes issue #79).

    Args:
        worktree_path: Path to the worktree root
        patterns: List of patterns to exclude (e.g., [".kittify/memory"])
    """
    # In a worktree, .git is a file pointing to the real git dir
    git_path = worktree_path / ".git"
    if not git_path.exists():
        return

    # Find the actual git directory
    if git_path.is_file():
        # Worktree: .git file contains "gitdir: /path/to/real/.git/worktrees/name"
        try:
            content = git_path.read_text().strip()
            if content.startswith("gitdir:"):
                git_dir = Path(content[7:].strip())
                exclude_file = git_dir / "info" / "exclude"
            else:
                return
        except (OSError, ValueError):
            return
    else:
        # Regular repo or already resolved
        exclude_file = git_path / "info" / "exclude"

    # Ensure info directory exists
    exclude_file.parent.mkdir(parents=True, exist_ok=True)

    # Read existing exclusions
    existing = set()
    if exclude_file.exists():
        try:
            existing = set(exclude_file.read_text().splitlines())
        except OSError:
            pass

    # Add new patterns if not already present
    new_patterns = [p for p in patterns if p not in existing]
    if new_patterns:
        try:
            with exclude_file.open("a") as f:
                # Add comment if this is our first addition
                marker = "# Added by spec-kitty (worktree symlinks)"
                if marker not in existing:
                    f.write(f"\n{marker}\n")
                for pattern in new_patterns:
                    f.write(None)
        except OSError:
            # If we can't write, just skip - not critical
            pass

x__exclude_from_git__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x__exclude_from_git__mutmut_1': x__exclude_from_git__mutmut_1, 
    'x__exclude_from_git__mutmut_2': x__exclude_from_git__mutmut_2, 
    'x__exclude_from_git__mutmut_3': x__exclude_from_git__mutmut_3, 
    'x__exclude_from_git__mutmut_4': x__exclude_from_git__mutmut_4, 
    'x__exclude_from_git__mutmut_5': x__exclude_from_git__mutmut_5, 
    'x__exclude_from_git__mutmut_6': x__exclude_from_git__mutmut_6, 
    'x__exclude_from_git__mutmut_7': x__exclude_from_git__mutmut_7, 
    'x__exclude_from_git__mutmut_8': x__exclude_from_git__mutmut_8, 
    'x__exclude_from_git__mutmut_9': x__exclude_from_git__mutmut_9, 
    'x__exclude_from_git__mutmut_10': x__exclude_from_git__mutmut_10, 
    'x__exclude_from_git__mutmut_11': x__exclude_from_git__mutmut_11, 
    'x__exclude_from_git__mutmut_12': x__exclude_from_git__mutmut_12, 
    'x__exclude_from_git__mutmut_13': x__exclude_from_git__mutmut_13, 
    'x__exclude_from_git__mutmut_14': x__exclude_from_git__mutmut_14, 
    'x__exclude_from_git__mutmut_15': x__exclude_from_git__mutmut_15, 
    'x__exclude_from_git__mutmut_16': x__exclude_from_git__mutmut_16, 
    'x__exclude_from_git__mutmut_17': x__exclude_from_git__mutmut_17, 
    'x__exclude_from_git__mutmut_18': x__exclude_from_git__mutmut_18, 
    'x__exclude_from_git__mutmut_19': x__exclude_from_git__mutmut_19, 
    'x__exclude_from_git__mutmut_20': x__exclude_from_git__mutmut_20, 
    'x__exclude_from_git__mutmut_21': x__exclude_from_git__mutmut_21, 
    'x__exclude_from_git__mutmut_22': x__exclude_from_git__mutmut_22, 
    'x__exclude_from_git__mutmut_23': x__exclude_from_git__mutmut_23, 
    'x__exclude_from_git__mutmut_24': x__exclude_from_git__mutmut_24, 
    'x__exclude_from_git__mutmut_25': x__exclude_from_git__mutmut_25, 
    'x__exclude_from_git__mutmut_26': x__exclude_from_git__mutmut_26, 
    'x__exclude_from_git__mutmut_27': x__exclude_from_git__mutmut_27, 
    'x__exclude_from_git__mutmut_28': x__exclude_from_git__mutmut_28, 
    'x__exclude_from_git__mutmut_29': x__exclude_from_git__mutmut_29, 
    'x__exclude_from_git__mutmut_30': x__exclude_from_git__mutmut_30, 
    'x__exclude_from_git__mutmut_31': x__exclude_from_git__mutmut_31, 
    'x__exclude_from_git__mutmut_32': x__exclude_from_git__mutmut_32, 
    'x__exclude_from_git__mutmut_33': x__exclude_from_git__mutmut_33, 
    'x__exclude_from_git__mutmut_34': x__exclude_from_git__mutmut_34, 
    'x__exclude_from_git__mutmut_35': x__exclude_from_git__mutmut_35, 
    'x__exclude_from_git__mutmut_36': x__exclude_from_git__mutmut_36, 
    'x__exclude_from_git__mutmut_37': x__exclude_from_git__mutmut_37, 
    'x__exclude_from_git__mutmut_38': x__exclude_from_git__mutmut_38, 
    'x__exclude_from_git__mutmut_39': x__exclude_from_git__mutmut_39, 
    'x__exclude_from_git__mutmut_40': x__exclude_from_git__mutmut_40, 
    'x__exclude_from_git__mutmut_41': x__exclude_from_git__mutmut_41, 
    'x__exclude_from_git__mutmut_42': x__exclude_from_git__mutmut_42, 
    'x__exclude_from_git__mutmut_43': x__exclude_from_git__mutmut_43, 
    'x__exclude_from_git__mutmut_44': x__exclude_from_git__mutmut_44, 
    'x__exclude_from_git__mutmut_45': x__exclude_from_git__mutmut_45, 
    'x__exclude_from_git__mutmut_46': x__exclude_from_git__mutmut_46, 
    'x__exclude_from_git__mutmut_47': x__exclude_from_git__mutmut_47
}
x__exclude_from_git__mutmut_orig.__name__ = 'x__exclude_from_git'


def get_next_feature_number(repo_root: Path) -> int:
    args = [repo_root]# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x_get_next_feature_number__mutmut_orig, x_get_next_feature_number__mutmut_mutants, args, kwargs, None)


def x_get_next_feature_number__mutmut_orig(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_1(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = None

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_2(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 1

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_3(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = None
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_4(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root * KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_5(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(None, key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_6(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=None):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_7(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_8(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), ):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_9(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: None):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_10(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 or item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_11(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() or len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_12(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) > 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_13(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 4 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_14(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:4].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_15(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = None
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_16(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(None)
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_17(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:4])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_18(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = None
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_19(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(None, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_20(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, None)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_21(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_22(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, )
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_23(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    break

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_24(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = None
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_25(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root * WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_26(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(None, key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_27(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=None):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_28(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_29(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), ):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_30(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: None):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_31(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 or item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_32(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() or len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_33(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) > 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_34(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 4 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_35(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:4].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_36(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = None
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_37(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(None)
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_38(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:4])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_39(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = None
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_40(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(None, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_41(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, None)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_42(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_43(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, )
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 1


def x_get_next_feature_number__mutmut_44(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    break

    return max_number + 1


def x_get_next_feature_number__mutmut_45(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number - 1


def x_get_next_feature_number__mutmut_46(repo_root: Path) -> int:
    """Determine next sequential feature number.

    Scans both kitty-specs/ and .worktrees/ directories for existing features
    (###-name format) and returns next number in sequence. This prevents number
    reuse when features exist only in worktrees.

    Args:
        repo_root: Repository root path

    Returns:
        Next feature number (e.g., 9 if highest existing is 008)

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> next_num = get_next_feature_number(repo_root)
        >>> assert next_num > 0
    """
    max_number = 0

    # Scan kitty-specs/ for feature numbers
    specs_dir = repo_root / KITTY_SPECS_DIR
    if specs_dir.exists():
        for item in sorted(specs_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    # Also scan .worktrees/ for feature numbers
    worktrees_dir = repo_root / WORKTREES_DIR
    if worktrees_dir.exists():
        for item in sorted(worktrees_dir.iterdir(), key=lambda p: p.name):
            if item.is_dir() and len(item.name) >= 3 and item.name[:3].isdigit():
                try:
                    number = int(item.name[:3])
                    max_number = max(max_number, number)
                except ValueError:
                    # Not a valid number, skip
                    continue

    return max_number + 2

x_get_next_feature_number__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x
    'x_get_next_feature_number__mutmut_42': x_get_next_feature_number__mutmut_42, 
    'x_get_next_feature_number__mutmut_43': x_get_next_feature_number__mutmut_43, 
    'x_get_next_feature_number__mutmut_44': x_get_next_feature_number__mutmut_44, 
    'x_get_next_feature_number__mutmut_45': x_get_next_feature_number__mutmut_45, 
    'x_get_next_feature_number__mutmut_46': x_get_next_feature_number__mutmut_46
}
x_get_next_feature_number__mutmut_orig.__name__ = 'x_get_next_feature_number'


def create_feature_worktree(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    args = [repo_root, feature_slug, feature_number]# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x_create_feature_worktree__mutmut_orig, x_create_feature_worktree__mutmut_mutants, args, kwargs, None)


def x_create_feature_worktree__mutmut_orig(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_1(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is not None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_2(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = None

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_3(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(None)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_4(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = None

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_5(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = None

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_6(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR * branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_7(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root * WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_8(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=None, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_9(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=None)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_10(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_11(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, )

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_12(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=False, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_13(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=False)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_14(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = None
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_15(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = True
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_16(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = None
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_17(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(None)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_18(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = None
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_19(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(None)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_20(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_21(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = None
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_22(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path * ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_23(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / "XX.gitXX"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_24(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".GIT"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_25(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = None

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_26(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = None
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_27(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR * branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_28(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path * KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_29(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(None)

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_30(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = None
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_31(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(None)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_32(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = None

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_33(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=None,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_34(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=None,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_35(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=None,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_36(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_37(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_38(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_39(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_40(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(None)

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_41(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = None
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_42(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "XXGit repository check failed:XX",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_43(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_44(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "GIT REPOSITORY CHECK FAILED:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_45(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "XXGit rejected repository ownership trustXX",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_46(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_47(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "GIT REJECTED REPOSITORY OWNERSHIP TRUST",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_48(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "XXGit worktree discovery failed:XX",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_49(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_50(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "GIT WORKTREE DISCOVERY FAILED:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_51(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(None):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_52(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker not in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_53(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(None) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_54(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            None,
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_55(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            None,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_56(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=None,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_57(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_58(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_59(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_60(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(None).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_61(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "XXfalling back to direct git commands. XX"
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_62(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "FALLING BACK TO DIRECT GIT COMMANDS. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_63(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "XXSee: kitty-specs/015-first-class-jujutsu-vcs-integration/XX",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_64(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "see: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_65(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "SEE: KITTY-SPECS/015-FIRST-CLASS-JUJUTSU-VCS-INTEGRATION/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_66(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=3,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_67(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                None,
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_68(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=None,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_69(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=None,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_70(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=None,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_71(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=None,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_72(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding=None,
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_73(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors=None
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_74(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_75(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_76(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_77(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_78(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_79(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_80(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_81(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["XXgitXX", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_82(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["GIT", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_83(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "XXworktreeXX", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_84(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "WORKTREE", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_85(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "XXaddXX", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_86(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "ADD", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_87(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(None), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_88(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "XX-bXX", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_89(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-B", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_90(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=False,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_91(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=False,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_92(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=False,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_93(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="XXutf-8XX",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_94(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="UTF-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_95(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="XXreplaceXX"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_96(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="REPLACE"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_97(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                None
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_98(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = None
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_99(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR * branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_100(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path * KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_101(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=None, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_102(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=None)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_103(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_104(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, )

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_105(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=False, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_106(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=False)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_107(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(None, worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_108(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, None, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_109(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, None)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_110(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(worktree_path, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_111(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, repo_root)

    return (worktree_path, feature_dir)


def x_create_feature_worktree__mutmut_112(
    repo_root: Path,
    feature_slug: str,
    feature_number: Optional[int] = None
) -> Tuple[Path, Path]:
    """Create workspace (git worktree or jj workspace) for feature development.

    Creates a new workspace with a feature branch and sets up the
    feature directory structure. Uses VCS abstraction to support both
    git and jujutsu backends.

    Args:
        repo_root: Repository root path
        feature_slug: Feature identifier (e.g., "test-feature")
        feature_number: Optional feature number (auto-detected if None)

    Returns:
        Tuple of (worktree_path, feature_dir)

    Raises:
        RuntimeError: If workspace creation fails
        FileExistsError: If worktree path already exists

    Examples:
        >>> repo_root = Path("/path/to/repo")
        >>> worktree, feature_dir = create_feature_worktree(repo_root, "new-feature")
        >>> assert worktree.exists()
        >>> assert feature_dir.exists()
    """
    # Auto-detect feature number if not provided
    if feature_number is None:
        feature_number = get_next_feature_number(repo_root)

    # Format: 001-test-feature
    branch_name = f"{feature_number:03d}-{feature_slug}"

    # Create worktree at .worktrees/001-test-feature
    worktree_path = repo_root / WORKTREES_DIR / branch_name

    # Ensure .worktrees directory exists
    worktree_path.parent.mkdir(parents=True, exist_ok=True)

    # Check if worktree already exists
    if worktree_path.exists():
        # Check if it's a valid workspace using VCS abstraction
        is_valid_workspace = False
        try:
            vcs = get_vcs(worktree_path)
            is_valid_workspace = vcs.is_repo(worktree_path)
        except Exception:
            pass

        # If VCS says no (or failed), fall back to simple git check
        # A valid git worktree has .git as a file (pointing to main repo)
        # or as a directory (standalone repo)
        if not is_valid_workspace:
            git_marker = worktree_path / ".git"
            is_valid_workspace = git_marker.exists()

        if is_valid_workspace:
            feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
            return (worktree_path, feature_dir)

        raise FileExistsError(f"Worktree path already exists: {worktree_path}")

    # Get VCS implementation and create workspace
    # NOTE: We do NOT use sparse_exclude for kitty-specs/ here because:
    # - Users need to add research artifacts, patterns, etc. to kitty-specs/
    # - The locate_work_package() function always uses main repo's kitty-specs/
    #   for WP operations, so stale copies in worktrees don't affect lane changes
    try:
        vcs = get_vcs(repo_root)
        result = vcs.create_workspace(
            workspace_path=worktree_path,
            workspace_name=branch_name,
            repo_root=repo_root,
        )

        if not result.success:
            raise RuntimeError(f"Failed to create workspace: {result.error}")

    except Exception as e:
        deterministic_preflight_markers = (
            "Git repository check failed:",
            "Git rejected repository ownership trust",
            "Git worktree discovery failed:",
        )
        if any(marker in str(e) for marker in deterministic_preflight_markers):
            raise

        # If VCS abstraction fails, fall back to direct git command with warning
        warnings.warn(
            f"VCS abstraction failed ({type(e).__name__}: {e}); "
            "falling back to direct git commands. "
            "See: kitty-specs/015-first-class-jujutsu-vcs-integration/",
            DeprecationWarning,
            stacklevel=2,
        )
        try:
            subprocess.run(
                ["git", "worktree", "add", str(worktree_path), "-b", branch_name],
                cwd=repo_root,
                check=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace"
            )
        except subprocess.CalledProcessError as git_error:
            raise RuntimeError(
                f"Failed to create workspace: {git_error.stderr}"
            ) from git_error

    # Create feature directory structure
    feature_dir = worktree_path / KITTY_SPECS_DIR / branch_name
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Setup feature directory (symlinks, subdirectories, etc.)
    setup_feature_directory(feature_dir, worktree_path, )

    return (worktree_path, feature_dir)

x_create_feature_worktree__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x_create_feature_worktree__mutmut_1': x_create_feature_worktree__mutmut_1, 
    'x_create_feature_worktree__mutmut_2': x_create_feature_worktree__mutmut_2, 
    'x_create_feature_worktree__mutmut_3': x_create_feature_worktree__mutmut_3, 
    'x_create_feature_worktree__mutmut_4': x_create_feature_worktree__mutmut_4, 
    'x_create_feature_worktree__mutmut_5': x_create_feature_worktree__mutmut_5, 
    'x_create_feature_worktree__mutmut_6': x_create_feature_worktree__mutmut_6, 
    'x_create_feature_worktree__mutmut_7': x_create_feature_worktree__mutmut_7, 
    'x_create_feature_worktree__mutmut_8': x_create_feature_worktree__mutmut_8, 
    'x_create_feature_worktree__mutmut_9': x_create_feature_worktree__mutmut_9, 
    'x_create_feature_worktree__mutmut_10': x_create_feature_worktree__mutmut_10, 
    'x_create_feature_worktree__mutmut_11': x_create_feature_worktree__mutmut_11, 
    'x_create_feature_worktree__mutmut_12': x_create_feature_worktree__mutmut_12, 
    'x_create_feature_worktree__mutmut_13': x_create_feature_worktree__mutmut_13, 
    'x_create_feature_worktree__mutmut_14': x_create_feature_worktree__mutmut_14, 
    'x_create_feature_worktree__mutmut_15': x_create_feature_worktree__mutmut_15, 
    'x_create_feature_worktree__mutmut_16': x_create_feature_worktree__mutmut_16, 
    'x_create_feature_worktree__mutmut_17': x_create_feature_worktree__mutmut_17, 
    'x_create_feature_worktree__mutmut_18': x_create_feature_worktree__mutmut_18, 
    'x_create_feature_worktree__mutmut_19': x_create_feature_worktree__mutmut_19, 
    'x_create_feature_worktree__mutmut_20': x_create_feature_worktree__mutmut_20, 
    'x_create_feature_worktree__mutmut_21': x_create_feature_worktree__mutmut_21, 
    'x_create_feature_worktree__mutmut_22': x_create_feature_worktree__mutmut_22, 
    'x_create_feature_worktree__mutmut_23': x_create_feature_worktree__mutmut_23, 
    'x_create_feature_worktree__mutmut_24': x_create_feature_worktree__mutmut_24, 
    'x_create_feature_worktree__mutmut_25': x_create_feature_worktree__mutmut_25, 
    'x_create_feature_worktree__mutmut_26': x_create_feature_worktree__mutmut_26, 
    'x_create_feature_worktree__mutmut_27': x_create_feature_worktree__mutmut_27, 
    'x_create_feature_worktree__mutmut_28': x_create_feature_worktree__mutmut_28, 
    'x_create_feature_worktree__mutmut_29': x_create_feature_worktree__mutmut_29, 
    'x_create_feature_worktree__mutmut_30': x_create_feature_worktree__mutmut_30, 
    'x_create_feature_worktree__mutmut_31': x_create_feature_worktree__mutmut_31, 
    'x_create_feature_worktree__mutmut_32': x_create_feature_worktree__mutmut_32, 
    'x_create_feature_worktree__mutmut_33': x_create_feature_worktree__mutmut_33, 
    'x_create_feature_worktree__mutmut_34': x_create_feature_worktree__mutmut_34, 
    'x_create_feature_worktree__mutmut_35': x_create_feature_worktree__mutmut_35, 
    'x_create_feature_worktree__mutmut_36': x_create_feature_worktree__mutmut_36, 
    'x_create_feature_worktree__mutmut_37': x_create_feature_worktree__mutmut_37, 

    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    args = [feature_dir, worktree_path, repo_root, create_symlinks]# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x_setup_feature_directory__mutmut_orig, x_setup_feature_directory__mutmut_mutants, args, kwargs, None)


def x_setup_feature_directory__mutmut_orig(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_1(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = False
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_2(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=None, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_3(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=None)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_4(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_5(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, )

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_6(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=False, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_7(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=False)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_8(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=None)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_9(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir * "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_10(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "XXchecklistsXX").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_11(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "CHECKLISTS").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_12(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=False)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_13(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=None)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_14(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir * "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_15(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "XXresearchXX").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_16(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "RESEARCH").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_17(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=False)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_18(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = None
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_19(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir * "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_20(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "XXtasksXX"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_21(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "TASKS"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_22(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=None)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_23(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=False)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_24(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir * ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_25(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / "XX.gitkeepXX").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_26(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".GITKEEP").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_27(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = None
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_28(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(None, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_29(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding=None)

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_30(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_31(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, )

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_32(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir * "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_33(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "XXREADME.mdXX").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_34(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "readme.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_35(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.MD").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_36(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='XXutf-8XX')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_37(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='UTF-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_38(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = None
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_39(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path * KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_40(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=None)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_41(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=False)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_42(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = None
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_43(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path(None)
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_44(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("XX../../../.kittify/memoryXX")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_45(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.KITTIFY/MEMORY")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_46(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = None

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_47(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path(None)

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_48(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("XX../../../.kittify/AGENTS.mdXX")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_49(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/agents.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_50(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.KITTIFY/AGENTS.MD")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_51(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = None
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_52(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify * "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_53(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "XXmemoryXX"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_54(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "MEMORY"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_55(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = None

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_56(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify * "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_57(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "XXAGENTS.mdXX"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_58(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "agents.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_59(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.MD"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_60(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = None
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_61(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() != "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_62(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "XXWindowsXX"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_63(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_64(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "WINDOWS"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_65(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = None

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_66(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows and not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_67(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_68(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() or worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_69(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(None)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_70(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = None
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_71(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR * "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_72(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root * KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_73(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "XXmemoryXX"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_74(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "MEMORY"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_75(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() or main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_76(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(None, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_77(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, None)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_78(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_79(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, )
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_80(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(None, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_81(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=None)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_82(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_83(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, )
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_84(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=False)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_85(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = None
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_86(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR * "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_87(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root * KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_88(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "XXmemoryXX"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_89(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "MEMORY"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_90(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() or main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_91(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(None, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_92(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, None)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_93(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_94(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, )

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_95(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = None
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_96(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR * "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_97(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root * KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_98(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "XXAGENTS.mdXX"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_99(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "agents.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_100(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.MD"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_101(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(None, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_102(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, None)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_103(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_104(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, )
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_105(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(None)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_106(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(None, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_107(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, None)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_108(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_109(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, )

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_110(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(None, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_111(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, None)

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_112(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git([".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_113(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, )

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_114(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, ["XX.kittify/memoryXX", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_115(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".KITTIFY/MEMORY", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_116(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", "XX.kittify/AGENTS.mdXX"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_117(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/agents.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_118(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".KITTIFY/AGENTS.MD"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_119(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = None
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_120(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir * "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_121(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "XXspec.mdXX"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_122(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "SPEC.MD"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_123(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_124(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = None

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_125(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" * "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_126(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR * "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_127(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root * KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_128(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "XXtemplatesXX" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_129(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "TEMPLATES" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_130(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "XXspec-template.mdXX",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_131(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "SPEC-TEMPLATE.MD",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_132(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" * "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_133(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root * "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_134(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "XXtemplatesXX" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_135(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "TEMPLATES" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_136(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "XXspec-template.mdXX",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_137(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "SPEC-TEMPLATE.MD",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_138(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(None, spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_139(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, None)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_140(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(spec_file)
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_141(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, )
                break
        else:
            # No template found, create empty spec.md
            spec_file.touch()


def x_setup_feature_directory__mutmut_142(
    feature_dir: Path,
    worktree_path: Path,
    repo_root: Path,
    create_symlinks: bool = True
) -> None:
    """Setup standard feature directory structure.

    Creates:
    - kitty-specs/###-name/ directory
    - Subdirectories: checklists/, research/, tasks/
    - Symlinks to .kittify/memory/ (or file copies on Windows)
    - spec.md from template
    - tasks/README.md

    Args:
        feature_dir: Feature directory path
        worktree_path: Worktree root path
        repo_root: Main repository root path
        create_symlinks: If True, create symlinks; else copy files (Windows)

    Examples:
        >>> feature_dir = Path("/path/to/.worktrees/001-feature/kitty-specs/001-feature")
        >>> setup_feature_directory(feature_dir, feature_dir.parent.parent, repo_root)
        >>> assert (feature_dir / "checklists").exists()
    """
    # Ensure feature directory exists
    feature_dir.mkdir(parents=True, exist_ok=True)

    # Create subdirectories
    (feature_dir / "checklists").mkdir(exist_ok=True)
    (feature_dir / "research").mkdir(exist_ok=True)
    tasks_dir = feature_dir / "tasks"
    tasks_dir.mkdir(exist_ok=True)

    # Create tasks/.gitkeep and README.md
    (tasks_dir / ".gitkeep").touch()

    # Create tasks/README.md with frontmatter format reference
    tasks_readme_content = '''# Tasks Directory

This directory contains work package (WP) prompt files with lane status in frontmatter.

## Directory Structure (v0.9.0+)

```
tasks/
├── WP01-setup-infrastructure.md
├── WP02-user-authentication.md
├── WP03-api-endpoints.md
└── README.md
```

All WP files are stored flat in `tasks/`. The lane (planned, doing, for_review, done) is stored in the YAML frontmatter `lane:` field.

## Work Package File Format

Each WP file **MUST** use YAML frontmatter:

```yaml
---
work_package_id: "WP01"
title: "Work Package Title"
lane: "planned"
subtasks:
  - "T001"
  - "T002"
phase: "Phase 1 - Setup"
assignee: ""
agent: ""
shell_pid: ""
review_status: ""
reviewed_by: ""
review_feedback: ""
history:
  - timestamp: "2025-01-01T00:00:00Z"
    lane: "planned"
    agent: "system"
    action: "Prompt generated via /spec-kitty.tasks"
---

# Work Package Prompt: WP01 – Work Package Title

[Content follows...]
```

## Valid Lane Values

- `planned` - Ready for implementation
- `doing` - Currently being worked on
- `for_review` - Awaiting review
- `done` - Completed

## Moving Between Lanes

Use the CLI (updates frontmatter only, no file movement):
```bash
spec-kitty agent tasks move-task <WPID> --to <lane>
```

Example:
```bash
spec-kitty agent tasks move-task WP01 --to doing
```

## File Naming

- Format: `WP01-kebab-case-slug.md`
- Examples: `WP01-setup-infrastructure.md`, `WP02-user-auth.md`
'''
    (tasks_dir / "README.md").write_text(tasks_readme_content, encoding='utf-8')

    # Create worktree .kittify directory if it doesn't exist
    worktree_kittify = worktree_path / KITTIFY_DIR
    worktree_kittify.mkdir(exist_ok=True)

    # Setup shared constitution and AGENTS.md via symlink (or copy on Windows)
    # Calculate relative path from worktree to main repo
    # Worktree: .worktrees/001-feature/.kittify/memory
    # Main:     .kittify/memory
    # Relative: ../../../.kittify/memory
    relative_memory_path = Path("../../../.kittify/memory")
    relative_agents_path = Path("../../../.kittify/AGENTS.md")

    worktree_memory = worktree_kittify / "memory"
    worktree_agents = worktree_kittify / "AGENTS.md"

    # Detect if we're on Windows or symlinks are not supported
    is_windows = platform.system() == "Windows"
    use_copy = is_windows or not create_symlinks

    # Setup memory/ symlink or copy
    if worktree_memory.is_symlink():
        # Remove existing symlink first (can't use rmtree on symlinks)
        worktree_memory.unlink()
    elif worktree_memory.exists() and worktree_memory.is_dir():
        # Remove existing directory (from git worktree add)
        shutil.rmtree(worktree_memory)

    if use_copy:
        # Copy memory directory
        main_memory = repo_root / KITTIFY_DIR / "memory"
        if main_memory.exists() and main_memory.is_dir():
            shutil.copytree(main_memory, worktree_memory)
    else:
        # Create relative symlink
        try:
            worktree_memory.symlink_to(relative_memory_path, target_is_directory=True)
        except (OSError, NotImplementedError):
            # Symlink failed, fall back to copy
            main_memory = repo_root / KITTIFY_DIR / "memory"
            if main_memory.exists() and main_memory.is_dir():
                shutil.copytree(main_memory, worktree_memory)

    # Setup AGENTS.md symlink or copy
    if worktree_agents.exists():
        worktree_agents.unlink()

    main_agents = repo_root / KITTIFY_DIR / "AGENTS.md"
    if main_agents.exists():
        if use_copy:
            shutil.copy2(main_agents, worktree_agents)
        else:
            try:
                worktree_agents.symlink_to(relative_agents_path)
            except (OSError, NotImplementedError):
                shutil.copy2(main_agents, worktree_agents)

    # Exclude symlinks from git to prevent them from being committed
    # This fixes issue #79: symlinks overwriting main repo files on merge
    _exclude_from_git(worktree_path, [".kittify/memory", ".kittify/AGENTS.md"])

    # Copy spec template if it exists
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        # Try to find spec template
        spec_template_candidates = [
            repo_root / KITTIFY_DIR / "templates" / "spec-template.md",
            repo_root / "templates" / "spec-template.md",
        ]

        for template in spec_template_candidates:
            if template.exists():
                shutil.copy2(template, spec_file)
                return
        else:
            # No template found, create empty spec.md
            spec_file.touch()

x_setup_feature_directory__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x_setup_feature_directory__mutmut_1': x_setup_feature_directory__mutmut_1, 
    'x_setup_feature_directory__mutmut_2': x_setup_feature_directory__mutmut_2, 
    'x_setup_feature_directory__mutmut_3': x_setup_feature_directory__mutmut_3, 
    'x_setup_feature_directory__mutmut_4': x_setup_feature_directory__mutmut_4, 
    'x_setup_feature_directory__mutmut_5': x_setup_feature_directory__mutmut_5, 
    'x_setup_feature_directory__mutmut_6': x_setup_feature_directory__mutmut_6, 
    'x_setup_feature_directory__mutmut_7': x_setup_feature_directory__mutmut_7, 
    'x_setup_feature_directory__mutmut_8': x_setup_feature_directory__mutmut_8, 
    'x_setup_feature_directory__mutmut_9': x_setup_feature_directory__mutmut_9, 
    'x_setup_feature_directory__mutmut_10': x_setup_feature_directory__mutmut_10, 
    'x_setup_feature_directory__mutmut_11': x_setup_feature_directory__mutmut_11, 
    'x_setup_feature_directory__mutmut_12': x_setup_feature_directory__mutmut_12, 
    'x_setup_feature_directory__mutmut_13': x_setup_feature_directory__mutmut_13, 
    'x_setup_feature_directory__mutmut_14': x_setup_feature_directory__mutmut_14, 
    'x_setup_feature_directory__mutmut_15': x_setup_feature_directory__mutmut_15, 
    'x_setup_feature_directory__mutmut_16': x_setup_feature_directory__mutmut_16, 
    'x_setup_feature_directory__mutmut_17': x_setup_feature_directory__mutmut_17, 
    'x_setup_feature_directory__mutmut_18': x_setup_feature_directory__mutmut_18, 
    'x_setup_feature_directory__mutmut_19': x_setup_feature_directory__mutmut_19, 
    'x_setup_feature_directory__mutmut_20': x_setup_feature_directory__mutmut_20, 
    'x_setup_feature_directory__mutmut_21': x_setup_feature_directory__mutmut_21, 
    'x_setup_feature_directory__mutmut_22': x_setup_feature_directory__mutmut_22, 
    'x_setup_feature_directory__mutmut_23': x_setup_feature_directory__mutmut_23, 
    'x_setup_feature_directory__mutmut_24': x_setup_feature_directory__mutmut_24, 
    'x_setup_feature_directory__mutmut_25': x_setup_feature_directory__mutmut_25, 
    'x_setup_feature_directory__mutmut_26': x_setup_feature_directory__mutmut_26, 
    'x_setup_feature_directory__mutmut_27': x_setup_feature_directory__mutmut_27, 
    'x_setup_feature_directory__mutmut_28': x_setup_feature_directory__mutmut_28, 
    'x_setup_feature_directory__mutmut_29': x_setup_feature_directory__mutmut_29, 
    'x_setup_feature_directory__mutmut_30': x_setup_feature_directory__mutmut_30, 
    'x_setup_feature_directory__mutmut_31': x_setup_feature_directory__mutmut_31, 
    'x_setup_feature_directory__mutmut_32': x_setup_feature_directory__mutmut_32, 
    'x_setup_feature_directory__mutmut_33': x_setup_feature_directory__mutmut_33, 
    'x_setup_feature_directory__mutmut_34': x_setup_feature_directory__mutmut_34, 
    'x_setup_feature_directory__mutmut_35': x_setup_feature_directory__mutmut_35, 
    'x_setup_feature_directory__mutmut_36': x_setup_feature_directory__mutmut_36, 
    'x_setup_feature_directory__mutmut_37': x_setup_feature_directory__mutmut_37, 
    'x_setup_feature_directory__mutmut_38': x_setup_feature_directory__mutmut_38, 
    'x_setup_feature_directory__mutmut_39': x_setup_feature_directory__mutmut_39, 
    'x_setup_feature_directory__mutmut_40': x_setup_feature_directory__mutmut_40, 
    'x_setup_feature_directory__mutmut_41': x_setup_feature_directory__mutmut_41, 
    'x_setup_feature_directory__mutmut_42': x_setup_feature_directory__mutmut_42, 
    'x_setup_feature_directory__mutmut_43': x_setup_feature_directory__mutmut_43, 
    'x_setup_feature_directory__mutmut_44': x_setup_feature_directory__mutmut_44, 
    'x_setup_feature_directory__mutmut_45': x_setup_feature_directory__mutmut_45, 
    'x_setup_feature_directory__mutmut_46': x_setup_feature_directory__mutmut_46, 
    'x_setup_feature_directory__mutmut_47': x_setup_feature_directory__mutmut_47, 
    'x_setup_feature_directory__mutmut_48': x_setup_feature_directory__mutmut_48, 
    'x_setup_feature_directory__mutmut_49': x_setup_feature_directory__mutmut_49, 
    'x_setup_feature_directory__mutmut_50': x_setup_feature_directory__mutmut_50, 
    'x_setup_feature_directory__mutmut_51': x_setup_feature_directory__mutmut_51, 
    'x_setup_feature_directory__mutmut_52': x_setup_feature_directory__mutmut_52, 
    'x_setup_feature_directory__mutmut_129': x_setup_feature_directory__mutmut_129, 
    'x_setup_feature_directory__mutmut_130': x_setup_feature_directory__mutmut_130, 
    'x_setup_feature_directory__mutmut_131': x_setup_feature_directory__mutmut_131, 
    'x_setup_feature_directory__mutmut_132': x_setup_feature_directory__mutmut_132, 
    'x_setup_feature_directory__mutmut_133': x_setup_feature_directory__mutmut_133, 
    'x_setup_feature_directory__mutmut_134': x_setup_feature_directory__mutmut_134, 
    'x_setup_feature_directory__mutmut_135': x_setup_feature_directory__mutmut_135, 
    'x_setup_feature_directory__mutmut_136': x_setup_feature_directory__mutmut_136, 
    'x_setup_feature_directory__mutmut_137': x_setup_feature_directory__mutmut_137, 
    'x_setup_feature_directory__mutmut_138': x_setup_feature_directory__mutmut_138, 
    'x_setup_feature_directory__mutmut_139': x_setup_feature_directory__mutmut_139, 
    'x_setup_feature_directory__mutmut_140': x_setup_feature_directory__mutmut_140, 
    'x_setup_feature_directory__mutmut_141': x_setup_feature_directory__mutmut_141, 
    'x_setup_feature_directory__mutmut_142': x_setup_feature_directory__mutmut_142
}
x_setup_feature_directory__mutmut_orig.__name__ = 'x_setup_feature_directory'


def validate_feature_structure(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    args = [feature_dir, check_tasks]# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x_validate_feature_structure__mutmut_orig, x_validate_feature_structure__mutmut_mutants, args, kwargs, None)


def x_validate_feature_structure__mutmut_orig(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_1(
    feature_dir: Path,
    check_tasks: bool = True
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_2(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = None
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_3(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = None
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_4(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = None
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_5(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = None
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_6(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = None
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_7(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = None

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_8(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_9(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(None)
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_10(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "XXvalidXX": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_11(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "VALID": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_12(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": True,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_13(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "XXerrorsXX": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_14(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "ERRORS": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_15(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "XXwarningsXX": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_16(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "WARNINGS": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_17(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "XXpathsXX": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_18(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "PATHS": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_19(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "XXartifact_filesXX": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_20(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "ARTIFACT_FILES": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_21(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "XXartifact_dirsXX": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_22(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "ARTIFACT_DIRS": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_23(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "XXavailable_docsXX": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_24(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "AVAILABLE_DOCS": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_25(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "XXFEATURE_DIRXX": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_26(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "feature_dir": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_27(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "XXXX",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_28(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "XXAVAILABLE_DOCSXX": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_29(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "available_docs": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_30(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = None
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_31(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir * "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_32(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "XXspec.mdXX"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_33(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "SPEC.MD"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_34(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_35(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append(None)
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_36(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("XXMissing required file: spec.mdXX")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_37(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_38(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("MISSING REQUIRED FILE: SPEC.MD")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_39(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = None
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_40(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(None)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_41(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = None
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_42(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["XXspec_fileXX"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_43(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["SPEC_FILE"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_44(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = None
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_45(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["XXspec_fileXX"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_46(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["SPEC_FILE"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_47(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append(None)

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_48(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("XXspec.mdXX")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_49(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("SPEC.MD")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_50(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = None
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_51(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir * "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_52(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "XXplan.mdXX"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_53(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "PLAN.MD"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_54(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = None
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_55(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(None)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_56(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = None
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_57(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["XXplan_fileXX"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_58(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["PLAN_FILE"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_59(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = None
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_60(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["XXplan_fileXX"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_61(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["PLAN_FILE"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_62(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append(None)

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_63(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("XXplan.mdXX")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_64(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("PLAN.MD")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_65(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = None
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_66(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["XXchecklistsXX", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_67(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["CHECKLISTS", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_68(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "XXresearchXX", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_69(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "RESEARCH", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_70(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "XXtasksXX"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_71(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "TASKS"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_72(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = None
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_73(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir * dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_74(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_75(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(None)
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_76(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = None
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_77(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(None)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_78(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = None
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_79(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = None

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_80(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = None
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_81(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir * "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_82(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "XXtasks.mdXX"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_83(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "TASKS.MD"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_84(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_85(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append(None)
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_86(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("XXMissing required file: tasks.mdXX")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_87(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_88(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("MISSING REQUIRED FILE: TASKS.MD")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_89(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = None
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_90(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(None)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_91(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = None
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_92(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["XXtasks_fileXX"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_93(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["TASKS_FILE"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_94(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = None
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_95(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["XXtasks_fileXX"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_96(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["TASKS_FILE"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_97(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "XXtasks.mdXX" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_98(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "TASKS.MD" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_99(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_100(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append(None)
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_101(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("XXtasks.mdXX")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_102(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("TASKS.MD")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_103(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = None
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_104(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir * "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_105(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "XXtasks.mdXX"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_106(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "TASKS.MD"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_107(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = None
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_108(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(None)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_109(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = None
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_110(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["XXtasks_fileXX"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_111(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["TASKS_FILE"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_112(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = None
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_113(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["XXtasks_fileXX"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_114(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["TASKS_FILE"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_115(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append(None)

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_116(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("XXtasks.mdXX")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_117(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("TASKS.MD")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_118(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = None
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_119(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(None)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_120(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = None
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_121(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["XXfeature_dirXX"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_122(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["FEATURE_DIR"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_123(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = None

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_124(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["XXfeature_dirXX"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_125(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["FEATURE_DIR"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_126(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = None
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_127(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir * "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_128(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "XXchecklistsXX"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_129(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "CHECKLISTS"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_130(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = None
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_131(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(None)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_132(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault(None, checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_133(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", None)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_134(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault(checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_135(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", )

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_136(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("XXchecklists_dirXX", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_137(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("CHECKLISTS_DIR", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_138(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = None
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_139(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir * "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_140(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "XXresearchXX"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_141(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "RESEARCH"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_142(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = None
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_143(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(None)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_144(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault(None, research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_145(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", None)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_146(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault(research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_147(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", )

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_148(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("XXresearch_dirXX", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_149(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("RESEARCH_DIR", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_150(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = None
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_151(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir * "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_152(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "XXtasksXX"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_153(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "TASKS"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_154(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = None
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_155(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(None)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_156(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault(None, tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_157(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", None)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_158(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault(tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_159(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", )

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_160(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("XXtasks_dirXX", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_161(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("TASKS_DIR", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_162(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = None

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_163(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(None)

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_164(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(None))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_165(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "XXvalidXX": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_166(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "VALID": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_167(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) != 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_168(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 1,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_169(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "XXerrorsXX": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_170(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "ERRORS": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_171(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "XXwarningsXX": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_172(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "WARNINGS": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_173(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "XXpathsXX": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_174(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "PATHS": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_175(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "XXartifact_filesXX": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_176(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "ARTIFACT_FILES": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_177(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "XXartifact_dirsXX": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_178(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "ARTIFACT_DIRS": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_179(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "XXavailable_docsXX": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_180(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "AVAILABLE_DOCS": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_181(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "XXFEATURE_DIRXX": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_182(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "feature_dir": feature_dir_str,
        "AVAILABLE_DOCS": available_docs,
    }


def x_validate_feature_structure__mutmut_183(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        "paths": paths,
        "artifact_files": artifact_files,
        "artifact_dirs": artifact_dirs,
        "available_docs": available_docs,
        # Compatibility aliases for older templates/prompts
        "FEATURE_DIR": feature_dir_str,
        "XXAVAILABLE_DOCSXX": available_docs,
    }


def x_validate_feature_structure__mutmut_184(
    feature_dir: Path,
    check_tasks: bool = False
) -> dict:
    """Validate feature directory structure and required files.

    Checks for:
    - Required files: spec.md
    - Recommended directories: checklists/, research/, tasks/
    - Optional: tasks.md (if check_tasks=True)

    Args:
        feature_dir: Feature directory path
        check_tasks: If True, validate tasks.md and task files exist

    Returns:
        Dictionary with validation results:
        {
            "valid": bool,
            "errors": [list of error messages],
            "warnings": [list of warning messages],
            "paths": {dict of important paths}
        }

    Examples:
        >>> feature_dir = Path("/path/to/kitty-specs/001-feature")
        >>> result = validate_feature_structure(feature_dir)
        >>> assert "valid" in result
        >>> assert "errors" in result
    """
    errors = []
    warnings = []
    paths: dict[str, str] = {}
    artifact_files: dict[str, str] = {}
    artifact_dirs: dict[str, str] = {}
    available_docs: list[str] = []

    # Check if feature directory exists
    if not feature_dir.exists():
        errors.append(f"Feature directory not found: {feature_dir}")
        return {
            "valid": False,
            "errors": errors,
            "warnings": warnings,
            "paths": paths,
            "artifact_files": artifact_files,
            "artifact_dirs": artifact_dirs,
            "available_docs": available_docs,
            "FEATURE_DIR": "",
            "AVAILABLE_DOCS": available_docs,
        }

    # Check required files exist
    spec_file = feature_dir / "spec.md"
    if not spec_file.exists():
        errors.append("Missing required file: spec.md")
    else:
        spec_file_str = str(spec_file)
        paths["spec_file"] = spec_file_str
        artifact_files["spec_file"] = spec_file_str
        available_docs.append("spec.md")

    plan_file = feature_dir / "plan.md"
    if plan_file.exists():
        plan_file_str = str(plan_file)
        paths["plan_file"] = plan_file_str
        artifact_files["plan_file"] = plan_file_str
        available_docs.append("plan.md")

    # Check directory structure
    recommended_dirs = ["checklists", "research", "tasks"]
    for dir_name in recommended_dirs:
        dir_path = feature_dir / dir_name
        if not dir_path.exists():
            warnings.append(f"Missing recommended directory: {dir_name}/")
        else:
            dir_path_str = str(dir_path)
            paths[f"{dir_name}_dir"] = dir_path_str
            artifact_dirs[f"{dir_name}_dir"] = dir_path_str

    # Check task files if requested
    if check_tasks:
        tasks_file = feature_dir / "tasks.md"
        if not tasks_file.exists():
            errors.append("Missing required file: tasks.md")
        else:
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            if "tasks.md" not in available_docs:
                available_docs.append("tasks.md")
    else:
        tasks_file = feature_dir / "tasks.md"
        if tasks_file.exists():
            tasks_file_str = str(tasks_file)
            paths["tasks_file"] = tasks_file_str
            artifact_files["tasks_file"] = tasks_file_str
            available_docs.append("tasks.md")

    # Always include feature_dir in paths
    feature_dir_str = str(feature_dir)
    paths["feature_dir"] = feature_dir_str
    artifact_dirs["feature_dir"] = feature_dir_str

    checklists_dir = feature_dir / "checklists"
    if checklists_dir.exists():
        checklists_dir_str = str(checklists_dir)
        artifact_dirs.setdefault("checklists_dir", checklists_dir_str)

    research_dir = feature_dir / "research"
    if research_dir.exists():
        research_dir_str = str(research_dir)
        artifact_dirs.setdefault("research_dir", research_dir_str)

    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        tasks_dir_str = str(tasks_dir)
        artifact_dirs.setdefault("tasks_dir", tasks_dir_str)

    available_docs = sorted(set(available_docs))

    return {
        "valid": len(errors) == 0,
        "errors": errors,
        "warnings": warnings,
        