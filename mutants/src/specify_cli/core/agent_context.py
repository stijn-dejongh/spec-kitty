"""Agent context file management for updating CLAUDE.md, GEMINI.md, etc."""

from datetime import datetime
from pathlib import Path
from typing import Dict, List, Optional
import re


# Agent types and their file paths
AGENT_CONFIGS = {
    "claude": "CLAUDE.md",
    "gemini": "GEMINI.md",
    "copilot": ".github/copilot-instructions.md",
    "cursor": ".cursor/rules/specify-rules.mdc",
    "qwen": "QWEN.md",
    "opencode": "AGENTS.md",
    "codex": "AGENTS.md",
    "windsurf": ".windsurf/rules/specify-rules.md",
    "kilocode": ".kilocode/rules/specify-rules.md",
    "auggie": ".augment/rules/specify-rules.md",
    "roo": ".roo/rules/specify-rules.md",
    "q": "AGENTS.md",
}
from typing import Annotated
from typing import Callable
from typing import ClassVar

MutantDict = Annotated[dict[str, Callable], "Mutant"] # type: ignore


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None): # type: ignore
    """Forward call to original or mutated function, depending on the environment"""
    import os # type: ignore
    mutant_under_test = os.environ['MUTANT_UNDER_TEST'] # type: ignore
    if mutant_under_test == 'fail': # type: ignore
        from mutmut.__main__ import MutmutProgrammaticFailException # type: ignore
        raise MutmutProgrammaticFailException('Failed programmatically')       # type: ignore
    elif mutant_under_test == 'stats': # type: ignore
        from mutmut.__main__ import record_trampoline_hit # type: ignore
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__) # type: ignore
        # (for class methods, orig is bound and thus does not need the explicit self argument)
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_' # type: ignore
    if not mutant_under_test.startswith(prefix): # type: ignore
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    mutant_name = mutant_under_test.rpartition('.')[-1] # type: ignore
    if self_arg is not None: # type: ignore
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs) # type: ignore
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs) # type: ignore
    return result # type: ignore


def parse_plan_for_tech_stack(plan_path: Path) -> Dict[str, Optional[str]]:
    args = [plan_path]# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x_parse_plan_for_tech_stack__mutmut_orig, x_parse_plan_for_tech_stack__mutmut_mutants, args, kwargs, None)


def x_parse_plan_for_tech_stack__mutmut_orig(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_1(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_2(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(None)

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_3(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = None

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_4(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = None
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_5(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(None, content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_6(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", None, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_7(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, None)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_8(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_9(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_10(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, )
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_11(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = None
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_12(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(None).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_13(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(2).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_14(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" or value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_15(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value or value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_16(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value == "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_17(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "XXNEEDS CLARIFICATIONXX" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_18(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "needs clarification" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_19(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value == "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_20(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "XXN/AXX":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_21(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "n/a":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_22(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "XXlanguageXX": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_23(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "LANGUAGE": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_24(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field(None),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_25(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("XXLanguage/VersionXX"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_26(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("language/version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_27(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("LANGUAGE/VERSION"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_28(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "XXdependenciesXX": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_29(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "DEPENDENCIES": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_30(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field(None),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_31(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("XXPrimary DependenciesXX"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_32(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("primary dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_33(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("PRIMARY DEPENDENCIES"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_34(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "XXstorageXX": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_35(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "STORAGE": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_36(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field(None),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_37(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("XXStorageXX"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_38(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_39(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("STORAGE"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_40(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "XXtestingXX": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_41(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "TESTING": extract_field("Testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_42(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field(None),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_43(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("XXTestingXX"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_44(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("testing"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_45(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("TESTING"),
        "project_type": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_46(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "XXproject_typeXX": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_47(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "PROJECT_TYPE": extract_field("Project Type"),
    }


def x_parse_plan_for_tech_stack__mutmut_48(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field(None),
    }


def x_parse_plan_for_tech_stack__mutmut_49(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("XXProject TypeXX"),
    }


def x_parse_plan_for_tech_stack__mutmut_50(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("project type"),
    }


def x_parse_plan_for_tech_stack__mutmut_51(plan_path: Path) -> Dict[str, Optional[str]]:
    """
    Extract tech stack information from plan.md Technical Context section.

    Args:
        plan_path: Path to plan.md file

    Returns:
        Dictionary with language, dependencies, storage, testing, project_type keys

    Example return:
        {
            "language": "Python 3.11+",
            "dependencies": "Typer, Rich, pathlib, subprocess",
            "storage": "Filesystem only (no database)",
            "testing": "pytest with unit + integration tests",
            "project_type": "Single Python package"
        }
    """
    if not plan_path.exists():
        raise FileNotFoundError(f"Plan file not found: {plan_path}")

    content = plan_path.read_text()

    # Extract fields from Technical Context section using markdown patterns
    def extract_field(pattern: str) -> Optional[str]:
        # Match pattern like "**Language/Version**: Python 3.11+"
        match = re.search(rf"\*\*{pattern}\*\*:\s*(.+?)(?:\n|$)", content, re.MULTILINE)
        if match:
            value = match.group(1).strip()
            # Filter out placeholders
            if value and value != "NEEDS CLARIFICATION" and value != "N/A":
                return value
        return None

    return {
        "language": extract_field("Language/Version"),
        "dependencies": extract_field("Primary Dependencies"),
        "storage": extract_field("Storage"),
        "testing": extract_field("Testing"),
        "project_type": extract_field("PROJECT TYPE"),
    }

x_parse_plan_for_tech_stack__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x_parse_plan_for_tech_stack__mutmut_1': x_parse_plan_for_tech_stack__mutmut_1, 
    'x_parse_plan_for_tech_stack__mutmut_2': x_parse_plan_for_tech_stack__mutmut_2, 
    'x_parse_plan_for_tech_stack__mutmut_3': x_parse_plan_for_tech_stack__mutmut_3, 
    'x_parse_plan_for_tech_stack__mutmut_4': x_parse_plan_for_tech_stack__mutmut_4, 
    'x_parse_plan_for_tech_stack__mutmut_5': x_parse_plan_for_tech_stack__mutmut_5, 
    'x_parse_plan_for_tech_stack__mutmut_6': x_parse_plan_for_tech_stack__mutmut_6, 
    'x_parse_plan_for_tech_stack__mutmut_7': x_parse_plan_for_tech_stack__mutmut_7, 
    'x_parse_plan_for_tech_stack__mutmut_8': x_parse_plan_for_tech_stack__mutmut_8, 
    'x_parse_plan_for_tech_stack__mutmut_9': x_parse_plan_for_tech_stack__mutmut_9, 
    'x_parse_plan_for_tech_stack__mutmut_10': x_parse_plan_for_tech_stack__mutmut_10, 
    'x_parse_plan_for_tech_stack__mutmut_11': x_parse_plan_for_tech_stack__mutmut_11, 
    'x_parse_plan_for_tech_stack__mutmut_12': x_parse_plan_for_tech_stack__mutmut_12, 
    'x_parse_plan_for_tech_stack__mutmut_13': x_parse_plan_for_tech_stack__mutmut_13, 
    'x_parse_plan_for_tech_stack__mutmut_14': x_parse_plan_for_tech_stack__mutmut_14, 
    'x_parse_plan_for_tech_stack__mutmut_15': x_parse_plan_for_tech_stack__mutmut_15, 
    'x_parse_plan_for_tech_stack__mutmut_16': x_parse_plan_for_tech_stack__mutmut_16, 
    'x_parse_plan_for_tech_stack__mutmut_17': x_parse_plan_for_tech_stack__mutmut_17, 
    'x_parse_plan_for_tech_stack__mutmut_18': x_parse_plan_for_tech_stack__mutmut_18, 
    'x_parse_plan_for_tech_stack__mutmut_19': x_parse_plan_for_tech_stack__mutmut_19, 
    'x_parse_plan_for_tech_stack__mutmut_20': x_parse_plan_for_tech_stack__mutmut_20, 
    'x_parse_plan_for_tech_stack__mutmut_21': x_parse_plan_for_tech_stack__mutmut_21, 
    'x_parse_plan_for_tech_stack__mutmut_22': x_parse_plan_for_tech_stack__mutmut_22, 
    'x_parse_plan_for_tech_stack__mutmut_23': x_parse_plan_for_tech_stack__mutmut_23, 
    'x_parse_plan_for_tech_stack__mutmut_24': x_parse_plan_for_tech_stack__mutmut_24, 
    'x_parse_plan_for_tech_stack__mutmut_25': x_parse_plan_for_tech_stack__mutmut_25, 
    'x_parse_plan_for_tech_stack__mutmut_26': x_parse_plan_for_tech_stack__mutmut_26, 
    'x_parse_plan_for_tech_stack__mutmut_27': x_parse_plan_for_tech_stack__mutmut_27, 
    'x_parse_plan_for_tech_stack__mutmut_28': x_parse_plan_for_tech_stack__mutmut_28, 
    'x_parse_plan_for_tech_stack__mutmut_29': x_parse_plan_for_tech_stack__mutmut_29, 
    'x_parse_plan_for_tech_stack__mutmut_30': x_parse_plan_for_tech_stack__mutmut_30, 
    'x_parse_plan_for_tech_stack__mutmut_31': x_parse_plan_for_tech_stack__mutmut_31, 
    'x_parse_plan_for_tech_stack__mutmut_32': x_parse_plan_for_tech_stack__mutmut_32, 
    'x_parse_plan_for_tech_stack__mutmut_33': x_parse_plan_for_tech_stack__mutmut_33, 
    'x_parse_plan_for_tech_stack__mutmut_34': x_parse_plan_for_tech_stack__mutmut_34, 
    'x_parse_plan_for_tech_stack__mutmut_35': x_parse_plan_for_tech_stack__mutmut_35, 
    'x_parse_plan_for_tech_stack__mutmut_36': x_parse_plan_for_tech_stack__mutmut_36, 
    'x_parse_plan_for_tech_stack__mutmut_37': x_parse_plan_for_tech_stack__mutmut_37, 
    'x_parse_plan_for_tech_stack__mutmut_38': x_parse_plan_for_tech_stack__mutmut_38, 
    'x_parse_plan_for_tech_stack__mutmut_39': x_parse_plan_for_tech_stack__mutmut_39, 
    'x_parse_plan_for_tech_stack__mutmut_40': x_parse_plan_for_tech_stack__mutmut_40, 
    'x_parse_plan_for_tech_stack__mutmut_41': x_parse_plan_for_tech_stack__mutmut_41, 
    'x_parse_plan_for_tech_stack__mutmut_42': x_parse_plan_for_tech_stack__mutmut_42, 
    'x_parse_plan_for_tech_stack__mutmut_43': x_parse_plan_for_tech_stack__mutmut_43, 
    'x_parse_plan_for_tech_stack__mutmut_44': x_parse_plan_for_tech_stack__mutmut_44, 
    'x_parse_plan_for_tech_stack__mutmut_45': x_parse_plan_for_tech_stack__mutmut_45, 
    'x_parse_plan_for_tech_stack__mutmut_46': x_parse_plan_for_tech_stack__mutmut_46, 
    'x_parse_plan_for_tech_stack__mutmut_47': x_parse_plan_for_tech_stack__mutmut_47, 
    'x_parse_plan_for_tech_stack__mutmut_48': x_parse_plan_for_tech_stack__mutmut_48, 
    'x_parse_plan_for_tech_stack__mutmut_49': x_parse_plan_for_tech_stack__mutmut_49, 
    'x_parse_plan_for_tech_stack__mutmut_50': x_parse_plan_for_tech_stack__mutmut_50, 
    'x_parse_plan_for_tech_stack__mutmut_51': x_parse_plan_for_tech_stack__mutmut_51
}
x_parse_plan_for_tech_stack__mutmut_orig.__name__ = 'x_parse_plan_for_tech_stack'


def format_technology_stack(tech_stack: Dict[str, Optional[str]], feature_slug: str) -> List[str]:
    args = [tech_stack, feature_slug]# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x_format_technology_stack__mutmut_orig, x_format_technology_stack__mutmut_mutants, args, kwargs, None)


def x_format_technology_stack__mutmut_orig(tech_stack: Dict[str, Optional[str]], feature_slug: str) -> List[str]:
    """
    Format tech stack data into markdown bullet points for Active Technologies section.

    Args:
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug (e.g., "008-unified-python-cli")

    Returns:
        List of formatted markdown lines

    Example:
        ["- Python 3.11+ (existing spec-kitty requirement) (008-unified-python-cli)",
         "- Filesystem only (no database) (008-unified-python-cli)"]
    """
    entries = []

    # Add language + dependencies as one line
    parts = []
    if tech_stack.get("language"):
        parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        parts.append(tech_stack["dependencies"])

    if parts:
        tech_line = " + ".join(parts)
        entries.append(f"- {tech_line} ({feature_slug})")

    # Add storage as separate line if present
    if tech_stack.get("storage"):
        entries.append(f"- {tech_stack['storage']} ({feature_slug})")

    return entries


def x_format_technology_stack__mutmut_1(tech_stack: Dict[str, Optional[str]], feature_slug: str) -> List[str]:
    """
    Format tech stack data into markdown bullet points for Active Technologies section.

    Args:
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug (e.g., "008-unified-python-cli")

    Returns:
        List of formatted markdown lines

    Example:
        ["- Python 3.11+ (existing spec-kitty requirement) (008-unified-python-cli)",
         "- Filesystem only (no database) (008-unified-python-cli)"]
    """
    entries = None

    # Add language + dependencies as one line
    parts = []
    if tech_stack.get("language"):
        parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        parts.append(tech_stack["dependencies"])

    if parts:
        tech_line = " + ".join(parts)
        entries.append(f"- {tech_line} ({feature_slug})")

    # Add storage as separate line if present
    if tech_stack.get("storage"):
        entries.append(f"- {tech_stack['storage']} ({feature_slug})")

    return entries


def x_format_technology_stack__mutmut_2(tech_stack: Dict[str, Optional[str]], feature_slug: str) -> List[str]:
    """
    Format tech stack data into markdown bullet points for Active Technologies section.

    Args:
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug (e.g., "008-unified-python-cli")

    Returns:
        List of formatted markdown lines

    Example:
        ["- Python 3.11+ (existing spec-kitty requirement) (008-unified-python-cli)",
         "- Filesystem only (no database) (008-unified-python-cli)"]
    """
    entries = []

    # Add language + dependencies as one line
    parts = None
    if tech_stack.get("language"):
        parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        parts.append(tech_stack["dependencies"])

    if parts:
        tech_line = " + ".join(parts)
        entries.append(f"- {tech_line} ({feature_slug})")

    # Add storage as separate line if present
    if tech_stack.get("storage"):
        entries.append(f"- {tech_stack['storage']} ({feature_slug})")

    return entries


def x_format_technology_stack__mutmut_3(tech_stack: Dict[str, Optional[str]], feature_slug: str) -> List[str]:
    """
    Format tech stack data into markdown bullet points for Active Technologies section.

    Args:
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug (e.g., "008-unified-python-cli")

    Returns:
        List of formatted markdown lines

    Example:
        ["- Python 3.11+ (existing spec-kitty requirement) (008-unified-python-cli)",
         "- Filesystem only (no database) (008-unified-python-cli)"]
    """
    entries = []

    # Add language + dependencies as one line
    parts = []
    if tech_stack.get(None):
        parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        parts.append(tech_stack["dependencies"])

    if parts:
        tech_line = " + ".join(parts)
        entries.append(f"- {tech_line} ({feature_slug})")

    # Add storage as separate line if present
    if tech_stack.get("storage"):
        entries.append(f"- {tech_stack['storage']} ({feature_slug})")

    return entries


def x_format_technology_stack__mutmut_4(tech_stack: Dict[str, Optional[str]], feature_slug: str) -> List[str]:
    """
    Format tech stack data into markdown bullet points for Active Technologies section.

    Args:
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug (e.g., "008-unified-python-cli")

    Returns:
        List of formatted markdown lines

    Example:
        ["- Python 3.11+ (existing spec-kitty requirement) (008-unified-python-cli)",
         "- Filesystem only (no database) (008-unified-python-cli)"]
    """
    entries = []

    # Add language + dependencies as one line
    parts = []
    if tech_stack.get("XXlanguageXX"):
        parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        parts.append(tech_stack["dependencies"])

    if parts:
        tech_line = " + ".join(parts)
        entries.append(f"- {tech_line} ({feature_slug})")

    # Add storage as separate line if present
    if tech_stack.get("storage"):
        entries.append(f"- {tech_stack['storage']} ({feature_slug})")

    return entries


def x_format_technology_stack__mutmut_5(tech_stack: Dict[str, Optional[str]], feature_slug: str) -> List[str]:
    """
    Format tech stack data into markdown bullet points for Active Technologies section.

    Args:
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug (e.g., "008-unified-python-cli")

    Returns:
        List of formatted markdown lines

    Example:
        ["- Python 3.11+ (existing spec-kitty requirement) (008-unified-python-cli)",
         "- Filesystem only (no database) (008-unified-python-cli)"]
    """
    entries = []

    # Add language + dependencies as one line
    parts = []
    if tech_stack.get("LANGUAGE"):
        parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        parts.append(tech_stack["dependencies"])

    if parts:
        tech_line = " + ".join(parts)
        entries.append(f"- {tech_line} ({feature_slug})")

    # Add storage as separate line if present
    if tech_stack.get("storage"):
        entries.append(f"- {tech_stack['storage']} ({feature_slug})")

    return entries


def x_format_technology_stack__mutmut_6(tech_stack: Dict[str, Optional[str]], feature_slug: str) -> List[str]:
    """
    Format tech stack data into markdown bullet points for Active Technologies section.

    Args:
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug (e.g., "008-unified-python-cli")

    Returns:
        List of formatted markdown lines

    Example:
        ["- Python 3.11+ (existing spec-kitty requirement) (008-unified-python-cli)",
         "- Filesystem only (no database) (008-unified-python-cli)"]
    """
    entries = []

    # Add language + dependencies as one line
    parts = []
    if tech_stack.get("language"):
        parts.append(None)
    if tech_stack.get("dependencies"):
        parts.append(tech_stack["dependencies"])

    if parts:
        tech_line = " + ".join(parts)
        entries.append(f"- {tech_line} ({feature_slug})")

    # Add storage as separate line if present
    if tech_stack.get("storage"):
        entries.append(f"- {tech_stack['storage']} ({feature_slug})")

    return entries


def x_format_technology_stack__mutmut_7(tech_stack: Dict[str, Optional[str]], feature_slug: str) -> List[str]:
    """
    Format tech stack data into markdown bullet points for Active Technologies section.

    Args:
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug (e.g., "008-unified-python-cli")

    Returns:
        List of formatted markdown lines

    Example:
        ["- Python 3.11+ (existing spec-kitty requirement) (008-unified-python-cli)",
         "- Filesystem only (no database) (008-unified-python-cli)"]
    """
    entries = []

    # Add language + dependencies as one line
    parts = []
    if tech_stack.get("language"):
        parts.append(tech_stack["XXlanguageXX"])
    if tech_stack.get("dependencies"):
        parts.append(tech_stack["dependencies"])

    if parts:
        tech_line = " + ".join(parts)
        entries.append(f"- {tech_line} ({feature_slug})")

    # Add storage as separate line if present
    if tech_stack.get("storage"):
        entries.append(f"- {tech_stack['storage']} ({feature_slug})")

    return entries


def x_format_technology_stack__mutmut_8(tech_stack: Dict[str, Optional[str]], feature_slug: str) -> List[str]:
    """
    Format tech stack data into markdown bullet points for Active Technologies section.

    Args:
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug (e.g., "008-unified-python-cli")

    Returns:
        List of formatted markdown lines

    Example:
        ["- Python 3.11+ (existing spec-kitty requirement) (008-unified-python-cli)",
         "- Filesystem only (no database) (008-unified-python-cli)"]
    """
    entries = []

    # Add language + dependencies as one line
    parts = []
    if tech_stack.get("language"):
        parts.append(tech_stack["LANGUAGE"])
    if tech_stack.get("dependencies"):
        parts.append(tech_stack["dependencies"])

    if parts:
        tech_line = " + ".join(parts)
        entries.append(f"- {tech_line} ({feature_slug})")

    # Add storage as separate line if present
    if tech_stack.get("storage"):
        entries.append(f"- {tech_stack['storage']} ({feature_slug})")

    return entries


def x_format_technology_stack__mutmut_9(tech_stack: Dict[str, Optional[str]], feature_slug: str) -> List[str]:
    """
    Format tech stack data into markdown bullet points for Active Technologies section.

    Args:
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug (e.g., "008-unified-python-cli")

    Returns:
        List of formatted markdown lines

    Example:
        ["- Python 3.11+ (existing spec-kitty requirement) (008-unified-python-cli)",
         "- Filesystem only (no database) (008-unified-python-cli)"]
    """
    entries = []

    # Add language + dependencies as one line
    parts = []
    if tech_stack.get("language"):
        parts.append(tech_stack["language"])
    if tech_stack.get(None):
        parts.append(tech_stack["dependencies"])

    if parts:
        tech_line = " + ".join(parts)
        entries.append(f"- {tech_line} ({feature_slug})")

    # Add storage as separate line if present
    if tech_stack.get("storage"):
        entries.append(f"- {tech_stack['storage']} ({feature_slug})")

    return entries


def x_format_technology_stack__mutmut_10(tech_stack: Dict[str, Optional[str]], feature_slug: str) -> List[str]:
    """
    Format tech stack data into markdown bullet points for Active Technologies section.

    Args:
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug (e.g., "008-unified-python-cli")

    Returns:
        List of formatted markdown lines

    Example:
        ["- Python 3.11+ (existing spec-kitty requirement) (008-unified-python-cli)",
         "- Filesystem only (no database) (008-unified-python-cli)"]
    """
    entries = []

    # Add language + dependencies as one line
    parts = []
    if tech_stack.get("language"):
        parts.append(tech_stack["language"])
    if tech_stack.get("XXdependenciesXX"):
        parts.append(tech_stack["dependencies"])

    if parts:
        tech_line = " + ".join(parts)
        entries.append(f"- {tech_line} ({feature_slug})")

    # Add storage as separate line if present
    if tech_stack.get("storage"):
        entries.append(f"- {tech_stack['storage']} ({feature_slug})")

    return entries


def x_format_technology_stack__mutmut_11(tech_stack: Dict[str, Optional[str]], feature_slug: str) -> List[str]:
    """
    Format tech stack data into markdown bullet points for Active Technologies section.

    Args:
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug (e.g., "008-unified-python-cli")

    Returns:
        List of formatted markdown lines

    Example:
        ["- Python 3.11+ (existing spec-kitty requirement) (008-unified-python-cli)",
         "- Filesystem only (no database) (008-unified-python-cli)"]
    """
    entries = []

    # Add language + dependencies as one line
    parts = []
    if tech_stack.get("language"):
        parts.append(tech_stack["language"])
    if tech_stack.get("DEPENDENCIES"):
        parts.append(tech_stack["dependencies"])

    if parts:
        tech_line = " + ".join(parts)
        entries.append(f"- {tech_line} ({feature_slug})")

    # Add storage as separate line if present
    if tech_stack.get("storage"):
        entries.append(f"- {tech_stack['storage']} ({feature_slug})")

    return entries


def x_format_technology_stack__mutmut_12(tech_stack: Dict[str, Optional[str]], feature_slug: str) -> List[str]:
    """
    Format tech stack data into markdown bullet points for Active Technologies section.

    Args:
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug (e.g., "008-unified-python-cli")

    Returns:
        List of formatted markdown lines

    Example:
        ["- Python 3.11+ (existing spec-kitty requirement) (008-unified-python-cli)",
         "- Filesystem only (no database) (008-unified-python-cli)"]
    """
    entries = []

    # Add language + dependencies as one line
    parts = []
    if tech_stack.get("language"):
        parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        parts.append(None)

    if parts:
        tech_line = " + ".join(parts)
        entries.append(f"- {tech_line} ({feature_slug})")

    # Add storage as separate line if present
    if tech_stack.get("storage"):
        entries.append(f"- {tech_stack['storage']} ({feature_slug})")

    return entries


def x_format_technology_stack__mutmut_13(tech_stack: Dict[str, Optional[str]], feature_slug: str) -> List[str]:
    """
    Format tech stack data into markdown bullet points for Active Technologies section.

    Args:
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug (e.g., "008-unified-python-cli")

    Returns:
        List of formatted markdown lines

    Example:
        ["- Python 3.11+ (existing spec-kitty requirement) (008-unified-python-cli)",
         "- Filesystem only (no database) (008-unified-python-cli)"]
    """
    entries = []

    # Add language + dependencies as one line
    parts = []
    if tech_stack.get("language"):
        parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        parts.append(tech_stack["XXdependenciesXX"])

    if parts:
        tech_line = " + ".join(parts)
        entries.append(f"- {tech_line} ({feature_slug})")

    # Add storage as separate line if present
    if tech_stack.get("storage"):
        entries.append(f"- {tech_stack['storage']} ({feature_slug})")

    return entries


def x_format_technology_stack__mutmut_14(tech_stack: Dict[str, Optional[str]], feature_slug: str) -> List[str]:
    """
    Format tech stack data into markdown bullet points for Active Technologies section.

    Args:
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug (e.g., "008-unified-python-cli")

    Returns:
        List of formatted markdown lines

    Example:
        ["- Python 3.11+ (existing spec-kitty requirement) (008-unified-python-cli)",
         "- Filesystem only (no database) (008-unified-python-cli)"]
    """
    entries = []

    # Add language + dependencies as one line
    parts = []
    if tech_stack.get("language"):
        parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        parts.append(tech_stack["DEPENDENCIES"])

    if parts:
        tech_line = " + ".join(parts)
        entries.append(f"- {tech_line} ({feature_slug})")

    # Add storage as separate line if present
    if tech_stack.get("storage"):
        entries.append(f"- {tech_stack['storage']} ({feature_slug})")

    return entries


def x_format_technology_stack__mutmut_15(tech_stack: Dict[str, Optional[str]], feature_slug: str) -> List[str]:
    """
    Format tech stack data into markdown bullet points for Active Technologies section.

    Args:
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug (e.g., "008-unified-python-cli")

    Returns:
        List of formatted markdown lines

    Example:
        ["- Python 3.11+ (existing spec-kitty requirement) (008-unified-python-cli)",
         "- Filesystem only (no database) (008-unified-python-cli)"]
    """
    entries = []

    # Add language + dependencies as one line
    parts = []
    if tech_stack.get("language"):
        parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        parts.append(tech_stack["dependencies"])

    if parts:
        tech_line = None
        entries.append(f"- {tech_line} ({feature_slug})")

    # Add storage as separate line if present
    if tech_stack.get("storage"):
        entries.append(f"- {tech_stack['storage']} ({feature_slug})")

    return entries


def x_format_technology_stack__mutmut_16(tech_stack: Dict[str, Optional[str]], feature_slug: str) -> List[str]:
    """
    Format tech stack data into markdown bullet points for Active Technologies section.

    Args:
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug (e.g., "008-unified-python-cli")

    Returns:
        List of formatted markdown lines

    Example:
        ["- Python 3.11+ (existing spec-kitty requirement) (008-unified-python-cli)",
         "- Filesystem only (no database) (008-unified-python-cli)"]
    """
    entries = []

    # Add language + dependencies as one line
    parts = []
    if tech_stack.get("language"):
        parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        parts.append(tech_stack["dependencies"])

    if parts:
        tech_line = " + ".join(None)
        entries.append(f"- {tech_line} ({feature_slug})")

    # Add storage as separate line if present
    if tech_stack.get("storage"):
        entries.append(f"- {tech_stack['storage']} ({feature_slug})")

    return entries


def x_format_technology_stack__mutmut_17(tech_stack: Dict[str, Optional[str]], feature_slug: str) -> List[str]:
    """
    Format tech stack data into markdown bullet points for Active Technologies section.

    Args:
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug (e.g., "008-unified-python-cli")

    Returns:
        List of formatted markdown lines

    Example:
        ["- Python 3.11+ (existing spec-kitty requirement) (008-unified-python-cli)",
         "- Filesystem only (no database) (008-unified-python-cli)"]
    """
    entries = []

    # Add language + dependencies as one line
    parts = []
    if tech_stack.get("language"):
        parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        parts.append(tech_stack["dependencies"])

    if parts:
        tech_line = "XX + XX".join(parts)
        entries.append(f"- {tech_line} ({feature_slug})")

    # Add storage as separate line if present
    if tech_stack.get("storage"):
        entries.append(f"- {tech_stack['storage']} ({feature_slug})")

    return entries


def x_format_technology_stack__mutmut_18(tech_stack: Dict[str, Optional[str]], feature_slug: str) -> List[str]:
    """
    Format tech stack data into markdown bullet points for Active Technologies section.

    Args:
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug (e.g., "008-unified-python-cli")

    Returns:
        List of formatted markdown lines

    Example:
        ["- Python 3.11+ (existing spec-kitty requirement) (008-unified-python-cli)",
         "- Filesystem only (no database) (008-unified-python-cli)"]
    """
    entries = []

    # Add language + dependencies as one line
    parts = []
    if tech_stack.get("language"):
        parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        parts.append(tech_stack["dependencies"])

    if parts:
        tech_line = " + ".join(parts)
        entries.append(None)

    # Add storage as separate line if present
    if tech_stack.get("storage"):
        entries.append(f"- {tech_stack['storage']} ({feature_slug})")

    return entries


def x_format_technology_stack__mutmut_19(tech_stack: Dict[str, Optional[str]], feature_slug: str) -> List[str]:
    """
    Format tech stack data into markdown bullet points for Active Technologies section.

    Args:
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug (e.g., "008-unified-python-cli")

    Returns:
        List of formatted markdown lines

    Example:
        ["- Python 3.11+ (existing spec-kitty requirement) (008-unified-python-cli)",
         "- Filesystem only (no database) (008-unified-python-cli)"]
    """
    entries = []

    # Add language + dependencies as one line
    parts = []
    if tech_stack.get("language"):
        parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        parts.append(tech_stack["dependencies"])

    if parts:
        tech_line = " + ".join(parts)
        entries.append(f"- {tech_line} ({feature_slug})")

    # Add storage as separate line if present
    if tech_stack.get(None):
        entries.append(f"- {tech_stack['storage']} ({feature_slug})")

    return entries


def x_format_technology_stack__mutmut_20(tech_stack: Dict[str, Optional[str]], feature_slug: str) -> List[str]:
    """
    Format tech stack data into markdown bullet points for Active Technologies section.

    Args:
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug (e.g., "008-unified-python-cli")

    Returns:
        List of formatted markdown lines

    Example:
        ["- Python 3.11+ (existing spec-kitty requirement) (008-unified-python-cli)",
         "- Filesystem only (no database) (008-unified-python-cli)"]
    """
    entries = []

    # Add language + dependencies as one line
    parts = []
    if tech_stack.get("language"):
        parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        parts.append(tech_stack["dependencies"])

    if parts:
        tech_line = " + ".join(parts)
        entries.append(f"- {tech_line} ({feature_slug})")

    # Add storage as separate line if present
    if tech_stack.get("XXstorageXX"):
        entries.append(f"- {tech_stack['storage']} ({feature_slug})")

    return entries


def x_format_technology_stack__mutmut_21(tech_stack: Dict[str, Optional[str]], feature_slug: str) -> List[str]:
    """
    Format tech stack data into markdown bullet points for Active Technologies section.

    Args:
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug (e.g., "008-unified-python-cli")

    Returns:
        List of formatted markdown lines

    Example:
        ["- Python 3.11+ (existing spec-kitty requirement) (008-unified-python-cli)",
         "- Filesystem only (no database) (008-unified-python-cli)"]
    """
    entries = []

    # Add language + dependencies as one line
    parts = []
    if tech_stack.get("language"):
        parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        parts.append(tech_stack["dependencies"])

    if parts:
        tech_line = " + ".join(parts)
        entries.append(f"- {tech_line} ({feature_slug})")

    # Add storage as separate line if present
    if tech_stack.get("STORAGE"):
        entries.append(f"- {tech_stack['storage']} ({feature_slug})")

    return entries


def x_format_technology_stack__mutmut_22(tech_stack: Dict[str, Optional[str]], feature_slug: str) -> List[str]:
    """
    Format tech stack data into markdown bullet points for Active Technologies section.

    Args:
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug (e.g., "008-unified-python-cli")

    Returns:
        List of formatted markdown lines

    Example:
        ["- Python 3.11+ (existing spec-kitty requirement) (008-unified-python-cli)",
         "- Filesystem only (no database) (008-unified-python-cli)"]
    """
    entries = []

    # Add language + dependencies as one line
    parts = []
    if tech_stack.get("language"):
        parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        parts.append(tech_stack["dependencies"])

    if parts:
        tech_line = " + ".join(parts)
        entries.append(f"- {tech_line} ({feature_slug})")

    # Add storage as separate line if present
    if tech_stack.get("storage"):
        entries.append(None)

    return entries


def x_format_technology_stack__mutmut_23(tech_stack: Dict[str, Optional[str]], feature_slug: str) -> List[str]:
    """
    Format tech stack data into markdown bullet points for Active Technologies section.

    Args:
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug (e.g., "008-unified-python-cli")

    Returns:
        List of formatted markdown lines

    Example:
        ["- Python 3.11+ (existing spec-kitty requirement) (008-unified-python-cli)",
         "- Filesystem only (no database) (008-unified-python-cli)"]
    """
    entries = []

    # Add language + dependencies as one line
    parts = []
    if tech_stack.get("language"):
        parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        parts.append(tech_stack["dependencies"])

    if parts:
        tech_line = " + ".join(parts)
        entries.append(f"- {tech_line} ({feature_slug})")

    # Add storage as separate line if present
    if tech_stack.get("storage"):
        entries.append(f"- {tech_stack['XXstorageXX']} ({feature_slug})")

    return entries


def x_format_technology_stack__mutmut_24(tech_stack: Dict[str, Optional[str]], feature_slug: str) -> List[str]:
    """
    Format tech stack data into markdown bullet points for Active Technologies section.

    Args:
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug (e.g., "008-unified-python-cli")

    Returns:
        List of formatted markdown lines

    Example:
        ["- Python 3.11+ (existing spec-kitty requirement) (008-unified-python-cli)",
         "- Filesystem only (no database) (008-unified-python-cli)"]
    """
    entries = []

    # Add language + dependencies as one line
    parts = []
    if tech_stack.get("language"):
        parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        parts.append(tech_stack["dependencies"])

    if parts:
        tech_line = " + ".join(parts)
        entries.append(f"- {tech_line} ({feature_slug})")

    # Add storage as separate line if present
    if tech_stack.get("storage"):
        entries.append(f"- {tech_stack['STORAGE']} ({feature_slug})")

    return entries

x_format_technology_stack__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x_format_technology_stack__mutmut_1': x_format_technology_stack__mutmut_1, 
    'x_format_technology_stack__mutmut_2': x_format_technology_stack__mutmut_2, 
    'x_format_technology_stack__mutmut_3': x_format_technology_stack__mutmut_3, 
    'x_format_technology_stack__mutmut_4': x_format_technology_stack__mutmut_4, 
    'x_format_technology_stack__mutmut_5': x_format_technology_stack__mutmut_5, 
    'x_format_technology_stack__mutmut_6': x_format_technology_stack__mutmut_6, 
    'x_format_technology_stack__mutmut_7': x_format_technology_stack__mutmut_7, 
    'x_format_technology_stack__mutmut_8': x_format_technology_stack__mutmut_8, 
    'x_format_technology_stack__mutmut_9': x_format_technology_stack__mutmut_9, 
    'x_format_technology_stack__mutmut_10': x_format_technology_stack__mutmut_10, 
    'x_format_technology_stack__mutmut_11': x_format_technology_stack__mutmut_11, 
    'x_format_technology_stack__mutmut_12': x_format_technology_stack__mutmut_12, 
    'x_format_technology_stack__mutmut_13': x_format_technology_stack__mutmut_13, 
    'x_format_technology_stack__mutmut_14': x_format_technology_stack__mutmut_14, 
    'x_format_technology_stack__mutmut_15': x_format_technology_stack__mutmut_15, 
    'x_format_technology_stack__mutmut_16': x_format_technology_stack__mutmut_16, 
    'x_format_technology_stack__mutmut_17': x_format_technology_stack__mutmut_17, 
    'x_format_technology_stack__mutmut_18': x_format_technology_stack__mutmut_18, 
    'x_format_technology_stack__mutmut_19': x_format_technology_stack__mutmut_19, 
    'x_format_technology_stack__mutmut_20': x_format_technology_stack__mutmut_20, 
    'x_format_technology_stack__mutmut_21': x_format_technology_stack__mutmut_21, 
    'x_format_technology_stack__mutmut_22': x_format_technology_stack__mutmut_22, 
    'x_format_technology_stack__mutmut_23': x_format_technology_stack__mutmut_23, 
    'x_format_technology_stack__mutmut_24': x_format_technology_stack__mutmut_24
}
x_format_technology_stack__mutmut_orig.__name__ = 'x_format_technology_stack'


def preserve_manual_additions(old_content: str, new_content: str) -> str:
    args = [old_content, new_content]# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x_preserve_manual_additions__mutmut_orig, x_preserve_manual_additions__mutmut_mutants, args, kwargs, None)


def x_preserve_manual_additions__mutmut_orig(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_1(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = None
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_2(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "XX<!-- MANUAL ADDITIONS START -->XX"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_3(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- manual additions start -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_4(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = None

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_5(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "XX<!-- MANUAL ADDITIONS END -->XX"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_6(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- manual additions end -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_7(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = None
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_8(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(None)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_9(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.rfind(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_10(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = None

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_11(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(None)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_12(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.rfind(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_13(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 and end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_14(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx != -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_15(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == +1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_16(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -2 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_17(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx != -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_18(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == +1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_19(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -2:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_20(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = None

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_21(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx - len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_22(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = None
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_23(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(None)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_24(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.rfind(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_25(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = None

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_26(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(None)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_27(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.rfind(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_28(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 and new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_29(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx != -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_30(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == +1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_31(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -2 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_32(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx != -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_33(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == +1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_34(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -2:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_35(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section - "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_36(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" - manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_37(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() - "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_38(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.lstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_39(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "XX\n\nXX" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_40(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "XX\nXX"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_41(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = None
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_42(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = None

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_43(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx - len(end_marker):]

    return before + manual_section + after


def x_preserve_manual_additions__mutmut_44(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before + manual_section - after


def x_preserve_manual_additions__mutmut_45(old_content: str, new_content: str) -> str:
    """
    Preserve content between <!-- MANUAL ADDITIONS START/END --> markers.

    Args:
        old_content: Original file content with manual additions
        new_content: New generated content

    Returns:
        Merged content with manual additions from old_content injected into new_content

    Note:
        If markers are not found in old_content, returns new_content unchanged.
        If markers are not found in new_content, manual section is appended.
    """
    # Extract manual additions from old content
    start_marker = "<!-- MANUAL ADDITIONS START -->"
    end_marker = "<!-- MANUAL ADDITIONS END -->"

    # Find manual additions in old content
    start_idx = old_content.find(start_marker)
    end_idx = old_content.find(end_marker)

    if start_idx == -1 or end_idx == -1:
        # No manual additions to preserve
        return new_content

    # Extract the manual section (including markers)
    manual_section = old_content[start_idx:end_idx + len(end_marker)]

    # Find where to inject in new content
    new_start_idx = new_content.find(start_marker)
    new_end_idx = new_content.find(end_marker)

    if new_start_idx == -1 or new_end_idx == -1:
        # New content doesn't have markers, append at end
        return new_content.rstrip() + "\n\n" + manual_section + "\n"

    # Replace the section in new content with the preserved manual section
    before = new_content[:new_start_idx]
    after = new_content[new_end_idx + len(end_marker):]

    return before - manual_section + after

x_preserve_manual_additions__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x_preserve_manual_additions__mutmut_1': x_preserve_manual_additions__mutmut_1, 
    'x_preserve_manual_additions__mutmut_2': x_preserve_manual_additions__mutmut_2, 
    'x_preserve_manual_additions__mutmut_3': x_preserve_manual_additions__mutmut_3, 
    'x_preserve_manual_additions__mutmut_4': x_preserve_manual_additions__mutmut_4, 
    'x_preserve_manual_additions__mutmut_5': x_preserve_manual_additions__mutmut_5, 
    'x_preserve_manual_additions__mutmut_6': x_preserve_manual_additions__mutmut_6, 
    'x_preserve_manual_additions__mutmut_7': x_preserve_manual_additions__mutmut_7, 
    'x_preserve_manual_additions__mutmut_8': x_preserve_manual_additions__mutmut_8, 
    'x_preserve_manual_additions__mutmut_9': x_preserve_manual_additions__mutmut_9, 
    'x_preserve_manual_additions__mutmut_10': x_preserve_manual_additions__mutmut_10, 
    'x_preserve_manual_additions__mutmut_11': x_preserve_manual_additions__mutmut_11, 
    'x_preserve_manual_additions__mutmut_12': x_preserve_manual_additions__mutmut_12, 
    'x_preserve_manual_additions__mutmut_13': x_preserve_manual_additions__mutmut_13, 
    'x_preserve_manual_additions__mutmut_14': x_preserve_manual_additions__mutmut_14, 
    'x_preserve_manual_additions__mutmut_15': x_preserve_manual_additions__mutmut_15, 
    'x_preserve_manual_additions__mutmut_16': x_preserve_manual_additions__mutmut_16, 
    'x_preserve_manual_additions__mutmut_17': x_preserve_manual_additions__mutmut_17, 
    'x_preserve_manual_additions__mutmut_18': x_preserve_manual_additions__mutmut_18, 
    'x_preserve_manual_additions__mutmut_19': x_preserve_manual_additions__mutmut_19, 
    'x_preserve_manual_additions__mutmut_20': x_preserve_manual_additions__mutmut_20, 
    'x_preserve_manual_additions__mutmut_21': x_preserve_manual_additions__mutmut_21, 
    'x_preserve_manual_additions__mutmut_22': x_preserve_manual_additions__mutmut_22, 
    'x_preserve_manual_additions__mutmut_23': x_preserve_manual_additions__mutmut_23, 
    'x_preserve_manual_additions__mutmut_24': x_preserve_manual_additions__mutmut_24, 
    'x_preserve_manual_additions__mutmut_25': x_preserve_manual_additions__mutmut_25, 
    'x_preserve_manual_additions__mutmut_26': x_preserve_manual_additions__mutmut_26, 
    'x_preserve_manual_additions__mutmut_27': x_preserve_manual_additions__mutmut_27, 
    'x_preserve_manual_additions__mutmut_28': x_preserve_manual_additions__mutmut_28, 
    'x_preserve_manual_additions__mutmut_29': x_preserve_manual_additions__mutmut_29, 
    'x_preserve_manual_additions__mutmut_30': x_preserve_manual_additions__mutmut_30, 
    'x_preserve_manual_additions__mutmut_31': x_preserve_manual_additions__mutmut_31, 
    'x_preserve_manual_additions__mutmut_32': x_preserve_manual_additions__mutmut_32, 
    'x_preserve_manual_additions__mutmut_33': x_preserve_manual_additions__mutmut_33, 
    'x_preserve_manual_additions__mutmut_34': x_preserve_manual_additions__mutmut_34, 
    'x_preserve_manual_additions__mutmut_35': x_preserve_manual_additions__mutmut_35, 
    'x_preserve_manual_additions__mutmut_36': x_preserve_manual_additions__mutmut_36, 
    'x_preserve_manual_additions__mutmut_37': x_preserve_manual_additions__mutmut_37, 
    'x_preserve_manual_additions__mutmut_38': x_preserve_manual_additions__mutmut_38, 
    'x_preserve_manual_additions__mutmut_39': x_preserve_manual_additions__mutmut_39, 
    'x_preserve_manual_additions__mutmut_40': x_preserve_manual_additions__mutmut_40, 
    'x_preserve_manual_additions__mutmut_41': x_preserve_manual_additions__mutmut_41, 
    'x_preserve_manual_additions__mutmut_42': x_preserve_manual_additions__mutmut_42, 
    'x_preserve_manual_additions__mutmut_43': x_preserve_manual_additions__mutmut_43, 
    'x_preserve_manual_additions__mutmut_44': x_preserve_manual_additions__mutmut_44, 
    'x_preserve_manual_additions__mutmut_45': x_preserve_manual_additions__mutmut_45
}
x_preserve_manual_additions__mutmut_orig.__name__ = 'x_preserve_manual_additions'


def update_agent_context(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    args = [agent_type, tech_stack, feature_slug, repo_root, feature_dir]# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x_update_agent_context__mutmut_orig, x_update_agent_context__mutmut_mutants, args, kwargs, None)


def x_update_agent_context__mutmut_orig(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_1(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_2(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            None
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_3(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(None)}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_4(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {'XX, XX'.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_5(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = None

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_6(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root * AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_7(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir or agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_8(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(None):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_9(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = None
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_10(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir * AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_11(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = None

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_12(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_13(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(None)

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_14(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = None

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_15(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = None

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_16(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(None, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_17(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, None)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_18(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_19(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, )

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_20(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = None
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_21(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get(None):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_22(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("XXlanguageXX"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_23(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("LANGUAGE"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_24(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(None)
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_25(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["XXlanguageXX"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_26(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["LANGUAGE"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_27(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get(None):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_28(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("XXdependenciesXX"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_29(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("DEPENDENCIES"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_30(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(None)

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_31(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["XXdependenciesXX"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_32(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["DEPENDENCIES"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_33(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = None
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_34(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(None) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_35(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = "XX + XX".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_36(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get(None, "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_37(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", None)
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_38(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_39(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", )
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_40(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("XXstorageXX", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_41(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("STORAGE", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_42(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "XXXX")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_43(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = None

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_44(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else "XXXX"

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_45(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = None
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_46(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=None)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_47(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=False)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_48(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = None

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_49(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = None
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_50(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = True
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_51(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = None
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_52(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = True
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_53(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = None
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_54(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = True
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_55(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = None
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_56(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 1
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_57(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = None

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_58(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime(None)

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_59(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("XX%Y-%m-%dXX")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_60(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_61(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%M-%D")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_62(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() != "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_63(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "XX## Active TechnologiesXX":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_64(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## active technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_65(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## ACTIVE TECHNOLOGIES":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_66(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(None)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_67(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = None
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_68(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = False
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_69(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            break

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_70(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() != "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_71(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "XX## Recent ChangesXX":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_72(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## recent changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_73(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## RECENT CHANGES":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_74(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added or new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_75(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section or not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_76(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_77(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(None)
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_78(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry - "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_79(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "XX\nXX")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_80(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = None
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_81(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = False
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_82(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = None

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_83(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = True

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_84(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(None)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_85(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = None
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_86(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = False
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_87(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(None)
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_88(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry - "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_89(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "XX\nXX")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_90(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            break

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_91(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section or line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_92(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith(None):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_93(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("XX##XX"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_94(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added or new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_95(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_96(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(None)
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_97(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry - "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_98(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "XX\nXX")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_99(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = None
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_100(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = False
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_101(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(None)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_102(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = None
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_103(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = True
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_104(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            break

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_105(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section or line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_106(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith(None):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_107(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("XX##XX"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_108(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(None)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_109(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = None
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_110(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = True
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_111(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            break

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_112(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith(None):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_113(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("XX- XX"):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_114(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count <= 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_115(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 3:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_116(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(None)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_117(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count = 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_118(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count -= 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_119(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 2
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_120(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                break
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_121(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() != "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_122(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "XXXX":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_123(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                break

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_124(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line and "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_125(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "XX**Last updated**:XX" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_126(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_127(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**LAST UPDATED**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_128(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" not in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_129(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "XX*Last updated*:XX" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_130(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_131(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*LAST UPDATED*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_132(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" not in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_133(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = None

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_134(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(None, current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_135(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', None, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_136(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, None)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_137(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_138(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_139(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, )

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_140(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'XX\d{4}-\d{2}-\d{2}XX', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_141(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(None)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_142(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added or new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_143(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section or not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_144(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_145(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(None)

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_146(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry - "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_147(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "XX\nXX")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_148(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = None

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_149(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(None)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_150(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "XXXX".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_151(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = None

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_152(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(None, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_153(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, None)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_154(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_155(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, )

    # Write updated content
    agent_file_path.write_text(final_content, encoding='utf-8')


def x_update_agent_context__mutmut_156(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(None, encoding='utf-8')


def x_update_agent_context__mutmut_157(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding=None)


def x_update_agent_context__mutmut_158(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(encoding='utf-8')


def x_update_agent_context__mutmut_159(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, )


def x_update_agent_context__mutmut_160(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='XXutf-8XX')


def x_update_agent_context__mutmut_161(
    agent_type: str,
    tech_stack: Dict[str, Optional[str]],
    feature_slug: str,
    repo_root: Path,
    feature_dir: Optional[Path] = None,
) -> None:
    """
    Update agent context file with tech stack from plan.md.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS (claude, gemini, etc.)
        tech_stack: Dictionary from parse_plan_for_tech_stack()
        feature_slug: Current feature branch/slug
        repo_root: Repository root directory
        feature_dir: Feature directory path (for worktree-local updates)

    Raises:
        ValueError: If agent_type is not supported
        FileNotFoundError: If agent file doesn't exist
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    agent_file_path = repo_root / AGENT_CONFIGS[agent_type]

    # If it's a worktree-local file, use feature_dir
    if feature_dir and agent_file_path.is_relative_to(repo_root):
        worktree_agent_file = feature_dir / AGENT_CONFIGS[agent_type]
        if worktree_agent_file.exists():
            agent_file_path = worktree_agent_file

    if not agent_file_path.exists():
        raise FileNotFoundError(f"Agent file not found: {agent_file_path}")

    # Read existing content
    old_content = agent_file_path.read_text()

    # Format new tech entries
    new_tech_entries = format_technology_stack(tech_stack, feature_slug)

    # Prepare change entry for Recent Changes section
    tech_parts = []
    if tech_stack.get("language"):
        tech_parts.append(tech_stack["language"])
    if tech_stack.get("dependencies"):
        tech_parts.append(tech_stack["dependencies"])

    tech_description = " + ".join(tech_parts) if tech_parts else tech_stack.get("storage", "")
    new_change_entry = f"- {feature_slug}: Added {tech_description}" if tech_description else ""

    # Process file line by line to update sections
    lines = old_content.splitlines(keepends=True)
    new_lines = []

    in_tech_section = False
    in_changes_section = False
    tech_entries_added = False
    existing_changes_count = 0
    current_date = datetime.now().strftime("%Y-%m-%d")

    for line in lines:
        # Handle Active Technologies section
        if line.strip() == "## Active Technologies":
            new_lines.append(line)
            in_tech_section = True
            continue

        # Handle Recent Changes section - MUST come before generic section exit checks
        if line.strip() == "## Recent Changes":
            # If we were in tech section, close it first
            if in_tech_section and not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
                in_tech_section = False

            new_lines.append(line)
            in_changes_section = True
            # Add new change entry right after heading
            if new_change_entry:
                new_lines.append(new_change_entry + "\n")
            continue

        # Check if we're exiting tech section
        if in_tech_section and line.strip().startswith("##"):
            # Add new tech entries before closing section
            if not tech_entries_added and new_tech_entries:
                for entry in new_tech_entries:
                    new_lines.append(entry + "\n")
                tech_entries_added = True
            new_lines.append(line)
            in_tech_section = False
            continue

        # Check if we're exiting changes section
        if in_changes_section and line.strip().startswith("##"):
            new_lines.append(line)
            in_changes_section = False
            continue

        # In changes section: keep only first 2 existing changes, skip empty lines
        if in_changes_section:
            if line.strip().startswith("- "):
                if existing_changes_count < 2:
                    new_lines.append(line)
                    existing_changes_count += 1
                continue
            elif line.strip() == "":
                # Skip empty lines in changes section
                continue

        # Update last updated timestamp
        if "**Last updated**:" in line or "*Last updated*:" in line:
            # Replace date in format YYYY-MM-DD
            line = re.sub(r'\d{4}-\d{2}-\d{2}', current_date, line)

        new_lines.append(line)

    # Post-loop: if still in tech section and haven't added entries
    if in_tech_section and not tech_entries_added and new_tech_entries:
        for entry in new_tech_entries:
            new_lines.append(entry + "\n")

    new_content = "".join(new_lines)

    # Preserve manual additions
    final_content = preserve_manual_additions(old_content, new_content)

    # Write updated content
    agent_file_path.write_text(final_content, encoding='UTF-8')

x_update_agent_context__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x_update_agent_context__mutmut_1': x_update_agent_context__mutmut_1, 
    'x_update_agent_context__mutmut_2': x_update_agent_context__mutmut_2, 
    'x_update_agent_context__mutmut_3': x_update_agent_context__mutmut_3, 
    'x_update_agent_context__mutmut_4': x_update_agent_context__mutmut_4, 
    'x_update_agent_context__mutmut_5': x_update_agent_context__mutmut_5, 
    'x_update_agent_context__mutmut_6': x_update_agent_context__mutmut_6, 
    'x_update_agent_context__mutmut_7': x_update_agent_context__mutmut_7, 
    'x_update_agent_context__mutmut_8': x_update_agent_context__mutmut_8, 
    'x_update_agent_context__mutmut_9': x_update_agent_context__mutmut_9, 
    'x_update_agent_context__mutmut_10': x_update_agent_context__mutmut_10, 
    'x_update_agent_context__mutmut_11': x_update_agent_context__mutmut_11, 
    'x_update_agent_context__mutmut_12': x_update_agent_context__mutmut_12, 
    'x_update_agent_context__mutmut_13': x_update_agent_context__mutmut_13, 
    'x_update_agent_context__mutmut_14': x_update_agent_context__mutmut_14, 
    'x_update_agent_context__mutmut_15': x_update_agent_context__mutmut_15, 
    'x_update_agent_context__mutmut_16': x_update_agent_context__mutmut_16, 
    'x_update_agent_context__mutmut_17': x_update_agent_context__mutmut_17, 
    'x_update_agent_context__mutmut_18': x_update_agent_context__mutmut_18, 
    'x_update_agent_context__mutmut_19': x_update_agent_context__mutmut_19, 
    'x_update_agent_context__mutmut_20': x_update_agent_context__mutmut_20, 
    'x_update_agent_context__mutmut_21': x_update_agent_context__mutmut_21, 
    'x_update_agent_context__mutmut_22': x_update_agent_context__mutmut_22, 
    'x_update_agent_context__mutmut_23': x_update_agent_context__mutmut_23, 
    'x_update_agent_context__mutmut_24': x_update_agent_context__mutmut_24, 
    'x_update_agent_context__mutmut_25': x_update_agent_context__mutmut_25, 
    'x_update_agent_context__mutmut_26': x_update_agent_context__mutmut_26, 
    'x_update_agent_context__mutmut_27': x_update_agent_context__mutmut_27, 
    'x_update_agent_context__mutmut_28': x_update_agent_context__mutmut_28, 
    'x_update_agent_context__mutmut_29': x_update_agent_context__mutmut_29, 
    'x_update_agent_context__mutmut_30': x_update_agent_context__mutmut_30, 
    'x_update_agent_context__mutmut_31': x_update_agent_context__mutmut_31, 
    'x_update_agent_context__mutmut_32': x_update_agent_context__mutmut_32, 
    'x_update_agent_context__mutmut_33': x_update_agent_context__mutmut_33, 
    'x_update_agent_context__mutmut_34': x_update_agent_context__mutmut_34, 
    'x_update_agent_context__mutmut_35': x_update_agent_context__mutmut_35, 
    'x_update_agent_context__mutmut_36': x_update_agent_context__mutmut_36, 
    'x_update_agent_context__mutmut_37': x_update_agent_context__mutmut_37, 
    'x_update_agent_context__mutmut_38': x_update_agent_context__mutmut_38, 
    'x_update_agent_context__mutmut_39': x_update_agent_context__mutmut_39, 
    'x_update_agent_context__mutmut_40': x_update_agent_context__mutmut_40, 
    'x_update_agent_context__mutmut_41': x_update_agent_context__mutmut_41, 
    'x_update_agent_context__mutmut_42': x_update_agent_context__mutmut_42, 
    'x_update_agent_context__mutmut_43': x_update_agent_context__mutmut_43, 
    'x_update_agent_context__mutmut_44': x_update_agent_context__mutmut_44, 
    'x_update_agent_context__mutmut_45': x_update_agent_context__mutmut_45, 
    'x_update_agent_context__mutmut_46': x_update_agent_context__mutmut_46, 
    'x_update_agent_context__mutmut_47': x_update_agent_context__mutmut_47, 
    'x_update_agent_context__mutmut_48': x_update_agent_context__mutmut_48, 
    'x_update_agent_context__mutmut_49': x_update_agent_context__mutmut_49, 
    'x_update_agent_context__mutmut_50': x_update_agent_context__mutmut_50, 
    'x_update_agent_context__mutmut_51': x_update_agent_context__mutmut_51, 
    'x_update_agent_context__mutmut_52': x_update_agent_context__mutmut_52, 
    'x_update_agent_context__mutmut_53': x_update_agent_context__mutmut_53, 
    'x_update_agent_context__mutmut_54': x_update_agent_context__mutmut_54, 
    'x_update_agent_context__mutmut_55': x_update_agent_context__mutmut_55, 
    'x_update_agent_context__mutmut_56': x_update_agent_context__mutmut_56, 
    'x_update_agent_context__mutmut_57': x_update_agent_context__mutmut_57, 
    'x_update_agent_context__mutmut_58': x_update_agent_context__mutmut_58, 
    'x_update_agent_context__mutmut_59': x_update_agent_context__mutmut_59, 
    'x_update_agent_context__mutmut_60': x_update_agent_context__mutmut_60, 
    'x_update_agent_context__mutmut_61': x_update_agent_context__mutmut_61, 
    'x_update_agent_context__mutmut_62': x_update_agent_context__mutmut_62, 
    'x_update_agent_context__mutmut_63': x_update_agent_context__mutmut_63, 
    'x_update_agent_context__mutmut_64': x_update_agent_context__mutmut_64, 
    'x_update_agent_context__mutmut_65': x_update_agent_context__mutmut_65, 
    'x_update_agent_context__mutmut_66': x_update_agent_context__mutmut_66, 
    'x_update_agent_context__mutmut_67': x_update_agent_context__mutmut_67, 
    'x_update_agent_context__mutmut_68': x_update_agent_context__mutmut_68, 
    'x_update_agent_context__mutmut_69': x_update_agent_context__mutmut_69, 
    'x_update_agent_context__mutmut_70': x_update_agent_context__mutmut_70, 
    'x_update_agent_context__mutmut_71': x_update_agent_context__mutmut_71, 
    'x_update_agent_context__mutmut_72': x_update_agent_context__mutmut_72, 
    'x_update_agent_context__mutmut_73': x_update_agent_context__mutmut_73, 
    'x_update_agent_context__mutmut_74': x_update_agent_context__mutmut_74, 
    'x_update_agent_context__mutmut_75': x_update_agent_context__mutmut_75, 
    'x_update_agent_context__mutmut_76': x_update_agent_context__mutmut_76, 
    'x_update_agent_context__mutmut_77': x_update_agent_context__mutmut_77, 
    'x_update_agent_context__mutmut_78': x_update_agent_context__mutmut_78, 
    'x_update_agent_context__mutmut_79': x_update_agent_context__mutmut_79, 
    'x_update_agent_context__mutmut_80': x_update_agent_context__mutmut_80, 
    'x_update_agent_context__mutmut_81': x_update_agent_context__mutmut_81, 
    'x_update_agent_context__mutmut_82': x_update_agent_context__mutmut_82, 
    'x_update_agent_context__mutmut_83': x_update_agent_context__mutmut_83, 
    'x_update_agent_context__mutmut_84': x_update_agent_context__mutmut_84, 
    'x_update_agent_context__mutmut_85': x_update_agent_context__mutmut_85, 
    'x_update_agent_context__mutmut_86': x_update_agent_context__mutmut_86, 
    'x_update_agent_context__mutmut_87': x_update_agent_context__mutmut_87, 
    'x_update_agent_context__mutmut_88': x_update_agent_context__mutmut_88, 
    'x_update_agent_context__mutmut_89': x_update_agent_context__mutmut_89, 
    'x_update_agent_context__mutmut_90': x_update_agent_context__mutmut_90, 
    'x_update_agent_context__mutmut_91': x_update_agent_context__mutmut_91, 
    'x_update_agent_context__mutmut_92': x_update_agent_context__mutmut_92, 
    'x_update_agent_context__mutmut_93': x_update_agent_context__mutmut_93, 
    'x_update_agent_context__mutmut_94': x_update_agent_context__mutmut_94, 
    'x_update_agent_context__mutmut_95': x_update_agent_context__mutmut_95, 
    'x_update_agent_context__mutmut_96': x_update_agent_context__mutmut_96, 
    'x_update_agent_context__mutmut_97': x_update_agent_context__mutmut_97, 
    'x_update_agent_context__mutmut_98': x_update_agent_context__mutmut_98, 
    'x_update_agent_context__mutmut_99': x_update_agent_context__mutmut_99, 
    'x_update_agent_context__mutmut_100': x_update_agent_context__mutmut_100, 
    'x_update_agent_context__mutmut_101': x_update_agent_context__mutmut_101, 
    'x_update_agent_context__mutmut_102': x_update_agent_context__mutmut_102, 
    'x_update_agent_context__mutmut_103': x_update_agent_context__mutmut_103, 
    'x_update_agent_context__mutmut_104': x_update_agent_context__mutmut_104, 
    'x_update_agent_context__mutmut_105': x_update_agent_context__mutmut_105, 
    'x_update_agent_context__mutmut_106': x_update_agent_context__mutmut_106, 
    'x_update_agent_context__mutmut_107': x_update_agent_context__mutmut_107, 
    'x_update_agent_context__mutmut_108': x_update_agent_context__mutmut_108, 
    'x_update_agent_context__mutmut_109': x_update_agent_context__mutmut_109, 
    'x_update_agent_context__mutmut_110': x_update_agent_context__mutmut_110, 
    'x_update_agent_context__mutmut_111': x_update_agent_context__mutmut_111, 
    'x_update_agent_context__mutmut_112': x_update_agent_context__mutmut_112, 
    'x_update_agent_context__mutmut_113': x_update_agent_context__mutmut_113, 
    'x_update_agent_context__mutmut_114': x_update_agent_context__mutmut_114, 
    'x_update_agent_context__mutmut_115': x_update_agent_context__mutmut_115, 
    'x_update_agent_context__mutmut_116': x_update_agent_context__mutmut_116, 
    'x_update_agent_context__mutmut_117': x_update_agent_context__mutmut_117, 
    'x_update_agent_context__mutmut_118': x_update_agent_context__mutmut_118, 
    'x_update_agent_context__mutmut_119': x_update_agent_context__mutmut_119, 
    'x_update_agent_context__mutmut_120': x_update_agent_context__mutmut_120, 
    'x_update_agent_context__mutmut_121': x_update_agent_context__mutmut_121, 
    'x_update_agent_context__mutmut_122': x_update_agent_context__mutmut_122, 
    'x_update_agent_context__mutmut_123': x_update_agent_context__mutmut_123, 
    'x_update_agent_context__mutmut_124': x_update_agent_context__mutmut_124, 
    'x_update_agent_context__mutmut_125': x_update_agent_context__mutmut_125, 
    'x_update_agent_context__mutmut_126': x_update_agent_context__mutmut_126, 
    'x_update_agent_context__mutmut_127': x_update_agent_context__mutmut_127, 
    'x_update_agent_context__mutmut_128': x_update_agent_context__mutmut_128, 
    'x_update_agent_context__mutmut_129': x_update_agent_context__mutmut_129, 
    'x_update_agent_context__mutmut_130': x_update_agent_context__mutmut_130, 
    'x_update_agent_context__mutmut_131': x_update_agent_context__mutmut_131, 
    'x_update_agent_context__mutmut_132': x_update_agent_context__mutmut_132, 
    'x_update_agent_context__mutmut_133': x_update_agent_context__mutmut_133, 
    'x_update_agent_context__mutmut_134': x_update_agent_context__mutmut_134, 
    'x_update_agent_context__mutmut_135': x_update_agent_context__mutmut_135, 
    'x_update_agent_context__mutmut_136': x_update_agent_context__mutmut_136, 
    'x_update_agent_context__mutmut_137': x_update_agent_context__mutmut_137, 
    'x_update_agent_context__mutmut_138': x_update_agent_context__mutmut_138, 
    'x_update_agent_context__mutmut_139': x_update_agent_context__mutmut_139, 
    'x_update_agent_context__mutmut_140': x_update_agent_context__mutmut_140, 
    'x_update_agent_context__mutmut_141': x_update_agent_context__mutmut_141, 
    'x_update_agent_context__mutmut_142': x_update_agent_context__mutmut_142, 
    'x_update_agent_context__mutmut_143': x_update_agent_context__mutmut_143, 
    'x_update_agent_context__mutmut_144': x_update_agent_context__mutmut_144, 
    'x_update_agent_context__mutmut_145': x_update_agent_context__mutmut_145, 
    'x_update_agent_context__mutmut_146': x_update_agent_context__mutmut_146, 
    'x_update_agent_context__mutmut_147': x_update_agent_context__mutmut_147, 
    'x_update_agent_context__mutmut_148': x_update_agent_context__mutmut_148, 
    'x_update_agent_context__mutmut_149': x_update_agent_context__mutmut_149, 
    'x_update_agent_context__mutmut_150': x_update_agent_context__mutmut_150, 
    'x_update_agent_context__mutmut_151': x_update_agent_context__mutmut_151, 
    'x_update_agent_context__mutmut_152': x_update_agent_context__mutmut_152, 
    'x_update_agent_context__mutmut_153': x_update_agent_context__mutmut_153, 
    'x_update_agent_context__mutmut_154': x_update_agent_context__mutmut_154, 
    'x_update_agent_context__mutmut_155': x_update_agent_context__mutmut_155, 
    'x_update_agent_context__mutmut_156': x_update_agent_context__mutmut_156, 
    'x_update_agent_context__mutmut_157': x_update_agent_context__mutmut_157, 
    'x_update_agent_context__mutmut_158': x_update_agent_context__mutmut_158, 
    'x_update_agent_context__mutmut_159': x_update_agent_context__mutmut_159, 
    'x_update_agent_context__mutmut_160': x_update_agent_context__mutmut_160, 
    'x_update_agent_context__mutmut_161': x_update_agent_context__mutmut_161
}
x_update_agent_context__mutmut_orig.__name__ = 'x_update_agent_context'


def get_supported_agent_types() -> List[str]:
    args = []# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x_get_supported_agent_types__mutmut_orig, x_get_supported_agent_types__mutmut_mutants, args, kwargs, None)


def x_get_supported_agent_types__mutmut_orig() -> List[str]:
    """Return list of supported agent types."""
    return list(AGENT_CONFIGS.keys())


def x_get_supported_agent_types__mutmut_1() -> List[str]:
    """Return list of supported agent types."""
    return list(None)

x_get_supported_agent_types__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x_get_supported_agent_types__mutmut_1': x_get_supported_agent_types__mutmut_1
}
x_get_supported_agent_types__mutmut_orig.__name__ = 'x_get_supported_agent_types'


def get_agent_file_path(agent_type: str, repo_root: Path) -> Path:
    args = [agent_type, repo_root]# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x_get_agent_file_path__mutmut_orig, x_get_agent_file_path__mutmut_mutants, args, kwargs, None)


def x_get_agent_file_path__mutmut_orig(agent_type: str, repo_root: Path) -> Path:
    """
    Get the file path for a specific agent type.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS
        repo_root: Repository root directory

    Returns:
        Path to the agent configuration file

    Raises:
        ValueError: If agent_type is not supported
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    return repo_root / AGENT_CONFIGS[agent_type]


def x_get_agent_file_path__mutmut_1(agent_type: str, repo_root: Path) -> Path:
    """
    Get the file path for a specific agent type.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS
        repo_root: Repository root directory

    Returns:
        Path to the agent configuration file

    Raises:
        ValueError: If agent_type is not supported
    """
    if agent_type in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    return repo_root / AGENT_CONFIGS[agent_type]


def x_get_agent_file_path__mutmut_2(agent_type: str, repo_root: Path) -> Path:
    """
    Get the file path for a specific agent type.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS
        repo_root: Repository root directory

    Returns:
        Path to the agent configuration file

    Raises:
        ValueError: If agent_type is not supported
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            None
        )

    return repo_root / AGENT_CONFIGS[agent_type]


def x_get_agent_file_path__mutmut_3(agent_type: str, repo_root: Path) -> Path:
    """
    Get the file path for a specific agent type.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS
        repo_root: Repository root directory

    Returns:
        Path to the agent configuration file

    Raises:
        ValueError: If agent_type is not supported
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(None)}"
        )

    return repo_root / AGENT_CONFIGS[agent_type]


def x_get_agent_file_path__mutmut_4(agent_type: str, repo_root: Path) -> Path:
    """
    Get the file path for a specific agent type.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS
        repo_root: Repository root directory

    Returns:
        Path to the agent configuration file

    Raises:
        ValueError: If agent_type is not supported
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {'XX, XX'.join(AGENT_CONFIGS.keys())}"
        )

    return repo_root / AGENT_CONFIGS[agent_type]


def x_get_agent_file_path__mutmut_5(agent_type: str, repo_root: Path) -> Path:
    """
    Get the file path for a specific agent type.

    Args:
        agent_type: One of the keys in AGENT_CONFIGS
        repo_root: Repository root directory

    Returns:
        Path to the agent configuration file

    Raises:
        ValueError: If agent_type is not supported
    """
    if agent_type not in AGENT_CONFIGS:
        raise ValueError(
            f"Unsupported agent type: {agent_type}. "
            f"Supported types: {', '.join(AGENT_CONFIGS.keys())}"
        )

    return repo_root * AGENT_CONFIGS[agent_type]

x_get_agent_file_path__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x_get_agent_file_path__mutmut_1': x_get_agent_file_path__mutmut_1, 
    'x_get_agent_file_path__mutmut_2': x_get_agent_file_path__mutmut_2, 
    'x_get_agent_file_path__mutmut_3': x_get_agent_file_path__mutmut_3, 
    'x_get_agent_file_path__mutmut_4': x_get_agent_file_path__mutmut_4, 
    'x_get_agent_file_path__mutmut_5': x_get_agent_file_path__mutmut_5
}
x_get_agent_file_path__mutmut_orig.__name__ = 'x_get_agent_file_path'
