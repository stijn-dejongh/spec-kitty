"""Validation engine for the canonical status model.

Provides schema validation, transition legality checks, done-evidence
completeness verification, materialization drift detection, and
derived-view drift comparison. Each function returns a list of
human-readable finding strings that reference specific event_ids.

This module is a library -- it reports problems but never modifies data.
"""

from __future__ import annotations

import json
import re
from dataclasses import dataclass, field
from datetime import datetime
from pathlib import Path

from specify_cli.spec_kitty_events import normalize_event_id

from .models import ULID_PATTERN
from .transitions import ALLOWED_TRANSITIONS, CANONICAL_LANES

STATUS_BLOCK_START = "<!-- status-model:start -->"
STATUS_BLOCK_END = "<!-- status-model:end -->"
from typing import Annotated
from typing import Callable
from typing import ClassVar

MutantDict = Annotated[dict[str, Callable], "Mutant"] # type: ignore


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None): # type: ignore
    """Forward call to original or mutated function, depending on the environment"""
    import os # type: ignore
    mutant_under_test = os.environ['MUTANT_UNDER_TEST'] # type: ignore
    if mutant_under_test == 'fail': # type: ignore
        from mutmut.__main__ import MutmutProgrammaticFailException # type: ignore
        raise MutmutProgrammaticFailException('Failed programmatically')       # type: ignore
    elif mutant_under_test == 'stats': # type: ignore
        from mutmut.__main__ import record_trampoline_hit # type: ignore
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__) # type: ignore
        # (for class methods, orig is bound and thus does not need the explicit self argument)
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_' # type: ignore
    if not mutant_under_test.startswith(prefix): # type: ignore
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    mutant_name = mutant_under_test.rpartition('.')[-1] # type: ignore
    if self_arg is not None: # type: ignore
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs) # type: ignore
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs) # type: ignore
    return result # type: ignore


@dataclass
class ValidationResult:
    """Aggregate result of all validation checks."""

    errors: list[str] = field(default_factory=list)
    warnings: list[str] = field(default_factory=list)
    phase_source: str = ""

    @property
    def passed(self) -> bool:
        return len(self.errors) == 0


def validate_event_schema(event: dict) -> list[str]:
    args = [event]# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x_validate_event_schema__mutmut_orig, x_validate_event_schema__mutmut_mutants, args, kwargs, None)


def x_validate_event_schema__mutmut_orig(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_1(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = None
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_2(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = None
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_3(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "XXevent_idXX",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_4(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "EVENT_ID",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_5(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "XXfeature_slugXX",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_6(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "FEATURE_SLUG",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_7(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "XXwp_idXX",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_8(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "WP_ID",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_9(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "XXfrom_laneXX",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_10(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "FROM_LANE",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_11(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "XXto_laneXX",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_12(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "TO_LANE",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_13(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "XXatXX",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_14(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "AT",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_15(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "XXactorXX",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_16(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "ACTOR",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_17(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "XXforceXX",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_18(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "FORCE",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_19(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "XXexecution_modeXX",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_20(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "EXECUTION_MODE",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_21(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_22(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(None)

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_23(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = None
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_24(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get(None, "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_25(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", None)
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_26(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_27(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", )
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_28(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("XXevent_idXX", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_29(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("EVENT_ID", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_30(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "XXXX")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_31(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id or not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_32(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_33(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(None):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_34(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(None)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_35(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(None)

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_36(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = None
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_37(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(None)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_38(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("XXfrom_laneXX", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_39(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("FROM_LANE", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_40(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "XXto_laneXX"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_41(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "TO_LANE"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_42(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = None
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_43(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(None)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_44(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None or val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_45(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_46(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_47(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(None)

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_48(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = None
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_49(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get(None)
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_50(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("XXatXX")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_51(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("AT")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_52(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_53(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_54(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(None):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_55(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(None)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_56(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(None)

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_57(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = None
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_58(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get(None)
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_59(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("XXforceXX")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_60(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("FORCE")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_61(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None or not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_62(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_63(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_64(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(None)

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_65(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(None).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_66(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = None
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_67(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get(None)
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_68(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("XXexecution_modeXX")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_69(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("EXECUTION_MODE")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_70(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None or exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_71(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_72(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_73(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("XXworktreeXX", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_74(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("WORKTREE", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_75(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "XXdirect_repoXX"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_76(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "DIRECT_REPO"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_77(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            None
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_78(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True or not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_79(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get(None) is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_80(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("XXforceXX") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_81(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("FORCE") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_82(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is not True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_83(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is False and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_84(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_85(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get(None):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_86(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("XXreasonXX"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_87(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("REASON"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_88(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(None)

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_89(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review" or event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_90(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get(None) == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_91(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("XXfrom_laneXX") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_92(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("FROM_LANE") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_93(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") != "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_94(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "XXfor_reviewXX"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_95(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "FOR_REVIEW"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_96(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get(None) == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_97(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("XXto_laneXX") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_98(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("TO_LANE") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_99(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") != "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_100(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "XXin_progressXX"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_101(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "IN_PROGRESS"
    ):
        if not event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_102(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if event.get("review_ref"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_103(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get(None):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_104(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("XXreview_refXX"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_105(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("REVIEW_REF"):
            findings.append(
                f"Event {event_id}: for_review->in_progress without review_ref"
            )

    return findings


def x_validate_event_schema__mutmut_106(event: dict) -> list[str]:
    """Validate a single event dict against the StatusEvent schema.

    Checks:
    - All required fields present: event_id, feature_slug, wp_id,
      from_lane, to_lane, at, actor, force, execution_mode
    - event_id is valid event ID (ULID Crockford base32, or UUID)
    - from_lane and to_lane are canonical lane values (never aliases)
    - at is valid ISO 8601 timestamp
    - force is boolean
    - execution_mode is "worktree" or "direct_repo"
    - If force=True, reason must be non-empty string
    - If from_lane=for_review and to_lane=in_progress, review_ref must be present

    Extra unknown fields are NOT flagged (forward-compatible).
    """
    findings: list[str] = []
    required_fields = [
        "event_id",
        "feature_slug",
        "wp_id",
        "from_lane",
        "to_lane",
        "at",
        "actor",
        "force",
        "execution_mode",
    ]
    for f in required_fields:
        if f not in event:
            findings.append(f"Missing required field: {f}")

    # ULID format check
    event_id = event.get("event_id", "")
    if event_id and not _is_valid_event_id(str(event_id)):
        findings.append(f"Invalid event ID format: {event_id}")

    # Canonical lane check (aliases like "doing" are NOT canonical)
    canonical_set = set(CANONICAL_LANES)
    for lane_field in ("from_lane", "to_lane"):
        val = event.get(lane_field)
        if val is not None and val not in canonical_set:
            findings.append(f"{lane_field} is not canonical: {val}")

    # ISO 8601 timestamp check
    at_val = event.get("at")
    if at_val is not None:
        if not _is_valid_iso8601(str(at_val)):
            findings.append(f"Event {event_id}: invalid ISO 8601 timestamp: {at_val}")

    # force must be boolean
    force_val = event.get("force")
    if force_val is not None and not isinstance(force_val, bool):
        findings.append(f"Event {event_id}: force must be boolean, got {type(force_val).__name__}")

    # execution_mode check
    exec_mode = event.get("execution_mode")
    if exec_mode is not None and exec_mode not in ("worktree", "direct_repo"):
        findings.append(
            f"Event {event_id}: execution_mode must be 'worktree' or 'direct_repo', got '{exec_mode}'"
        )

    # Force audit check: force=true requires reason
    if event.get("force") is True and not event.get("reason"):
        findings.append(f"Event {event_id}: force=true without reason")

    # Review ref check: for_review -> in_progress requires review_ref
    if (
        event.get("from_lane") == "for_review"
        and event.get("to_lane") == "in_progress"
    ):
        if not event.get("review_ref"):
            findings.append(
                None
            )

    return findings

x_validate_event_schema__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x_validate_event_schema__mutmut_1': x_validate_event_schema__mutmut_1, 
    'x_validate_event_schema__mutmut_2': x_validate_event_schema__mutmut_2, 
    'x_validate_event_schema__mutmut_3': x_validate_event_schema__mutmut_3, 
    'x_validate_event_schema__mutmut_4': x_validate_event_schema__mutmut_4, 
    'x_validate_event_schema__mutmut_5': x_validate_event_schema__mutmut_5, 
    'x_validate_event_schema__mutmut_6': x_validate_event_schema__mutmut_6, 
    'x_validate_event_schema__mutmut_7': x_validate_event_schema__mutmut_7, 
    'x_validate_event_schema__mutmut_8': x_validate_event_schema__mutmut_8, 
    'x_validate_event_schema__mutmut_9': x_validate_event_schema__mutmut_9, 
    'x_validate_event_schema__mutmut_10': x_validate_event_schema__mutmut_10, 
    'x_validate_event_schema__mutmut_11': x_validate_event_schema__mutmut_11, 
    'x_validate_event_schema__mutmut_12': x_validate_event_schema__mutmut_12, 
    'x_validate_event_schema__mutmut_13': x_validate_event_schema__mutmut_13, 
    'x_validate_event_schema__mutmut_14': x_validate_event_schema__mutmut_14, 
    'x_validate_event_schema__mutmut_15': x_validate_event_schema__mutmut_15, 
    'x_validate_event_schema__mutmut_16': x_validate_event_schema__mutmut_16, 
    'x_validate_event_schema__mutmut_17': x_validate_event_schema__mutmut_17, 
    'x_validate_event_schema__mutmut_18': x_validate_event_schema__mutmut_18, 
    'x_validate_event_schema__mutmut_19': x_validate_event_schema__mutmut_19, 
    'x_validate_event_schema__mutmut_20': x_validate_event_schema__mutmut_20, 
    'x_validate_event_schema__mutmut_21': x_validate_event_schema__mutmut_21, 
    'x_validate_event_schema__mutmut_22': x_validate_event_schema__mutmut_22, 
    'x_validate_event_schema__mutmut_23': x_validate_event_schema__mutmut_23, 
    'x_validate_event_schema__mutmut_24': x_validate_event_schema__mutmut_24, 
    'x_validate_event_schema__mutmut_25': x_validate_event_schema__mutmut_25, 
    'x_validate_event_schema__mutmut_26': x_validate_event_schema__mutmut_26, 
    'x_validate_event_schema__mutmut_27': x_validate_event_schema__mutmut_27, 
    'x_validate_event_schema__mutmut_28': x_validate_event_schema__mutmut_28, 
    'x_validate_event_schema__mutmut_29': x_validate_event_schema__mutmut_29, 
    'x_validate_event_schema__mutmut_30': x_validate_event_schema__mutmut_30, 
    'x_validate_event_schema__mutmut_31': x_validate_event_schema__mutmut_31, 
    'x_validate_event_schema__mutmut_32': x_validate_event_schema__mutmut_32, 
    'x_validate_event_schema__mutmut_33': x_validate_event_schema__mutmut_33, 
    'x_validate_event_schema__mutmut_34': x_validate_event_schema__mutmut_34, 
    'x_validate_event_schema__mutmut_35': x_validate_event_schema__mutmut_35, 
    'x_validate_event_schema__mutmut_36': x_validate_event_schema__mutmut_36, 
    'x_validate_event_schema__mutmut_37': x_validate_event_schema__mutmut_37, 
    'x_validate_event_schema__mutmut_38': x_validate_event_schema__mutmut_38, 
    'x_validate_event_schema__mutmut_39': x_validate_event_schema__mutmut_39, 
    'x_validate_event_schema__mutmut_40': x_validate_event_schema__mutmut_40, 
    'x_validate_event_schema__mutmut_41': x_validate_event_schema__mutmut_41, 
    'x_validate_event_schema__mutmut_42': x_validate_event_schema__mutmut_42, 
    'x_validate_event_schema__mutmut_43': x_validate_event_schema__mutmut_43, 
    'x_validate_event_schema__mutmut_44': x_validate_event_schema__mutmut_44, 
    'x_validate_event_schema__mutmut_45': x_validate_event_schema__mutmut_45, 
    'x_validate_event_schema__mutmut_46': x_validate_event_schema__mutmut_46, 
    'x_validate_event_schema__mutmut_47': x_validate_event_schema__mutmut_47, 
    'x_validate_event_schema__mutmut_48': x_validate_event_schema__mutmut_48, 
    'x_validate_event_schema__mutmut_49': x_validate_event_schema__mutmut_49, 
    'x_validate_event_schema__mutmut_50': x_validate_event_schema__mutmut_50, 
    'x_validate_event_schema__mutmut_51': x_validate_event_schema__mutmut_51, 
    'x_validate_event_schema__mutmut_52': x_validate_event_schema__mutmut_52, 
    'x_validate_event_schema__mutmut_53': x_validate_event_schema__mutmut_53, 
    'x_validate_event_schema__mutmut_54': x_validate_event_schema__mutmut_54, 
    'x_validate_event_schema__mutmut_55': x_validate_event_schema__mutmut_55, 
    'x_validate_event_schema__mutmut_56': x_validate_event_schema__mutmut_56, 
    'x_validate_event_schema__mutmut_57': x_validate_event_schema__mutmut_57, 
    'x_validate_event_schema__mutmut_58': x_validate_event_schema__mutmut_58, 
    'x_validate_event_schema__mutmut_59': x_validate_event_schema__mutmut_59, 
    'x_validate_event_schema__mutmut_60': x_validate_event_schema__mutmut_60, 
    'x_validate_event_schema__mutmut_61': x_validate_event_schema__mutmut_61, 
    'x_validate_event_schema__mutmut_62': x_validate_event_schema__mutmut_62, 
    'x_validate_event_schema__mutmut_63': x_validate_event_schema__mutmut_63, 
    'x_validate_event_schema__mutmut_64': x_validate_event_schema__mutmut_64, 
    'x_validate_event_schema__mutmut_65': x_validate_event_schema__mutmut_65, 
    'x_validate_event_schema__mutmut_66': x_validate_event_schema__mutmut_66, 
    'x_validate_event_schema__mutmut_67': x_validate_event_schema__mutmut_67, 
    'x_validate_event_schema__mutmut_68': x_validate_event_schema__mutmut_68, 
    'x_validate_event_schema__mutmut_69': x_validate_event_schema__mutmut_69, 
    'x_validate_event_schema__mutmut_70': x_validate_event_schema__mutmut_70, 
    'x_validate_event_schema__mutmut_71': x_validate_event_schema__mutmut_71, 
    'x_validate_event_schema__mutmut_72': x_validate_event_schema__mutmut_72, 
    'x_validate_event_schema__mutmut_73': x_validate_event_schema__mutmut_73, 
    'x_validate_event_schema__mutmut_74': x_validate_event_schema__mutmut_74, 
    'x_validate_event_schema__mutmut_75': x_validate_event_schema__mutmut_75, 
    'x_validate_event_schema__mutmut_76': x_validate_event_schema__mutmut_76, 
    'x_validate_event_schema__mutmut_77': x_validate_event_schema__mutmut_77, 
    'x_validate_event_schema__mutmut_78': x_validate_event_schema__mutmut_78, 
    'x_validate_event_schema__mutmut_79': x_validate_event_schema__mutmut_79, 
    'x_validate_event_schema__mutmut_80': x_validate_event_schema__mutmut_80, 
    'x_validate_event_schema__mutmut_81': x_validate_event_schema__mutmut_81, 
    'x_validate_event_schema__mutmut_82': x_validate_event_schema__mutmut_82, 
    'x_validate_event_schema__mutmut_83': x_validate_event_schema__mutmut_83, 
    'x_validate_event_schema__mutmut_84': x_validate_event_schema__mutmut_84, 
    'x_validate_event_schema__mutmut_85': x_validate_event_schema__mutmut_85, 
    'x_validate_event_schema__mutmut_86': x_validate_event_schema__mutmut_86, 
    'x_validate_event_schema__mutmut_87': x_validate_event_schema__mutmut_87, 
    'x_validate_event_schema__mutmut_88': x_validate_event_schema__mutmut_88, 
    'x_validate_event_schema__mutmut_89': x_validate_event_schema__mutmut_89, 
    'x_validate_event_schema__mutmut_90': x_validate_event_schema__mutmut_90, 
    'x_validate_event_schema__mutmut_91': x_validate_event_schema__mutmut_91, 
    'x_validate_event_schema__mutmut_92': x_validate_event_schema__mutmut_92, 
    'x_validate_event_schema__mutmut_93': x_validate_event_schema__mutmut_93, 
    'x_validate_event_schema__mutmut_94': x_validate_event_schema__mutmut_94, 
    'x_validate_event_schema__mutmut_95': x_validate_event_schema__mutmut_95, 
    'x_validate_event_schema__mutmut_96': x_validate_event_schema__mutmut_96, 
    'x_validate_event_schema__mutmut_97': x_validate_event_schema__mutmut_97, 
    'x_validate_event_schema__mutmut_98': x_validate_event_schema__mutmut_98, 
    'x_validate_event_schema__mutmut_99': x_validate_event_schema__mutmut_99, 
    'x_validate_event_schema__mutmut_100': x_validate_event_schema__mutmut_100, 
    'x_validate_event_schema__mutmut_101': x_validate_event_schema__mutmut_101, 
    'x_validate_event_schema__mutmut_102': x_validate_event_schema__mutmut_102, 
    'x_validate_event_schema__mutmut_103': x_validate_event_schema__mutmut_103, 
    'x_validate_event_schema__mutmut_104': x_validate_event_schema__mutmut_104, 
    'x_validate_event_schema__mutmut_105': x_validate_event_schema__mutmut_105, 
    'x_validate_event_schema__mutmut_106': x_validate_event_schema__mutmut_106
}
x_validate_event_schema__mutmut_orig.__name__ = 'x_validate_event_schema'


def validate_transition_legality(events: list[dict]) -> list[str]:
    args = [events]# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x_validate_transition_legality__mutmut_orig, x_validate_transition_legality__mutmut_mutants, args, kwargs, None)


def x_validate_transition_legality__mutmut_orig(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_1(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = None

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_2(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = None

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_3(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        None, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_4(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=None
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_5(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_6(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_7(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: None
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_8(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get(None, ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_9(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", None), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_10(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get(""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_11(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_12(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("XXatXX", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_13(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("AT", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_14(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", "XXXX"), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_15(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get(None, ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_16(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", None))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_17(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get(""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_18(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_19(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("XXevent_idXX", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_20(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("EVENT_ID", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_21(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", "XXXX"))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_22(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = None
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_23(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get(None)
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_24(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("XXfrom_laneXX")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_25(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("FROM_LANE")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_26(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = None
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_27(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get(None)
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_28(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("XXto_laneXX")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_29(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("TO_LANE")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_30(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = None
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_31(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get(None, "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_32(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", None)
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_33(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_34(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", )
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_35(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("XXevent_idXX", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_36(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("EVENT_ID", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_37(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "XXunknownXX")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_38(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "UNKNOWN")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_39(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = None

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_40(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get(None, False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_41(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", None)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_42(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get(False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_43(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", )

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_44(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("XXforceXX", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_45(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("FORCE", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_46(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", True)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_47(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None and to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_48(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is not None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_49(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is not None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_50(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            break

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_51(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            break

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_52(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) in ALLOWED_TRANSITIONS:
            findings.append(
                f"Event {event_id}: illegal transition {from_lane} -> {to_lane}"
            )

    return findings


def x_validate_transition_legality__mutmut_53(events: list[dict]) -> list[str]:
    """Replay events in order and check each transition is legal.

    Uses ALLOWED_TRANSITIONS from status/transitions.py.
    Force transitions are always legal (skipped).
    Events are sorted by (at, event_id) before checking.

    Missing from_lane or to_lane is skipped (caught by schema validator).
    """
    findings: list[str] = []

    # Sort by (at, event_id) to ensure correct replay order
    sorted_events = sorted(
        events, key=lambda e: (e.get("at", ""), e.get("event_id", ""))
    )

    for event in sorted_events:
        from_lane = event.get("from_lane")
        to_lane = event.get("to_lane")
        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        # Skip events with missing lane fields (schema validator catches these)
        if from_lane is None or to_lane is None:
            continue

        if force:
            # Forced transitions are legal but noteworthy
            continue

        if (from_lane, to_lane) not in ALLOWED_TRANSITIONS:
            findings.append(
                None
            )

    return findings

x_validate_transition_legality__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x_validate_transition_legality__mutmut_1': x_validate_transition_legality__mutmut_1, 
    'x_validate_transition_legality__mutmut_2': x_validate_transition_legality__mutmut_2, 
    'x_validate_transition_legality__mutmut_3': x_validate_transition_legality__mutmut_3, 
    'x_validate_transition_legality__mutmut_4': x_validate_transition_legality__mutmut_4, 
    'x_validate_transition_legality__mutmut_5': x_validate_transition_legality__mutmut_5, 
    'x_validate_transition_legality__mutmut_6': x_validate_transition_legality__mutmut_6, 
    'x_validate_transition_legality__mutmut_7': x_validate_transition_legality__mutmut_7, 
    'x_validate_transition_legality__mutmut_8': x_validate_transition_legality__mutmut_8, 
    'x_validate_transition_legality__mutmut_9': x_validate_transition_legality__mutmut_9, 
    'x_validate_transition_legality__mutmut_10': x_validate_transition_legality__mutmut_10, 
    'x_validate_transition_legality__mutmut_11': x_validate_transition_legality__mutmut_11, 
    'x_validate_transition_legality__mutmut_12': x_validate_transition_legality__mutmut_12, 
    'x_validate_transition_legality__mutmut_13': x_validate_transition_legality__mutmut_13, 
    'x_validate_transition_legality__mutmut_14': x_validate_transition_legality__mutmut_14, 
    'x_validate_transition_legality__mutmut_15': x_validate_transition_legality__mutmut_15, 
    'x_validate_transition_legality__mutmut_16': x_validate_transition_legality__mutmut_16, 
    'x_validate_transition_legality__mutmut_17': x_validate_transition_legality__mutmut_17, 
    'x_validate_transition_legality__mutmut_18': x_validate_transition_legality__mutmut_18, 
    'x_validate_transition_legality__mutmut_19': x_validate_transition_legality__mutmut_19, 
    'x_validate_transition_legality__mutmut_20': x_validate_transition_legality__mutmut_20, 
    'x_validate_transition_legality__mutmut_21': x_validate_transition_legality__mutmut_21, 
    'x_validate_transition_legality__mutmut_22': x_validate_transition_legality__mutmut_22, 
    'x_validate_transition_legality__mutmut_23': x_validate_transition_legality__mutmut_23, 
    'x_validate_transition_legality__mutmut_24': x_validate_transition_legality__mutmut_24, 
    'x_validate_transition_legality__mutmut_25': x_validate_transition_legality__mutmut_25, 
    'x_validate_transition_legality__mutmut_26': x_validate_transition_legality__mutmut_26, 
    'x_validate_transition_legality__mutmut_27': x_validate_transition_legality__mutmut_27, 
    'x_validate_transition_legality__mutmut_28': x_validate_transition_legality__mutmut_28, 
    'x_validate_transition_legality__mutmut_29': x_validate_transition_legality__mutmut_29, 
    'x_validate_transition_legality__mutmut_30': x_validate_transition_legality__mutmut_30, 
    'x_validate_transition_legality__mutmut_31': x_validate_transition_legality__mutmut_31, 
    'x_validate_transition_legality__mutmut_32': x_validate_transition_legality__mutmut_32, 
    'x_validate_transition_legality__mutmut_33': x_validate_transition_legality__mutmut_33, 
    'x_validate_transition_legality__mutmut_34': x_validate_transition_legality__mutmut_34, 
    'x_validate_transition_legality__mutmut_35': x_validate_transition_legality__mutmut_35, 
    'x_validate_transition_legality__mutmut_36': x_validate_transition_legality__mutmut_36, 
    'x_validate_transition_legality__mutmut_37': x_validate_transition_legality__mutmut_37, 
    'x_validate_transition_legality__mutmut_38': x_validate_transition_legality__mutmut_38, 
    'x_validate_transition_legality__mutmut_39': x_validate_transition_legality__mutmut_39, 
    'x_validate_transition_legality__mutmut_40': x_validate_transition_legality__mutmut_40, 
    'x_validate_transition_legality__mutmut_41': x_validate_transition_legality__mutmut_41, 
    'x_validate_transition_legality__mutmut_42': x_validate_transition_legality__mutmut_42, 
    'x_validate_transition_legality__mutmut_43': x_validate_transition_legality__mutmut_43, 
    'x_validate_transition_legality__mutmut_44': x_validate_transition_legality__mutmut_44, 
    'x_validate_transition_legality__mutmut_45': x_validate_transition_legality__mutmut_45, 
    'x_validate_transition_legality__mutmut_46': x_validate_transition_legality__mutmut_46, 
    'x_validate_transition_legality__mutmut_47': x_validate_transition_legality__mutmut_47, 
    'x_validate_transition_legality__mutmut_48': x_validate_transition_legality__mutmut_48, 
    'x_validate_transition_legality__mutmut_49': x_validate_transition_legality__mutmut_49, 
    'x_validate_transition_legality__mutmut_50': x_validate_transition_legality__mutmut_50, 
    'x_validate_transition_legality__mutmut_51': x_validate_transition_legality__mutmut_51, 
    'x_validate_transition_legality__mutmut_52': x_validate_transition_legality__mutmut_52, 
    'x_validate_transition_legality__mutmut_53': x_validate_transition_legality__mutmut_53
}
x_validate_transition_legality__mutmut_orig.__name__ = 'x_validate_transition_legality'


def validate_done_evidence(events: list[dict]) -> list[str]:
    args = [events]# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x_validate_done_evidence__mutmut_orig, x_validate_done_evidence__mutmut_mutants, args, kwargs, None)


def x_validate_done_evidence__mutmut_orig(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_1(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = None

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_2(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get(None) != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_3(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("XXto_laneXX") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_4(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("TO_LANE") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_5(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") == "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_6(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "XXdoneXX":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_7(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "DONE":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_8(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            break

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_9(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = None
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_10(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get(None, "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_11(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", None)
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_12(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_13(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", )
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_14(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("XXevent_idXX", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_15(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("EVENT_ID", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_16(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "XXunknownXX")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_17(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "UNKNOWN")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_18(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = None

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_19(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get(None, False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_20(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", None)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_21(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get(False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_22(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", )

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_23(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("XXforceXX", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_24(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("FORCE", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_25(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", True)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_26(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            break  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_27(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = None
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_28(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get(None)
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_29(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("XXevidenceXX")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_30(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("EVIDENCE")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_31(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_32(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                None
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_33(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            break

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_34(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_35(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                None
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_36(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            break

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_37(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = None
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_38(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get(None)
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_39(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("XXreviewXX")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_40(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("REVIEW")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_41(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_42(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                None
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_43(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_44(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                None
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_45(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_46(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get(None):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_47(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("XXreviewerXX"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_48(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("REVIEWER"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_49(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    None
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_50(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_51(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get(None):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_52(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("XXverdictXX"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_53(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("VERDICT"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_54(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    None
                )
            if not review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_55(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if review.get("reference"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_56(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get(None):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_57(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("XXreferenceXX"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_58(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("REFERENCE"):
                findings.append(
                    f"Event {event_id}: done evidence missing approval reference"
                )

    return findings


def x_validate_done_evidence__mutmut_59(events: list[dict]) -> list[str]:
    """Check all done-transitions have evidence or force flag.

    Events not transitioning to done are skipped.
    Force-flagged done transitions bypass the evidence requirement.
    """
    findings: list[str] = []

    for event in events:
        if event.get("to_lane") != "done":
            continue

        event_id = event.get("event_id", "unknown")
        force = event.get("force", False)

        if force:
            continue  # Forced done transitions bypass evidence requirement

        evidence = event.get("evidence")
        if not evidence:
            findings.append(
                f"Event {event_id}: done without evidence (not forced)"
            )
            continue

        # Check evidence structure
        if not isinstance(evidence, dict):
            findings.append(
                f"Event {event_id}: done evidence is not a dict"
            )
            continue

        review = evidence.get("review")
        if not review:
            findings.append(
                f"Event {event_id}: done evidence missing review section"
            )
        elif not isinstance(review, dict):
            findings.append(
                f"Event {event_id}: done evidence review is not a dict"
            )
        else:
            if not review.get("reviewer"):
                findings.append(
                    f"Event {event_id}: done evidence missing reviewer identity"
                )
            if not review.get("verdict"):
                findings.append(
                    f"Event {event_id}: done evidence missing verdict"
                )
            if not review.get("reference"):
                findings.append(
                    None
                )

    return findings

x_validate_done_evidence__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x_validate_done_evidence__mutmut_1': x_validate_done_evidence__mutmut_1, 
    'x_validate_done_evidence__mutmut_2': x_validate_done_evidence__mutmut_2, 
    'x_validate_done_evidence__mutmut_3': x_validate_done_evidence__mutmut_3, 
    'x_validate_done_evidence__mutmut_4': x_validate_done_evidence__mutmut_4, 
    'x_validate_done_evidence__mutmut_5': x_validate_done_evidence__mutmut_5, 
    'x_validate_done_evidence__mutmut_6': x_validate_done_evidence__mutmut_6, 
    'x_validate_done_evidence__mutmut_7': x_validate_done_evidence__mutmut_7, 
    'x_validate_done_evidence__mutmut_8': x_validate_done_evidence__mutmut_8, 
    'x_validate_done_evidence__mutmut_9': x_validate_done_evidence__mutmut_9, 
    'x_validate_done_evidence__mutmut_10': x_validate_done_evidence__mutmut_10, 
    'x_validate_done_evidence__mutmut_11': x_validate_done_evidence__mutmut_11, 
    'x_validate_done_evidence__mutmut_12': x_validate_done_evidence__mutmut_12, 
    'x_validate_done_evidence__mutmut_13': x_validate_done_evidence__mutmut_13, 
    'x_validate_done_evidence__mutmut_14': x_validate_done_evidence__mutmut_14, 
    'x_validate_done_evidence__mutmut_15': x_validate_done_evidence__mutmut_15, 
    'x_validate_done_evidence__mutmut_16': x_validate_done_evidence__mutmut_16, 
    'x_validate_done_evidence__mutmut_17': x_validate_done_evidence__mutmut_17, 
    'x_validate_done_evidence__mutmut_18': x_validate_done_evidence__mutmut_18, 
    'x_validate_done_evidence__mutmut_19': x_validate_done_evidence__mutmut_19, 
    'x_validate_done_evidence__mutmut_20': x_validate_done_evidence__mutmut_20, 
    'x_validate_done_evidence__mutmut_21': x_validate_done_evidence__mutmut_21, 
    'x_validate_done_evidence__mutmut_22': x_validate_done_evidence__mutmut_22, 
    'x_validate_done_evidence__mutmut_23': x_validate_done_evidence__mutmut_23, 
    'x_validate_done_evidence__mutmut_24': x_validate_done_evidence__mutmut_24, 
    'x_validate_done_evidence__mutmut_25': x_validate_done_evidence__mutmut_25, 
    'x_validate_done_evidence__mutmut_26': x_validate_done_evidence__mutmut_26, 
    'x_validate_done_evidence__mutmut_27': x_validate_done_evidence__mutmut_27, 
    'x_validate_done_evidence__mutmut_28': x_validate_done_evidence__mutmut_28, 
    'x_validate_done_evidence__mutmut_29': x_validate_done_evidence__mutmut_29, 
    'x_validate_done_evidence__mutmut_30': x_validate_done_evidence__mutmut_30, 
    'x_validate_done_evidence__mutmut_31': x_validate_done_evidence__mutmut_31, 
    'x_validate_done_evidence__mutmut_32': x_validate_done_evidence__mutmut_32, 
    'x_validate_done_evidence__mutmut_33': x_validate_done_evidence__mutmut_33, 
    'x_validate_done_evidence__mutmut_34': x_validate_done_evidence__mutmut_34, 
    'x_validate_done_evidence__mutmut_35': x_validate_done_evidence__mutmut_35, 
    'x_validate_done_evidence__mutmut_36': x_validate_done_evidence__mutmut_36, 
    'x_validate_done_evidence__mutmut_37': x_validate_done_evidence__mutmut_37, 
    'x_validate_done_evidence__mutmut_38': x_validate_done_evidence__mutmut_38, 
    'x_validate_done_evidence__mutmut_39': x_validate_done_evidence__mutmut_39, 
    'x_validate_done_evidence__mutmut_40': x_validate_done_evidence__mutmut_40, 
    'x_validate_done_evidence__mutmut_41': x_validate_done_evidence__mutmut_41, 
    'x_validate_done_evidence__mutmut_42': x_validate_done_evidence__mutmut_42, 
    'x_validate_done_evidence__mutmut_43': x_validate_done_evidence__mutmut_43, 
    'x_validate_done_evidence__mutmut_44': x_validate_done_evidence__mutmut_44, 
    'x_validate_done_evidence__mutmut_45': x_validate_done_evidence__mutmut_45, 
    'x_validate_done_evidence__mutmut_46': x_validate_done_evidence__mutmut_46, 
    'x_validate_done_evidence__mutmut_47': x_validate_done_evidence__mutmut_47, 
    'x_validate_done_evidence__mutmut_48': x_validate_done_evidence__mutmut_48, 
    'x_validate_done_evidence__mutmut_49': x_validate_done_evidence__mutmut_49, 
    'x_validate_done_evidence__mutmut_50': x_validate_done_evidence__mutmut_50, 
    'x_validate_done_evidence__mutmut_51': x_validate_done_evidence__mutmut_51, 
    'x_validate_done_evidence__mutmut_52': x_validate_done_evidence__mutmut_52, 
    'x_validate_done_evidence__mutmut_53': x_validate_done_evidence__mutmut_53, 
    'x_validate_done_evidence__mutmut_54': x_validate_done_evidence__mutmut_54, 
    'x_validate_done_evidence__mutmut_55': x_validate_done_evidence__mutmut_55, 
    'x_validate_done_evidence__mutmut_56': x_validate_done_evidence__mutmut_56, 
    'x_validate_done_evidence__mutmut_57': x_validate_done_evidence__mutmut_57, 
    'x_validate_done_evidence__mutmut_58': x_validate_done_evidence__mutmut_58, 
    'x_validate_done_evidence__mutmut_59': x_validate_done_evidence__mutmut_59
}
x_validate_done_evidence__mutmut_orig.__name__ = 'x_validate_done_evidence'


def validate_materialization_drift(feature_dir: Path) -> list[str]:
    args = [feature_dir]# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x_validate_materialization_drift__mutmut_orig, x_validate_materialization_drift__mutmut_mutants, args, kwargs, None)


def x_validate_materialization_drift__mutmut_orig(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_1(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = None

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_2(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = None
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_3(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir * SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_4(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = None

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_5(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir * EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_6(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_7(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                None
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_8(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "XXstatus.json exists but status.events.jsonl is missingXX"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_9(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "STATUS.JSON EXISTS BUT STATUS.EVENTS.JSONL IS MISSING"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_10(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_11(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            None
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_12(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "XXstatus.events.jsonl exists but status.json is missing XX"
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_13(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "STATUS.EVENTS.JSONL EXISTS BUT STATUS.JSON IS MISSING "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_14(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "XX(run 'spec-kitty agent status materialize' to generate)XX"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_15(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(RUN 'SPEC-KITTY AGENT STATUS MATERIALIZE' TO GENERATE)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_16(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = None

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_17(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(None)

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_18(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding=None))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_19(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="XXutf-8XX"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_20(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="UTF-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_21(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = None
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_22(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(None)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_23(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = None

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_24(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(None)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_25(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = None
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_26(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get(None, {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_27(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", None)
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_28(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get({})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_29(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", )
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_30(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("XXwork_packagesXX", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_31(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("WORK_PACKAGES", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_32(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = None

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_33(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps == expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_34(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = None
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_35(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) & set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_36(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(None) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_37(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(None)
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_38(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(None):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_39(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = None
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_40(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(None)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_41(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = None

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_42(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(None)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_43(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is not None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_44(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    None
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_45(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get(None, '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_46(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', None)})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_47(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_48(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', )})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_49(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('XXlaneXX', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_50(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('LANE', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_51(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', 'XX?XX')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_52(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is not None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_53(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    None
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_54(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get(None) != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_55(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("XXlaneXX") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_56(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("LANE") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_57(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") == expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_58(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get(None):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_59(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("XXlaneXX"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_60(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("LANE"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_61(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    None
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_62(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get(None)} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_63(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('XXlaneXX')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_64(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('LANE')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_65(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get(None)}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_66(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('XXlaneXX')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_67(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('LANE')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_68(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp == expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_69(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    None
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_70(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get(None) != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_71(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("XXevent_countXX") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_72(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("EVENT_COUNT") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_73(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") == expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_74(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            None
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_75(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get(None)} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_76(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('XXevent_countXX')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_77(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('EVENT_COUNT')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_78(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get(None) != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_79(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("XXlast_event_idXX") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_80(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("LAST_EVENT_ID") != expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_81(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") == expected_snapshot.last_event_id:
        findings.append(
            f"Materialization drift: last_event_id mismatch between "
            f"status.json and reducer output"
        )

    return findings


def x_validate_materialization_drift__mutmut_82(feature_dir: Path) -> list[str]:
    """Compare status.json on disk vs reducer output from the event log.

    Returns findings describing any drift detected. An empty list means
    no drift.
    """
    from .reducer import SNAPSHOT_FILENAME, reduce
    from .store import EVENTS_FILENAME, read_events

    findings: list[str] = []

    status_path = feature_dir / SNAPSHOT_FILENAME
    events_path = feature_dir / EVENTS_FILENAME

    if not events_path.exists():
        if status_path.exists():
            findings.append(
                "status.json exists but status.events.jsonl is missing"
            )
        return findings

    if not status_path.exists():
        findings.append(
            "status.events.jsonl exists but status.json is missing "
            "(run 'spec-kitty agent status materialize' to generate)"
        )
        return findings

    # Read on-disk snapshot
    disk_data = json.loads(status_path.read_text(encoding="utf-8"))

    # Compute expected snapshot from events
    events = read_events(feature_dir)
    expected_snapshot = reduce(events)

    # Compare work_packages and summary (skip materialized_at which is timestamp)
    disk_wps = disk_data.get("work_packages", {})
    expected_wps = expected_snapshot.work_packages

    if disk_wps != expected_wps:
        # Find specific differences
        all_wp_ids = set(disk_wps.keys()) | set(expected_wps.keys())
        for wp_id in sorted(all_wp_ids):
            disk_wp = disk_wps.get(wp_id)
            expected_wp = expected_wps.get(wp_id)

            if disk_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} missing from status.json "
                    f"(expected lane={expected_wp.get('lane', '?')})"
                )
            elif expected_wp is None:
                findings.append(
                    f"Materialization drift: {wp_id} in status.json but not in reducer output"
                )
            elif disk_wp.get("lane") != expected_wp.get("lane"):
                findings.append(
                    f"Materialization drift: {wp_id} lane={disk_wp.get('lane')} in status.json "
                    f"but reducer says lane={expected_wp.get('lane')}"
                )
            elif disk_wp != expected_wp:
                findings.append(
                    f"Materialization drift: {wp_id} state differs between status.json and reducer output"
                )

    # Also check event count and last_event_id
    if disk_data.get("event_count") != expected_snapshot.event_count:
        findings.append(
            f"Materialization drift: event_count={disk_data.get('event_count')} "
            f"in status.json but reducer counted {expected_snapshot.event_count}"
        )

    if disk_data.get("last_event_id") != expected_snapshot.last_event_id:
        findings.append(
            None
        )

    return findings

x_validate_materialization_drift__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x_validate_materialization_drift__mutmut_1': x_validate_materialization_drift__mutmut_1, 
    'x_validate_materialization_drift__mutmut_2': x_validate_materialization_drift__mutmut_2, 
    'x_validate_materialization_drift__mutmut_3': x_validate_materialization_drift__mutmut_3, 
    'x_validate_materialization_drift__mutmut_4': x_validate_materialization_drift__mutmut_4, 
    'x_validate_materialization_drift__mutmut_5': x_validate_materialization_drift__mutmut_5, 
    'x_validate_materialization_drift__mutmut_6': x_validate_materialization_drift__mutmut_6, 
    'x_validate_materialization_drift__mutmut_7': x_validate_materialization_drift__mutmut_7, 
    'x_validate_materialization_drift__mutmut_8': x_validate_materialization_drift__mutmut_8, 
    'x_validate_materialization_drift__mutmut_9': x_validate_materialization_drift__mutmut_9, 
    'x_validate_materialization_drift__mutmut_10': x_validate_materialization_drift__mutmut_10, 
    'x_validate_materialization_drift__mutmut_11': x_validate_materialization_drift__mutmut_11, 
    'x_validate_materialization_drift__mutmut_12': x_validate_materialization_drift__mutmut_12, 
    'x_validate_materialization_drift__mutmut_13': x_validate_materialization_drift__mutmut_13, 
    'x_validate_materialization_drift__mutmut_14': x_validate_materialization_drift__mutmut_14, 
    'x_validate_materialization_drift__mutmut_15': x_validate_materialization_drift__mutmut_15, 
    'x_validate_materialization_drift__mutmut_16': x_validate_materialization_drift__mutmut_16, 
    'x_validate_materialization_drift__mutmut_17': x_validate_materialization_drift__mutmut_17, 
    'x_validate_materialization_drift__mutmut_18': x_validate_materialization_drift__mutmut_18, 
    'x_validate_materialization_drift__mutmut_19': x_validate_materialization_drift__mutmut_19, 
    'x_validate_materialization_drift__mutmut_20': x_validate_materialization_drift__mutmut_20, 
    'x_validate_materialization_drift__mutmut_21': x_validate_materialization_drift__mutmut_21, 
    'x_validate_materialization_drift__mutmut_22': x_validate_materialization_drift__mutmut_22, 
    'x_validate_materialization_drift__mutmut_23': x_validate_materialization_drift__mutmut_23, 
    'x_validate_materialization_drift__mutmut_24': x_validate_materialization_drift__mutmut_24, 
    'x_validate_materialization_drift__mutmut_25': x_validate_materialization_drift__mutmut_25, 
    'x_validate_materialization_drift__mutmut_26': x_validate_materialization_drift__mutmut_26, 
    'x_validate_materialization_drift__mutmut_27': x_validate_materialization_drift__mutmut_27, 
    'x_validate_materialization_drift__mutmut_28': x_validate_materialization_drift__mutmut_28, 
    'x_validate_materialization_drift__mutmut_29': x_validate_materialization_drift__mutmut_29, 
    'x_validate_materialization_drift__mutmut_30': x_validate_materialization_drift__mutmut_30, 
    'x_validate_materialization_drift__mutmut_31': x_validate_materialization_drift__mutmut_31, 
    'x_validate_materialization_drift__mutmut_32': x_validate_materialization_drift__mutmut_32, 
    'x_validate_materialization_drift__mutmut_33': x_validate_materialization_drift__mutmut_33, 
    'x_validate_materialization_drift__mutmut_34': x_validate_materialization_drift__mutmut_34, 
    'x_validate_materialization_drift__mutmut_35': x_validate_materialization_drift__mutmut_35, 
    'x_validate_materialization_drift__mutmut_36': x_validate_materialization_drift__mutmut_36, 
    'x_validate_materialization_drift__mutmut_37': x_validate_materialization_drift__mutmut_37, 
    'x_validate_materialization_drift__mutmut_38': x_validate_materialization_drift__mutmut_38, 
    'x_validate_materialization_drift__mutmut_39': x_validate_materialization_drift__mutmut_39, 
    'x_validate_materialization_drift__mutmut_40': x_validate_materialization_drift__mutmut_40, 
    'x_validate_materialization_drift__mutmut_41': x_validate_materialization_drift__mutmut_41, 
    'x_validate_materialization_drift__mutmut_42': x_validate_materialization_drift__mutmut_42, 
    'x_validate_materialization_drift__mutmut_43': x_validate_materialization_drift__mutmut_43, 
    'x_validate_materialization_drift__mutmut_44': x_validate_materialization_drift__mutmut_44, 
    'x_validate_materialization_drift__mutmut_45': x_validate_materialization_drift__mutmut_45, 
    'x_validate_materialization_drift__mutmut_46': x_validate_materialization_drift__mutmut_46, 
    'x_validate_materialization_drift__mutmut_47': x_validate_materialization_drift__mutmut_47, 
    'x_validate_materialization_drift__mutmut_48': x_validate_materialization_drift__mutmut_48, 
    'x_validate_materialization_drift__mutmut_49': x_validate_materialization_drift__mutmut_49, 
    'x_validate_materialization_drift__mutmut_50': x_validate_materialization_drift__mutmut_50, 
    'x_validate_materialization_drift__mutmut_51': x_validate_materialization_drift__mutmut_51, 
    'x_validate_materialization_drift__mutmut_52': x_validate_materialization_drift__mutmut_52, 
    'x_validate_materialization_drift__mutmut_53': x_validate_materialization_drift__mutmut_53, 
    'x_validate_materialization_drift__mutmut_54': x_validate_materialization_drift__mutmut_54, 
    'x_validate_materialization_drift__mutmut_55': x_validate_materialization_drift__mutmut_55, 
    'x_validate_materialization_drift__mutmut_56': x_validate_materialization_drift__mutmut_56, 
    'x_validate_materialization_drift__mutmut_57': x_validate_materialization_drift__mutmut_57, 
    'x_validate_materialization_drift__mutmut_58': x_validate_materialization_drift__mutmut_58, 
    'x_validate_materialization_drift__mutmut_59': x_validate_materialization_drift__mutmut_59, 
    'x_validate_materialization_drift__mutmut_60': x_validate_materialization_drift__mutmut_60, 
    'x_validate_materialization_drift__mutmut_61': x_validate_materialization_drift__mutmut_61, 
    'x_validate_materialization_drift__mutmut_62': x_validate_materialization_drift__mutmut_62, 
    'x_validate_materialization_drift__mutmut_63': x_validate_materialization_drift__mutmut_63, 
    'x_validate_materialization_drift__mutmut_64': x_validate_materialization_drift__mutmut_64, 
    'x_validate_materialization_drift__mutmut_65': x_validate_materialization_drift__mutmut_65, 
    'x_validate_materialization_drift__mutmut_66': x_validate_materialization_drift__mutmut_66, 
    'x_validate_materialization_drift__mutmut_67': x_validate_materialization_drift__mutmut_67, 
    'x_validate_materialization_drift__mutmut_68': x_validate_materialization_drift__mutmut_68, 
    'x_validate_materialization_drift__mutmut_69': x_validate_materialization_drift__mutmut_69, 
    'x_validate_materialization_drift__mutmut_70': x_validate_materialization_drift__mutmut_70, 
    'x_validate_materialization_drift__mutmut_71': x_validate_materialization_drift__mutmut_71, 
    'x_validate_materialization_drift__mutmut_72': x_validate_materialization_drift__mutmut_72, 
    'x_validate_materialization_drift__mutmut_73': x_validate_materialization_drift__mutmut_73, 
    'x_validate_materialization_drift__mutmut_74': x_validate_materialization_drift__mutmut_74, 
    'x_validate_materialization_drift__mutmut_75': x_validate_materialization_drift__mutmut_75, 
    'x_validate_materialization_drift__mutmut_76': x_validate_materialization_drift__mutmut_76, 
    'x_validate_materialization_drift__mutmut_77': x_validate_materialization_drift__mutmut_77, 
    'x_validate_materialization_drift__mutmut_78': x_validate_materialization_drift__mutmut_78, 
    'x_validate_materialization_drift__mutmut_79': x_validate_materialization_drift__mutmut_79, 
    'x_validate_materialization_drift__mutmut_80': x_validate_materialization_drift__mutmut_80, 
    'x_validate_materialization_drift__mutmut_81': x_validate_materialization_drift__mutmut_81, 
    'x_validate_materialization_drift__mutmut_82': x_validate_materialization_drift__mutmut_82
}
x_validate_materialization_drift__mutmut_orig.__name__ = 'x_validate_materialization_drift'


def validate_derived_views(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    args = [feature_dir, snapshot_wps, phase]# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x_validate_derived_views__mutmut_orig, x_validate_derived_views__mutmut_mutants, args, kwargs, None)


def x_validate_derived_views__mutmut_orig(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_1(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = None
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_2(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "XXERRORXX" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_3(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "error" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_4(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase > 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_5(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 3 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_6(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "XXWARNINGXX"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_7(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "warning"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_8(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = None
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_9(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = None
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_10(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir * "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_11(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "XXtasksXX"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_12(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "TASKS"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_13(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_14(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = None

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_15(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get(None)

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_16(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("XXlaneXX")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_17(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("LANE")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_18(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = None
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_19(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) - list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_20(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(None) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_21(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(None)) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_22(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            None
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_23(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(None)
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_24(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_25(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                None
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_26(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            break

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_27(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = None
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_28(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[1]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_29(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = None

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_30(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding=None)

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_31(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="XXutf-8-sigXX")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_32(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="UTF-8-SIG")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_33(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = None
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_34(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            None, content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_35(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', None, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_36(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, None
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_37(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_38(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_39(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_40(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'XX^lane:\s*["\']?(\S+?)["\']?\s*$XX', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_41(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^LANE:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_42(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_43(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                None
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_44(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            break

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_45(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = None

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_46(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(None)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_47(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(2)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_48(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane != "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_49(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "XXdoingXX":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_50(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "DOING":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_51(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = None

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_52(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "XXin_progressXX"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_53(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "IN_PROGRESS"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_54(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane == canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_55(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                None
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_56(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = None
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_57(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir * "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_58(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "XXtasks.mdXX"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_59(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "TASKS.MD"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_60(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = None
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_61(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(None)
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_62(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding=None))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_63(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="XXutf-8XX"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_64(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="UTF-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_65(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is not None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_66(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                None
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_67(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = None
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_68(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = None
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_69(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(None, line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_70(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", None)
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_71(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_72(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", )
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_73(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"XX^- (WP\d{2}): ([a-z_]+)$XX", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_74(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (wp\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_75(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([A-Z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_76(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is not None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_77(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        None
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_78(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    break
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_79(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = None

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_80(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(None)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_81(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(2)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_82(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(None)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_83(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(3)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_84(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = None
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_85(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get(None)
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_86(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("XXlaneXX")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_87(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("LANE")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_88(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = None
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_89(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(None)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_90(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is not None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_91(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        None
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_92(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    break
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_93(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane == canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_94(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        None
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_95(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(None):
                if wp_id not in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_96(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id in snapshot_wps:
                    findings.append(
                        f"{severity}: tasks.md status block includes unknown {wp_id}"
                    )

    return findings


def x_validate_derived_views__mutmut_97(
    feature_dir: Path,
    snapshot_wps: dict,
    phase: int,
) -> list[str]:
    """Compare frontmatter lanes with canonical snapshot work package states.

    Args:
        feature_dir: Path to the feature directory (kitty-specs/###-feature/).
        snapshot_wps: The work_packages dict from the StatusSnapshot
            (mapping wp_id -> state dict with at least a "lane" key).
        phase: Current status phase (1 or 2). Phase 1 drift is WARNING;
            Phase 2 drift is ERROR.

    Returns:
        List of finding strings. Empty means no drift.
    """
    severity = "ERROR" if phase >= 2 else "WARNING"
    findings: list[str] = []
    tasks_dir = feature_dir / "tasks"
    if not tasks_dir.exists():
        return findings

    for wp_id, wp_state in snapshot_wps.items():
        canonical_lane = wp_state.get("lane")

        # Find the corresponding WP file
        wp_files = list(tasks_dir.glob(f"{wp_id}-*.md")) + list(
            tasks_dir.glob(f"{wp_id}.md")
        )
        if not wp_files:
            findings.append(
                f"{wp_id}: no WP file found in tasks/ "
                f"(canonical state: {canonical_lane})"
            )
            continue

        wp_file = wp_files[0]
        content = wp_file.read_text(encoding="utf-8-sig")

        # Extract lane from frontmatter
        lane_match = re.search(
            r'^lane:\s*["\']?(\S+?)["\']?\s*$', content, re.MULTILINE
        )
        if not lane_match:
            findings.append(
                f"{wp_id}: no lane field in frontmatter "
                f"(canonical state: {canonical_lane})"
            )
            continue

        frontmatter_lane = lane_match.group(1)

        # Resolve alias for comparison (doing -> in_progress)
        if frontmatter_lane == "doing":
            frontmatter_lane = "in_progress"

        if frontmatter_lane != canonical_lane:
            findings.append(
                f"{severity}: {wp_id} frontmatter lane={frontmatter_lane} "
                f"but canonical state={canonical_lane}"
            )

    tasks_md = feature_dir / "tasks.md"
    if tasks_md.exists():
        status_lines = _extract_tasks_status_lines(tasks_md.read_text(encoding="utf-8"))
        if status_lines is None:
            findings.append(
                f"{severity}: tasks.md is missing generated canonical status block"
            )
        else:
            tasks_status: dict[str, str] = {}
            for line in status_lines:
                match = re.match(r"^- (WP\d{2}): ([a-z_]+)$", line.strip())
                if match is None:
                    findings.append(
                        f"{severity}: tasks.md status block has malformed line: {line.strip()}"
                    )
                    continue
                tasks_status[match.group(1)] = match.group(2)

            for wp_id, wp_state in snapshot_wps.items():
                canonical_lane = wp_state.get("lane")
                tasks_lane = tasks_status.get(wp_id)
                if tasks_lane is None:
                    findings.append(
                        f"{severity}: tasks.md status block missing {wp_id} "
                        f"(canonical state: {canonical_lane})"
                    )
                    continue
                if tasks_lane != canonical_lane:
                    findings.append(
                        f"{severity}: {wp_id} tasks.md lane={tasks_lane} "
                        f"but canonical state={canonical_lane}"
                    )

            for wp_id in sorted(tasks_status):
                if wp_id not in snapshot_wps:
                    findings.append(
                        None
                    )

    return findings

x_validate_derived_views__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x_validate_derived_views__mutmut_1': x_validate_derived_views__mutmut_1, 
    'x_validate_derived_views__mutmut_2': x_validate_derived_views__mutmut_2, 
    'x_validate_derived_views__mutmut_3': x_validate_derived_views__mutmut_3, 
    'x_validate_derived_views__mutmut_4': x_validate_derived_views__mutmut_4, 
    'x_validate_derived_views__mutmut_5': x_validate_derived_views__mutmut_5, 
    'x_validate_derived_views__mutmut_6': x_validate_derived_views__mutmut_6, 
    'x_validate_derived_views__mutmut_7': x_validate_derived_views__mutmut_7, 
    'x_validate_derived_views__mutmut_8': x_validate_derived_views__mutmut_8, 
    'x_validate_derived_views__mutmut_9': x_validate_derived_views__mutmut_9, 
    'x_validate_derived_views__mutmut_10': x_validate_derived_views__mutmut_10, 
    'x_validate_derived_views__mutmut_11': x_validate_derived_views__mutmut_11, 
    'x_validate_derived_views__mutmut_12': x_validate_derived_views__mutmut_12, 
    'x_validate_derived_views__mutmut_13': x_validate_derived_views__mutmut_13, 
    'x_validate_derived_views__mutmut_14': x_validate_derived_views__mutmut_14, 
    'x_validate_derived_views__mutmut_15': x_validate_derived_views__mutmut_15, 
    'x_validate_derived_views__mutmut_16': x_validate_derived_views__mutmut_16, 
    'x_validate_derived_views__mutmut_17': x_validate_derived_views__mutmut_17, 
    'x_validate_derived_views__mutmut_18': x_validate_derived_views__mutmut_18, 
    'x_validate_derived_views__mutmut_19': x_validate_derived_views__mutmut_19, 
    'x_validate_derived_views__mutmut_20': x_validate_derived_views__mutmut_20, 
    'x_validate_derived_views__mutmut_21': x_validate_derived_views__mutmut_21, 
    'x_validate_derived_views__mutmut_22': x_validate_derived_views__mutmut_22, 
    'x_validate_derived_views__mutmut_23': x_validate_derived_views__mutmut_23, 
    'x_validate_derived_views__mutmut_24': x_validate_derived_views__mutmut_24, 
    'x_validate_derived_views__mutmut_25': x_validate_derived_views__mutmut_25, 
    'x_validate_derived_views__mutmut_26': x_validate_derived_views__mutmut_26, 
    'x_validate_derived_views__mutmut_27': x_validate_derived_views__mutmut_27, 
    'x_validate_derived_views__mutmut_28': x_validate_derived_views__mutmut_28, 
    'x_validate_derived_views__mutmut_29': x_validate_derived_views__mutmut_29, 
    'x_validate_derived_views__mutmut_30': x_validate_derived_views__mutmut_30, 
    'x_validate_derived_views__mutmut_31': x_validate_derived_views__mutmut_31, 
    'x_validate_derived_views__mutmut_32': x_validate_derived_views__mutmut_32, 
    'x_validate_derived_views__mutmut_33': x_validate_derived_views__mutmut_33, 
    'x_validate_derived_views__mutmut_34': x_validate_derived_views__mutmut_34, 
    'x_validate_derived_views__mutmut_35': x_validate_derived_views__mutmut_35, 
    'x_validate_derived_views__mutmut_36': x_validate_derived_views__mutmut_36, 
    'x_validate_derived_views__mutmut_37': x_validate_derived_views__mutmut_37, 
    'x_validate_derived_views__mutmut_38': x_validate_derived_views__mutmut_38, 
    'x_validate_derived_views__mutmut_39': x_validate_derived_views__mutmut_39, 
    'x_validate_derived_views__mutmut_40': x_validate_derived_views__mutmut_40, 
    'x_validate_derived_views__mutmut_41': x_validate_derived_views__mutmut_41, 
    'x_validate_derived_views__mutmut_42': x_validate_derived_views__mutmut_42, 
    'x_validate_derived_views__mutmut_43': x_validate_derived_views__mutmut_43, 
    'x_validate_derived_views__mutmut_44': x_validate_derived_views__mutmut_44, 
    'x_validate_derived_views__mutmut_45': x_validate_derived_views__mutmut_45, 
    'x_validate_derived_views__mutmut_46': x_validate_derived_views__mutmut_46, 
    'x_validate_derived_views__mutmut_47': x_validate_derived_views__mutmut_47, 
    'x_validate_derived_views__mutmut_48': x_validate_derived_views__mutmut_48, 
    'x_validate_derived_views__mutmut_49': x_validate_derived_views__mutmut_49, 
    'x_validate_derived_views__mutmut_50': x_validate_derived_views__mutmut_50, 
    'x_validate_derived_views__mutmut_51': x_validate_derived_views__mutmut_51, 
    'x_validate_derived_views__mutmut_52': x_validate_derived_views__mutmut_52, 
    'x_validate_derived_views__mutmut_53': x_validate_derived_views__mutmut_53, 
    'x_validate_derived_views__mutmut_54': x_validate_derived_views__mutmut_54, 
    'x_validate_derived_views__mutmut_55': x_validate_derived_views__mutmut_55, 
    'x_validate_derived_views__mutmut_56': x_validate_derived_views__mutmut_56, 
    'x_validate_derived_views__mutmut_57': x_validate_derived_views__mutmut_57, 
    'x_validate_derived_views__mutmut_58': x_validate_derived_views__mutmut_58, 
    'x_validate_derived_views__mutmut_59': x_validate_derived_views__mutmut_59, 
    'x_validate_derived_views__mutmut_60': x_validate_derived_views__mutmut_60, 
    'x_validate_derived_views__mutmut_61': x_validate_derived_views__mutmut_61, 
    'x_validate_derived_views__mutmut_62': x_validate_derived_views__mutmut_62, 
    'x_validate_derived_views__mutmut_63': x_validate_derived_views__mutmut_63, 
    'x_validate_derived_views__mutmut_64': x_validate_derived_views__mutmut_64, 
    'x_validate_derived_views__mutmut_65': x_validate_derived_views__mutmut_65, 
    'x_validate_derived_views__mutmut_66': x_validate_derived_views__mutmut_66, 
    'x_validate_derived_views__mutmut_67': x_validate_derived_views__mutmut_67, 
    'x_validate_derived_views__mutmut_68': x_validate_derived_views__mutmut_68, 
    'x_validate_derived_views__mutmut_69': x_validate_derived_views__mutmut_69, 
    'x_validate_derived_views__mutmut_70': x_validate_derived_views__mutmut_70, 
    'x_validate_derived_views__mutmut_71': x_validate_derived_views__mutmut_71, 
    'x_validate_derived_views__mutmut_72': x_validate_derived_views__mutmut_72, 
    'x_validate_derived_views__mutmut_73': x_validate_derived_views__mutmut_73, 
    'x_validate_derived_views__mutmut_74': x_validate_derived_views__mutmut_74, 
    'x_validate_derived_views__mutmut_75': x_validate_derived_views__mutmut_75, 
    'x_validate_derived_views__mutmut_76': x_validate_derived_views__mutmut_76, 
    'x_validate_derived_views__mutmut_77': x_validate_derived_views__mutmut_77, 
    'x_validate_derived_views__mutmut_78': x_validate_derived_views__mutmut_78, 
    'x_validate_derived_views__mutmut_79': x_validate_derived_views__mutmut_79, 
    'x_validate_derived_views__mutmut_80': x_validate_derived_views__mutmut_80, 
    'x_validate_derived_views__mutmut_81': x_validate_derived_views__mutmut_81, 
    'x_validate_derived_views__mutmut_82': x_validate_derived_views__mutmut_82, 
    'x_validate_derived_views__mutmut_83': x_validate_derived_views__mutmut_83, 
    'x_validate_derived_views__mutmut_84': x_validate_derived_views__mutmut_84, 
    'x_validate_derived_views__mutmut_85': x_validate_derived_views__mutmut_85, 
    'x_validate_derived_views__mutmut_86': x_validate_derived_views__mutmut_86, 
    'x_validate_derived_views__mutmut_87': x_validate_derived_views__mutmut_87, 
    'x_validate_derived_views__mutmut_88': x_validate_derived_views__mutmut_88, 
    'x_validate_derived_views__mutmut_89': x_validate_derived_views__mutmut_89, 
    'x_validate_derived_views__mutmut_90': x_validate_derived_views__mutmut_90, 
    'x_validate_derived_views__mutmut_91': x_validate_derived_views__mutmut_91, 
    'x_validate_derived_views__mutmut_92': x_validate_derived_views__mutmut_92, 
    'x_validate_derived_views__mutmut_93': x_validate_derived_views__mutmut_93, 
    'x_validate_derived_views__mutmut_94': x_validate_derived_views__mutmut_94, 
    'x_validate_derived_views__mutmut_95': x_validate_derived_views__mutmut_95, 
    'x_validate_derived_views__mutmut_96': x_validate_derived_views__mutmut_96, 
    'x_validate_derived_views__mutmut_97': x_validate_derived_views__mutmut_97
}
x_validate_derived_views__mutmut_orig.__name__ = 'x_validate_derived_views'


def _extract_tasks_status_lines(content: str) -> list[str] | None:
    args = [content]# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x__extract_tasks_status_lines__mutmut_orig, x__extract_tasks_status_lines__mutmut_mutants, args, kwargs, None)


def x__extract_tasks_status_lines__mutmut_orig(content: str) -> list[str] | None:
    """Extract generated status lines from tasks.md status block markers."""
    start_idx = content.find(STATUS_BLOCK_START)
    if start_idx == -1:
        return None
    end_idx = content.find(STATUS_BLOCK_END, start_idx)
    if end_idx == -1:
        return None
    block = content[start_idx + len(STATUS_BLOCK_START):end_idx]
    lines = [line.strip() for line in block.strip().splitlines() if line.strip()]
    if not lines:
        return []
    # Strip optional heading line.
    if lines[0].startswith("## "):
        return lines[1:]
    return lines


def x__extract_tasks_status_lines__mutmut_1(content: str) -> list[str] | None:
    """Extract generated status lines from tasks.md status block markers."""
    start_idx = None
    if start_idx == -1:
        return None
    end_idx = content.find(STATUS_BLOCK_END, start_idx)
    if end_idx == -1:
        return None
    block = content[start_idx + len(STATUS_BLOCK_START):end_idx]
    lines = [line.strip() for line in block.strip().splitlines() if line.strip()]
    if not lines:
        return []
    # Strip optional heading line.
    if lines[0].startswith("## "):
        return lines[1:]
    return lines


def x__extract_tasks_status_lines__mutmut_2(content: str) -> list[str] | None:
    """Extract generated status lines from tasks.md status block markers."""
    start_idx = content.find(None)
    if start_idx == -1:
        return None
    end_idx = content.find(STATUS_BLOCK_END, start_idx)
    if end_idx == -1:
        return None
    block = content[start_idx + len(STATUS_BLOCK_START):end_idx]
    lines = [line.strip() for line in block.strip().splitlines() if line.strip()]
    if not lines:
        return []
    # Strip optional heading line.
    if lines[0].startswith("## "):
        return lines[1:]
    return lines


def x__extract_tasks_status_lines__mutmut_3(content: str) -> list[str] | None:
    """Extract generated status lines from tasks.md status block markers."""
    start_idx = content.rfind(STATUS_BLOCK_START)
    if start_idx == -1:
        return None
    end_idx = content.find(STATUS_BLOCK_END, start_idx)
    if end_idx == -1:
        return None
    block = content[start_idx + len(STATUS_BLOCK_START):end_idx]
    lines = [line.strip() for line in block.strip().splitlines() if line.strip()]
    if not lines:
        return []
    # Strip optional heading line.
    if lines[0].startswith("## "):
        return lines[1:]
    return lines


def x__extract_tasks_status_lines__mutmut_4(content: str) -> list[str] | None:
    """Extract generated status lines from tasks.md status block markers."""
    start_idx = content.find(STATUS_BLOCK_START)
    if start_idx != -1:
        return None
    end_idx = content.find(STATUS_BLOCK_END, start_idx)
    if end_idx == -1:
        return None
    block = content[start_idx + len(STATUS_BLOCK_START):end_idx]
    lines = [line.strip() for line in block.strip().splitlines() if line.strip()]
    if not lines:
        return []
    # Strip optional heading line.
    if lines[0].startswith("## "):
        return lines[1:]
    return lines


def x__extract_tasks_status_lines__mutmut_5(content: str) -> list[str] | None:
    """Extract generated status lines from tasks.md status block markers."""
    start_idx = content.find(STATUS_BLOCK_START)
    if start_idx == +1:
        return None
    end_idx = content.find(STATUS_BLOCK_END, start_idx)
    if end_idx == -1:
        return None
    block = content[start_idx + len(STATUS_BLOCK_START):end_idx]
    lines = [line.strip() for line in block.strip().splitlines() if line.strip()]
    if not lines:
        return []
    # Strip optional heading line.
    if lines[0].startswith("## "):
        return lines[1:]
    return lines


def x__extract_tasks_status_lines__mutmut_6(content: str) -> list[str] | None:
    """Extract generated status lines from tasks.md status block markers."""
    start_idx = content.find(STATUS_BLOCK_START)
    if start_idx == -2:
        return None
    end_idx = content.find(STATUS_BLOCK_END, start_idx)
    if end_idx == -1:
        return None
    block = content[start_idx + len(STATUS_BLOCK_START):end_idx]
    lines = [line.strip() for line in block.strip().splitlines() if line.strip()]
    if not lines:
        return []
    # Strip optional heading line.
    if lines[0].startswith("## "):
        return lines[1:]
    return lines


def x__extract_tasks_status_lines__mutmut_7(content: str) -> list[str] | None:
    """Extract generated status lines from tasks.md status block markers."""
    start_idx = content.find(STATUS_BLOCK_START)
    if start_idx == -1:
        return None
    end_idx = None
    if end_idx == -1:
        return None
    block = content[start_idx + len(STATUS_BLOCK_START):end_idx]
    lines = [line.strip() for line in block.strip().splitlines() if line.strip()]
    if not lines:
        return []
    # Strip optional heading line.
    if lines[0].startswith("## "):
        return lines[1:]
    return lines


def x__extract_tasks_status_lines__mutmut_8(content: str) -> list[str] | None:
    """Extract generated status lines from tasks.md status block markers."""
    start_idx = content.find(STATUS_BLOCK_START)
    if start_idx == -1:
        return None
    end_idx = content.find(None, start_idx)
    if end_idx == -1:
        return None
    block = content[start_idx + len(STATUS_BLOCK_START):end_idx]
    lines = [line.strip() for line in block.strip().splitlines() if line.strip()]
    if not lines:
        return []
    # Strip optional heading line.
    if lines[0].startswith("## "):
        return lines[1:]
    return lines


def x__extract_tasks_status_lines__mutmut_9(content: str) -> list[str] | None:
    """Extract generated status lines from tasks.md status block markers."""
    start_idx = content.find(STATUS_BLOCK_START)
    if start_idx == -1:
        return None
    end_idx = content.find(STATUS_BLOCK_END, None)
    if end_idx == -1:
        return None
    block = content[start_idx + len(STATUS_BLOCK_START):end_idx]
    lines = [line.strip() for line in block.strip().splitlines() if line.strip()]
    if not lines:
        return []
    # Strip optional heading line.
    if lines[0].startswith("## "):
        return lines[1:]
    return lines


def x__extract_tasks_status_lines__mutmut_10(content: str) -> list[str] | None:
    """Extract generated status lines from tasks.md status block markers."""
    start_idx = content.find(STATUS_BLOCK_START)
    if start_idx == -1:
        return None
    end_idx = content.find(start_idx)
    if end_idx == -1:
        return None
    block = content[start_idx + len(STATUS_BLOCK_START):end_idx]
    lines = [line.strip() for line in block.strip().splitlines() if line.strip()]
    if not lines:
        return []
    # Strip optional heading line.
    if lines[0].startswith("## "):
        return lines[1:]
    return lines


def x__extract_tasks_status_lines__mutmut_11(content: str) -> list[str] | None:
    """Extract generated status lines from tasks.md status block markers."""
    start_idx = content.find(STATUS_BLOCK_START)
    if start_idx == -1:
        return None
    end_idx = content.find(STATUS_BLOCK_END, )
    if end_idx == -1:
        return None
    block = content[start_idx + len(STATUS_BLOCK_START):end_idx]
    lines = [line.strip() for line in block.strip().splitlines() if line.strip()]
    if not lines:
        return []
    # Strip optional heading line.
    if lines[0].startswith("## "):
        return lines[1:]
    return lines


def x__extract_tasks_status_lines__mutmut_12(content: str) -> list[str] | None:
    """Extract generated status lines from tasks.md status block markers."""
    start_idx = content.find(STATUS_BLOCK_START)
    if start_idx == -1:
        return None
    end_idx = content.rfind(STATUS_BLOCK_END, start_idx)
    if end_idx == -1:
        return None
    block = content[start_idx + len(STATUS_BLOCK_START):end_idx]
    lines = [line.strip() for line in block.strip().splitlines() if line.strip()]
    if not lines:
        return []
    # Strip optional heading line.
    if lines[0].startswith("## "):
        return lines[1:]
    return lines


def x__extract_tasks_status_lines__mutmut_13(content: str) -> list[str] | None:
    """Extract generated status lines from tasks.md status block markers."""
    start_idx = content.find(STATUS_BLOCK_START)
    if start_idx == -1:
        return None
    end_idx = content.find(STATUS_BLOCK_END, start_idx)
    if end_idx != -1:
        return None
    block = content[start_idx + len(STATUS_BLOCK_START):end_idx]
    lines = [line.strip() for line in block.strip().splitlines() if line.strip()]
    if not lines:
        return []
    # Strip optional heading line.
    if lines[0].startswith("## "):
        return lines[1:]
    return lines


def x__extract_tasks_status_lines__mutmut_14(content: str) -> list[str] | None:
    """Extract generated status lines from tasks.md status block markers."""
    start_idx = content.find(STATUS_BLOCK_START)
    if start_idx == -1:
        return None
    end_idx = content.find(STATUS_BLOCK_END, start_idx)
    if end_idx == +1:
        return None
    block = content[start_idx + len(STATUS_BLOCK_START):end_idx]
    lines = [line.strip() for line in block.strip().splitlines() if line.strip()]
    if not lines:
        return []
    # Strip optional heading line.
    if lines[0].startswith("## "):
        return lines[1:]
    return lines


def x__extract_tasks_status_lines__mutmut_15(content: str) -> list[str] | None:
    """Extract generated status lines from tasks.md status block markers."""
    start_idx = content.find(STATUS_BLOCK_START)
    if start_idx == -1:
        return None
    end_idx = content.find(STATUS_BLOCK_END, start_idx)
    if end_idx == -2:
        return None
    block = content[start_idx + len(STATUS_BLOCK_START):end_idx]
    lines = [line.strip() for line in block.strip().splitlines() if line.strip()]
    if not lines:
        return []
    # Strip optional heading line.
    if lines[0].startswith("## "):
        return lines[1:]
    return lines


def x__extract_tasks_status_lines__mutmut_16(content: str) -> list[str] | None:
    """Extract generated status lines from tasks.md status block markers."""
    start_idx = content.find(STATUS_BLOCK_START)
    if start_idx == -1:
        return None
    end_idx = content.find(STATUS_BLOCK_END, start_idx)
    if end_idx == -1:
        return None
    block = None
    lines = [line.strip() for line in block.strip().splitlines() if line.strip()]
    if not lines:
        return []
    # Strip optional heading line.
    if lines[0].startswith("## "):
        return lines[1:]
    return lines


def x__extract_tasks_status_lines__mutmut_17(content: str) -> list[str] | None:
    """Extract generated status lines from tasks.md status block markers."""
    start_idx = content.find(STATUS_BLOCK_START)
    if start_idx == -1:
        return None
    end_idx = content.find(STATUS_BLOCK_END, start_idx)
    if end_idx == -1:
        return None
    block = content[start_idx - len(STATUS_BLOCK_START):end_idx]
    lines = [line.strip() for line in block.strip().splitlines() if line.strip()]
    if not lines:
        return []
    # Strip optional heading line.
    if lines[0].startswith("## "):
        return lines[1:]
    return lines


def x__extract_tasks_status_lines__mutmut_18(content: str) -> list[str] | None:
    """Extract generated status lines from tasks.md status block markers."""
    start_idx = content.find(STATUS_BLOCK_START)
    if start_idx == -1:
        return None
    end_idx = content.find(STATUS_BLOCK_END, start_idx)
    if end_idx == -1:
        return None
    block = content[start_idx + len(STATUS_BLOCK_START):end_idx]
    lines = None
    if not lines:
        return []
    # Strip optional heading line.
    if lines[0].startswith("## "):
        return lines[1:]
    return lines


def x__extract_tasks_status_lines__mutmut_19(content: str) -> list[str] | None:
    """Extract generated status lines from tasks.md status block markers."""
    start_idx = content.find(STATUS_BLOCK_START)
    if start_idx == -1:
        return None
    end_idx = content.find(STATUS_BLOCK_END, start_idx)
    if end_idx == -1:
        return None
    block = content[start_idx + len(STATUS_BLOCK_START):end_idx]
    lines = [line.strip() for line in block.strip().splitlines() if line.strip()]
    if lines:
        return []
    # Strip optional heading line.
    if lines[0].startswith("## "):
        return lines[1:]
    return lines


def x__extract_tasks_status_lines__mutmut_20(content: str) -> list[str] | None:
    """Extract generated status lines from tasks.md status block markers."""
    start_idx = content.find(STATUS_BLOCK_START)
    if start_idx == -1:
        return None
    end_idx = content.find(STATUS_BLOCK_END, start_idx)
    if end_idx == -1:
        return None
    block = content[start_idx + len(STATUS_BLOCK_START):end_idx]
    lines = [line.strip() for line in block.strip().splitlines() if line.strip()]
    if not lines:
        return []
    # Strip optional heading line.
    if lines[0].startswith(None):
        return lines[1:]
    return lines


def x__extract_tasks_status_lines__mutmut_21(content: str) -> list[str] | None:
    """Extract generated status lines from tasks.md status block markers."""
    start_idx = content.find(STATUS_BLOCK_START)
    if start_idx == -1:
        return None
    end_idx = content.find(STATUS_BLOCK_END, start_idx)
    if end_idx == -1:
        return None
    block = content[start_idx + len(STATUS_BLOCK_START):end_idx]
    lines = [line.strip() for line in block.strip().splitlines() if line.strip()]
    if not lines:
        return []
    # Strip optional heading line.
    if lines[1].startswith("## "):
        return lines[1:]
    return lines


def x__extract_tasks_status_lines__mutmut_22(content: str) -> list[str] | None:
    """Extract generated status lines from tasks.md status block markers."""
    start_idx = content.find(STATUS_BLOCK_START)
    if start_idx == -1:
        return None
    end_idx = content.find(STATUS_BLOCK_END, start_idx)
    if end_idx == -1:
        return None
    block = content[start_idx + len(STATUS_BLOCK_START):end_idx]
    lines = [line.strip() for line in block.strip().splitlines() if line.strip()]
    if not lines:
        return []
    # Strip optional heading line.
    if lines[0].startswith("XX## XX"):
        return lines[1:]
    return lines


def x__extract_tasks_status_lines__mutmut_23(content: str) -> list[str] | None:
    """Extract generated status lines from tasks.md status block markers."""
    start_idx = content.find(STATUS_BLOCK_START)
    if start_idx == -1:
        return None
    end_idx = content.find(STATUS_BLOCK_END, start_idx)
    if end_idx == -1:
        return None
    block = content[start_idx + len(STATUS_BLOCK_START):end_idx]
    lines = [line.strip() for line in block.strip().splitlines() if line.strip()]
    if not lines:
        return []
    # Strip optional heading line.
    if lines[0].startswith("## "):
        return lines[2:]
    return lines

x__extract_tasks_status_lines__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x__extract_tasks_status_lines__mutmut_1': x__extract_tasks_status_lines__mutmut_1, 
    'x__extract_tasks_status_lines__mutmut_2': x__extract_tasks_status_lines__mutmut_2, 
    'x__extract_tasks_status_lines__mutmut_3': x__extract_tasks_status_lines__mutmut_3, 
    'x__extract_tasks_status_lines__mutmut_4': x__extract_tasks_status_lines__mutmut_4, 
    'x__extract_tasks_status_lines__mutmut_5': x__extract_tasks_status_lines__mutmut_5, 
    'x__extract_tasks_status_lines__mutmut_6': x__extract_tasks_status_lines__mutmut_6, 
    'x__extract_tasks_status_lines__mutmut_7': x__extract_tasks_status_lines__mutmut_7, 
    'x__extract_tasks_status_lines__mutmut_8': x__extract_tasks_status_lines__mutmut_8, 
    'x__extract_tasks_status_lines__mutmut_9': x__extract_tasks_status_lines__mutmut_9, 
    'x__extract_tasks_status_lines__mutmut_10': x__extract_tasks_status_lines__mutmut_10, 
    'x__extract_tasks_status_lines__mutmut_11': x__extract_tasks_status_lines__mutmut_11, 
    'x__extract_tasks_status_lines__mutmut_12': x__extract_tasks_status_lines__mutmut_12, 
    'x__extract_tasks_status_lines__mutmut_13': x__extract_tasks_status_lines__mutmut_13, 
    'x__extract_tasks_status_lines__mutmut_14': x__extract_tasks_status_lines__mutmut_14, 
    'x__extract_tasks_status_lines__mutmut_15': x__extract_tasks_status_lines__mutmut_15, 
    'x__extract_tasks_status_lines__mutmut_16': x__extract_tasks_status_lines__mutmut_16, 
    'x__extract_tasks_status_lines__mutmut_17': x__extract_tasks_status_lines__mutmut_17, 
    'x__extract_tasks_status_lines__mutmut_18': x__extract_tasks_status_lines__mutmut_18, 
    'x__extract_tasks_status_lines__mutmut_19': x__extract_tasks_status_lines__mutmut_19, 
    'x__extract_tasks_status_lines__mutmut_20': x__extract_tasks_status_lines__mutmut_20, 
    'x__extract_tasks_status_lines__mutmut_21': x__extract_tasks_status_lines__mutmut_21, 
    'x__extract_tasks_status_lines__mutmut_22': x__extract_tasks_status_lines__mutmut_22, 
    'x__extract_tasks_status_lines__mutmut_23': x__extract_tasks_status_lines__mutmut_23
}
x__extract_tasks_status_lines__mutmut_orig.__name__ = 'x__extract_tasks_status_lines'


def _is_valid_event_id(value: str) -> bool:
    args = [value]# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x__is_valid_event_id__mutmut_orig, x__is_valid_event_id__mutmut_mutants, args, kwargs, None)


def x__is_valid_event_id__mutmut_orig(value: str) -> bool:
    """Check if a string is a valid event ID (ULID or UUID)."""
    try:
        normalize_event_id(value)
        return True
    except (ValueError, TypeError):
        return False


def x__is_valid_event_id__mutmut_1(value: str) -> bool:
    """Check if a string is a valid event ID (ULID or UUID)."""
    try:
        normalize_event_id(None)
        return True
    except (ValueError, TypeError):
        return False


def x__is_valid_event_id__mutmut_2(value: str) -> bool:
    """Check if a string is a valid event ID (ULID or UUID)."""
    try:
        normalize_event_id(value)
        return False
    except (ValueError, TypeError):
        return False


def x__is_valid_event_id__mutmut_3(value: str) -> bool:
    """Check if a string is a valid event ID (ULID or UUID)."""
    try:
        normalize_event_id(value)
        return True
    except (ValueError, TypeError):
        return True

x__is_valid_event_id__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x__is_valid_event_id__mutmut_1': x__is_valid_event_id__mutmut_1, 
    'x__is_valid_event_id__mutmut_2': x__is_valid_event_id__mutmut_2, 
    'x__is_valid_event_id__mutmut_3': x__is_valid_event_id__mutmut_3
}
x__is_valid_event_id__mutmut_orig.__name__ = 'x__is_valid_event_id'


def _is_valid_iso8601(value: str) -> bool:
    args = [value]# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x__is_valid_iso8601__mutmut_orig, x__is_valid_iso8601__mutmut_mutants, args, kwargs, None)


def x__is_valid_iso8601__mutmut_orig(value: str) -> bool:
    """Check if a string looks like a valid ISO 8601 timestamp.

    Accepts common formats: YYYY-MM-DDTHH:MM:SSZ, YYYY-MM-DDTHH:MM:SS+00:00,
    and the output of datetime.isoformat().
    """
    try:
        # Python 3.11+ datetime.fromisoformat handles Z suffix
        datetime.fromisoformat(value.replace("Z", "+00:00"))
        return True
    except (ValueError, AttributeError):
        return False


def x__is_valid_iso8601__mutmut_1(value: str) -> bool:
    """Check if a string looks like a valid ISO 8601 timestamp.

    Accepts common formats: YYYY-MM-DDTHH:MM:SSZ, YYYY-MM-DDTHH:MM:SS+00:00,
    and the output of datetime.isoformat().
    """
    try:
        # Python 3.11+ datetime.fromisoformat handles Z suffix
        datetime.fromisoformat(None)
        return True
    except (ValueError, AttributeError):
        return False


def x__is_valid_iso8601__mutmut_2(value: str) -> bool:
    """Check if a string looks like a valid ISO 8601 timestamp.

    Accepts common formats: YYYY-MM-DDTHH:MM:SSZ, YYYY-MM-DDTHH:MM:SS+00:00,
    and the output of datetime.isoformat().
    """
    try:
        # Python 3.11+ datetime.fromisoformat handles Z suffix
        datetime.fromisoformat(value.replace(None, "+00:00"))
        return True
    except (ValueError, AttributeError):
        return False


def x__is_valid_iso8601__mutmut_3(value: str) -> bool:
    """Check if a string looks like a valid ISO 8601 timestamp.

    Accepts common formats: YYYY-MM-DDTHH:MM:SSZ, YYYY-MM-DDTHH:MM:SS+00:00,
    and the output of datetime.isoformat().
    """
    try:
        # Python 3.11+ datetime.fromisoformat handles Z suffix
        datetime.fromisoformat(value.replace("Z", None))
        return True
    except (ValueError, AttributeError):
        return False


def x__is_valid_iso8601__mutmut_4(value: str) -> bool:
    """Check if a string looks like a valid ISO 8601 timestamp.

    Accepts common formats: YYYY-MM-DDTHH:MM:SSZ, YYYY-MM-DDTHH:MM:SS+00:00,
    and the output of datetime.isoformat().
    """
    try:
        # Python 3.11+ datetime.fromisoformat handles Z suffix
        datetime.fromisoformat(value.replace("+00:00"))
        return True
    except (ValueError, AttributeError):
        return False


def x__is_valid_iso8601__mutmut_5(value: str) -> bool:
    """Check if a string looks like a valid ISO 8601 timestamp.

    Accepts common formats: YYYY-MM-DDTHH:MM:SSZ, YYYY-MM-DDTHH:MM:SS+00:00,
    and the output of datetime.isoformat().
    """
    try:
        # Python 3.11+ datetime.fromisoformat handles Z suffix
        datetime.fromisoformat(value.replace("Z", ))
        return True
    except (ValueError, AttributeError):
        return False


def x__is_valid_iso8601__mutmut_6(value: str) -> bool:
    """Check if a string looks like a valid ISO 8601 timestamp.

    Accepts common formats: YYYY-MM-DDTHH:MM:SSZ, YYYY-MM-DDTHH:MM:SS+00:00,
    and the output of datetime.isoformat().
    """
    try:
        # Python 3.11+ datetime.fromisoformat handles Z suffix
        datetime.fromisoformat(value.replace("XXZXX", "+00:00"))
        return True
    except (ValueError, AttributeError):
        return False


def x__is_valid_iso8601__mutmut_7(value: str) -> bool:
    """Check if a string looks like a valid ISO 8601 timestamp.

    Accepts common formats: YYYY-MM-DDTHH:MM:SSZ, YYYY-MM-DDTHH:MM:SS+00:00,
    and the output of datetime.isoformat().
    """
    try:
        # Python 3.11+ datetime.fromisoformat handles Z suffix
        datetime.fromisoformat(value.replace("z", "+00:00"))
        return True
    except (ValueError, AttributeError):
        return False


def x__is_valid_iso8601__mutmut_8(value: str) -> bool:
    """Check if a string looks like a valid ISO 8601 timestamp.

    Accepts common formats: YYYY-MM-DDTHH:MM:SSZ, YYYY-MM-DDTHH:MM:SS+00:00,
    and the output of datetime.isoformat().
    """
    try:
        # Python 3.11+ datetime.fromisoformat handles Z suffix
        datetime.fromisoformat(value.replace("Z", "XX+00:00XX"))
        return True
    except (ValueError, AttributeError):
        return False


def x__is_valid_iso8601__mutmut_9(value: str) -> bool:
    """Check if a string looks like a valid ISO 8601 timestamp.

    Accepts common formats: YYYY-MM-DDTHH:MM:SSZ, YYYY-MM-DDTHH:MM:SS+00:00,
    and the output of datetime.isoformat().
    """
    try:
        # Python 3.11+ datetime.fromisoformat handles Z suffix
        datetime.fromisoformat(value.replace("Z", "+00:00"))
        return False
    except (ValueError, AttributeError):
        return False


def x__is_valid_iso8601__mutmut_10(value: str) -> bool:
    """Check if a string looks like a valid ISO 8601 timestamp.

    Accepts common formats: YYYY-MM-DDTHH:MM:SSZ, YYYY-MM-DDTHH:MM:SS+00:00,
    and the output of datetime.isoformat().
    """
    try:
        # Python 3.11+ datetime.fromisoformat handles Z suffix
        datetime.fromisoformat(value.replace("Z", "+00:00"))
        return True
    except (ValueError, AttributeError):
        return True

x__is_valid_iso8601__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x__is_valid_iso8601__mutmut_1': x__is_valid_iso8601__mutmut_1, 
    'x__is_valid_iso8601__mutmut_2': x__is_valid_iso8601__mutmut_2, 
    'x__is_valid_iso8601__mutmut_3': x__is_valid_iso8601__mutmut_3, 
    'x__is_valid_iso8601__mutmut_4': x__is_valid_iso8601__mutmut_4, 
    'x__is_valid_iso8601__mutmut_5': x__is_valid_iso8601__mutmut_5, 
    'x__is_valid_iso8601__mutmut_6': x__is_valid_iso8601__mutmut_6, 
    'x__is_valid_iso8601__mutmut_7': x__is_valid_iso8601__mutmut_7, 
    'x__is_valid_iso8601__mutmut_8': x__is_valid_iso8601__mutmut_8, 
    'x__is_valid_iso8601__mutmut_9': x__is_valid_iso8601__mutmut_9, 
    'x__is_valid_iso8601__mutmut_10': x__is_valid_iso8601__mutmut_10
}
x__is_valid_iso8601__mutmut_orig.__name__ = 'x__is_valid_iso8601'
