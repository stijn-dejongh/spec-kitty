"""Cross-repo drift detection and reconciliation event generation.

Scans target repositories for WP-linked branches and commits,
compares against the canonical snapshot state, and generates
StatusEvent objects to align planning with implementation reality.
"""

from __future__ import annotations

import json
import logging
import re
import subprocess
from dataclasses import dataclass, field
from datetime import datetime, timezone
from pathlib import Path
from typing import Any

import ulid

from specify_cli.status.models import Lane, StatusEvent, StatusSnapshot
from specify_cli.status.reducer import reduce, SNAPSHOT_FILENAME
from specify_cli.status.store import read_events
from specify_cli.status.transitions import (
    ALLOWED_TRANSITIONS,
    TERMINAL_LANES,
    is_terminal,
    validate_transition,
)
from specify_cli.status.phase import resolve_phase
from specify_cli.status.emit import emit_status_transition

logger = logging.getLogger(__name__)

# Regex for extracting WP IDs from branch names and commit messages
_WP_PATTERN = re.compile(r"\bWP(\d{2})\b")

# Subprocess timeout for git commands (seconds)
_GIT_TIMEOUT = 30
_GIT_LOG_TIMEOUT = 60
from typing import Annotated
from typing import Callable
from typing import ClassVar

MutantDict = Annotated[dict[str, Callable], "Mutant"] # type: ignore


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None): # type: ignore
    """Forward call to original or mutated function, depending on the environment"""
    import os # type: ignore
    mutant_under_test = os.environ['MUTANT_UNDER_TEST'] # type: ignore
    if mutant_under_test == 'fail': # type: ignore
        from mutmut.__main__ import MutmutProgrammaticFailException # type: ignore
        raise MutmutProgrammaticFailException('Failed programmatically')       # type: ignore
    elif mutant_under_test == 'stats': # type: ignore
        from mutmut.__main__ import record_trampoline_hit # type: ignore
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__) # type: ignore
        # (for class methods, orig is bound and thus does not need the explicit self argument)
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_' # type: ignore
    if not mutant_under_test.startswith(prefix): # type: ignore
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    mutant_name = mutant_under_test.rpartition('.')[-1] # type: ignore
    if self_arg is not None: # type: ignore
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs) # type: ignore
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs) # type: ignore
    return result # type: ignore


@dataclass(frozen=True)
class CommitInfo:
    """Evidence of a commit linked to a work package."""

    sha: str  # 7-40 hex chars
    branch: str  # Branch where found
    message: str  # Commit message (first line)
    author: str  # Author name
    date: str  # ISO 8601 UTC timestamp


@dataclass
class ReconcileResult:
    """Result of a reconciliation scan."""

    suggested_events: list[StatusEvent] = field(default_factory=list)
    drift_detected: bool = False
    details: list[str] = field(default_factory=list)
    errors: list[str] = field(default_factory=list)
    target_repos_scanned: int = 0
    wps_analyzed: int = 0


def scan_for_wp_commits(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    args = [repo_path, feature_slug]# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x_scan_for_wp_commits__mutmut_orig, x_scan_for_wp_commits__mutmut_mutants, args, kwargs, None)


def x_scan_for_wp_commits__mutmut_orig(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_1(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_2(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(None)

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_3(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = None
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_4(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = None

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_5(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = None
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_6(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            None,
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_7(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=None,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_8(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=None,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_9(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=None,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_10(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding=None,
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_11(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors=None,
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_12(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=None,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_13(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_14(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_15(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_16(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_17(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_18(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_19(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_20(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["XXgitXX", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_21(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["GIT", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_22(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "XXbranchXX", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_23(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "BRANCH", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_24(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "XX-aXX", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_25(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-A", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_26(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "XX--listXX", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_27(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--LIST", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_28(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=False,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_29(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=False,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_30(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="XXutf-8XX",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_31(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="UTF-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_32(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="XXreplaceXX",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_33(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="REPLACE",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_34(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 or branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_35(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode != 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_36(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 1 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_37(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split(None):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_38(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("XX\nXX"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_39(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = None
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_40(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip(None)
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_41(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().rstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_42(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("XX* XX")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_43(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(None):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_44(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = None
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_45(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(None)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_46(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(2)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_47(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(None)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_48(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = None
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_49(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith(None):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_50(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("XXremotes/origin/XX"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_51(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("REMOTES/ORIGIN/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_52(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = None

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_53(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = None
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_54(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            None,
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_55(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=None,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_56(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=None,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_57(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=None,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_58(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding=None,
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_59(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors=None,
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_60(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=None,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_61(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_62(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_63(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_64(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_65(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_66(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_67(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_68(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "XXgitXX", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_69(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "GIT", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_70(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "XXlogXX", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_71(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "LOG", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_72(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "XX-1XX",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_73(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "XX--format=%H%n%s%n%an%n%aIXX",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_74(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%h%n%s%n%an%n%ai",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_75(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--FORMAT=%H%N%S%N%AN%N%AI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_76(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=False,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_77(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=False,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_78(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="XXutf-8XX",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_79(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="UTF-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_80(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="XXreplaceXX",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_81(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="REPLACE",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_82(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 or log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_83(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode != 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_84(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 1 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_85(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = None
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_86(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split(None)
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_87(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("XX\nXX")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_88(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) > 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_89(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 5:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_90(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = None
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_91(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=None,
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_92(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=None,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_93(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=None,
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_94(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=None,
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_95(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=None,
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_96(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_97(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_98(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_99(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_100(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_101(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[1][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_102(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:41],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_103(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[2],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_104(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[3],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_105(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[4],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_106(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(None)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_107(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(None, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_108(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, None).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_109(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault([]).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_110(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, ).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_111(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            None,
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_112(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            None, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_113(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, None,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_114(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_115(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_116(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_117(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "XXTimeout getting log for branch %s in %sXX",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_118(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_119(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "TIMEOUT GETTING LOG FOR BRANCH %S IN %S",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_120(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning(None, repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_121(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", None)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_122(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning(repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_123(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", )

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_124(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("XXTimeout listing branches in %sXX", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_125(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_126(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("TIMEOUT LISTING BRANCHES IN %S", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_127(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = None
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_128(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            None,
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_129(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=None,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_130(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=None,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_131(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=None,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_132(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding=None,
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_133(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors=None,
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_134(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=None,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_135(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_136(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_137(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_138(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_139(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_140(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_141(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_142(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "XXgitXX", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_143(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "GIT", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_144(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "XXlogXX", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_145(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "LOG", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_146(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "XX--allXX", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_147(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--ALL", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_148(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "XX--onelineXX",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_149(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--ONELINE",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_150(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "XX--format=%H %sXX",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_151(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%h %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_152(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--FORMAT=%H %S",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_153(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=False,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_154(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=False,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_155(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="XXutf-8XX",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_156(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="UTF-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_157(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="XXreplaceXX",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_158(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="REPLACE",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_159(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 or grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_160(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode != 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_161(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 1 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_162(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split(None):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_163(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("XX\nXX"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_164(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_165(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    break
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_166(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = None
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_167(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(None, 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_168(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", None)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_169(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_170(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", )
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_171(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.rsplit(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_172(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split("XX XX", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_173(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 2)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_174(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) <= 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_175(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 3:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_176(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    break
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_177(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = None
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_178(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[1]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_179(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = None

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_180(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[2]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_181(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(None):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_182(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = None
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_183(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(None)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_184(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(2)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_185(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(None)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_186(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = None
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_187(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            None,
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_188(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=None,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_189(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=None,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_190(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=None,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_191(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding=None,
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_192(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors=None,
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_193(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=None,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_194(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_195(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_196(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_197(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_198(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_199(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_200(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_201(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "XXgitXX", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_202(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "GIT", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_203(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "XXlogXX", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_204(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "LOG", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_205(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "XX-1XX",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_206(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "XX--format=%an%n%aIXX",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_207(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%ai",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_208(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--FORMAT=%AN%N%AI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_209(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=False,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_210(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=False,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_211(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="XXutf-8XX",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_212(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="UTF-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_213(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="XXreplaceXX",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_214(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="REPLACE",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_215(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 or detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_216(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode != 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_217(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 1 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_218(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = None
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_219(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split(None)
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_220(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("XX\nXX")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_221(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) > 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_222(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 3:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_223(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = None
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_224(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=None,
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_225(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch=None,
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_226(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=None,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_227(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=None,
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_228(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=None,
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_229(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_230(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_231(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_232(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_233(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_234(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:41],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_235(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="XX(commit-message)XX",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_236(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(COMMIT-MESSAGE)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_237(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[1],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_238(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[2],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_239(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = None
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_240(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(None, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_241(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, None)
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_242(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get([])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_243(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, )
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_244(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:41] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_245(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_246(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        None
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_247(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(None, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_248(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, None).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_249(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault([]).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_250(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, ).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_251(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            None,
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_252(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            None, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_253(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], None,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_254(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_255(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_256(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_257(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "XXTimeout getting commit detail for %s in %sXX",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_258(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_259(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "TIMEOUT GETTING COMMIT DETAIL FOR %S IN %S",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_260(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:9], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_261(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning(None, repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_262(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", None)

    return result_map


def x_scan_for_wp_commits__mutmut_263(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning(repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_264(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout scanning commit messages in %s", )

    return result_map


def x_scan_for_wp_commits__mutmut_265(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("XXTimeout scanning commit messages in %sXX", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_266(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("timeout scanning commit messages in %s", repo_path)

    return result_map


def x_scan_for_wp_commits__mutmut_267(
    repo_path: Path,
    feature_slug: str,
) -> dict[str, list[CommitInfo]]:
    """Scan a repository for WP-linked branches and commit messages.

    Uses subprocess.run with timeouts for all git operations.
    Returns mapping of WP ID -> list of CommitInfo found.
    """
    if not repo_path.exists():
        raise FileNotFoundError(f"Repository path does not exist: {repo_path}")

    result_map: dict[str, list[CommitInfo]] = {}
    discovered_wp_ids: set[str] = set()

    # Step 1: Branch detection -- find branches matching the feature slug
    try:
        branch_result = subprocess.run(
            ["git", "branch", "-a", "--list", f"*{feature_slug}*"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_TIMEOUT,
        )
        if branch_result.returncode == 0 and branch_result.stdout.strip():
            for raw_line in branch_result.stdout.strip().split("\n"):
                branch_name = raw_line.strip().lstrip("* ")
                # Extract WP IDs from branch name
                for match in _WP_PATTERN.finditer(branch_name):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get the latest commit on this branch
                    display_branch = branch_name
                    if branch_name.startswith("remotes/origin/"):
                        display_branch = branch_name[len("remotes/origin/"):]

                    try:
                        log_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%H%n%s%n%an%n%aI",
                                branch_name,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if log_result.returncode == 0 and log_result.stdout.strip():
                            parts = log_result.stdout.strip().split("\n")
                            if len(parts) >= 4:
                                commit_info = CommitInfo(
                                    sha=parts[0][:40],
                                    branch=display_branch,
                                    message=parts[1],
                                    author=parts[2],
                                    date=parts[3],
                                )
                                result_map.setdefault(wp_id, []).append(commit_info)
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting log for branch %s in %s",
                            branch_name, repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("Timeout listing branches in %s", repo_path)

    # Step 2: Commit message scanning -- search for commits mentioning WP IDs
    try:
        grep_result = subprocess.run(
            [
                "git", "log", "--all", "--oneline",
                f"--grep={feature_slug}",
                "--format=%H %s",
            ],
            cwd=repo_path,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
            timeout=_GIT_LOG_TIMEOUT,
        )
        if grep_result.returncode == 0 and grep_result.stdout.strip():
            for line in grep_result.stdout.strip().split("\n"):
                if not line.strip():
                    continue
                # Parse "SHA message"
                parts = line.split(" ", 1)
                if len(parts) < 2:
                    continue
                sha = parts[0]
                message = parts[1]

                for match in _WP_PATTERN.finditer(message):
                    wp_id = f"WP{match.group(1)}"
                    discovered_wp_ids.add(wp_id)

                    # Get full commit metadata
                    try:
                        detail_result = subprocess.run(
                            [
                                "git", "log", "-1",
                                "--format=%an%n%aI",
                                sha,
                            ],
                            cwd=repo_path,
                            capture_output=True,
                            text=True,
                            encoding="utf-8",
                            errors="replace",
                            timeout=_GIT_TIMEOUT,
                        )
                        if detail_result.returncode == 0 and detail_result.stdout.strip():
                            detail_parts = detail_result.stdout.strip().split("\n")
                            if len(detail_parts) >= 2:
                                commit_info = CommitInfo(
                                    sha=sha[:40],
                                    branch="(commit-message)",
                                    message=message,
                                    author=detail_parts[0],
                                    date=detail_parts[1],
                                )
                                # Avoid duplicate SHAs per WP
                                existing_shas = {
                                    c.sha for c in result_map.get(wp_id, [])
                                }
                                if sha[:40] not in existing_shas:
                                    result_map.setdefault(wp_id, []).append(
                                        commit_info
                                    )
                    except subprocess.TimeoutExpired:
                        logger.warning(
                            "Timeout getting commit detail for %s in %s",
                            sha[:8], repo_path,
                        )
    except subprocess.TimeoutExpired:
        logger.warning("TIMEOUT SCANNING COMMIT MESSAGES IN %S", repo_path)

    return result_map

x_scan_for_wp_commits__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x_scan_for_wp_commits__mutmut_1': x_scan_for_wp_commits__mutmut_1, 
    'x_scan_for_wp_commits__mutmut_2': x_scan_for_wp_commits__mutmut_2, 
    'x_scan_for_wp_commits__mutmut_3': x_scan_for_wp_commits__mutmut_3, 
    'x_scan_for_wp_commits__mutmut_4': x_scan_for_wp_commits__mutmut_4, 
    'x_scan_for_wp_commits__mutmut_5': x_scan_for_wp_commits__mutmut_5, 
    'x_scan_for_wp_commits__mutmut_6': x_scan_for_wp_commits__mutmut_6, 
    'x_scan_for_wp_commits__mutmut_7': x_scan_for_wp_commits__mutmut_7, 
    'x_scan_for_wp_commits__mutmut_8': x_scan_for_wp_commits__mutmut_8, 
    'x_scan_for_wp_commits__mutmut_9': x_scan_for_wp_commits__mutmut_9, 
    'x_scan_for_wp_commits__mutmut_10': x_scan_for_wp_commits__mutmut_10, 
    'x_scan_for_wp_commits__mutmut_11': x_scan_for_wp_commits__mutmut_11, 
    'x_scan_for_wp_commits__mutmut_12': x_scan_for_wp_commits__mutmut_12, 
    'x_scan_for_wp_commits__mutmut_13': x_scan_for_wp_commits__mutmut_13, 
    'x_scan_for_wp_commits__mutmut_14': x_scan_for_wp_commits__mutmut_14, 
    'x_scan_for_wp_commits__mutmut_15': x_scan_for_wp_commits__mutmut_15, 
    'x_scan_for_wp_commits__mutmut_16': x_scan_for_wp_commits__mutmut_16, 
    'x_scan_for_wp_commits__mutmut_17': x_scan_for_wp_commits__mutmut_17, 
    'x_scan_for_wp_commits__mutmut_18': x_scan_for_wp_commits__mutmut_18, 
    'x_scan_for_wp_commits__mutmut_19': x_scan_for_wp_commits__mutmut_19, 
    'x_scan_for_wp_commits__mutmut_20': x_scan_for_wp_commits__mutmut_20, 
    'x_scan_for_wp_commits__mutmut_21': x_scan_for_wp_commits__mutmut_21, 
    'x_scan_for_wp_commits__mutmut_22': x_scan_for_wp_commits__mutmut_22, 
    'x_scan_for_wp_commits__mutmut_23': x_scan_for_wp_commits__mutmut_23, 
    'x_scan_for_wp_commits__mutmut_24': x_scan_for_wp_commits__mutmut_24, 
    'x_scan_for_wp_commits__mutmut_25': x_scan_for_wp_commits__mutmut_25, 
    'x_scan_for_wp_commits__mutmut_26': x_scan_for_wp_commits__mutmut_26, 
    'x_scan_for_wp_commits__mutmut_27': x_scan_for_wp_commits__mutmut_27, 
    'x_scan_for_wp_commits__mutmut_28': x_scan_for_wp_commits__mutmut_28, 

    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_19(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_20(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_21(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "XXgitXX", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_22(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "GIT", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_23(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "XXbranchXX", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_24(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "BRANCH", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_25(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "XX--mergedXX", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_26(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--MERGED", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_27(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "XX--listXX", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_28(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--LIST", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_29(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=False,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_30(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=False,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_31(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="XXutf-8XX",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_32(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="UTF-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_33(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="XXreplaceXX",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_34(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="REPLACE",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_35(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 or result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_36(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode != 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_37(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 1 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_38(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split(None):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_39(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("XX\nXX"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_40(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = None
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_41(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip(None)
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_42(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().rstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_43(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("XX* XX")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_44(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(None):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_45(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(None)
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_46(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(None)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_47(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(2)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_48(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                return  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_49(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                None,
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_50(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                None, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_51(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, None,
            )

    return merged


def x__get_merged_wps__mutmut_52(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_53(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_54(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "Timeout checking merged branches against %s in %s",
                base_branch, )

    return merged


def x__get_merged_wps__mutmut_55(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "XXTimeout checking merged branches against %s in %sXX",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_56(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "timeout checking merged branches against %s in %s",
                base_branch, repo_path,
            )

    return merged


def x__get_merged_wps__mutmut_57(
    repo_path: Path,
    feature_slug: str,
) -> set[str]:
    """Check which WP branches are merged into main/master."""
    merged: set[str] = set()

    # Try main, then master
    for base_branch in ("main", "master"):
        try:
            result = subprocess.run(
                [
                    "git", "branch", "--merged", base_branch,
                    "--list", f"*{feature_slug}*",
                ],
                cwd=repo_path,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
                timeout=_GIT_TIMEOUT,
            )
            if result.returncode == 0 and result.stdout.strip():
                for line in result.stdout.strip().split("\n"):
                    branch_name = line.strip().lstrip("* ")
                    for match in _WP_PATTERN.finditer(branch_name):
                        merged.add(f"WP{match.group(1)}")
                break  # Found a valid base branch
        except subprocess.TimeoutExpired:
            logger.warning(
                "TIMEOUT CHECKING MERGED BRANCHES AGAINST %S IN %S",
                base_branch, repo_path,
            )

    return merged

x__get_merged_wps__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x__get_merged_wps__mutmut_1': x__get_merged_wps__mutmut_1, 
    'x__get_merged_wps__mutmut_2': x__get_merged_wps__mutmut_2, 
    'x__get_merged_wps__mutmut_3': x__get_merged_wps__mutmut_3, 
    'x__get_merged_wps__mutmut_4': x__get_merged_wps__mutmut_4, 
    'x__get_merged_wps__mutmut_5': x__get_merged_wps__mutmut_5, 
    'x__get_merged_wps__mutmut_6': x__get_merged_wps__mutmut_6, 
    'x__get_merged_wps__mutmut_7': x__get_merged_wps__mutmut_7, 
    'x__get_merged_wps__mutmut_8': x__get_merged_wps__mutmut_8, 
    'x__get_merged_wps__mutmut_9': x__get_merged_wps__mutmut_9, 
    'x__get_merged_wps__mutmut_10': x__get_merged_wps__mutmut_10, 
    'x__get_merged_wps__mutmut_11': x__get_merged_wps__mutmut_11, 
    'x__get_merged_wps__mutmut_12': x__get_merged_wps__mutmut_12, 
    'x__get_merged_wps__mutmut_13': x__get_merged_wps__mutmut_13, 
    'x__get_merged_wps__mutmut_14': x__get_merged_wps__mutmut_14, 
    'x__get_merged_wps__mutmut_15': x__get_merged_wps__mutmut_15, 
    'x__get_merged_wps__mutmut_16': x__get_merged_wps__mutmut_16, 
    'x__get_merged_wps__mutmut_17': x__get_merged_wps__mutmut_17, 
# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x__get_current_lane__mutmut_orig, x__get_current_lane__mutmut_mutants, args, kwargs, None)


def x__get_current_lane__mutmut_orig(
    snapshot: StatusSnapshot,
    wp_id: str,
) -> Lane:
    """Get current lane for a WP from the snapshot, defaulting to PLANNED."""
    wp_state = snapshot.work_packages.get(wp_id)
    if wp_state is None:
        return Lane.PLANNED
    try:
        return Lane(wp_state["lane"])
    except (KeyError, ValueError):
        return Lane.PLANNED


def x__get_current_lane__mutmut_1(
    snapshot: StatusSnapshot,
    wp_id: str,
) -> Lane:
    """Get current lane for a WP from the snapshot, defaulting to PLANNED."""
    wp_state = None
    if wp_state is None:
        return Lane.PLANNED
    try:
        return Lane(wp_state["lane"])
    except (KeyError, ValueError):
        return Lane.PLANNED


def x__get_current_lane__mutmut_2(
    snapshot: StatusSnapshot,
    wp_id: str,
) -> Lane:
    """Get current lane for a WP from the snapshot, defaulting to PLANNED."""
    wp_state = snapshot.work_packages.get(None)
    if wp_state is None:
        return Lane.PLANNED
    try:
        return Lane(wp_state["lane"])
    except (KeyError, ValueError):
        return Lane.PLANNED


def x__get_current_lane__mutmut_3(
    snapshot: StatusSnapshot,
    wp_id: str,
) -> Lane:
    """Get current lane for a WP from the snapshot, defaulting to PLANNED."""
    wp_state = snapshot.work_packages.get(wp_id)
    if wp_state is not None:
        return Lane.PLANNED
    try:
        return Lane(wp_state["lane"])
    except (KeyError, ValueError):
        return Lane.PLANNED


def x__get_current_lane__mutmut_4(
    snapshot: StatusSnapshot,
    wp_id: str,
) -> Lane:
    """Get current lane for a WP from the snapshot, defaulting to PLANNED."""
    wp_state = snapshot.work_packages.get(wp_id)
    if wp_state is None:
        return Lane.PLANNED
    try:
        return Lane(None)
    except (KeyError, ValueError):
        return Lane.PLANNED


def x__get_current_lane__mutmut_5(
    snapshot: StatusSnapshot,
    wp_id: str,
) -> Lane:
    """Get current lane for a WP from the snapshot, defaulting to PLANNED."""
    wp_state = snapshot.work_packages.get(wp_id)
    if wp_state is None:
        return Lane.PLANNED
    try:
        return Lane(wp_state["XXlaneXX"])
    except (KeyError, ValueError):
        return Lane.PLANNED


def x__get_current_lane__mutmut_6(
    snapshot: StatusSnapshot,
    wp_id: str,
) -> Lane:
    """Get current lane for a WP from the snapshot, defaulting to PLANNED."""
    wp_state = snapshot.work_packages.get(wp_id)
    if wp_state is None:
        return Lane.PLANNED
    try:
        return Lane(wp_state["LANE"])
    except (KeyError, ValueError):
        return Lane.PLANNED

x__get_current_lane__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x__get_current_lane__mutmut_1': x__get_current_lane__mutmut_1, 
    'x__get_current_lane__mutmut_2': x__get_current_lane__mutmut_2, 
    'x__get_current_lane__mutmut_3': x__get_current_lane__mutmut_3, 
    'x__get_current_lane__mutmut_4': x__get_current_lane__mutmut_4, 
    'x__get_current_lane__mutmut_5': x__get_current_lane__mutmut_5, 
    'x__get_current_lane__mutmut_6': x__get_current_lane__mutmut_6
}
x__get_current_lane__mutmut_orig.__name__ = 'x__get_current_lane'


def _lane_advancement_chain(
    from_lane: Lane,
    to_lane: Lane,
) -> list[tuple[Lane, Lane]]:
    args = [from_lane, to_lane]# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x__lane_advancement_chain__mutmut_orig, x__lane_advancement_chain__mutmut_mutants, args, kwargs, None)


def x__lane_advancement_chain__mutmut_orig(
    from_lane: Lane,
    to_lane: Lane,
) -> list[tuple[Lane, Lane]]:
    """Generate legal lane transition chain from from_lane to to_lane.

    The state machine does not allow skipping lanes without force,
    so we generate intermediate transitions.
    """
    # Define the forward progression order
    progression = [
        Lane.PLANNED,
        Lane.CLAIMED,
        Lane.IN_PROGRESS,
        Lane.FOR_REVIEW,
        Lane.DONE,
    ]

    try:
        from_idx = progression.index(from_lane)
        to_idx = progression.index(to_lane)
    except ValueError:
        # Lane not in normal progression (blocked, canceled, etc.)
        return []

    if to_idx <= from_idx:
        return []  # No forward advancement needed

    chain: list[tuple[Lane, Lane]] = []
    for i in range(from_idx, to_idx):
        pair = (progression[i], progression[i + 1])
        # Verify this is an allowed transition
        if (str(pair[0]), str(pair[1])) in ALLOWED_TRANSITIONS:
            chain.append(pair)
        else:
            # Can't build a legal chain
            return []

    return chain


def x__lane_advancement_chain__mutmut_1(
    from_lane: Lane,
    to_lane: Lane,
) -> list[tuple[Lane, Lane]]:
    """Generate legal lane transition chain from from_lane to to_lane.

    The state machine does not allow skipping lanes without force,
    so we generate intermediate transitions.
    """
    # Define the forward progression order
    progression = None

    try:
        from_idx = progression.index(from_lane)
        to_idx = progression.index(to_lane)
    except ValueError:
        # Lane not in normal progression (blocked, canceled, etc.)
        return []

    if to_idx <= from_idx:
        return []  # No forward advancement needed

    chain: list[tuple[Lane, Lane]] = []
    for i in range(from_idx, to_idx):
        pair = (progression[i], progression[i + 1])
        # Verify this is an allowed transition
        if (str(pair[0]), str(pair[1])) in ALLOWED_TRANSITIONS:
            chain.append(pair)
        else:
            # Can't build a legal chain
            return []

    return chain


def x__lane_advancement_chain__mutmut_2(
    from_lane: Lane,
    to_lane: Lane,
) -> list[tuple[Lane, Lane]]:
    """Generate legal lane transition chain from from_lane to to_lane.

    The state machine does not allow skipping lanes without force,
    so we generate intermediate transitions.
    """
    # Define the forward progression order
    progression = [
        Lane.PLANNED,
        Lane.CLAIMED,
        Lane.IN_PROGRESS,
        Lane.FOR_REVIEW,
        Lane.DONE,
    ]

    try:
        from_idx = None
        to_idx = progression.index(to_lane)
    except ValueError:
        # Lane not in normal progression (blocked, canceled, etc.)
        return []

    if to_idx <= from_idx:
        return []  # No forward advancement needed

    chain: list[tuple[Lane, Lane]] = []
    for i in range(from_idx, to_idx):
        pair = (progression[i], progression[i + 1])
        # Verify this is an allowed transition
        if (str(pair[0]), str(pair[1])) in ALLOWED_TRANSITIONS:
            chain.append(pair)
        else:
            # Can't build a legal chain
            return []

    return chain


def x__lane_advancement_chain__mutmut_3(
    from_lane: Lane,
    to_lane: Lane,
) -> list[tuple[Lane, Lane]]:
    """Generate legal lane transition chain from from_lane to to_lane.

    The state machine does not allow skipping lanes without force,
    so we generate intermediate transitions.
    """
    # Define the forward progression order
    progression = [
        Lane.PLANNED,
        Lane.CLAIMED,
        Lane.IN_PROGRESS,
        Lane.FOR_REVIEW,
        Lane.DONE,
    ]

    try:
        from_idx = progression.index(None)
        to_idx = progression.index(to_lane)
    except ValueError:
        # Lane not in normal progression (blocked, canceled, etc.)
        return []

    if to_idx <= from_idx:
        return []  # No forward advancement needed

    chain: list[tuple[Lane, Lane]] = []
    for i in range(from_idx, to_idx):
        pair = (progression[i], progression[i + 1])
        # Verify this is an allowed transition
        if (str(pair[0]), str(pair[1])) in ALLOWED_TRANSITIONS:
            chain.append(pair)
        else:
            # Can't build a legal chain
            return []

    return chain


def x__lane_advancement_chain__mutmut_4(
    from_lane: Lane,
    to_lane: Lane,
) -> list[tuple[Lane, Lane]]:
    """Generate legal lane transition chain from from_lane to to_lane.

    The state machine does not allow skipping lanes without force,
    so we generate intermediate transitions.
    """
    # Define the forward progression order
    progression = [
        Lane.PLANNED,
        Lane.CLAIMED,
        Lane.IN_PROGRESS,
        Lane.FOR_REVIEW,
        Lane.DONE,
    ]

    try:
        from_idx = progression.rindex(from_lane)
        to_idx = progression.index(to_lane)
    except ValueError:
        # Lane not in normal progression (blocked, canceled, etc.)
        return []

    if to_idx <= from_idx:
        return []  # No forward advancement needed

    chain: list[tuple[Lane, Lane]] = []
    for i in range(from_idx, to_idx):
        pair = (progression[i], progression[i + 1])
        # Verify this is an allowed transition
        if (str(pair[0]), str(pair[1])) in ALLOWED_TRANSITIONS:
            chain.append(pair)
        else:
            # Can't build a legal chain
            return []

    return chain


def x__lane_advancement_chain__mutmut_5(
    from_lane: Lane,
    to_lane: Lane,
) -> list[tuple[Lane, Lane]]:
    """Generate legal lane transition chain from from_lane to to_lane.

    The state machine does not allow skipping lanes without force,
    so we generate intermediate transitions.
    """
    # Define the forward progression order
    progression = [
        Lane.PLANNED,
        Lane.CLAIMED,
        Lane.IN_PROGRESS,
        Lane.FOR_REVIEW,
        Lane.DONE,
    ]

    try:
        from_idx = progression.index(from_lane)
        to_idx = None
    except ValueError:
        # Lane not in normal progression (blocked, canceled, etc.)
        return []

    if to_idx <= from_idx:
        return []  # No forward advancement needed

    chain: list[tuple[Lane, Lane]] = []
    for i in range(from_idx, to_idx):
        pair = (progression[i], progression[i + 1])
        # Verify this is an allowed transition
        if (str(pair[0]), str(pair[1])) in ALLOWED_TRANSITIONS:
            chain.append(pair)
        else:
            # Can't build a legal chain
            return []

    return chain


def x__lane_advancement_chain__mutmut_6(
    from_lane: Lane,
    to_lane: Lane,
) -> list[tuple[Lane, Lane]]:
    """Generate legal lane transition chain from from_lane to to_lane.

    The state machine does not allow skipping lanes without force,
    so we generate intermediate transitions.
    """
    # Define the forward progression order
    progression = [
        Lane.PLANNED,
        Lane.CLAIMED,
        Lane.IN_PROGRESS,
        Lane.FOR_REVIEW,
        Lane.DONE,
    ]

    try:
        from_idx = progression.index(from_lane)
        to_idx = progression.index(None)
    except ValueError:
        # Lane not in normal progression (blocked, canceled, etc.)
        return []

    if to_idx <= from_idx:
        return []  # No forward advancement needed

    chain: list[tuple[Lane, Lane]] = []
    for i in range(from_idx, to_idx):
        pair = (progression[i], progression[i + 1])
        # Verify this is an allowed transition
        if (str(pair[0]), str(pair[1])) in ALLOWED_TRANSITIONS:
            chain.append(pair)
        else:
            # Can't build a legal chain
            return []

    return chain


def x__lane_advancement_chain__mutmut_7(
    from_lane: Lane,
    to_lane: Lane,
) -> list[tuple[Lane, Lane]]:
    """Generate legal lane transition chain from from_lane to to_lane.

    The state machine does not allow skipping lanes without force,
    so we generate intermediate transitions.
    """
    # Define the forward progression order
    progression = [
        Lane.PLANNED,
        Lane.CLAIMED,
        Lane.IN_PROGRESS,
        Lane.FOR_REVIEW,
        Lane.DONE,
    ]

    try:
        from_idx = progression.index(from_lane)
        to_idx = progression.rindex(to_lane)
    except ValueError:
        # Lane not in normal progression (blocked, canceled, etc.)
        return []

    if to_idx <= from_idx:
        return []  # No forward advancement needed

    chain: list[tuple[Lane, Lane]] = []
    for i in range(from_idx, to_idx):
        pair = (progression[i], progression[i + 1])
        # Verify this is an allowed transition
        if (str(pair[0]), str(pair[1])) in ALLOWED_TRANSITIONS:
            chain.append(pair)
        else:
            # Can't build a legal chain
            return []

    return chain


def x__lane_advancement_chain__mutmut_8(
    from_lane: Lane,
    to_lane: Lane,
) -> list[tuple[Lane, Lane]]:
    """Generate legal lane transition chain from from_lane to to_lane.

    The state machine does not allow skipping lanes without force,
    so we generate intermediate transitions.
    """
    # Define the forward progression order
    progression = [
        Lane.PLANNED,
        Lane.CLAIMED,
        Lane.IN_PROGRESS,
        Lane.FOR_REVIEW,
        Lane.DONE,
    ]

    try:
        from_idx = progression.index(from_lane)
        to_idx = progression.index(to_lane)
    except ValueError:
        # Lane not in normal progression (blocked, canceled, etc.)
        return []

    if to_idx < from_idx:
        return []  # No forward advancement needed

    chain: list[tuple[Lane, Lane]] = []
    for i in range(from_idx, to_idx):
        pair = (progression[i], progression[i + 1])
        # Verify this is an allowed transition
        if (str(pair[0]), str(pair[1])) in ALLOWED_TRANSITIONS:
            chain.append(pair)
        else:
            # Can't build a legal chain
            return []

    return chain


def x__lane_advancement_chain__mutmut_9(
    from_lane: Lane,
    to_lane: Lane,
) -> list[tuple[Lane, Lane]]:
    """Generate legal lane transition chain from from_lane to to_lane.

    The state machine does not allow skipping lanes without force,
    so we generate intermediate transitions.
    """
    # Define the forward progression order
    progression = [
        Lane.PLANNED,
        Lane.CLAIMED,
        Lane.IN_PROGRESS,
        Lane.FOR_REVIEW,
        Lane.DONE,
    ]

    try:
        from_idx = progression.index(from_lane)
        to_idx = progression.index(to_lane)
    except ValueError:
        # Lane not in normal progression (blocked, canceled, etc.)
        return []

    if to_idx <= from_idx:
        return []  # No forward advancement needed

    chain: list[tuple[Lane, Lane]] = None
    for i in range(from_idx, to_idx):
        pair = (progression[i], progression[i + 1])
        # Verify this is an allowed transition
        if (str(pair[0]), str(pair[1])) in ALLOWED_TRANSITIONS:
            chain.append(pair)
        else:
            # Can't build a legal chain
            return []

    return chain


def x__lane_advancement_chain__mutmut_10(
    from_lane: Lane,
    to_lane: Lane,
) -> list[tuple[Lane, Lane]]:
    """Generate legal lane transition chain from from_lane to to_lane.

    The state machine does not allow skipping lanes without force,
    so we generate intermediate transitions.
    """
    # Define the forward progression order
    progression = [
        Lane.PLANNED,
        Lane.CLAIMED,
        Lane.IN_PROGRESS,
        Lane.FOR_REVIEW,
        Lane.DONE,
    ]

    try:
        from_idx = progression.index(from_lane)
        to_idx = progression.index(to_lane)
    except ValueError:
        # Lane not in normal progression (blocked, canceled, etc.)
        return []

    if to_idx <= from_idx:
        return []  # No forward advancement needed

    chain: list[tuple[Lane, Lane]] = []
    for i in range(None, to_idx):
        pair = (progression[i], progression[i + 1])
        # Verify this is an allowed transition
        if (str(pair[0]), str(pair[1])) in ALLOWED_TRANSITIONS:
            chain.append(pair)
        else:
            # Can't build a legal chain
            return []

    return chain


def x__lane_advancement_chain__mutmut_11(
    from_lane: Lane,
    to_lane: Lane,
) -> list[tuple[Lane, Lane]]:
    """Generate legal lane transition chain from from_lane to to_lane.

    The state machine does not allow skipping lanes without force,
    so we generate intermediate transitions.
    """
    # Define the forward progression order
    progression = [
        Lane.PLANNED,
        Lane.CLAIMED,
        Lane.IN_PROGRESS,
        Lane.FOR_REVIEW,
        Lane.DONE,
    ]

    try:
        from_idx = progression.index(from_lane)
        to_idx = progression.index(to_lane)
    except ValueError:
        # Lane not in normal progression (blocked, canceled, etc.)
        return []

    if to_idx <= from_idx:
        return []  # No forward advancement needed

    chain: list[tuple[Lane, Lane]] = []
    for i in range(from_idx, None):
        pair = (progression[i], progression[i + 1])
        # Verify this is an allowed transition
        if (str(pair[0]), str(pair[1])) in ALLOWED_TRANSITIONS:
            chain.append(pair)
        else:
            # Can't build a legal chain
            return []

    return chain


def x__lane_advancement_chain__mutmut_12(
    from_lane: Lane,
    to_lane: Lane,
) -> list[tuple[Lane, Lane]]:
    """Generate legal lane transition chain from from_lane to to_lane.

    The state machine does not allow skipping lanes without force,
    so we generate intermediate transitions.
    """
    # Define the forward progression order
    progression = [
        Lane.PLANNED,
        Lane.CLAIMED,
        Lane.IN_PROGRESS,
        Lane.FOR_REVIEW,
        Lane.DONE,
    ]

    try:
        from_idx = progression.index(from_lane)
        to_idx = progression.index(to_lane)
    except ValueError:
        # Lane not in normal progression (blocked, canceled, etc.)
        return []

    if to_idx <= from_idx:
        return []  # No forward advancement needed

    chain: list[tuple[Lane, Lane]] = []
    for i in range(to_idx):
        pair = (progression[i], progression[i + 1])
        # Verify this is an allowed transition
        if (str(pair[0]), str(pair[1])) in ALLOWED_TRANSITIONS:
            chain.append(pair)
        else:
            # Can't build a legal chain
            return []

    return chain


def x__lane_advancement_chain__mutmut_13(
    from_lane: Lane,
    to_lane: Lane,
) -> list[tuple[Lane, Lane]]:
    """Generate legal lane transition chain from from_lane to to_lane.

    The state machine does not allow skipping lanes without force,
    so we generate intermediate transitions.
    """
    # Define the forward progression order
    progression = [
        Lane.PLANNED,
        Lane.CLAIMED,
        Lane.IN_PROGRESS,
        Lane.FOR_REVIEW,
        Lane.DONE,
    ]

    try:
        from_idx = progression.index(from_lane)
        to_idx = progression.index(to_lane)
    except ValueError:
        # Lane not in normal progression (blocked, canceled, etc.)
        return []

    if to_idx <= from_idx:
        return []  # No forward advancement needed

    chain: list[tuple[Lane, Lane]] = []
    for i in range(from_idx, ):
        pair = (progression[i], progression[i + 1])
        # Verify this is an allowed transition
        if (str(pair[0]), str(pair[1])) in ALLOWED_TRANSITIONS:
            chain.append(pair)
        else:
            # Can't build a legal chain
            return []

    return chain


def x__lane_advancement_chain__mutmut_14(
    from_lane: Lane,
    to_lane: Lane,
) -> list[tuple[Lane, Lane]]:
    """Generate legal lane transition chain from from_lane to to_lane.

    The state machine does not allow skipping lanes without force,
    so we generate intermediate transitions.
    """
    # Define the forward progression order
    progression = [
        Lane.PLANNED,
        Lane.CLAIMED,
        Lane.IN_PROGRESS,
        Lane.FOR_REVIEW,
        Lane.DONE,
    ]

    try:
        from_idx = progression.index(from_lane)
        to_idx = progression.index(to_lane)
    except ValueError:
        # Lane not in normal progression (blocked, canceled, etc.)
        return []

    if to_idx <= from_idx:
        return []  # No forward advancement needed

    chain: list[tuple[Lane, Lane]] = []
    for i in range(from_idx, to_idx):
        pair = None
        # Verify this is an allowed transition
        if (str(pair[0]), str(pair[1])) in ALLOWED_TRANSITIONS:
            chain.append(pair)
        else:
            # Can't build a legal chain
            return []

    return chain


def x__lane_advancement_chain__mutmut_15(
    from_lane: Lane,
    to_lane: Lane,
) -> list[tuple[Lane, Lane]]:
    """Generate legal lane transition chain from from_lane to to_lane.

    The state machine does not allow skipping lanes without force,
    so we generate intermediate transitions.
    """
    # Define the forward progression order
    progression = [
        Lane.PLANNED,
        Lane.CLAIMED,
        Lane.IN_PROGRESS,
        Lane.FOR_REVIEW,
        Lane.DONE,
    ]

    try:
        from_idx = progression.index(from_lane)
        to_idx = progression.index(to_lane)
    except ValueError:
        # Lane not in normal progression (blocked, canceled, etc.)
        return []

    if to_idx <= from_idx:
        return []  # No forward advancement needed

    chain: list[tuple[Lane, Lane]] = []
    for i in range(from_idx, to_idx):
        pair = (progression[i], progression[i - 1])
        # Verify this is an allowed transition
        if (str(pair[0]), str(pair[1])) in ALLOWED_TRANSITIONS:
            chain.append(pair)
        else:
            # Can't build a legal chain
            return []

    return chain


def x__lane_advancement_chain__mutmut_16(
    from_lane: Lane,
    to_lane: Lane,
) -> list[tuple[Lane, Lane]]:
    """Generate legal lane transition chain from from_lane to to_lane.

    The state machine does not allow skipping lanes without force,
    so we generate intermediate transitions.
    """
    # Define the forward progression order
    progression = [
        Lane.PLANNED,
        Lane.CLAIMED,
        Lane.IN_PROGRESS,
        Lane.FOR_REVIEW,
        Lane.DONE,
    ]

    try:
        from_idx = progression.index(from_lane)
        to_idx = progression.index(to_lane)
    except ValueError:
        # Lane not in normal progression (blocked, canceled, etc.)
        return []

    if to_idx <= from_idx:
        return []  # No forward advancement needed

    chain: list[tuple[Lane, Lane]] = []
    for i in range(from_idx, to_idx):
        pair = (progression[i], progression[i + 2])
        # Verify this is an allowed transition
        if (str(pair[0]), str(pair[1])) in ALLOWED_TRANSITIONS:
            chain.append(pair)
        else:
            # Can't build a legal chain
            return []

    return chain


def x__lane_advancement_chain__mutmut_17(
    from_lane: Lane,
    to_lane: Lane,
) -> list[tuple[Lane, Lane]]:
    """Generate legal lane transition chain from from_lane to to_lane.

    The state machine does not allow skipping lanes without force,
    so we generate intermediate transitions.
    """
    # Define the forward progression order
    progression = [
        Lane.PLANNED,
        Lane.CLAIMED,
        Lane.IN_PROGRESS,
        Lane.FOR_REVIEW,
        Lane.DONE,
    ]

    try:
        from_idx = progression.index(from_lane)
        to_idx = progression.index(to_lane)
    except ValueError:
        # Lane not in normal progression (blocked, canceled, etc.)
        return []

    if to_idx <= from_idx:
        return []  # No forward advancement needed

    chain: list[tuple[Lane, Lane]] = []
    for i in range(from_idx, to_idx):
        pair = (progression[i], progression[i + 1])
        # Verify this is an allowed transition
        if (str(None), str(pair[1])) in ALLOWED_TRANSITIONS:
            chain.append(pair)
        else:
            # Can't build a legal chain
            return []

    return chain


def x__lane_advancement_chain__mutmut_18(
    from_lane: Lane,
    to_lane: Lane,
) -> list[tuple[Lane, Lane]]:
    """Generate legal lane transition chain from from_lane to to_lane.

    The state machine does not allow skipping lanes without force,
    so we generate intermediate transitions.
    """
    # Define the forward progression order
    progression = [
        Lane.PLANNED,
        Lane.CLAIMED,
        Lane.IN_PROGRESS,
        Lane.FOR_REVIEW,
        Lane.DONE,
    ]

    try:
        from_idx = progression.index(from_lane)
        to_idx = progression.index(to_lane)
    except ValueError:
        # Lane not in normal progression (blocked, canceled, etc.)
        return []

    if to_idx <= from_idx:
        return []  # No forward advancement needed

    chain: list[tuple[Lane, Lane]] = []
    for i in range(from_idx, to_idx):
        pair = (progression[i], progression[i + 1])
        # Verify this is an allowed transition
        if (str(pair[1]), str(pair[1])) in ALLOWED_TRANSITIONS:
            chain.append(pair)
        else:
            # Can't build a legal chain
            return []

    return chain


def x__lane_advancement_chain__mutmut_19(
    from_lane: Lane,
    to_lane: Lane,
) -> list[tuple[Lane, Lane]]:
    """Generate legal lane transition chain from from_lane to to_lane.

    The state machine does not allow skipping lanes without force,
    so we generate intermediate transitions.
    """
    # Define the forward progression order
    progression = [
        Lane.PLANNED,
        Lane.CLAIMED,
        Lane.IN_PROGRESS,
        Lane.FOR_REVIEW,
        Lane.DONE,
    ]

    try:
        from_idx = progression.index(from_lane)
        to_idx = progression.index(to_lane)
    except ValueError:
        # Lane not in normal progression (blocked, canceled, etc.)
        return []

    if to_idx <= from_idx:
        return []  # No forward advancement needed

    chain: list[tuple[Lane, Lane]] = []
    for i in range(from_idx, to_idx):
        pair = (progression[i], progression[i + 1])
        # Verify this is an allowed transition
        if (str(pair[0]), str(None)) in ALLOWED_TRANSITIONS:
            chain.append(pair)
        else:
            # Can't build a legal chain
            return []

    return chain


def x__lane_advancement_chain__mutmut_20(
    from_lane: Lane,
    to_lane: Lane,
) -> list[tuple[Lane, Lane]]:
    """Generate legal lane transition chain from from_lane to to_lane.

    The state machine does not allow skipping lanes without force,
    so we generate intermediate transitions.
    """
    # Define the forward progression order
    progression = [
        Lane.PLANNED,
        Lane.CLAIMED,
        Lane.IN_PROGRESS,
        Lane.FOR_REVIEW,
        Lane.DONE,
    ]

    try:
        from_idx = progression.index(from_lane)
        to_idx = progression.index(to_lane)
    except ValueError:
        # Lane not in normal progression (blocked, canceled, etc.)
        return []

    if to_idx <= from_idx:
        return []  # No forward advancement needed

    chain: list[tuple[Lane, Lane]] = []
    for i in range(from_idx, to_idx):
        pair = (progression[i], progression[i + 1])
        # Verify this is an allowed transition
        if (str(pair[0]), str(pair[2])) in ALLOWED_TRANSITIONS:
            chain.append(pair)
        else:
            # Can't build a legal chain
            return []

    return chain


def x__lane_advancement_chain__mutmut_21(
    from_lane: Lane,
    to_lane: Lane,
) -> list[tuple[Lane, Lane]]:
    """Generate legal lane transition chain from from_lane to to_lane.

    The state machine does not allow skipping lanes without force,
    so we generate intermediate transitions.
    """
    # Define the forward progression order
    progression = [
        Lane.PLANNED,
        Lane.CLAIMED,
        Lane.IN_PROGRESS,
        Lane.FOR_REVIEW,
        Lane.DONE,
    ]

    try:
        from_idx = progression.index(from_lane)
        to_idx = progression.index(to_lane)
    except ValueError:
        # Lane not in normal progression (blocked, canceled, etc.)
        return []

    if to_idx <= from_idx:
        return []  # No forward advancement needed

    chain: list[tuple[Lane, Lane]] = []
    for i in range(from_idx, to_idx):
        pair = (progression[i], progression[i + 1])
        # Verify this is an allowed transition
        if (str(pair[0]), str(pair[1])) not in ALLOWED_TRANSITIONS:
            chain.append(pair)
        else:
            # Can't build a legal chain
            return []

    return chain


def x__lane_advancement_chain__mutmut_22(
    from_lane: Lane,
    to_lane: Lane,
) -> list[tuple[Lane, Lane]]:
    """Generate legal lane transition chain from from_lane to to_lane.

    The state machine does not allow skipping lanes without force,
    so we generate intermediate transitions.
    """
    # Define the forward progression order
    progression = [
        Lane.PLANNED,
        Lane.CLAIMED,
        Lane.IN_PROGRESS,
        Lane.FOR_REVIEW,
        Lane.DONE,
    ]

    try:
        from_idx = progression.index(from_lane)
        to_idx = progression.index(to_lane)
    except ValueError:
        # Lane not in normal progression (blocked, canceled, etc.)
        return []

    if to_idx <= from_idx:
        return []  # No forward advancement needed

    chain: list[tuple[Lane, Lane]] = []
    for i in range(from_idx, to_idx):
        pair = (progression[i], progression[i + 1])
        # Verify this is an allowed transition
        if (str(pair[0]), str(pair[1])) in ALLOWED_TRANSITIONS:
            chain.append(None)
        else:
            # Can't build a legal chain
            return []

    return chain

x__lane_advancement_chain__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x__lane_advancement_chain__mutmut_1': x__lane_advancement_chain__mutmut_1, 
    'x__lane_advancement_chain__mutmut_2': x__lane_advancement_chain__mutmut_2, 
    'x__lane_advancement_chain__mutmut_3': x__lane_advancement_chain__mutmut_3, 
    'x__lane_advancement_chain__mutmut_4': x__lane_advancement_chain__mutmut_4, 
    'x__lane_advancement_chain__mutmut_5': x__lane_advancement_chain__mutmut_5, 
    'x__lane_advancement_chain__mutmut_6': x__lane_advancement_chain__mutmut_6, 
    'x__lane_advancement_chain__mutmut_7': x__lane_advancement_chain__mutmut_7, 
    'x__lane_advancement_chain__mutmut_8': x__lane_advancement_chain__mutmut_8, 
    'x__lane_advancement_chain__mutmut_9': x__lane_advancement_chain__mutmut_9, 
    'x__lane_advancement_chain__mutmut_10': x__lane_advancement_chain__mutmut_10, 
    'x__lane_advancement_chain__mutmut_11': x__lane_advancement_chain__mutmut_11, 
    'x__lane_advancement_chain__mutmut_12': x__lane_advancement_chain__mutmut_12, 
    'x__lane_advancement_chain__mutmut_13': x__lane_advancement_chain__mutmut_13, 
    'x__lane_advancement_chain__mutmut_14': x__lane_advancement_chain__mutmut_14, 
    'x__lane_advancement_chain__mutmut_15': x__lane_advancement_chain__mutmut_15, 
    'x__lane_advancement_chain__mutmut_16': x__lane_advancement_chain__mutmut_16, 
    'x__lane_advancement_chain__mutmut_17': x__lane_advancement_chain__mutmut_17, 
    'x__lane_advancement_chain__mutmut_18': x__lane_advancement_chain__mutmut_18, 
    'x__lane_advancement_chain__mutmut_19': x__lane_advancement_chain__mutmut_19, 
    'x__lane_advancement_chain__mutmut_20': x__lane_advancement_chain__mutmut_20, 
    'x__lane_advancement_chain__mutmut_21': x__lane_advancement_chain__mutmut_21, 
    'x__lane_advancement_chain__mutmut_22': x__lane_advancement_chain__mutmut_22
}
x__lane_advancement_chain__mutmut_orig.__name__ = 'x__lane_advancement_chain'


def _generate_reconciliation_events(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    args = [feature_slug, snapshot, commit_map, merged_wps]# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x__generate_reconciliation_events__mutmut_orig, x__generate_reconciliation_events__mutmut_mutants, args, kwargs, None)


def x__generate_reconciliation_events__mutmut_orig(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_1(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = None
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_2(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = None
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_3(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = None

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_4(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(None).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_5(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(None):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_6(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = None

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_7(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(None, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_8(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, None)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_9(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_10(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, )

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_11(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(None):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_12(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(None)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_13(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                None
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_14(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            break

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_15(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane != Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_16(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                None
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_17(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            break

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_18(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = None

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_19(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id not in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_20(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged or current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_21(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane not in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_22(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = None
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_23(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = None
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_24(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged or current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_25(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane != Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_26(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                None
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_27(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            break
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_28(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits or current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_29(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane != Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_30(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = None
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_31(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = None
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_32(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits or current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_33(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane != Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_34(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = None
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_35(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = None
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_36(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS or wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_37(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits or current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_38(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane != Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_39(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = None
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_40(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = None
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_41(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            break

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_42(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = None
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_43(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(None, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_44(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, None)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_45(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_46(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, )
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_47(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_48(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                None
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_49(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            break

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_50(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            None
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_51(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = None

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_52(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=None,
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_53(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=None,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_54(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=None,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_55(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=None,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_56(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=None,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_57(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=None,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_58(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor=None,
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_59(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=None,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_60(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode=None,
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_61(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=None,
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_62(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_63(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_64(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_65(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_66(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_67(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_68(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_69(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_70(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_71(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_72(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(None),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_73(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="XXreconcileXX",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_74(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="RECONCILE",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_75(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=True,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_76(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="XXdirect_repoXX",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_77(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="DIRECT_REPO",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_78(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = None
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_79(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                None,
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_80(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                None,
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_81(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=None,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_82(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor=None,
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_83(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context=None,
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_84(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_85(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=None,
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_86(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_87(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_88(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_89(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_90(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_91(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_92(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_93(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(None),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_94(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(None),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_95(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=True,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_96(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="XXreconcileXX",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_97(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="RECONCILE",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_98(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="XXreconcileXX",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_99(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="RECONCILE",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_100(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=False if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_101(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(None) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_102(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) != "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_103(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "XXfor_reviewXX" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_104(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "FOR_REVIEW" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_105(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    False if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_106(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(None) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_107(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) != "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_108(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "XXfor_reviewXX" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_109(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "FOR_REVIEW" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_110(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(None)
            else:
                details.append(
                    f"{wp_id}: skipping {from_lane} -> {to_lane}: {err}"
                )

    return events, details


def x__generate_reconciliation_events__mutmut_111(
    feature_slug: str,
    snapshot: StatusSnapshot,
    commit_map: dict[str, list[CommitInfo]],
    merged_wps: set[str],
) -> tuple[list[StatusEvent], list[str]]:
    """Generate events to reconcile planning state with implementation evidence.

    Returns (events, detail_messages).
    """
    events: list[StatusEvent] = []
    details: list[str] = []
    now = datetime.now(timezone.utc).isoformat()

    for wp_id, commits in sorted(commit_map.items()):
        current_lane = _get_current_lane(snapshot, wp_id)

        # Terminal lanes: no reconciliation
        if is_terminal(str(current_lane)):
            details.append(
                f"{wp_id}: in terminal lane {current_lane}, skipping"
            )
            continue

        # Blocked lane: note but don't advance
        if current_lane == Lane.BLOCKED:
            details.append(
                f"{wp_id}: currently blocked, has {len(commits)} commit(s) "
                f"-- manual review needed"
            )
            continue

        # Determine target lane based on evidence
        wp_merged = wp_id in merged_wps

        if wp_merged and current_lane in (
            Lane.PLANNED, Lane.CLAIMED, Lane.IN_PROGRESS,
        ):
            # Branch merged but WP not even at for_review
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged to main with {len(commits)} commit(s)"
        elif wp_merged and current_lane == Lane.FOR_REVIEW:
            # Merged and at for_review -- could suggest done, but
            # done requires reviewer approval evidence which reconcile
            # cannot fabricate. Note the drift instead.
            details.append(
                f"{wp_id}: branch merged and in for_review -- "
                f"may be ready for done (requires reviewer approval)"
            )
            continue
        elif commits and current_lane == Lane.PLANNED:
            # Has commits but still planned -> should be at least claimed
            target_lane = Lane.CLAIMED
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.CLAIMED:
            # Has commits and claimed -> should be in_progress
            target_lane = Lane.IN_PROGRESS
            evidence_summary = f"{len(commits)} commit(s) found"
        elif commits and current_lane == Lane.IN_PROGRESS and wp_merged:
            target_lane = Lane.FOR_REVIEW
            evidence_summary = f"branch merged with {len(commits)} commit(s)"
        else:
            # No actionable drift for this combination
            continue

        # Generate the chain of legal transitions
        chain = _lane_advancement_chain(current_lane, target_lane)
        if not chain:
            details.append(
                f"{wp_id}: cannot generate legal transition chain from "
                f"{current_lane} to {target_lane}"
            )
            continue

        details.append(
            f"{wp_id}: {current_lane} -> {target_lane} ({evidence_summary})"
        )

        for from_lane, to_lane in chain:
            event = StatusEvent(
                event_id=str(ulid.ULID()),
                feature_slug=feature_slug,
                wp_id=wp_id,
                from_lane=from_lane,
                to_lane=to_lane,
                at=now,
                actor="reconcile",
                force=False,
                execution_mode="direct_repo",
                reason=f"Reconciliation: {evidence_summary}",
            )

            # Validate the transition (skip guard conditions that require
            # human input like reviewer approval)
            ok, err = validate_transition(
                str(from_lane),
                str(to_lane),
                force=False,
                actor="reconcile",
                workspace_context="reconcile",
                subtasks_complete=True if str(to_lane) == "for_review" else None,
                implementation_evidence_present=(
                    True if str(to_lane) == "for_review" else None
                ),
            )
            if ok:
                events.append(event)
            else:
                details.append(
                    None
                )

    return events, details

x__generate_reconciliation_events__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x
    'x__generate_reconciliation_events__mutmut_103': x__generate_reconciliation_events__mutmut_103, 
    'x__generate_reconciliation_events__mutmut_104': x__generate_reconciliation_events__mutmut_104, 
    'x__generate_reconciliation_events__mutmut_105': x__generate_reconciliation_events__mutmut_105, 
    'x__generate_reconciliation_events__mutmut_106': x__generate_reconciliation_events__mutmut_106, 
    'x__generate_reconciliation_events__mutmut_107': x__generate_reconciliation_events__mutmut_107, 
    'x__generate_reconciliation_events__mutmut_108': x__generate_reconciliation_events__mutmut_108, 
    'x__generate_reconciliation_events__mutmut_109': x__generate_reconciliation_events__mutmut_109, 
    'x__generate_reconciliation_events__mutmut_110': x__generate_reconciliation_events__mutmut_110, 
    'x__generate_reconciliation_events__mutmut_111': x__generate_reconciliation_events__mutmut_111
}
x__generate_reconciliation_events__mutmut_orig.__name__ = 'x__generate_reconciliation_events'


def reconcile(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    args = [feature_dir, repo_root, target_repos]# type: ignore
    kwargs = {'dry_run': dry_run}# type: ignore
    return _mutmut_trampoline(x_reconcile__mutmut_orig, x_reconcile__mutmut_mutants, args, kwargs, None)


def x_reconcile__mutmut_orig(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_1(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = False,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_2(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = None
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_3(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = None

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_4(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = None
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_5(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir * SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_6(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = None
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_7(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(None)
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_8(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding=None))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_9(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="XXutf-8XX"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_10(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="UTF-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_11(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = None
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_12(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(None)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_13(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = None
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_14(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(None)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_15(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = None
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_16(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(None)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_17(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    None
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_18(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = None
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_19(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(None)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_20(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = None
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_21(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(None)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_22(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = None

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_23(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=None,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_24(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=None,
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_25(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=None,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_26(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages=None,
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_27(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary=None,
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_28(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_29(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_30(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_31(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_32(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_33(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_34(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(None).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_35(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=1,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_36(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 1 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_37(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = None
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_38(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = None

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_39(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_40(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(None)
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_41(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            break

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_42(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned = 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_43(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned -= 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_44(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 2

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_45(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = None
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_46(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(None, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_47(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, None)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_48(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_49(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, )
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_50(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(None)
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_51(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(None))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_52(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            break

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_53(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = None
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_54(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(None, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_55(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, None)}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_56(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get([])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_57(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, )}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_58(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_59(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(None)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_60(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(None, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_61(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, None).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_62(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault([]).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_63(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, ).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_64(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(None)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_65(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = None
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_66(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(None, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_67(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, None)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_68(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_69(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, )
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_70(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(None)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_71(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = None

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_72(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = None
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_73(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            None, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_74(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, None, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_75(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, None, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_76(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, None,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_77(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_78(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_79(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_80(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_81(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = None
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_82(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = None
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_83(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = None
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_84(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) >= 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_85(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 1
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_86(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append(None)

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_87(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("XXNo WP-linked commits found in target reposXX")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_88(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("no wp-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_89(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("NO WP-LINKED COMMITS FOUND IN TARGET REPOS")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_90(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run or result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_91(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_92(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = None

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_93(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(None, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_94(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, None)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_95(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_96(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, )

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_97(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase <= 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_98(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 2:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_99(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                None
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_100(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "XXCannot apply reconciliation events at Phase 0. XX"
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_101(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "cannot apply reconciliation events at phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_102(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "CANNOT APPLY RECONCILIATION EVENTS AT PHASE 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_103(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "XXUpgrade to Phase 1+ to enable event persistence.XX"
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_104(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "upgrade to phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_105(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "UPGRADE TO PHASE 1+ TO ENABLE EVENT PERSISTENCE."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_106(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = None
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_107(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 1
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_108(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=None,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_109(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=None,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_110(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=None,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_111(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=None,
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_112(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=None,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_113(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=None,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_114(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=None,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_115(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=None,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_116(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=None,
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_117(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=None,
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_118(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=None,
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_119(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode=None,
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_120(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=None,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_121(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_122(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_123(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_124(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_125(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_126(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_127(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_128(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_129(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_130(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_131(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_132(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_133(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_134(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(None),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_135(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        False if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_136(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(None) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_137(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) != "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_138(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "XXfor_reviewXX" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_139(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "FOR_REVIEW" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_140(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        False if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_141(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(None) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_142(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) != "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_143(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "XXfor_reviewXX" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_144(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "FOR_REVIEW" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_145(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="XXdirect_repoXX",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_146(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="DIRECT_REPO",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_147(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count = 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_148(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count -= 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_149(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 2
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_150(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    None
                )

        if applied_count > 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_151(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count >= 0:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_152(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 1:
            result.details.append(
                f"Applied {applied_count} reconciliation event(s) "
                f"(phase={phase}, source={source})"
            )

    return result


def x_reconcile__mutmut_153(
    feature_dir: Path,
    repo_root: Path,
    target_repos: list[Path],
    *,
    dry_run: bool = True,
) -> ReconcileResult:
    """Scan target repos for WP-linked commits and generate reconciliation events.

    Args:
        feature_dir: Path to the kitty-specs feature directory.
        repo_root: Root of the main repository.
        target_repos: List of repository paths to scan for evidence.
        dry_run: If True, return suggested events without persisting.
                 If False, emit events through the orchestration pipeline.

    Returns:
        ReconcileResult with suggested events, drift info, and diagnostics.
    """
    result = ReconcileResult()
    feature_slug = feature_dir.name

    # Load or materialize the current snapshot
    snapshot_path = feature_dir / SNAPSHOT_FILENAME
    if snapshot_path.exists():
        try:
            data = json.loads(snapshot_path.read_text(encoding="utf-8"))
            snapshot = StatusSnapshot.from_dict(data)
        except (json.JSONDecodeError, KeyError, ValueError) as exc:
            # Re-materialize from events
            try:
                events = read_events(feature_dir)
                snapshot = reduce(events)
            except Exception as inner_exc:
                result.errors.append(
                    f"Failed to load snapshot or events: {exc}; {inner_exc}"
                )
                return result
    else:
        # No snapshot file -- try materializing from event log
        try:
            events = read_events(feature_dir)
            snapshot = reduce(events)
        except Exception as exc:
            # No events either -- empty snapshot (all WPs effectively planned)
            snapshot = StatusSnapshot(
                feature_slug=feature_slug,
                materialized_at=datetime.now(timezone.utc).isoformat(),
                event_count=0,
                last_event_id=None,
                work_packages={},
                summary={lane.value: 0 for lane in Lane},
            )

    # Scan each target repo
    all_commits: dict[str, list[CommitInfo]] = {}
    all_merged: set[str] = set()

    for repo_path in target_repos:
        if not repo_path.exists():
            result.errors.append(f"Target repo does not exist: {repo_path}")
            continue

        result.target_repos_scanned += 1

        try:
            commit_map = scan_for_wp_commits(repo_path, feature_slug)
        except FileNotFoundError as exc:
            result.errors.append(str(exc))
            continue

        # Merge results (deduplicate by SHA per WP)
        for wp_id, commits in commit_map.items():
            existing_shas = {c.sha for c in all_commits.get(wp_id, [])}
            for commit in commits:
                if commit.sha not in existing_shas:
                    all_commits.setdefault(wp_id, []).append(commit)
                    existing_shas.add(commit.sha)

        # Check merged branches
        merged = _get_merged_wps(repo_path, feature_slug)
        all_merged.update(merged)

    result.wps_analyzed = len(all_commits)

    # Generate reconciliation events
    if all_commits:
        events, details = _generate_reconciliation_events(
            feature_slug, snapshot, all_commits, all_merged,
        )
        result.suggested_events = events
        result.details = details
        result.drift_detected = len(events) > 0
    else:
        result.details.append("No WP-linked commits found in target repos")

    # Apply mode
    if not dry_run and result.suggested_events:
        phase, source = resolve_phase(repo_root, feature_slug)

        # Phase gating: phase 0 blocks apply
        if phase < 1:
            raise ValueError(
                "Cannot apply reconciliation events at Phase 0. "
                "Upgrade to Phase 1+ to enable event persistence."
            )

        applied_count = 0
        for event in result.suggested_events:
            try:
                emit_status_transition(
                    feature_dir=feature_dir,
                    feature_slug=feature_slug,
                    wp_id=event.wp_id,
                    to_lane=str(event.to_lane),
                    actor=event.actor,
                    force=event.force,
                    reason=event.reason,
                    review_ref=event.review_ref,
                    workspace_context=f"reconcile:{repo_root}",
                    subtasks_complete=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    implementation_evidence_present=(
                        True if str(event.to_lane) == "for_review" else None
                    ),
                    execution_mode="direct_repo",
                    repo_root=repo_root,
                )
                applied_count += 1
            except Exception as exc:
                result.errors.append(
                    f"Failed to apply event for {event.wp_id} "
                    f"({event.from_lane} -> {event.to_lane}): {exc}"
                )

        if applied_count > 0:
            result.details.append(
                None
            )

    return result

x_reconcile__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x_reconcile__mutmut_1': x_reconcile__mutmut_1, 
    'x_reconcile__mutmut_2': x_reconcile__mutmut_2, 
    'x_reconcile__mutmut_3': x_reconcile__mutmut_3, 
    'x_reconcile__mutmut_4': x_reconcile__mutmut_4, 
    'x_reconcile__mutmut_5': x_reconcile__mutmut_5, 
    'x_reconcile__mutmut_6': x_reconcile__mutmut_6, 
    'x_reconcile__mutmut_7': x_reconcile__mutmut_7, 
    'x_reconcile__mutmut_8': x_reconcile__mutmut_8, 
    'x_reconcile__mutmut_9': x_reconcile__mutmut_9, 
    'x_reconcile__mutmut_10': x_reconcile__mutmut_10, 
    'x_reconcile__mutmut_11': x_reconcile__mutmut_11, 
    'x_reconcile__mutmut_12': x_reconcile__mutmut_12, 
    'x_reconcile__mutmut_13': x_reconcile__mutmut_13, 
    'x_reconcile__mutmut_14': x_reconcile__mutmut_14, 
    'x_reconcile__mutmut_15': x_reconcile__mutmut_15, 
    'x_reconcile__mutmut_16': x_reconcile__mutmut_16, 
    'x_reconcile__mutmut_17': x_reconcile__mutmut_17, 
    'x_reconcile__mutmut_18': x_reconcile__mutmut_18, 
    'x_reconcile__mutmut_19': x_reconcile__mutmut_19, 
    'x_reconcile__mutmut_20': x_reconcile__mutmut_20, 
    'x_reconcile__mutmut_21': x_reconcile__mutmut_21, 
    'x_reconcile__mutmut_22': x_reconcile__mutmut_22, 
    'x_reconcile__mutmut_23': x_reconcile__mutmut_23, 
    'x_reconcile__mutmut_24': x_reconcile__mutmut_24, 
    'x_reconcile__mutmut_25': x_reconcile__mutmut_25, 
    'x_reconcile__mutmut_26': x_reconcile__mutmut_26, 
    'x_reconcile__mutmut_27': x_reconcile__mutmut_27, 
    'x_reconcile__mutmut_28': x_reconcile__mutmut_28, 
    'x_reconcile__mutmut_29': x_reconcile__mutmut_29, 
    'x_reconcile__mutmut_126': x_reconcile__mutmut_126, 
    'x_reconcile__mutmut_127': x_reconcile__mutmut_127, 
    'x_reconcile__mutmut_128': x_reconcile__mutmut_128, 
    'x_reconcile__mutmut_129': x_reconcile__mutmut_129, 
    'x_reconcile__mutmut_130': x_reconcile__mutmut_130, 
    'x_reconcile__mutmut_131': x_reconcile__mutmut_131, 
    'x_reconcile__mutmut_132': x_reconcile__mutmut_132, 
    'x_reconcile__mutmut_133': x_reconcile__mutmut_133, 
    'x_reconcile__mutmut_134': x_reconcile__mutmut_134, 
    'x_reconcile__mutmut_135': x_reconcile__mutmut_135, 
    'x_reconcile__mutmut_136': x_reconcile__mutmut_136, 
    'x_reconcile__mutmut_137': x_reconcile__mutmut_137, 
    'x_reconcile__mutmut_138': x_reconcile__mutmut_138, 
    'x_reconcile__mutmut_139': x_reconcile__mutmut_139, 
    'x_reconcile__mutmut_140': x_reconcile__mutmut_140, 
    'x_reconcile__mutmut_141': x_reconcile__mutmut_141, 
    'x_reconcile__mutmut_142': x_reconcile__mutmut_142, 
    'x_reconcile__mutmut_143': x_reconcile__mutmut_143, 
    'x_reconcile__mutmut_144': x_reconcile__mutmut_144, 
    'x_reconcile__mutmut_145': x_reconcile__mutmut_145, 
    'x_reconcile__mutmut_146': x_reconcile__mutmut_146, 
    'x_reconcile__mutmut_147': x_reconcile__mutmut_147, 
    'x_reconcile__mutmut_148': x_reconcile__mutmut_148, 
    'x_reconcile__mutmut_149': x_reconcile__mutmut_149, 
    'x_reconcile__mutmut_150': x_reconcile__mutmut_150, 
    'x_reconcile__mutmut_151': x_reconcile__mutmut_151, 
    'x_reconcile__mutmut_152': x_reconcile__mutmut_152, 
    'x_reconcile__mutmut_153': x_reconcile__mutmut_153
}
x_reconcile__mutmut_orig.__name__ = 'x_reconcile'


def format_reconcile_report(result: ReconcileResult) -> None:
    args = [result]# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x_format_reconcile_report__mutmut_orig, x_format_reconcile_report__mutmut_mutants, args, kwargs, None)


def x_format_reconcile_report__mutmut_orig(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_1(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = None

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_2(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected or not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_3(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_4(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_5(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            None
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_6(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                None,
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_7(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title=None,
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_8(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style=None,
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_9(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_10(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_11(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_12(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "XX[green]No drift detected[/green] -- XX"
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_13(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]no drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_14(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[GREEN]NO DRIFT DETECTED[/GREEN] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_15(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "XXplanning state matches implementation evidence.XX",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_16(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "PLANNING STATE MATCHES IMPLEMENTATION EVIDENCE.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_17(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="XXReconciliation ResultXX",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_18(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="reconciliation result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_19(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="RECONCILIATION RESULT",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_20(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="XXgreenXX",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_21(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="GREEN",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_22(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            None
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_23(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = None
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_24(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title=None,
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_25(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=None,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_26(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style=None,
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_27(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style=None,
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_28(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_29(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_30(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_31(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_32(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="XXSuggested Reconciliation EventsXX",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_33(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="suggested reconciliation events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_34(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="SUGGESTED RECONCILIATION EVENTS",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_35(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=False,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_36(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="XXbold magentaXX",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_37(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="BOLD MAGENTA",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_38(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="XXdimXX",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_39(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="DIM",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_40(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column(None, style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_41(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style=None, width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_42(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=None)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_43(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column(style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_44(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_45(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", )
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_46(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("XXWP IDXX", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_47(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("wp id", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_48(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="XXcyanXX", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_49(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="CYAN", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_50(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=9)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_51(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column(None, style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_52(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style=None, width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_53(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=None)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_54(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column(style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_55(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_56(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", )
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_57(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("XXCurrent LaneXX", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_58(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("current lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_59(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("CURRENT LANE", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_60(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="XXyellowXX", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_61(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="YELLOW", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_62(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=15)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_63(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column(None, style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_64(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style=None, width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_65(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=None)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_66(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column(style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_67(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_68(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", )
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_69(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("XXSuggested LaneXX", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_70(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("suggested lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_71(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("SUGGESTED LANE", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_72(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="XXgreenXX", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_73(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="GREEN", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_74(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=15)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_75(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column(None, width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_76(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=None)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_77(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column(width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_78(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", )
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_79(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("XXEvidenceXX", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_80(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_81(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("EVIDENCE", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_82(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=41)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_83(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column(None, width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_84(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=None)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_85(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column(width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_86(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", )

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_87(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("XXActionXX", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_88(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_89(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("ACTION", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_90(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=21)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_91(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                None,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_92(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                None,
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_93(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                None,
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_94(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                None,
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_95(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                None,
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_96(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_97(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_98(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_99(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_100(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_101(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(None),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_102(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(None),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_103(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason and "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_104(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "XXXX",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_105(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "XXemit eventXX" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_106(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "EMIT EVENT" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_107(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "XXdry-runXX",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_108(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "DRY-RUN",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_109(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(None)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_110(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print(None)
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_111(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("XX[bold]Details:[/bold]XX")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_112(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_113(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[BOLD]DETAILS:[/BOLD]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_114(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(None)
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_115(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print(None)
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_116(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("XX[bold red]Errors:[/bold red]XX")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_117(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_118(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[BOLD RED]ERRORS:[/BOLD RED]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_119(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(None)
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_120(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print(None)
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_121(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("XX[bold]Summary:[/bold]XX")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_122(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_123(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[BOLD]SUMMARY:[/BOLD]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_124(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(None)
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_125(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(None)
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_126(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        None
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_127(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'XX[red]yes[/red]XX' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_128(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[RED]YES[/RED]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_129(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else 'XX[green]no[/green]XX'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_130(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[GREEN]NO[/GREEN]'}"
    )
    console.print(f"  Suggested events: {len(result.suggested_events)}")


def x_format_reconcile_report__mutmut_131(result: ReconcileResult) -> None:
    """Print a human-readable reconciliation report using Rich."""
    from rich.console import Console
    from rich.table import Table
    from rich.panel import Panel

    console = Console()

    if not result.drift_detected and not result.errors:
        console.print(
            Panel(
                "[green]No drift detected[/green] -- "
                "planning state matches implementation evidence.",
                title="Reconciliation Result",
                border_style="green",
            )
        )
        console.print(
            f"  Repos scanned: {result.target_repos_scanned}  |  "
            f"WPs analyzed: {result.wps_analyzed}"
        )
        return

    # Build suggested events table
    if result.suggested_events:
        table = Table(
            title="Suggested Reconciliation Events",
            show_header=True,
            header_style="bold magenta",
            border_style="dim",
        )
        table.add_column("WP ID", style="cyan", width=8)
        table.add_column("Current Lane", style="yellow", width=14)
        table.add_column("Suggested Lane", style="green", width=14)
        table.add_column("Evidence", width=40)
        table.add_column("Action", width=20)

        for event in result.suggested_events:
            table.add_row(
                event.wp_id,
                str(event.from_lane),
                str(event.to_lane),
                event.reason or "",
                "emit event" if result.drift_detected else "dry-run",
            )

        console.print(table)
        console.print()

    # Details
    if result.details:
        console.print("[bold]Details:[/bold]")
        for detail in result.details:
            console.print(f"  {detail}")
        console.print()

    # Errors
    if result.errors:
        console.print("[bold red]Errors:[/bold red]")
        for error in result.errors:
            console.print(f"  [red]{error}[/red]")
        console.print()

    # Summary statistics
    console.print("[bold]Summary:[/bold]")
    console.print(f"  Target repos scanned: {result.target_repos_scanned}")
    console.print(f"  WPs analyzed: {result.wps_analyzed}")
    console.print(
        f"  Drift detected: "
        f"{'[red]yes[/red]' if result.drift_detected else '[green]no[/green]'}"
    )
    console.print(None)

x_format_reconcile_report__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x_format_reconcile_report__mutmut_1': x_format_reconcile_report__mutmut_1, 
    'x_format_reconcile_report__mutmut_2': x_format_reconcile_report__mutmut_2, 
    'x_format_reconcile_report__mutmut_3': x_format_reconcile_report__mutmut_3, 
    'x_format_reconcile_report__mutmut_4': x_format_reconcile_report__mutmut_4, 
    'x_format_reconcile_report__mutmut_5': x_format_reconcile_report__mutmut_5, 
    'x_format_reconcile_report__mutmut_6': x_format_reconcile_report__mutmut_6, 
    'x_format_reconcile_report__mutmut_7': x_format_reconcile_report__mutmut_7, 
    'x_format_reconcile_report__mutmut_8': x_format_reconcile_report__mutmut_8, 
    'x_format_reconcile_report__mutmut_9': x_format_reconcile_report__mutmut_9, 
    'x_format_reconcile_report__mutmut_10': x_format_reconcile_report__mutmut_10, 
    'x_format_reconcile_report__mutmut_11': x_format_reconcile_report__mutmut_11, 
    'x_format_reconcile_report__mutmut_12': x_format_reconcile_report__mutmut_12, 
    'x_format_reconcile_report__mutmut_13': x_format_reconcile_report__mutmut_13, 
    'x_format_reconcile_report__mutmut_14': x_format_reconcile_report__mutmut_14, 
    'x_format_reconcile_report__mutmut_15': x_format_reconcile_report__mutmut_15, 
    'x_format_reconcile_report__mutmut_16': x_format_reconcile_report__mutmut_16, 
    'x_format_reconcile_report__mutmut_17': x_format_reconcile_report__mutmut_17, 
    'x_format_reconcile_report__mutmut_18': x_format_reconcile_report__mutmut_18, 
    'x_format_reconcile_report__mutmut_19': x_format_reconcile_report__mutmut_19, 
    'x_format_reconcile_report__mutmut_20': x_format_reconcile_report__mutmut_20, 
    'x_format_reconcile_report__mutmut_21': x_format_reconcile_report__mutmut_21, 
    'x_format_reconcile_report__mutmut_22': x_format_reconcile_report__mutmut_22, 
    'x_format_reconcile_report__mutmut_23': x_format_reconcile_report__mutmut_23, 
    'x_format_reconcile_report__mutmut_24': x_format_reconcile_report__mutmut_24, 
    'x_format_reconcile_report__mutmut_25': x_format_reconcile_report__mutmut_25, 
    'x_format_reconcile_report__mutmut_26': x_format_reconcile_report__mutmut_26, 
    'x_format_reconcile_report__mutmut_27': x_format_reconcile_report__mutmut_27, 
    'x_format_reconcile_report__mutmut_28': x_format_reconcile_report__mutmut_28, 
    'x_format_reconcile_report__mutmut_29': x_format_reconcile_report__mutmut_29, 
    'x_format_reconcile_report__mutmut_30': x_format_reconcile_report__mutmut_30, 
    'x_format_reconcile_report__mutmut_31': x_format_reconcile_report__mutmut_31, 
    'x_format_reconcile_report__mutmut_32': x_format_reconcile_report__mutmut_32, 
    'x_format_reconcile_report__mutmut_33': x_format_reconcile_report__mutmut_33, 
    'x_format_reconcile_report__mutmut_34': x_format_reconcile_report__mutmut_34, 
    'x_format_reconcile_report__mutmut_35': x_format_reconcile_report__mutmut_35, 
    'x_format_reconcile_report__mutmut_36': x_format_reconcile_report__mutmut_36, 
    'x_format_reconcile_report__mutmut_37': x_format_reconcile_report__mutmut_37, 
    'x_format_reconcile_report__mutmut_38': x_format_reconcile_report__mutmut_38, 
    'x_format_reconcile_report__mutmut_39': x_format_reconcile_report__mutmut_39, 
    'x_format_reconcile_report__mutmut_40': x_format_reconcile_report__mutmut_40, 
    'x_format_reconcile_report__mutmut_41': x_format_reconcile_report__mutmut_41, 
    'x_format_reconcile_report__mutmut_42': x_format_reconcile_report__mutmut_42, 
    'x_format_reconcile_report__mutmut_43': x_format_reconcile_report__mutmut_43, 
    'x_format_reconcile_report__mutmut_44': x_format_reconcile_report__mutmut_44, 
    'x_format_reconcile_report__mutmut_45': x_format_reconcile_report__mutmut_45, 
    'x_format_reconcile_report__mutmut_46': x_format_reconcile_report__mutmut_46, 
    'x_format_reconcile_report__mutmut_47': x_format_reconcile_report__mutmut_47, 
    'x_format_reconcile_report__mutmut_48': x_format_reconcile_report__mutmut_48, 
    'x_format_reconcile_report__mutmut_49': x_format_reconcile_report__mutmut_49, 
    'x_format_reconcile_report__mutmut_50': x_format_reconcile_report__mutmut_50, 
    'x_format_reconcile_report__mutmut_51': x_format_reconcile_report__mutmut_51, 
    'x_format_reconcile_report__mutmut_52': x_format_reconcile_report__mutmut_52, 
    'x_format_reconcile_report__mutmut_53': x_format_reconcile_report__mutmut_53, 
    'x_format_reconcile_report__mutmut_54': x_format_reconcile_report__mutmut_54, 
    'x_format_reconcile_report__mutmut_55': x_format_reconcile_report__mutmut_55, 
    'x_format_reconcile_report__mutmut_56': x_format_reconcile_report__mutmut_56, 
    'x_format_reconcile_report__mutmut_57': x_format_reconcile_report__mutmut_57, 
    'x_format_reconcile_report__mutmut_58': x_format_reconcile_report__mutmut_58, 
    'x_format_reconcile_report__mutmut_59': x_format_reconcile_report__mutmut_59, 
    'x_format_reconcile_report__mutmut_60': x_format_reconcile_report__mutmut_60, 
    'x_format_reconcile_report__mutmut_61': x_format_reconcile_report__mutmut_61, 
    'x_format_reconcile_report__mutmut_62': x_format_reconcile_report__mutmut_62, 
    'x_format_reconcile_report__mutmut_63': x_format_reconcile_report__mutmut_63, 
    'x_format_reconcile_report__mutmut_64': x_format_reconcile_report__mutmut_64, 
    'x_format_reconcile_report__mutmut_65': x_format_reconcile_report__mutmut_65, 
    'x_format_reconcile_report__mutmut_66': x_format_reconcile_report__mutmut_66, 
    'x_format_reconcile_report__mutmut_67': x_format_reconcile_report__mutmut_67, 
    'x_format_reconcile_report__mutmut_68': x_format_reconcile_report__mutmut_68, 
    'x_format_reconcile_report__mutmut_69': x_format_reconcile_report__mutmut_69, 
    'x_format_reconcile_report__mutmut_70': x_format_reconcile_report__mutmut_70, 
    'x_format_reconcile_report__mutmut_71': x_format_reconcile_report__mutmut_71, 
    'x_format_reconcile_report__mutmut_72': x_format_reconcile_report__mutmut_72, 
    'x_format_reconcile_report__mutmut_73': x_format_reconcile_report__mutmut_73, 
    'x_format_reconcile_report__mutmut_74': x_format_reconcile_report__mutmut_74, 
    'x_format_reconcile_report__mutmut_75': x_format_reconcile_report__mutmut_75, 
    'x_format_reconcile_report__mutmut_76': x_format_reconcile_report__mutmut_76, 
    'x_format_reconcile_report__mutmut_77': x_format_reconcile_report__mutmut_77, 
    'x_format_reconcile_report__mutmut_78': x_format_reconcile_report__mutmut_78, 
    'x_format_reconcile_report__mutmut_79': x_format_reconcile_report__mutmut_79, 
    'x_format_reconcile_report__mutmut_80': x_format_reconcile_report__mutmut_80, 
    'x_format_reconcile_report__mutmut_81': x_format_reconcile_report__mutmut_81, 
    'x_format_reconcile_report__mutmut_82': x_format_reconcile_report__mutmut_82, 
    'x_format_reconcile_report__mutmut_83': x_format_reconcile_report__mutmut_83, 
    'x_format_reconcile_report__mutmut_84': x_format_reconcile_report__mutmut_84, 
    'x_format_reconcile_report__mutmut_85': x_format_reconcile_report__mutmut_85, 
    'x_format_reconcile_report__mutmut_86': x_format_reconcile_report__mutmut_86, 
    'x_format_reconcile_report__mutmut_87': x_format_reconcile_report__mutmut_87, 
    'x_format_reconcile_report__mutmut_88': x_format_reconcile_report__mutmut_88, 
    'x_format_reconcile_report__mutmut_89': x_format_reconcile_report__mutmut_89, 
    'x_format_reconcile_report__mutmut_90': x_format_reconcile_report__mutmut_90, 
    'x_format_reconcile_report__mutmut_91': x_format_reconcile_report__mutmut_91, 
    'x_format_reconcile_report__mutmut_92': x_format_reconcile_report__mutmut_92, 
    'x_format_reconcile_report__mutmut_93': x_format_reconcile_report__mutmut_93, 
    'x_format_reconcile_report__mutmut_94': x_format_reconcile_report__mutmut_94, 
    'x_format_reconcile_report__mutmut_95': x_format_reconcile_report__mutmut_95, 
    'x_format_reconcile_report__mutmut_96': x_format_reconcile_report__mutmut_96, 
    'x_format_reconcile_report__mutmut_97': x_format_reconcile_report__mutmut_97, 
    'x_format_reconcile_report__mutmut_98': x_format_reconcile_report__mutmut_98, 
    'x_format_reconcile_report__mutmut_99': x_format_reconcile_report__mutmut_99, 
    'x_format_reconcile_report__mutmut_100': x_format_reconcile_report__mutmut_100, 
    'x_format_reconcile_report__mutmut_101': x_format_reconcile_report__mutmut_101, 
    'x_format_reconcile_report__mutmut_102': x_format_reconcile_report__mutmut_102, 
    'x_format_reconcile_report__mutmut_103': x_format_reconcile_report__mutmut_103, 
    'x_format_reconcile_report__mutmut_104': x_format_reconcile_report__mutmut_104, 
    'x_format_reconcile_report__mutmut_105': x_format_reconcile_report__mutmut_105, 
    'x_format_reconcile_report__mutmut_106': x_format_reconcile_report__mutmut_106, 
    'x_format_reconcile_report__mutmut_107': x_format_reconcile_report__mutmut_107, 
    'x_format_reconcile_report__mutmut_108': x_format_reconcile_report__mutmut_108, 
    'x_format_reconcile_report__mutmut_109': x_format_reconcile_report__mutmut_109, 
    'x_format_reconcile_report__mutmut_110': x_format_reconcile_report__mutmut_110, 
    'x_format_reconcile_report__mutmut_111': x_format_reconcile_report__mutmut_111, 
    'x_format_reconcile_report__mutmut_112': x_format_reconcile_report__mutmut_112, 
    'x_format_reconcile_report__mutmut_113': x_format_reconcile_report__mutmut_113, 
    'x_format_reconcile_report__mutmut_114': x_format_reconcile_report__mutmut_114, 
    'x_format_reconcile_report__mutmut_115': x_format_reconcile_report__mutmut_115, 
    'x_format_reconcile_report__mutmut_116': x_format_reconcile_report__mutmut_116, 
    'x_format_reconcile_report__mutmut_117': x_format_reconcile_report__mutmut_117, 
    'x_format_reconcile_report__mutmut_118': x_format_reconcile_report__mutmut_118, 
    'x_format_reconcile_report__mutmut_119': x_format_reconcile_report__mutmut_119, 
    'x_format_reconcile_report__mutmut_120': x_format_reconcile_report__mutmut_120, 
    'x_format_reconcile_report__mutmut_121': x_format_reconcile_report__mutmut_121, 
    'x_format_reconcile_report__mutmut_122': x_format_reconcile_report__mutmut_122, 
    'x_format_reconcile_report__mutmut_123': x_format_reconcile_report__mutmut_123, 
    'x_format_reconcile_report__mutmut_124': x_format_reconcile_report__mutmut_124, 
    'x_format_reconcile_report__mutmut_125': x_format_reconcile_report__mutmut_125, 
    'x_format_reconcile_report__mutmut_126': x_format_reconcile_report__mutmut_126, 
    'x_format_reconcile_report__mutmut_127': x_format_reconcile_report__mutmut_127, 
    'x_format_reconcile_report__mutmut_128': x_format_reconcile_report__mutmut_128, 
    'x_format_reconcile_report__mutmut_129': x_format_reconcile_report__mutmut_129, 
    'x_format_reconcile_report__mutmut_130': x_format_reconcile_report__mutmut_130, 
    'x_format_reconcile_report__mutmut_131': x_format_reconcile_report__mutmut_131
}
x_format_reconcile_report__mutmut_orig.__name__ = 'x_format_reconcile_report'


def reconcile_result_to_json(result: ReconcileResult) -> dict[str, Any]:
    args = [result]# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x_reconcile_result_to_json__mutmut_orig, x_reconcile_result_to_json__mutmut_mutants, args, kwargs, None)


def x_reconcile_result_to_json__mutmut_orig(result: ReconcileResult) -> dict[str, Any]:
    """Convert ReconcileResult to a JSON-serializable dict."""
    return {
        "drift_detected": result.drift_detected,
        "suggested_events": [e.to_dict() for e in result.suggested_events],
        "details": result.details,
        "errors": result.errors,
        "stats": {
            "target_repos_scanned": result.target_repos_scanned,
            "wps_analyzed": result.wps_analyzed,
        },
    }


def x_reconcile_result_to_json__mutmut_1(result: ReconcileResult) -> dict[str, Any]:
    """Convert ReconcileResult to a JSON-serializable dict."""
    return {
        "XXdrift_detectedXX": result.drift_detected,
        "suggested_events": [e.to_dict() for e in result.suggested_events],
        "details": result.details,
        "errors": result.errors,
        "stats": {
            "target_repos_scanned": result.target_repos_scanned,
            "wps_analyzed": result.wps_analyzed,
        },
    }


def x_reconcile_result_to_json__mutmut_2(result: ReconcileResult) -> dict[str, Any]:
    """Convert ReconcileResult to a JSON-serializable dict."""
    return {
        "DRIFT_DETECTED": result.drift_detected,
        "suggested_events": [e.to_dict() for e in result.suggested_events],
        "details": result.details,
        "errors": result.errors,
        "stats": {
            "target_repos_scanned": result.target_repos_scanned,
            "wps_analyzed": result.wps_analyzed,
        },
    }


def x_reconcile_result_to_json__mutmut_3(result: ReconcileResult) -> dict[str, Any]:
    """Convert ReconcileResult to a JSON-serializable dict."""
    return {
        "drift_detected": result.drift_detected,
        "XXsuggested_eventsXX": [e.to_dict() for e in result.suggested_events],
        "details": result.details,
        "errors": result.errors,
        "stats": {
            "target_repos_scanned": result.target_repos_scanned,
            "wps_analyzed": result.wps_analyzed,
        },
    }


def x_reconcile_result_to_json__mutmut_4(result: ReconcileResult) -> dict[str, Any]:
    """Convert ReconcileResult to a JSON-serializable dict."""
    return {
        "drift_detected": result.drift_detected,
        "SUGGESTED_EVENTS": [e.to_dict() for e in result.suggested_events],
        "details": result.details,
        "errors": result.errors,
        "stats": {
            "target_repos_scanned": result.target_repos_scanned,
            "wps_analyzed": result.wps_analyzed,
        },
    }


def x_reconcile_result_to_json__mutmut_5(result: ReconcileResult) -> dict[str, Any]:
    """Convert ReconcileResult to a JSON-serializable dict."""
    return {
        "drift_detected": result.drift_detected,
        "suggested_events": [e.to_dict() for e in result.suggested_events],
        "XXdetailsXX": result.details,
        "errors": result.errors,
        "stats": {
            "target_repos_scanned": result.target_repos_scanned,
            "wps_analyzed": result.wps_analyzed,
        },
    }


def x_reconcile_result_to_json__mutmut_6(result: ReconcileResult) -> dict[str, Any]:
    """Convert ReconcileResult to a JSON-serializable dict."""
    return {
        "drift_detected": result.drift_detected,
        "suggested_events": [e.to_dict() for e in result.suggested_events],
        "DETAILS": result.details,
        "errors": result.errors,
        "stats": {
            "target_repos_scanned": result.target_repos_scanned,
            "wps_analyzed": result.wps_analyzed,
        },
    }


def x_reconcile_result_to_json__mutmut_7(result: ReconcileResult) -> dict[str, Any]:
    """Convert ReconcileResult to a JSON-serializable dict."""
    return {
        "drift_detected": result.drift_detected,
        "suggested_events": [e.to_dict() for e in result.suggested_events],
        "details": result.details,
        "XXerrorsXX": result.errors,
        "stats": {
            "target_repos_scanned": result.target_repos_scanned,
            "wps_analyzed": result.wps_analyzed,
        },
    }


def x_reconcile_result_to_json__mutmut_8(result: ReconcileResult) -> dict[str, Any]:
    """Convert ReconcileResult to a JSON-serializable dict."""
    return {
        "drift_detected": result.drift_detected,
        "suggested_events": [e.to_dict() for e in result.suggested_events],
        "details": result.details,
        "ERRORS": result.errors,
        "stats": {
            "target_repos_scanned": result.target_repos_scanned,
            "wps_analyzed": result.wps_analyzed,
        },
    }


def x_reconcile_result_to_json__mutmut_9(result: ReconcileResult) -> dict[str, Any]:
    """Convert ReconcileResult to a JSON-serializable dict."""
    return {
        "drift_detected": result.drift_detected,
        "suggested_events": [e.to_dict() for e in result.suggested_events],
        "details": result.details,
        "errors": result.errors,
        "XXstatsXX": {
            "target_repos_scanned": result.target_repos_scanned,
            "wps_analyzed": result.wps_analyzed,
        },
    }


def x_reconcile_result_to_json__mutmut_10(result: ReconcileResult) -> dict[str, Any]:
    """Convert ReconcileResult to a JSON-serializable dict."""
    return {
        "drift_detected": result.drift_detected,
        "suggested_events": [e.to_dict() for e in result.suggested_events],
        "details": result.details,
        "errors": result.errors,
        "STATS": {
            "target_repos_scanned": result.target_repos_scanned,
            "wps_analyzed": result.wps_analyzed,
        },
    }


def x_reconcile_result_to_json__mutmut_11(result: ReconcileResult) -> dict[str, Any]:
    """Convert ReconcileResult to a JSON-serializable dict."""
    return {
        "drift_detected": result.drift_detected,
        "suggested_events": [e.to_dict() for e in result.suggested_events],
        "details": result.details,
        "errors": result.errors,
        "stats": {
            "XXtarget_repos_scannedXX": result.target_repos_scanned,
            "wps_analyzed": result.wps_analyzed,
        },
    }


def x_reconcile_result_to_json__mutmut_12(result: ReconcileResult) -> dict[str, Any]:
    """Convert ReconcileResult to a JSON-serializable dict."""
    return {
        "drift_detected": result.drift_detected,
        "suggested_events": [e.to_dict() for e in result.suggested_events],
        "details": result.details,
        "errors": result.errors,
        "stats": {
            "TARGET_REPOS_SCANNED": result.target_repos_scanned,
            "wps_analyzed": result.wps_analyzed,
        },
    }


def x_reconcile_result_to_json__mutmut_13(result: ReconcileResult) -> dict[str, Any]:
    """Convert ReconcileResult to a JSON-serializable dict."""
    return {
        "drift_detected": result.drift_detected,
        "suggested_events": [e.to_dict() for e in result.suggested_events],
        "details": result.details,
        "errors": result.errors,
        "stats": {
            "target_repos_scanned": result.target_repos_scanned,
            "XXwps_analyzedXX": result.wps_analyzed,
        },
    }


def x_reconcile_result_to_json__mutmut_14(result: ReconcileResult) -> dict[str, Any]:
    """Convert ReconcileResult to a JSON-serializable dict."""
    return {
        "drift_detected": result.drift_detected,
        "suggested_events": [e.to_dict() for e in result.suggested_events],
        "details": result.details,
        "errors": result.errors,
        "stats": {
            "target_repos_scanned": result.target_repos_scanned,
            "WPS_ANALYZED": result.wps_analyzed,
        },
    }

x_reconcile_result_to_json__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x_reconcile_result_to_json__mutmut_1': x_reconcile_result_to_json__mutmut_1, 
    'x_reconcile_result_to_json__mutmut_2': x_reconcile_result_to_json__mutmut_2, 
    'x_reconcile_result_to_json__mutmut_3': x_reconcile_result_to_json__mutmut_3, 
    'x_reconcile_result_to_json__mutmut_4': x_reconcile_result_to_json__mutmut_4, 
    'x_reconcile_result_to_json__mutmut_5': x_reconcile_result_to_json__mutmut_5, 
    'x_reconcile_result_to_json__mutmut_6': x_reconcile_result_to_json__mutmut_6, 
    'x_reconcile_result_to_json__mutmut_7': x_reconcile_result_to_json__mutmut_7, 
    'x_reconcile_result_to_json__mutmut_8': x_reconcile_result_to_json__mutmut_8, 
    'x_reconcile_result_to_json__mutmut_9': x_reconcile_result_to_json__mutmut_9, 
    'x_reconcile_result_to_json__mutmut_10': x_reconcile_result_to_json__mutmut_10, 
    'x_reconcile_result_to_json__mutmut_11': x_reconcile_result_to_json__mutmut_11, 
    'x_reconcile_result_to_json__mutmut_12': x_reconcile_result_to_json__mutmut_12, 
    'x_reconcile_result_to_json__mutmut_13': x_reconcile_result_to_json__mutmut_13, 
    'x_reconcile_result_to_json__mutmut_14': x_reconcile_result_to_json__mutmut_14
}
x_reconcile_result_to_json__mutmut_orig.__name__ = 'x_reconcile_result_to_json'
