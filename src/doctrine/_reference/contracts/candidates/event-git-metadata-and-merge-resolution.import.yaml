id: "imp-contracts-event-git-metadata-001"
source:
  title: "Batch API contract git metadata and merge-resolution semantics"
  type: "api-contract"
  publisher: "spec-kitty contracts"
  url: "contracts/batch-api-contract.md"
  accessed_on: "2026-02-18"
classification:
  target_concepts:
    - "directive"
    - "template"
  rationale: "Defines canonical git context fields and merge-related event semantics for synchronization and auditability."
adaptation:
  summary: "Treat git branch, commit SHA, target branch, and dependency resolution status as first-class sync telemetry fields."
  notes:
    - "Event payloads may include `git_branch` and `head_commit_sha` for commit-level traceability."
    - "Feature creation events require `target_branch` to preserve routing intent."
    - "Dependency resolution semantics include explicit `merged` state."
source_references:
  - kind: "local"
    path: "contracts/batch-api-contract.md"
    lines: "154-156"
    note: "Core git metadata fields on emitted events."
  - kind: "local"
    path: "contracts/batch-api-contract.md"
    lines: "374-375"
    note: "FeatureCreated payload includes required target branch."
  - kind: "local"
    path: "contracts/batch-api-contract.md"
    lines: "444-445"
    note: "Dependency resolution type includes `merged`."
status: "proposed"
