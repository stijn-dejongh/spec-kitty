# Software-Dev Command Templates

Prompt files for `/spec-kitty.*` slash commands specific to the software development
mission. Each file guides the agent through one workflow step.

These are loaded at runtime based on the active mission type and agent configuration.
