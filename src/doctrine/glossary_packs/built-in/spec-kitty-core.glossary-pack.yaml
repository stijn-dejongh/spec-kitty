# spec-kitty-core.glossary-pack.yaml
# GENERATED (migrated) from .kittify/glossaries/spec_kitty_core.yaml by
# scratchpad/migrate_glossary_pack.py (WP03, mission
# glossary-pack-doctrine-kind-01KY30SW). Do not hand-edit; re-run the
# migration script and re-commit if the seed changes before Mission C
# retires it.
description: Canonical Spec Kitty terminology (migrated from 
  spec_kitty_core.yaml).
id: spec-kitty-core
provenance: built-in
terms:
- confidence: 0.95
  definition: Aggregated runtime information resolved for a specific agent 
    action, including feature slug, work package ID, base branch, workspace 
    path, and dependency state. Passed to agents at step boundaries.
  status: active
  surface: action context
- confidence: 0.75
  definition: An execution mode in which the harness proposes answers to 
    interview questions for mission owner confirmation, without acting 
    autonomously. The mission owner retains final authority on every decision.
  status: draft
  surface: advise mode
- confidence: 0.9
  definition: The skills-first agent command discovery model in which 
    capabilities are installed as skill packages under .agents/skills/ and 
    resolved at runtime by name.
  status: active
  surface: agent skills
- confidence: 0.9
  definition: "At the mission level (branch-context output): alias for target_branch
    — the branch the mission is rooted in. At the worktree/WP level (WP prompt frontmatter):
    the specific git branch from which the individual lane worktree was created; may
    be a lane branch (e.g. kitty/mission-010-feature-lane-a) rather than the target
    branch itself. The same key therefore has different scopes: in branch-context
    JSON base_branch == target_branch; in WP frontmatter base_branch records the worktree's
    immediate parent.\n"
  status: active
  surface: base branch
- confidence: 0.95
  definition: "The mission-create guard for PR-bound missions that requires an explicit
    operator or agent decision before planning artifacts are written on a primary
    branch. Interactive callers may confirm at the prompt; non-interactive callers
    record the already-made decision with --branch-strategy already-confirmed. When
    the recommended feature-branch path is chosen, pass --start-branch so the CLI
    switches before scaffold writes.\n"
  status: active
  surface: branch strategy gate
- confidence: 1.0
  definition: One checkout or worktree of one repository with its own execution 
    context.
  status: active
  surface: build
- confidence: 1.0
  definition: Per-checkout/worktree identity, unique per working tree. Unchanged
    from pre-081.
  status: active
  surface: build_id
- confidence: 0.95
  definition: A codebase-wide mechanical rename or terminology migration tracked
    as a mission with change_mode set to bulk_edit. Governed by the occurrence 
    classification guardrail workflow.
  status: active
  surface: bulk edit
- confidence: 0.95
  definition: "A regular-expression engine failure mode in which a vulnerable pattern
    combined with\nadversarial input drives matching time into exponential or polynomial
    complexity.\nBacktracking engines (Python `re`, JavaScript, Java, .NET) attempt
    every possible\npartition of the input when a match fails; patterns with nested
    or consecutive\nnon-possessive repetitions can produce O(2^N) or O(N^c) retry
    storms. The\n`secure-regex-catastrophic-backtracking` tactic at\n`src/doctrine/tactics/built-in/secure-regex-catastrophic-backtracking.tactic.yaml`\n\
    codifies the four dangerous-shape patterns, the rewrite ladder (bound quantifier
    →\nrefactor nested → possessive/atomic group → negated char class), and the escape\n\
    hatches (drop the regex, re2, multi-pass, bounded-input cap). Every fix requires
    a\nwall-clock regression test asserting linear runtime on an adversarial input\n\
    (default budget: ≤100 ms for 100 000 chars), per FR-008. Synonym \"ReDoS\"\n(Regular-expression
    Denial of Service) is acceptable only as a parenthetical.\n"
  see_also:
  - 'tactic: secure-regex-catastrophic-backtracking, path: src/doctrine/tactics/built-in/secure-regex-catastrophic-backtracking.tactic.yaml'
  - 'fr: FR-008, description: Wall-clock regression test requirement for every regex
    change'
  status: active
  surface: catastrophic backtracking
- confidence: 1.0
  definition: DEPRECATED. Replaced by `status commit`. This term obscures intent
    and is forbidden in active source. See the canonical `status commit` entry.
  status: deprecated
  surface: ceremony commit
- confidence: 0.95
  definition: 'A mission-level metadata flag (change_mode: bulk_edit) that activates
    the bulk-edit guardrail workflow, requiring an occurrence map before any work
    package may be approved.'
  status: active
  surface: change mode
- confidence: 0.95
  definition: A governance document synthesizing a project's purpose, 
    constraints, team norms, and doctrine into a durable reference artifact. 
    Produced by the charter interview workflow.
  status: active
  surface: charter
- confidence: 0.95
  definition: "A test that captures the observable behavior of existing code as fixture-driven
    assertions,\nwritten before any refactor of that code. The refactor must leave
    the characterization test\ngreen. See the `tdd-red-green-refactor` tactic. Distinguished
    from snapshot tests (overloaded\nterm) and regression tests (too broad).\n"
  status: active
  surface: characterization test
- confidence: 0.75
  definition: Graph-native decay detection that validates the internal 
    consistency and freshness of doctrine artifacts in the doctrine reference 
    graph. Identifies stale references, missing edges, and definition drift.
  status: draft
  surface: charter lint
- confidence: 0.6
  definition: A dry-run forecast of which files will conflict and which 
    conflicts are auto-resolvable, produced before a merge begins to help the 
    mission owner decide whether to proceed.
  status: draft
  surface: conflict prediction
- confidence: 1.0
  definition: "The git branch checked out in the main repository at the moment a spec-kitty
    workflow command (specify, plan, tasks, implement) is invoked. Read ephemerally
    from the working tree; never persisted in any mission artifact. Exposed as current_branch
    / CURRENT_BRANCH in branch-context JSON output. Since WP07 (FR-012), spec-kitty
    refuses to derive the mission target from current_branch — it reads target_branch
    from meta.json instead, so the branch contract is stable regardless of which branch
    the operator is on when driving the mission.\n"
  status: active
  surface: current branch
- confidence: 0.75
  definition: A local CLI artifact representing a choice point in an interview 
    flow (charter, specify, or plan) where the mission owner or a delegated 
    agent must provide input before the flow can continue.
  status: draft
  surface: decision moment
- confidence: 0.95
  definition: "A function whose body is intentionally kept long-and-linear (rather
    than extracted into\nnamed helpers) for traceability against a documented contract,
    code-review map, or\noutput-section sequence. Deliberate linearity functions are
    NOT refactor candidates\nregardless of Sonar S3776 score. Identified by an explicit
    code comment justifying the\nstructure and/or a published output-section contract
    that reviewers verify line-by-line.\nRefactoring a deliberately linear function
    without updating its contract is a regression\nin traceability. Canonical example:
    `src/specify_cli/cli/commands/_auth_doctor.py::render_report`\n(CC 53; intentional
    7-section layout). Distinct from structural debt.\n"
  status: active
  surface: deliberate linearity
- confidence: 0.95
  definition: The review-time check that validates a work package diff against 
    its occurrence map. Blocks approval if uncategorized or improperly changed 
    occurrences are found.
  status: active
  surface: diff compliance gate
- confidence: 0.95
  definition: A doctrine artifact encoding a specific governance rule or 
    constraint. The primary building block of project doctrine; referenced by 
    agent profiles and action scopes.
  status: active
  surface: directive
- confidence: 0.95
  definition: The body of project-specific governance artifacts — directives, 
    tactics, and styleguides — that shape how agents and humans collaborate on a
    project.
  status: active
  surface: doctrine
- confidence: 0.95
  definition: The directed graph of interconnected doctrine directives, tactics,
    and styleguides that defines a project's governance structure. Abbreviated 
    as drg. Edges encode requires, suggests, and replaces relationships between 
    nodes.
  status: active
  surface: doctrine reference graph
- confidence: 1.0
  definition: Abbreviation for doctrine reference graph — the directed graph of 
    interconnected governance artifacts (directives, tactics, styleguides) 
    defining a project's doctrine.
  status: active
  surface: drg
- confidence: 0.75
  definition: The set of inputs provided to charter synthesis — code-reading 
    signals, configured URLs, and bundled corpus snapshots. Determines what the 
    harness can reason about when generating doctrine artifacts.
  status: draft
  surface: evidence bundle
- confidence: 0.95
  definition: Human-readable, kebab-cased identifier for a feature, used in 
    branch names, directory paths, and artifact keys. Derived from mission_slug 
    in feature missions.
  status: active
  surface: feature slug
- confidence: 0.95
  definition: "A dedicated git branch for PR-bound mission planning and implementation
    work, typically named feat/<slug> for feature work or fix/<slug> for bug-fix work.
    It is distinct from the primary branch and is the recommended start point when
    a mission is expected to become a pull request.\n"
  status: active
  surface: feature branch
- confidence: 1.0
  definition: The human-readable title string of a mission, used for display in
    dashboards and logs. Mutable — changing it does not affect routing or
    identity.
  status: active
  surface: friendly_name
- confidence: 0.95
  definition: A versioned doctrine declaration attaching a named gate handler to
    a status-transition edge (`{on_transition, handler, handler_kind, schema_version,
    fail_open, provenance}`), authored on the review `MissionStepContract`. A
    binding is a field/relationship, not a standalone artefact. Do NOT confuse
    with the five pre-existing `*gate*` senses (branch strategy gate / diff
    compliance gate / dependency gate / merge dependency gate / sonar quality
    gate) — those are unrelated one-off guards, not doctrine-resolved lane-edge
    checks.
  status: active
  surface: gate binding
- confidence: 0.95
  definition: A named, dispatchable check registered in `GATE_REGISTRY`; the
    Spec-Kitty pre-review engine is the first handler, keyed to the `for_review`
    edge. Registry membership is the callable source; activation decides whether
    it runs. Do NOT confuse with the five pre-existing `*gate*` senses (branch
    strategy gate / diff compliance gate / dependency gate / merge dependency
    gate / sonar quality gate) — those are unrelated one-off guards, not
    doctrine-resolved dispatch callables.
  status: active
  surface: gate handler
- confidence: 0.75
  definition: The middleware enforcement point in the agent pipeline where
    glossary semantic checks are applied before generation proceeds. Configured
    by the strictness setting.
  status: draft
  surface: glossary chokepoint
- confidence: 0.9
  definition: The host LLM or external system responsible for generating 
    doctrine content during charter synthesis. Operates under a contract with 
    spec-kitty's validation and staging pipeline; does not write artifacts 
    directly.
  status: active
  surface: harness
- confidence: 1.0
  definition: Plan-level architectural unit of intent identified as IC-## in 
    plan.md. An implementation concern captures purpose, requirements, affected 
    surfaces, sequencing, and risks. It is not executable and is translated into
    one or more work packages by the tasks workflow.
  status: active
  surface: implementation concern
- confidence: 1.0
  definition: Work package lifecycle state. Canonical lanes are planned, 
    claimed, in_progress, for_review, in_review, approved, done, blocked, and 
    canceled. The alias doing means in_progress. approved means review passed 
    and merge pending; done means merged/integrated.
  status: active
  surface: lane
- confidence: 1.0
  definition: Work package lane meaning review passed, feedback resolved, and 
    the WP is ready to merge. approved is not integrated and must not be treated
    as done. The merge workflow owns the approved -> done transition.
  status: active
  surface: approved
- confidence: 1.0
  definition: Work package lane meaning the WP has been merged/integrated into 
    the mission target branch, or explicitly force-marked as integrated with 
    evidence. Review approval alone is not done.
  status: active
  surface: done
- confidence: 1.0
  definition: A WP counted by mission acceptance readiness because its lane is 
    approved or done. Accepted does not imply done; approved accepted work 
    packages remain merge-pending.
  status: active
  surface: accepted work package
- confidence: 1.0
  definition: An approved WP that mission acceptance may count as accepted-ready
    but that has not yet been merged/integrated. Merge-pending WPs stay in the 
    approved lane until the merge workflow records done.
  status: active
  surface: merge-pending work package
- confidence: 1.0
  definition: "The condition the dependency gate evaluates before a work package may
    be claimed or implemented: every WP listed in its ``dependencies`` frontmatter
    must be in the ``approved`` or ``done`` lane. ``approved`` is included deliberately
    — ``done`` is emitted only by the whole-mission merge, so requiring a dependency
    to be strictly ``done`` would deadlock every same-mission dependency chain (the
    dependent WP could never start, so the mission could never merge, so the dependency
    could never reach ``done``). Computed by ``dependency_readiness_for_wp`` in ``src/specify_cli/core/dependency_graph.py``;
    intentionally matches the merge dependency gate (``policy/merge_gates.py`` treats
    ``{approved, done}`` as satisfied). ``for_review``, ``in_review``, ``blocked``,
    ``canceled``, and missing status are not ready.\n"
  status: active
  surface: dependency readiness
- confidence: 1.0
  definition: "The pre-implementation enforcement point that refuses to start a work
    package until it meets dependency readiness (all declared dependencies are ``approved``
    or ``done``). Enforced consistently at every claim path. The read-only surfaces
    — the ``next`` claim-discovery preview (``preview_claimable_wp``) and orchestrator-api
    ``list-ready`` — only ever surface ``planned`` candidates, so a dependency-blocked
    planned WP is simply withheld (preview returns ``dependencies_not_satisfied``;
    list-ready omits it). The imperative claim verbs — ``agent action implement``,
    the low-level ``implement`` command (before any workspace is created), and orchestrator-api
    ``start-implementation`` — additionally apply a resume guard: they enforce readiness
    only on the not-yet-started (``planned``/``claimed``) claim transition, so re-invoking
    implement on an already ``in_progress`` WP is a no-op resume, not a re-gated claim,
    and a dependency that later regresses out of ``approved``/``done`` does not block
    resuming in-flight work. On block the imperative verbs surface the stable ``dependencies_not_satisfied``
    reason/error code.\n"
  status: active
  surface: dependency gate
- confidence: 0.95
  definition: The ~/.kittify/ directory on a developer's machine, containing 
    templates, global agent configs, and skills shared across all projects on 
    that machine. Distinct from the per-project .kittify/ directory.
  status: active
  surface: machine-level runtime
- confidence: 1.0
  definition: DEPRECATED ambiguous phrase. It conflates checkout location with 
    branch choice. Use 'repository root checkout' for location or the explicit 
    branch term you mean.
  status: deprecated
  surface: main repo
- confidence: 1.0
  definition: DEPRECATED ambiguous phrase. It conflates checkout location with 
    branch choice. Use 'repository root checkout' for location or the explicit 
    branch term you mean.
  status: deprecated
  surface: main repository
- confidence: 1.0
  definition: DEPRECATED ambiguous phrase. It conflates checkout location with 
    branch choice. Use 'repository root checkout' for location or the explicit 
    branch term you mean.
  status: deprecated
  surface: main repository root
- confidence: 0.75
  definition: The persistent JSON artifact (.kittify/merge-state.json) tracking 
    progress of a resumable multi-work-package merge operation. Allows an 
    interrupted merge to be restarted without data loss.
  status: draft
  surface: merge state
- confidence: 1.0
  definition: "The branch where completed work-package code must ultimately land.
    Stored in every WP prompt's frontmatter (alongside planning_base_branch) to give
    implementing agents an explicit merge destination. In meta.json: legacy alias
    for target_branch, read as a fallback by resolve_planning_branch_from_meta() when
    target_branch is absent (pre-WP03 missions). In 3.x all three — target_branch,
    planning_base_branch, and merge_target_branch — carry the same value; the separation
    makes intent explicit in agent-facing prompts and prevents the \"prep branch leak\"\
    \ (pre-WP07 pattern in which finalize-tasks inherited the current branch instead
    of the persisted target).\n"
  status: active
  surface: merge_target_branch
- confidence: 1.0
  definition: The first 8 characters of a mission_id, used as a compact 
    disambiguator in branch names and worktree paths. Not a stable identity on 
    its own — always paired with mission_slug.
  status: active
  surface: mid8
- confidence: 0.95
  definition: A versioned, idempotent transformation that upgrades project 
    filesystem state from one spec-kitty version to another. Migrations are 
    applied in sequence and must not be re-applied once completed.
  status: active
  surface: migration
- confidence: 0.75
  definition: The minimum auditable artifact trail that every spec-kitty action 
    is required to leave, scaled to action weight. Lightweight actions leave a 
    log entry; heavy actions leave provenance sidecars and durable state 
    artifacts.
  status: draft
  surface: minimal viable trail
- confidence: 1.0
  definition: Purpose-specific workflow machine with defined inputs, process, 
    outcomes, and states
  status: active
  surface: mission
- confidence: 1.0
  definition: The ULID-based canonical, immutable machine identity for a 
    mission, assigned at creation and used for all event routing and 
    disambiguation. Never displayed as a mission number.
  status: active
  surface: mission_id
- confidence: 1.0
  definition: A display-only sequential integer assigned to a mission at merge 
    time. Never used for lookup, locking, or event routing — use mission_id for 
    those purposes.
  status: active
  surface: mission_number
- confidence: 1.0
  definition: The human-readable kebab-cased identifier for a mission (e.g., 
    my-feature), used alongside mid8 in branch and worktree names. Chosen by the
    mission owner at creation.
  status: active
  surface: mission_slug
- confidence: 1.0
  definition: Stable machine fingerprint (12-char hex). Unchanged from pre-081.
  status: active
  surface: node_id
- confidence: 0.95
  definition: The act of categorizing each instance of a changing string by 
    semantic context — source code, import paths, serialized keys, CLI commands,
    documentation, and so on. Required before bulk-edit work packages are 
    approved.
  status: active
  surface: occurrence classification
- confidence: 0.95
  definition: A mission-level artifact (occurrence_map.yaml) that classifies 
    every occurrence of a changing identifier by semantic context. Required 
    before any bulk-edit work package may proceed.
  status: active
  surface: occurrence map
- confidence: 1.0
  definition: Field in wps.yaml work package entries listing the IC-## 
    implementation concerns from plan.md that the work package addresses. The 
    field is wps.yaml-only and must not be copied into WP prompt frontmatter.
  status: active
  surface: plan_concern_refs
- confidence: 1.0
  definition: Grouping container for mission primitives, not the enforcement 
    unit
  status: active
  surface: phase
- confidence: 0.9
  definition: "A classification label applied to a function whose body is a sequence
    of independent\ndecisions over a shared input. Pipeline-shape functions are the
    principal refactor target\nfor the `chain-of-responsibility-rule-pipeline` tactic.
    The shape marker is \"3+ independent\ndecisions over a common input\" — not \"\
    function is long\". Distinct from deliberate linearity,\nwhere the sequential
    structure is intentional and documented. See\n`docs/architecture/04_implementation_mapping/code-patterns.md`
    entry 1.\n"
  status: active
  surface: pipeline-shape
- confidence: 1.0
  definition: DEPRECATED alias for 'repository root checkout'. Do not use — 
    agents interpret this as a separate repository. Use 'repository root 
    checkout' instead.
  status: deprecated
  surface: planning repository
- confidence: 1.0
  definition: "The branch that was active in the repository root checkout when WP
    prompts were generated (at finalize-tasks time). Stored in every WP prompt's frontmatter
    and in lanes.json to root lane-worktree allocation correctly regardless of which
    branch the operator is on when running finalize-tasks. In 3.x equals target_branch.
    Introduced alongside merge_target_branch to close the \"prep branch leak\" bug
    (pre-WP07): when finalize-tasks ran from a temporary prep/... branch, that branch
    leaked into WP frontmatter and crashed lane allocation once the prep branch was
    deleted.\n"
  status: active
  surface: planning_base_branch
- confidence: 0.75
  definition: Pre-merge checks that verify all resolved execution workspaces are
    clean and ready before a merge operation begins. A failing preflight check 
    blocks the merge entirely.
  status: draft
  surface: preflight validation
- confidence: 0.95
  definition: "The repository's default integration branch, normally resolved from
    origin/HEAD and commonly named main, master, or develop. The specify branch-context
    output exposes this value as primary_branch only for branch-strategy recommendation;
    it is not automatically the mission's target_branch once target_branch has been
    persisted in meta.json.\n"
  status: active
  surface: primary branch
- confidence: 1.0
  definition: Custom mission-defined executable operation or step unit
  status: active
  surface: primitive
- confidence: 1.0
  definition: SaaS collaboration surface grouping one or more repositories. Not 
    the local Git resource.
  status: active
  surface: project
- confidence: 1.0
  definition: "DEPRECATED (mission 081): Use 'repository root checkout' instead. The
    non-worktree checkout at the root of the current repository."
  status: deprecated
  surface: project root checkout
- confidence: 1.0
  definition: Optional SaaS-assigned collaboration identity. Absent until a 
    repository is bound to a SaaS project. Never locally minted.
  status: active
  surface: project_uuid
- confidence: 0.95
  definition: "A mission expected to land through a pull request rather than direct
    work on the primary branch. During mission create, --pr-bound activates the branch
    strategy gate so the branch choice is explicit before planning artifacts are written.\n"
  status: active
  surface: pr-bound mission
- confidence: 0.95
  definition: A record of what generated a given artifact — the actor identity, 
    timestamp, and source (seed file, user clarification, or LLM generation). 
    Enables audit and reproducibility.
  status: active
  surface: provenance
- confidence: 0.95
  definition: A metadata file co-located with a generated artifact, recording 
    its synthesis provenance. Allows any artifact to be traced back to its 
    origin without modifying the artifact itself.
  status: active
  surface: provenance sidecar
- confidence: 1.0
  definition: Optional owner/repo Git provider reference (e.g. 
    Priivacy-ai/spec-kitty). Unchanged from pre-081 meaning.
  status: active
  surface: repo_slug
- confidence: 1.0
  definition: Local Git resource (one .git directory) holding mission artifacts.
    Formerly called "project" in pre-081 code.
  status: active
  surface: repository
- confidence: 1.0
  definition: The non-worktree checkout at the root of the current repository. 
    Planning commands (specify, plan, tasks) run here. It is a location, not a 
    branch name.
  status: active
  surface: repository root checkout
- confidence: 1.0
  definition: Human-readable repository display name derived from git remote or 
    directory name. Mutable, not a stable identity. Was called project_slug 
    before mission 081.
  status: active
  surface: repository_label
- confidence: 1.0
  definition: Stable local repository identity, minted once per repository. 
    Required namespace key for body sync and dedup. Was mislabeled as 
    project_uuid before mission 081.
  status: active
  surface: repository_uuid
- confidence: 0.95
  definition: "A pipeline of small pure functions, each checking applicability, optionally
    executing its\nnarrow piece of work, and returning a result threaded to the next
    function. Three flavors are\nrecognized in this codebase: Validator (accumulating
    `list[Finding]`), Transformer (chained\n`(state, actions, error?)`), Scorer (weighted
    `float`). The Transformer flavor is implemented\nin `src/specify_cli/migration/canonicalization.py::CanonicalRule`
    and consumed by\n`mission_state.py::_canonicalize_status_row` and `rebuild_state.py`.
    See the\n`chain-of-responsibility-rule-pipeline` tactic and the code-patterns
    catalog at\n`docs/architecture/04_implementation_mapping/code-patterns.md` entry
    1.\n"
  status: active
  surface: rule pipeline
- confidence: 1.0
  definition: Bounded semantic scope (mission_local, team_domain, 
    audience_domain, spec_kitty_core)
  status: active
  surface: scope
- confidence: 1.0
  definition: Mismatch or ambiguity in term usage with assigned severity score
  status: active
  surface: semantic conflict
- confidence: 0.95
  definition: A markdown file encoding a domain-specific agent workflow, 
    discoverable as a named command. Skills are the primary unit of reusable 
    agent capability in spec-kitty.
  status: active
  surface: skill
- confidence: 0.95
  definition: A self-contained agent capability directory with a SKILL.md 
    descriptor, installable under .agents/skills/. Provides the discoverable 
    packaging format for a skill.
  status: active
  surface: skill package
- confidence: 0.95
  definition: "The SonarCloud project-level pass/fail evaluation for `Priivacy-ai_spec-kitty`
    on the\nnew-code window. Aggregates six conditions (reliability rating, security
    rating,\nmaintainability rating, new coverage, new duplicated lines density, new
    security hotspots\nreviewed). Reports `OK` or `ERROR` per condition plus a top-level
    status. Pulled via the\npublic REST API at `https://sonarcloud.io/api/qualitygates/project_status`;
    the helper\n`work/snippets/sonarcloud_branch_review.sh` formats this for terminal
    use. Thresholds (as\nof 2026-05-14): new_reliability_rating GT 1 (A), new_security_rating
    GT 1 (A),\nnew_maintainability_rating GT 1 (A), new_coverage LT 80%, new_duplicated_lines_density\n\
    GT 3%, new_security_hotspots_reviewed LT 100%.\n"
  status: active
  surface: sonar quality gate
- confidence: 0.95
  definition: An auto-commit created by spec-kitty to record workflow state 
    changes (task status transitions, lane metadata, WP claims). Status commits 
    target lane branches, not protected branches.
  status: active
  surface: status commit
  synonyms_to_avoid:
  - ceremony commit
  - ceremony
  - ceremony write
  - status-writing operation
  - status-writing command
- confidence: 1.0
  definition: DEPRECATED. Replaced by `status commit`. This phrasing landed 
    during a partial rename; it is forbidden in active source. See the canonical
    `status commit` entry.
  status: deprecated
  surface: status-writing operation
- confidence: 0.75
  definition: A formal specification of what a mission step must accomplish, 
    including its inputs, outputs, and success criteria. The reviewer uses the 
    step contract to validate work package completion.
  status: draft
  surface: step contract
- confidence: 1.0
  definition: Glossary enforcement mode (off, medium, max)
  status: active
  surface: strictness
- confidence: 0.95
  definition: "The branch passed to mission create with --start-branch. The CLI creates
    or switches to this branch before writing mission scaffolding, and it must match
    --target-branch when both options are supplied so mission metadata and the checked-out
    branch describe the same planning branch.\n"
  status: active
  surface: start branch
- confidence: 0.9
  definition: "A function whose cognitive complexity has accumulated beyond reviewable
    bounds AND whose\nresponsibilities are independently dispatching concerns with
    no shared state between branches.\nA structural debt function is a refactor candidate
    per the `refactoring-extract-first-order-concept`\nand `refactoring-extract-class-by-responsibility-split`
    tactics. Distinguished from deliberate\nlinearity by the *absence* of a documented
    design intent and by the presence of independent\ndispatch arms or triplicated
    patterns. Canonical example: `doctor.py::mission_state` at CC 57\n(three independent
    dispatch arms — `--audit`, `--fix`, `--teamspace-dry-run` — sharing no state\n\
    after option parsing, plus triplicated JSON-vs-pretty emit logic). WP06 of mission\n\
    `quality-devex-hardening-3-2-01KRJGKH` extracted it to a CC 3 thin orchestrator
    plus per-mode\nrunners.\n"
  status: active
  surface: structural debt
- confidence: 0.95
  definition: A doctrine artifact specifying coding or writing conventions for a
    project. Applied during implementation and review to keep outputs 
    consistent.
  status: active
  surface: styleguide
- confidence: 0.95
  definition: The authoritative commit marker written last during charter 
    synthesis. Its presence and hash integrity indicate a complete, valid 
    synthesis run. Absence or hash mismatch signals a partial or corrupted run.
  status: active
  surface: synthesis manifest
- confidence: 0.95
  definition: A doctrine artifact describing a reusable workflow or procedure 
    that agents follow to accomplish a specific class of task. Composed of 
    ordered steps and cross-references.
  status: active
  surface: tactic
- confidence: 1.0
  definition: "The git branch on which a mission's code, planning artifacts, and status
    events must ultimately land. Persisted in meta.json under the key target_branch
    at mission-create time and never overwritten. Read by resolve_planning_branch_from_meta()
    as the canonical key; merge_target_branch is its legacy alias in older meta.json
    fixtures. Since WP07 (FR-012), all downstream commands — finalize-tasks, implement,
    review, merge — derive the branch contract from this stored value, not from the
    operator's current_branch at invocation time. In branch-context JSON output, target_branch
    and base_branch carry the same value when queried at the mission level.\n"
  status: active
  surface: target branch
- confidence: 0.6
  definition: A shared workspace or channel used for collaborative input to 
    spec-kitty decision moments, particularly in widen mode flows.
  status: draft
  surface: teamspace
- confidence: 0.75
  definition: A CLI feature that allows a mission owner to escalate an interview
    question to a collaborative channel for group input, with the response 
    written back into the local decision moment.
  status: draft
  surface: widen mode
- confidence: 1.0
  definition: Execution slice inside a mission run, the unit of human/agent 
    handoff
  status: active
  surface: work package
- confidence: 1.0
  definition: Git worktree directory created for implementing a work package
  status: active
  surface: workspace
- confidence: 1.0
  definition: Abbreviation for work package
  status: active
  surface: wp
- confidence: 0.95
  definition: '`spec-kitty review` invocation that performs consistency checks but
    is explicitly NOT a release gate. Skips dead-code and BLE001 audits; reports state
    with that limitation.'
  status: active
  surface: lightweight mode
- confidence: 0.95
  definition: '`spec-kitty review` invocation that enforces the full mission-review
    release-gate contract; requires `issue-matrix.md`, Gate 1-4 records, and `mission-exception.md`
    when applicable.'
  status: active
  surface: post-merge mode
- confidence: 0.95
  definition: Diagnostic class fired when an operator explicitly requests 
    `--mode post-merge` against a mission whose `baseline_merge_commit` is 
    absent.
  status: active
  surface: mode mismatch
- confidence: 0.95
  definition: Diagnostic class fired when an `issue-matrix.md` file uses columns
    outside the canonical mandatory + named-optional vocabulary.
  status: active
  surface: issue-matrix schema drift
- confidence: 0.95
  definition: The single ingestion boundary at which charter content's source 
    encoding is detected and recorded as provenance before downstream consumers 
    see it.
  status: active
  surface: encoding chokepoint
- confidence: 0.95
  definition: The persisted audit trail (JSONL) of every encoding decision the 
    chokepoint made, including detected encoding, confidence, normalization 
    action, and bypass usage.
  status: active
  surface: encoding provenance
- confidence: 0.95
  definition: A check that must pass before a work-package status-transition edge
    (e.g. `in_progress->for_review`) is allowed. Resolved from the repo's active
    doctrine and dispatched through the inverted move-task hook (`_mt_run_transition_gates`);
    the flagship handler is the scoped pre-review regression check. Do NOT confuse
    with the five pre-existing `*gate*` senses (branch strategy gate / diff
    compliance gate / dependency gate / merge dependency gate / sonar quality
    gate) — those are unrelated one-off guards, not doctrine-resolved lane-edge
    checks.
  status: active
  surface: transition gate
- confidence: 0.95
  definition: "An operator-chosen override of the encoding chokepoint's hard-fail
    behavior; uses the highest-confidence decode candidate and records `bypass_used:
    true` in provenance."
  status: active
  surface: unsafe bypass
- confidence: 0.9
  definition: The post-WP07 layout of `src/specify_cli/cli/commands/review/` as 
    a package of sibling files; was a single file pre-WP07.
  status: active
  surface: review.py package
- confidence: 0.95
  definition: The WP08 scan + normalize-or-fail-loud flow that brings existing 
    missions' charter content into compliance with the WP06 chokepoint contract.
  status: active
  surface: charter-content migration
- confidence: 0.95
  definition: "Declarative field on a doctrine pack artifact (tactic, styleguide,
    paradigm, procedure, agent profile) that asserts the artifact augments a built-in
    artifact with the same ID via field-merge. Suppresses the same-ID-collision advisory
    and auto-emits an `ENHANCES` DRG edge.\n"
  introduced_in_mission: charter-ux-and-org-pack-vocabulary-01KSAF14
  status: active
  surface: enhances
  synonyms_to_avoid:
  - augments
  - extends
- confidence: 0.95
  definition: "Declarative field on a doctrine pack artifact that asserts the artifact
    fully replaces a built-in artifact with the same ID. Suppresses the same-ID advisory
    and auto-emits an `OVERRIDES` DRG edge.\n"
  introduced_in_mission: charter-ux-and-org-pack-vocabulary-01KSAF14
  status: active
  surface: overrides
  synonyms_to_avoid:
  - replaces
  - supersedes
