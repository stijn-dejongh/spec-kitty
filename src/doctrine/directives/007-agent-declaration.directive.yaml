schema_version: "1.0"
id: AGENT_DECLARATION
title: Agent Declaration
intent: Require explicit acknowledgment of scope, authority, and constraints before high-impact execution.
enforcement: advisory
paradigm_refs:
- spec-driven-development
fetch_policy:
  strategy: lazy_fetch
  token_discipline: on_demand
  fetch_order:
  - paradigms
  - tactics
  - toolguides
  - styleguides
  note: Load only the minimum required entrypoints for the current task context.
