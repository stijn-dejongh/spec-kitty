schema_version: "1.0"
id: TOOLING_SETUP_AND_FALLBACKS
title: Tooling Setup and Fallbacks
intent: Define core tooling baselines and fallback strategies so workflows remain executable across environments.
enforcement: advisory
paradigm_refs:
- spec-driven-development
toolguide_refs:
- powershell-syntax
fetch_policy:
  strategy: lazy_fetch
  token_discipline: on_demand
  fetch_order:
  - paradigms
  - tactics
  - toolguides
  - styleguides
  note: Load only the minimum required entrypoints for the current task context.
