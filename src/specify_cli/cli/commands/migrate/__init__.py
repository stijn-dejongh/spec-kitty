"""CLI command modules for spec-kitty migrate subcommands.

This package contains individual command modules registered under the
``spec-kitty migrate`` typer subgroup (defined in migrate_cmd.py).

Modules:
    charter_encoding    --  ``spec-kitty migrate charter-encoding``
    backfill_provenance --  ``spec-kitty migrate backfill-provenance``
"""
