# Spec Kitty Constitution

> Auto-generated by spec-kitty constitution command
> Created: 2026-02-21
> Version: 1.0.0

## Purpose

This constitution captures the technical standards and governance rules for
spec-kitty. All features and pull requests should align with these principles.

## Fork Context

This is a fork of [Priivacy-ai/spec-kitty](https://github.com/Priivacy-ai/spec-kitty)
focused on the **2.x version** of spec-kitty (see upstream `2.x` branch).

- **`2.x`** — Local clone of the upstream 2.x branch
- **`develop`** — Active development integration branch (acts as the main branch for day-to-day work)
- **`main`** — Stable release branch

All feature branches should target `develop`. The `main` branch receives merges
from `develop` when a release is ready.

## Technical Standards

### Languages and Frameworks

- **Python 3.12+** — Primary language for the spec-kitty CLI and all domain logic
- **Node / npx** — Used for validation tooling and git hooks
- **Markdown** — Documentation format

### Testing Requirements

- **pytest** with tiered coverage enforcement (see `docs/development/tiered-quality-enforcement.md`):
  - **Core tier** (80%+): `status/`, `core/`, `merge/`, `constitution/`, `git/`, `events/`, `next/`, `spec_kitty_events/`, `doctrine/`
  - **Supporting tier** (55%+): `cli/`, `glossary/`, `sync/`, `frontmatter.py`
  - **Glue tier** (0%): `orchestrator/`, `dashboard/`, `scripts/`, `missions/`
- **mypy** — Static type checking
- **ruff** — Linting and formatting
- **Pre-commit hooks** — Linting enforced at commit time
- Thresholds defined in `pyproject.toml` under `[tool.coverage_tiers]`, enforced by `scripts/check_coverage.py`

### Performance and Scale

- CLI operations must complete within **2 seconds**
- No specific throughput targets at this stage

### Deployment and Constraints

- **Cross-platform**: Linux, macOS, Windows
- Distributed via **pip** (PyPI) and **npx**
- Current focus is **local CLI** tooling
- **SaaS in-house deployment** is an envisioned future direction

## Governance

### Amendment Process

Any team member can propose amendments via pull request targeting `develop`.
Changes are discussed and merged following standard PR review process.

### Compliance Validation

Code reviewers validate compliance during PR review. Constitution violations
should be flagged and addressed before merge.

### Exception Handling

Exceptions discussed case-by-case with team. Strong justification required.
Consider updating constitution if exceptions become common.
