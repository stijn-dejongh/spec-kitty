---
work_package_id: WP04
title: Scope the _status_collectors legacy charter.md gate + backward-compat test
dependencies: []
requirement_refs:
- FR-006
- NFR-001
- NFR-002
planning_base_branch: feat/doctrine-charter-split-unification
merge_target_branch: feat/doctrine-charter-split-unification
branch_strategy: Planning artifacts for this mission were generated on feat/doctrine-charter-split-unification. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/doctrine-charter-split-unification unless the human explicitly redirects the landing branch.
subtasks:
- T001
- T002
history:
- Generated by /spec-kitty.tasks 2026-08-02
agent_profile: python-pedro
authoritative_surface: src/specify_cli/cli/commands/charter/_status_collectors.py
create_intent:
- tests/specify_cli/cli/commands/charter/test_status_collectors_legacy_md_shape.py
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- src/specify_cli/cli/commands/charter/_status_collectors.py
- tests/specify_cli/cli/commands/charter/test_status_collectors_legacy_md_shape.py
role: implementer
tags: []
tracker_refs: []
---

## ⚡ Do This First: Load Agent Profile

Load your assigned profile: run `/ad-hoc-profile-load python-pedro` and adopt its identity/boundaries before reading further.

## Objective

Scope the `_status_collectors.py:85-87` "legacy charter.md gate" **explicitly** as a documented pre-consolidation
migration-compat branch (`charter.md` present / `charter.yaml` absent), and add a backward-compat regression test
for the `charter.md`-only status-collector shape (FR-006). If that shape is declared unsupported, remove the
branch instead — do **not** leave it comment-scoped and untested. The staleness display header/listing (`:74-84`,
`:103`) is legitimate display and stays unchanged (C-001).

Read first (authoritative — do not re-derive): `../spec.md` FR-006 + Edge Cases ("charter.md-only is a
migration-compat shape") + C-001; `../research.md` **D2**. Ground in the live `:85-87` branch around the
`CHARTER_YAML_FILENAME` / `_resolve_charter_path` fallback in
`src/specify_cli/cli/commands/charter/_status_collectors.py`.

## Guidance

#### T001 — Characterization pin (green-first, charter C-011)

Author `tests/specify_cli/cli/commands/charter/test_status_collectors_legacy_md_shape.py` seeding a
`charter.md`-only project (present md, **absent** `charter.yaml`) and asserting the status collector resolves the
legacy shape (backward-compat). This is a **characterization pin (green-first), NOT an ATDD red** — the md-only
shape ALREADY resolves today (`_status_collectors.py:72-87`, research.md **D10**), so the test passes on first
authoring; do **not** commit it red first. It pins today's already-passing behaviour so the scope/document step
(or the unsupported-shape decision) cannot silently change it.

#### T002 — Scope explicitly (or remove) + document

Wrap the `:85-87` legacy branch behind an explicit, documented migration-compat condition and comment naming it
a pre-consolidation shape. Leave the display header/listing at `:74-84`/`:103` untouched (legitimate display,
C-001). If the team decides the `charter.md`-only shape is unsupported, delete the branch and flip the test to
assert the unsupported-shape error path — either way the shape is now tested, not silently comment-scoped.

## Branch Strategy

Planning branch: `feat/doctrine-charter-split-unification`. Final merge target: same. The execution worktree is
allocated per computed lane from `lanes.json`.

## Definition of Done

- Characterization pin (T001) GREEN green-first: the `charter.md`-only status-collector shape resolves (or errors
  deterministically if declared unsupported) — pinned by a regression test, not a bare comment.
- The `:85-87` branch is explicitly scoped/documented; display header/listing (`:74-84`,`:103`) unchanged (C-001).
- The "declare the `charter.md`-only shape unsupported → error deterministically" option is a **support-scope
  product decision routed through the HiC + issue-matrix** (DIR-012 / C-005), **never** implementer-unilateral —
  do not flip the shape to an error path without that recorded decision.
- `ruff` + `mypy` zero new issues (NFR-002).

## Risks / Reviewer guidance

- This is the one surface that legitimately serves the legacy `charter.md`-only shape — do not "unify" it away
  without the explicit unsupported-shape decision. Do not touch the display listing.
