---
work_package_id: WP06
title: Retrospective resolver — yaml-first precedence + one shared _CHARTER_REL
dependencies:
- WP05
requirement_refs:
- FR-005
- C-003
- NFR-002
planning_base_branch: feat/doctrine-charter-split-unification
merge_target_branch: feat/doctrine-charter-split-unification
branch_strategy: Planning artifacts for this mission were generated on feat/doctrine-charter-split-unification. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/doctrine-charter-split-unification unless the human explicitly redirects the landing branch.
subtasks:
- T001
- T002
- T003
history:
- Generated by /spec-kitty.tasks 2026-08-02
agent_profile: python-pedro
authoritative_surface: src/specify_cli/retrospective/policy.py
create_intent:
- tests/specify_cli/retrospective/test_retrospective_yaml_first_resolution.py
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- src/specify_cli/retrospective/policy.py
- src/specify_cli/retrospective/mode.py
- src/specify_cli/retrospective/gate.py
- tests/specify_cli/retrospective/test_retrospective_yaml_first_resolution.py
role: implementer
tags: []
tracker_refs: []
---

## ⚡ Do This First: Load Agent Profile

Load your assigned profile: run `/ad-hoc-profile-load python-pedro` and adopt its identity/boundaries before reading further.

## Objective

Land FR-005 **(c)**: change `retrospective/{policy,mode,gate}.py` to resolve the retrospective policy
**yaml-first** — `charter.yaml` `governance.retrospective` (authoritative, takes precedence) with `charter.md`
frontmatter as an **overridden secondary** for legacy `charter.md`-only projects (SC-002, C-003 backward-compat)
— and collapse the three duplicate `_CHARTER_REL` constants to one shared definition. Depends on **WP05** (the
emitted `governance.retrospective` block must exist).

Read first (authoritative — do not re-derive): `../spec.md` FR-005 + User Story 2 (all 4 scenarios) + SC-002 +
C-003; `../research.md` **D3** (resolver is a **downward** import: `charter.charter_yaml_io.load_charter_yaml` +
`charter.bundle`, feeding the dict as highest-precedence through `_apply_block_to_policy`). Ground in the live
anchors: the three redeclared `_CHARTER_REL` at `policy.py:39`, `mode.py:67`, `gate.py:116`; the precedence
merge point `_apply_block_to_policy` at `policy.py:381`; the md-frontmatter reads at `policy.py:233-234`,
`mode.py:124`, `gate.py:136`.

## Guidance

#### T001 — ATDD red (charter C-011)

Author `tests/specify_cli/retrospective/test_retrospective_yaml_first_resolution.py` with three cases per User
Story 2: (1) yaml-only governance policy resolves from `charter.yaml`; (2) **both** present with a divergent
`charter.md` frontmatter → `charter.yaml` **wins** (precedence); (3) legacy `charter.md`-only still resolves from
frontmatter (C-003). Cases 1-2 fail today (resolvers read md-frontmatter only). Commit red first.

#### T002 — Yaml-first resolution (D3)

In each of `policy.py` / `mode.py` / `gate.py`, load `governance.retrospective` via
`charter.charter_yaml_io.load_charter_yaml` + `charter.bundle` (downward import — allowed) and feed it as the
highest-precedence block through the existing `_apply_block_to_policy` (`policy.py:381`); keep the `charter.md`
frontmatter read as the **overridden secondary** so legacy projects still resolve (C-003).

#### T003 — Collapse the three `_CHARTER_REL`

Replace the three redeclarations (`policy.py:39`, `mode.py:67`, `gate.py:116`) with one shared definition
imported by all three (FR-005 Acceptance Scenario 4 / User Story 2 Scenario 4). Update the `:233-234`/`:124`/`:136`
call sites to consume the shared constant.

## Branch Strategy

Planning branch: `feat/doctrine-charter-split-unification`. Final merge target: same. The execution worktree is
allocated per computed lane from `lanes.json`.

## Definition of Done

- ATDD red (T001) GREEN: yaml-wins-over-md; both-present precedence; legacy-md-only still resolves (SC-002, C-003).
- Exactly one shared `_CHARTER_REL` definition (no three redeclarations).
- `ruff` + `mypy` zero new issues (NFR-002); the precedence-flip is recorded in PR body + CHANGELOG (NFR-005).

## Risks / Reviewer guidance

- Backward-compat is load-bearing (C-003): a legacy `charter.md`-only project must NOT break. Verify the md read
  survives as a secondary, not deleted. Confirm the resolver imports the charter reader **downward** (no upward
  `specify_cli` edge introduced).
