---
work_package_id: WP11
title: FR-016 anti-regression gate — AST charter path-literal authority (SINK, last)
dependencies:
- WP01
- WP02
- WP03
- WP06
requirement_refs:
- FR-016
- NFR-004
- NFR-002
planning_base_branch: feat/doctrine-charter-split-unification
merge_target_branch: feat/doctrine-charter-split-unification
branch_strategy: Planning artifacts for this mission were generated on feat/doctrine-charter-split-unification. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/doctrine-charter-split-unification unless the human explicitly redirects the landing branch.
subtasks:
- T001
- T002
- T003
history:
- Generated by /spec-kitty.tasks 2026-08-02
agent_profile: python-pedro
authoritative_surface: tests/architectural/test_charter_path_literal_authority.py
create_intent:
- tests/architectural/test_charter_path_literal_authority.py
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- tests/architectural/test_charter_path_literal_authority.py
role: implementer
tags: []
tracker_refs: []
---

## ⚡ Do This First: Load Agent Profile

Load your assigned profile: run `/ad-hoc-profile-load python-pedro` and adopt its identity/boundaries before reading further.

## Objective

Ship the FR-016 **anti-regression durability gate**: an AST path-literal authority test that FAILS if (a) a new
inline `.kittify/charter/charter.{yaml,md}` path literal is introduced outside `charter/bundle.py`, or (b) a new
`charter.md`-keyed `.exists()` presence gate is added — so the single read authority stays durable, not a
point-in-time cleanup. This is the **SINK — LAST** WP: it depends on **WP01, WP02, WP03, WP06** (the IC-01
repoints + FR-005c resolver) because the gate can only go green once those inline literals are removed. Do
**NOT** merge this with WP10 (FR-008) — WP10 is first-wave.

Read first (authoritative — do not re-derive): `../spec.md` FR-016 + NFR-004 + C-001 (prose readers allowlisted);
`../research.md` **D6** (AST detection not raw grep — else it floods on ~161 docstring/prose mentions; frozen
shrink-only allowlist; the plan's allowlist is UNDERSTATED). Ground in the live residual path-builders D6 names
as expected allowlist seeds: `invocation/empty_charter.py:60` (`_CHARTER_BUNDLE_PATH`),
`charter_runtime/lint/checks/org_layer.py:291`, `doctrine/versioning.py:190,451`,
`doctrine/spdd_reasons/activation.py:49`, plus `upgrade/migrations/**` (historical determinism) and the C-003
prose readers.

## Guidance

#### T001 — ATDD red (charter C-011), self-mutation proof (NFR-004)

Author `tests/architectural/test_charter_path_literal_authority.py` that AST-walks for
`.kittify/charter/charter.{yaml,md}` string literals inside `Path(...)` constructions / const assignments (NOT
raw text grep — D6) outside `charter/bundle.py`, plus new `charter.md`-keyed `.exists()` presence gates. Seed the
**frozen shrink-only allowlist** from a **full AST census** run in this WP. The self-mutation proof: re-introduce
an inline charter literal → the gate goes RED. Commit the mutation-proof case red first.

#### T002 — Decide the `src/doctrine/**` scope (D6 open decision)

Make and record the open D6 decision: does the gate police `src/doctrine/**` (then allowlist
`doctrine/versioning.py:190,451` + `doctrine/spdd_reasons/activation.py:49`) or scope to `charter/` + `specify_cli/`?
Seed the allowlist accordingly from the AST census; a hard zero is unshippable (shrink-only).

#### T003 — Land green after the dependency repoints

Because WP01/WP02/WP03/WP06 remove the inline literals they own, the residual set the allowlist must capture is
smaller — run the census **after** rebasing on those lanes so the frozen allowlist is minimal. Confirm the gate
is GREEN on the merged tree and RED under self-mutation (NFR-004).

## Branch Strategy

Planning branch: `feat/doctrine-charter-split-unification`. Final merge target: same. The execution worktree is
allocated per computed lane from `lanes.json`. **Sink ordering:** land AFTER WP01/WP02/WP03/WP06 merge.

## Definition of Done

- ATDD red (T001) GREEN on the merged tree; gate FAILS on a re-introduced inline charter path literal
  (self-mutation proof, NFR-004). The self-mutation proof MUST inject the test literal into a **NON-allowlisted**
  module (an allowlisted target would falsely pass) (research.md D10).
- **Allowlist non-vacuity (research.md D10):** each allowlist entry carries an **inline one-line justification**
  — **no blanket directory globs**; entries are per-module/per-line and named. Second guard: **adding a file to
  the allowlist must not be a way to green a real violation** (a test that adding an offending module to the
  allowlist would still fail the gate, or an equivalent shrink-only assertion), so the allowlist cannot become a
  growable escape hatch.
- Detection is AST path-construction (not raw grep, D6); allowlist is frozen shrink-only, seeded from a full AST
  census; the `src/doctrine/**` scope decision (D6) is recorded as a **bounded, justified list** (each allowlisted
  `src/doctrine/**` path enumerated with its rationale), not a wildcard.
- `ruff` + `mypy` zero new issues (NFR-002).

## Risks / Reviewer guidance

- Ordering is load-bearing: run the census/allowlist AFTER the dependency repoints, or it freezes literals that
  WP01-03/06 will delete. Verify AST (not grep) detection. Do NOT merge with WP10. Confirm the allowlist is
  shrink-only, not a growable escape hatch.
