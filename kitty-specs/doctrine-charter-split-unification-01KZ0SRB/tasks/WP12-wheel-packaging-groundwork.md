---
work_package_id: WP12
title: Wheel packaging groundwork — kernel pyproject + doctrine kernel-dep + packs build-hook + real hatch build
dependencies: []
requirement_refs:
- FR-009
- FR-010
- NFR-004
- NFR-002
- C-002
planning_base_branch: feat/doctrine-charter-split-unification
merge_target_branch: feat/doctrine-charter-split-unification
branch_strategy: Planning artifacts for this mission were generated on feat/doctrine-charter-split-unification. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/doctrine-charter-split-unification unless the human explicitly redirects the landing branch.
subtasks:
- T001
- T002
- T003
- T004
history:
- Generated by /spec-kitty.tasks 2026-08-02
agent_profile: python-pedro
authoritative_surface: src/doctrine/pyproject.toml
create_intent:
- src/kernel/pyproject.toml
- src/doctrine/hatch_build.py
- tests/architectural/test_doctrine_wheel_closure.py
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- src/kernel/pyproject.toml
- src/doctrine/pyproject.toml
- src/doctrine/hatch_build.py
- tests/architectural/test_doctrine_wheel_closure.py
role: implementer
tags: []
tracker_refs: []
---

## ⚡ Do This First: Load Agent Profile

Load your assigned profile: run `/ad-hoc-profile-load python-pedro` and adopt its identity/boundaries before reading further.

## Objective

Land the wheel packaging **groundwork** (FR-009 + FR-010, SC-005): mint `src/kernel/pyproject.toml`
(`spec-kitty-kernel`, **zero** first-party deps — the true root of the wheel chain), fix
`src/doctrine/pyproject.toml` to declare its `spec-kitty-kernel` dependency **and** carry the repo-root sibling
`packs/` via a spike-verified out-of-tree mechanism, **execute a real `hatch build`** of the nested doctrine
wheel and record the wheel contents in `research.md §D7`, and ship a non-vacuous closure test. **NO cutover**
(C-002): do **NOT** remove `src/kernel`/`src/doctrine`/`src/charter` from the root `pyproject.toml` `packages`,
and confirm **no** CI job builds/installs the nested wheel standalone this mission.

Read first (authoritative — do not re-derive): `../spec.md` FR-009/FR-010 + User Story 4 Scenarios 2-3 + SC-005 +
C-002 + NFR-004; `../research.md` **D7** (hatchling build hook mechanism; the spike MUST run; fallback options).
Ground in the live anchors: `_resolve_built_in` at `pack_paths.py:195` (`files("doctrine").parent / "packs" /
"built-in"` — packs is a **site-packages sibling**, not in-tree); the real kernel import-closure need at
`resolver.py:32`, `missions/primitives.py`, `shared/schema_utils.py` (`from kernel...`). A naive
`force-include ../../packs` escapes the project root and hatchling refuses it.

## Guidance

#### T001 — ATDD red (charter C-011), self-mutation proof (NFR-004)

Author `tests/architectural/test_doctrine_wheel_closure.py` asserting the doctrine wheel manifest declares the
`spec-kitty-kernel` dep **and** the chosen `packs/` mechanism; it FAILS if either is removed (self-mutation
proof, NFR-004). Commit red first (kernel pyproject + doctrine kernel-dep + hook not yet present).

#### T002 — Mint kernel pyproject (FR-009)

Create `src/kernel/pyproject.toml` for `spec-kitty-kernel`, **zero** first-party dependencies, following the
`2026-04-25-1` precedent shape. Groundwork only — not wired into the release/build path.

#### T003 — Doctrine kernel-dep + packs build-hook (FR-010, D7)

Add the `spec-kitty-kernel` dependency to `src/doctrine/pyproject.toml`. Implement `src/doctrine/hatch_build.py`
as a hatchling `BuildHookInterface.initialize` custom hook injecting a computed absolute `force_include` that
lands the repo-root sibling `packs/` as a `doctrine` sibling in the wheel (D7). If the hook fails the real
build, use a D7 fallback (thin MANIFEST/data-dir copy, or defer the functional packs-carry to the cutover
follow-on while still shipping the kernel-dep closure) and record the chosen mechanism.

#### T004 — Run the real `hatch build` + record in research.md §D7 (D7 guard)

**Actually execute** `hatch build` of the nested doctrine wheel and confirm the built wheel shows `packs/built-in/`
as a `doctrine` sibling. Record the build-verified mechanism + wheel-contents result in `research.md §D7 spike
result` (currently **PENDING**). A manifest-shape test alone is insufficient — the groundwork is inert without
the executed build (D7).

## Branch Strategy

Planning branch: `feat/doctrine-charter-split-unification`. Final merge target: same. The execution worktree is
allocated per computed lane from `lanes.json`.

## Definition of Done

- ATDD red (T001) GREEN: closure test fails if the `kernel` dep **or** `packs/` mechanism is removed (NFR-004).
- `src/kernel/pyproject.toml` exists (`spec-kitty-kernel`, zero first-party deps, FR-009).
- A **real `hatch build`** was executed and its wheel contents (`packs/built-in/` as doctrine sibling) recorded
  in `research.md §D7` — not just a manifest-shape test (D7).
- **C-002 preserved:** root `pyproject.toml` `packages` still lists `src/kernel`/`src/doctrine`/`src/charter`
  (no cutover); confirmed no CI job builds/installs the nested wheel standalone this mission.
- **D7 fallback governance (research.md D10):** invoking any D7 packs-mechanism fallback (esp. deferring the
  functional packs-carry) is an **explicit HiC decision recorded in the issue-matrix** (DIR-012 / C-005),
  **not** implementer-unilateral. If packs-carry is deferred: **SC-005 is re-scoped in writing**, and the closure
  test's `packs/` assertion is **NOT silently removed** — it becomes an **explicit, documented kernel-dep-only
  state** (the assertion is retained and marked/deferred with rationale, not deleted).
- `ruff` + `mypy` zero new issues (NFR-002).

## Risks / Reviewer guidance

- The D7 guard is the trap: reject a manifest-shape-only proof — demand the executed `hatch build` result in
  research.md §D7. Verify C-002 (root packages untouched; no standalone nested-wheel CI job — the kernel dep is
  unresolvable until the follow-on publishes the kernel wheel). Confirm the closure test is self-mutation
  non-vacuous.
