---
work_package_id: WP05
title: RetrospectiveGovernance schema + compiler emitter + omit-when-empty pruner
dependencies: []
requirement_refs:
- FR-005
- NFR-002
- NFR-005
planning_base_branch: feat/doctrine-charter-split-unification
merge_target_branch: feat/doctrine-charter-split-unification
branch_strategy: Planning artifacts for this mission were generated on feat/doctrine-charter-split-unification. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/doctrine-charter-split-unification unless the human explicitly redirects the landing branch.
subtasks:
- T001
- T002
- T003
- T004
history:
- Generated by /spec-kitty.tasks 2026-08-02
agent_profile: python-pedro
authoritative_surface: src/charter/schemas.py
create_intent:
- tests/charter/test_retrospective_governance_emit.py
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- src/charter/schemas.py
- src/charter/compiler.py
- src/charter/sync.py
- tests/charter/test_retrospective_governance_emit.py
role: implementer
tags: []
tracker_refs: []
---

## ⚡ Do This First: Load Agent Profile

Load your assigned profile: run `/ad-hoc-profile-load python-pedro` and adopt its identity/boundaries before reading further.

## Objective

Land FR-005 **(a) + (b)**: add a pure-data `RetrospectiveGovernance` sub-model to `GovernanceConfig` and wire
the compiler/emitter so `charter generate` populates `governance.retrospective` — **omitting it when empty** so
existing `charter.yaml` bytes are unchanged (NFR-005 byte-stability). This is a **schema + compiler change, NOT
a read swap** (`GovernanceConfig` has no `retrospective` field today). The downstream yaml-first resolver is
WP06 (depends on this WP).

Read first (authoritative — do not re-derive): `../spec.md` FR-005 + User Story 2 + SC-002 + C-003;
`../research.md` **D3** (the governing decision — no layer inversion, pure-data sub-model, list-only pruner
fix). Ground in the live anchors: `GovernanceConfig` at `src/charter/schemas.py:135`; the
**list-only** `_prune_optional_empties` at `schemas.py:346` (`isinstance(value, list) and not value` at the
loop over `:360`, invoked at `:386`); `load_governance_config` at `src/charter/sync.py:233`;
`write_compiled_charter` at `src/charter/compiler.py:441` (and `:601`).

## Guidance

#### T001 — ATDD red (charter C-011)

Author `tests/charter/test_retrospective_governance_emit.py`: (a) a charter whose governance sets a retrospective
policy → `charter generate` emits `governance.retrospective`; (b) a charter with **no** retrospective config →
the emitted `charter.yaml` is **byte-identical** to today (block omitted).

**Red-timing note (research.md D10 — do not conflate the two cases):**
- Case (a) "emit `governance.retrospective`" is the **committed-first ATDD red** (no field exists today, so it
  fails on first authoring). Commit case (a) red first.
- Case (b) "no-config → byte-identical `charter.yaml`" is **byte-stable today** (no field emitted at all) and
  goes **red only AFTER the T002 schema field lands** — a default `retrospective` block then leaks until the
  T004 pruner drops it. It is the **NFR-005 pruner guard, a mid-implementation gate**, **not** a
  committed-first red; do **not** label it as such or expect it red before the field exists.

#### T002 — RetrospectiveGovernance sub-model (FR-005a)

Add a pure-data `RetrospectiveGovernance` pydantic sub-model to `schemas.py` (bool / `Literal` / nested
permissions per D3) composed into `GovernanceConfig` at `:135`. **Zero `specify_cli` import** — do NOT import
`specify_cli.retrospective.policy.RetrospectivePolicy` upward (D3; the FR-008/WP10 gate polices this). Model the
field as `| None` so an unset charter carries no block.

#### T003 — Emitter wiring (FR-005b)

Wire `charter.sync.load_governance_config` (`sync.py:233`) → `write_compiled_charter`
(`compiler.py:441`/`:601`) to populate `governance.retrospective`. Stay in-layer (charter → charter).

#### T004 — Extend the pruner for byte-stability (D3, NFR-005)

Extend `_prune_optional_empties` (`schemas.py:346`) to also drop an allowlisted key whose value is `None` or an
empty dict (today it is list-only), **or** model the field with a None-drop branch. This is the load-bearing
NFR-005 fix — without it a default `retrospective` block leaks into every `charter.yaml`. Ship the byte-stability
regression (T001 case b) as the proof.

## Branch Strategy

Planning branch: `feat/doctrine-charter-split-unification`. Final merge target: same. The execution worktree is
allocated per computed lane from `lanes.json`.

## Definition of Done

- ATDD red (T001) GREEN: `charter generate` emits `governance.retrospective` when set; omits it (byte-stable
  `charter.yaml`) when empty (NFR-005).
- `RetrospectiveGovernance` is pure-data (no `specify_cli` import, D3); pruner extended per D3.
- `ruff` + `mypy` zero new issues (NFR-002). The precedence-flip behaviour-change (charter.yaml > charter.md
  frontmatter) is recorded in the PR body + CHANGELOG (NFR-005).

## Risks / Reviewer guidance

- The NFR-005 trap: without the pruner fix an empty block leaks into every compiled charter — insist on the
  byte-identical fixture. Verify no upward `specify_cli` import was added (D3). This is the biggest single change
  in the mission; WP06 depends on the emitted block existing.
