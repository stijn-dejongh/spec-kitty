---
work_package_id: WP03
title: analysis_report — hash charter.yaml, drop both charter.md hash entries
dependencies: []
requirement_refs:
- FR-004
- NFR-001
- NFR-002
planning_base_branch: feat/doctrine-charter-split-unification
merge_target_branch: feat/doctrine-charter-split-unification
branch_strategy: Planning artifacts for this mission were generated on feat/doctrine-charter-split-unification. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/doctrine-charter-split-unification unless the human explicitly redirects the landing branch.
subtasks:
- T001
- T002
history:
- Generated by /spec-kitty.tasks 2026-08-02
agent_profile: python-pedro
authoritative_surface: src/specify_cli/analysis_report.py
create_intent:
- tests/specify_cli/test_analysis_report_charter_yaml_staleness.py
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- src/specify_cli/analysis_report.py
- tests/specify_cli/test_analysis_report_charter_yaml_staleness.py
role: implementer
tags: []
tracker_refs: []
---

## ⚡ Do This First: Load Agent Profile

Load your assigned profile: run `/ad-hoc-profile-load python-pedro` and adopt its identity/boundaries before reading further.

## Objective

Key the analysis-report charter **staleness input** on `charter.yaml` (the resolving authority, FR-004): the
resulting hash-input set contains `charter.yaml` and **removes both** `charter.md` entries — the `:191`
canonical companion **and** the `:192` legacy `charter/charter.md` fallback that every other resolver refuses.
Acceptance: staleness computes without error when `charter.md` is absent (SC-001).

Read first (authoritative — do not re-derive): `../spec.md` FR-004 + User Story 1 Scenario 2 + C-001;
`../research.md` **D2**. Ground in the live `_charter_path` at `src/specify_cli/analysis_report.py:178`, the
two `charter.md` candidates at `:191` and `:192`, and the hash-input assembly at `:206-207`.

## Guidance

#### T001 — ATDD red (charter C-011)

Author `tests/specify_cli/test_analysis_report_charter_yaml_staleness.py` seeding `charter.yaml` and **deleting**
`charter.md` (NFR-001), asserting the staleness input hashes `charter.yaml` and computes without error. It fails
today because `_charter_path` only offers the two `charter.md` candidates (`:191`, `:192`). Commit it red first.

#### T002 — Repoint the hash input

In `_charter_path`, resolve `charter.yaml` (prefer `charter.bundle.CHARTER_YAML` over an inline literal — FR-001
single door) and **remove both** `charter.md` candidates at `:191`/`:192`. Confirm the `inputs["charter"]`
assembly at `:206-207` now hashes `charter.yaml`. Keep a both-files no-regression pin (behaviour unchanged when
both exist).

## Branch Strategy

Planning branch: `feat/doctrine-charter-split-unification`. Final merge target: same. The execution worktree is
allocated per computed lane from `lanes.json`.

## Definition of Done

- ATDD red (T001) GREEN: staleness computes with `charter.md` absent; hashes `charter.yaml` (SC-001).
- **Both** `charter.md` hash entries (`:191` companion + `:192` legacy fallback) removed.
- `ruff` + `mypy` zero new issues (NFR-002).

## Risks / Reviewer guidance

- Verify **both** md entries are gone, not just `:191` — the `:192` legacy fallback is the subtle one. Confirm
  the fixture deletes `charter.md` (NFR-001), not seeds both.
