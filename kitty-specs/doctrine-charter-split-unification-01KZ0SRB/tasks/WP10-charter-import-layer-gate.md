---
work_package_id: WP10
title: Delete charter→specify_cli edge + non-vacuous AST charter-import gate
dependencies: []
requirement_refs:
- FR-008
- NFR-004
- NFR-002
planning_base_branch: feat/doctrine-charter-split-unification
merge_target_branch: feat/doctrine-charter-split-unification
branch_strategy: Planning artifacts for this mission were generated on feat/doctrine-charter-split-unification. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/doctrine-charter-split-unification unless the human explicitly redirects the landing branch.
subtasks:
- T001
- T002
- T003
history:
- Generated by /spec-kitty.tasks 2026-08-02
agent_profile: python-pedro
authoritative_surface: tests/architectural/test_charter_no_specify_cli_import.py
create_intent:
- tests/architectural/test_charter_no_specify_cli_import.py
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- src/charter/synthesizer/synthesize_pipeline.py
- tests/architectural/test_charter_no_specify_cli_import.py
role: implementer
tags: []
tracker_refs: []
---

## ⚡ Do This First: Load Agent Profile

Load your assigned profile: run `/ad-hoc-profile-load python-pedro` and adopt its identity/boundaries before reading further.

## Objective

Delete the **single real upward layer edge** — the lazy in-function `import specify_cli` at
`charter/synthesizer/synthesize_pipeline.py:68` (used only for a `__version__` fallback) → resolve via
`importlib.metadata` only (FR-008, SC-004) — and ship a **non-vacuous** AST-walk gate that FAILS if any
`src/charter/**` module imports `specify_cli` at **any** scope. This is first-wave (no deps) and does **not**
merge with the WP11 literal-authority sink.

Read first (authoritative — do not re-derive): `../spec.md` FR-008 + User Story 4 Scenario 1 + NFR-004 + SC-004;
`../research.md` **D2** (this gate is a new test file, disjoint). Ground in the live anchors: the
`importlib.metadata` path already at `synthesize_pipeline.py:64` (`from importlib.metadata import version`) and
the edge to delete at `:68-69` (`import specify_cli; v = str(specify_cli.__version__)`). Note **pytestarch /
`test_layer_rules` is GREEN with the edge present** — it does not catch an in-function import — so it is **not**
the guard; mirror the AST walker `_collect_specify_cli_imports` used in the `mission_runtime` boundary ledger.

## Guidance

#### T001 — ATDD red (charter C-011), self-mutation proof (NFR-004)

Author `tests/architectural/test_charter_no_specify_cli_import.py` that AST-walks every `src/charter/**` module
and FAILS on any `import specify_cli` at any scope. With the `:68` edge still present the test is RED — commit it
red first (the self-mutation proof: the gate fails **with** the edge, per NFR-004).

#### T002 — Delete the edge (FR-008)

Remove the `:68-69` `import specify_cli` / `__version__` fallback; rely on the `importlib.metadata` `version(...)`
path already at `:64`. Confirm `test_layer_rules` still green (it was green before too — that is the point).

#### T003 — Prove non-vacuity

Confirm the new gate goes GREEN after the deletion, and re-add the import locally to confirm it goes RED again
(self-mutation, NFR-004). Mirror `_collect_specify_cli_imports` so in-function imports are actually walked.

## Branch Strategy

Planning branch: `feat/doctrine-charter-split-unification`. Final merge target: same. The execution worktree is
allocated per computed lane from `lanes.json`.

## Definition of Done

- The `synthesize_pipeline.py:68` edge is gone; `__version__` resolves via `importlib.metadata` only (FR-008).
- The AST gate FAILS with the edge present and PASSES after deletion (self-mutation proof, NFR-004); it walks
  **in-function** imports (not a pytestarch alias). `test_layer_rules` green (SC-004).
- `ruff` + `mypy` zero new issues (NFR-002).

## Risks / Reviewer guidance

- A vacuous gate is worse than none — verify the walker catches an **in-function** import (the pytestarch rule
  does not). Confirm the self-mutation RED/GREEN was actually demonstrated. WP13 (charter-wheel ADR) softly
  depends on this "zero entanglement" claim.
