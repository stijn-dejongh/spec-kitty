---
work_package_id: WP14
title: CI hygiene + un-inert parity gate + regen docs + timeboxed
dependencies: []
requirement_refs:
- FR-012
- FR-013
- FR-014
- FR-015
- NFR-002
planning_base_branch: feat/doctrine-charter-split-unification
merge_target_branch: feat/doctrine-charter-split-unification
branch_strategy: Planning artifacts for this mission were generated on feat/doctrine-charter-split-unification. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/doctrine-charter-split-unification unless the human explicitly redirects the landing branch.
subtasks:
- T001
- T002
- T003
- T004
history:
- Generated by /spec-kitty.tasks 2026-08-02
agent_profile: python-pedro
authoritative_surface: tests/architectural/test_docs_cli_reference_parity.py
create_intent: []
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- .github/workflows/doctrine-charter-tests.yml
- tests/architectural/test_docs_cli_reference_parity.py
- docs/api/cli-commands.md
- docs/api/agent-subcommands.md
role: implementer
tags: []
tracker_refs:
- '3149'
- '3107'
- '3102'
- '2831'
- '2992'
---

## ⚡ Do This First: Load Agent Profile

Load your assigned profile: run `/ad-hoc-profile-load python-pedro` and adopt its identity/boundaries before reading further.

## Objective

Fold the CI/docs hygiene items (SC-006) plus the timeboxed adjacency investigation: **#3149** (FR-012 — add the
charter CLI command dir to the `doctrine-charter-tests.yml` path filter + pytest selection), **#3107** (FR-013 —
un-inert **both** parity fixtures, regen `docs/api/{cli-commands,agent-subcommands}.md`, assert the test RAN
GREEN), **#3102** (FR-014 — closeout in the PR body), and **#2831/#2992** (FR-015 — timeboxed investigation,
**default defer-with-reason**; fold only on a *proven* shared root cause, DIR-013).

Read first (authoritative — do not re-derive): `../spec.md` FR-012/013/014/015 + User Story 5 + SC-006;
`../research.md` **D8** (FR-012→FR-014 closeout order; FR-015 is a timeboxed investigation with a deferred
issue-matrix verdict, not a code WP). Ground in the live inert-fixture anchors: `test_docs_cli_reference_parity.py`
has **two** `_read_or_skip` fixtures — `REFERENCE_PATH` (→ non-existent `docs/reference/cli-commands.md`) and
`AGENT_REFERENCE_PATH` (→ non-existent `docs/reference/agent-subcommands.md`); `test_visible_paths_match_reference`
SKIPs if **either** is missing (live docs are at `docs/api/`).

## Guidance

#### T001 — FR-012 (#3149): path-filter + pytest selection

Add `src/specify_cli/cli/commands/charter/**` (+ `tests/specify_cli/cli/commands/charter/**` and
`test_analyze_surface_agreement.py`) to **both** the trigger `paths:` and the pytest selection in
`.github/workflows/doctrine-charter-tests.yml`, so the charter CLI command layer is no longer false-green under
that workflow.

#### T002 — FR-013 (#3107): un-inert BOTH parity fixtures + regen docs

Repoint **both** `REFERENCE_PATH` and `AGENT_REFERENCE_PATH` in `test_docs_cli_reference_parity.py` to the live
`docs/api/` docs, and regenerate `docs/api/cli-commands.md` + `docs/api/agent-subcommands.md` for the current
`charter pack list/path/apply` surface. Acceptance asserts `test_visible_paths_match_reference` **RAN GREEN**
(not merely that its first fixture stopped skipping — a half-fix leaves it SKIPPED, reviewer-renata).

#### T003 — FR-014 (#3102): closeout

Confirm the `doctrine-charter-tests.yml` workflow is delivered (post-FR-012) and record the #3102 closing keyword
in the PR body (fixed-but-still-open hygiene).

#### T004 — FR-015 (#2831/#2992): timeboxed investigation, default DEFER

Timebox a bounded read of #2831 (implement gate false-fails `charter_source missing` despite sync passing, P0)
and #2992 (`charter sync --force` "already in sync" while `status` MISSING, P1). **Default defer-with-reason** —
only fold a fix if the root cause is *provably* the same charter presence/read split-brain this mission unifies
(DIR-013 — never silently expand the WP set). Record the defer/fold decision + reason in the issue-matrix.

## Branch Strategy

Planning branch: `feat/doctrine-charter-split-unification`. Final merge target: same. The execution worktree is
allocated per computed lane from `lanes.json`.

## Definition of Done

- FR-012: `doctrine-charter-tests.yml` triggers/selects `cli/commands/charter/**` (+ its tests) — no longer
  false-green (SC-006).
- FR-013: **both** `REFERENCE_PATH` **and** `AGENT_REFERENCE_PATH` repointed to `docs/api/`; **both** docs
  (`cli-commands.md` + `agent-subcommands.md`) regenerated; `test_docs_cli_reference_parity` **RAN GREEN** (not
  skipped) (SC-006).
- **Mechanical not-skipped guard (research.md D10):** add a meta-assertion that `test_visible_paths_match_reference`
  does **NOT** emit a skip — either an in-test assertion that **both** fixture paths exist (so the `_read_or_skip`
  branch cannot trigger), or a CI `-rs` skip-report gate that fails the job if this test reports SKIPPED — so
  "ran GREEN" is **enforced mechanically, not eyeballed**.
- FR-014: #3102 closeout keyword recorded in the PR body.
- FR-015: #2831/#2992 investigation timeboxed with an explicit defer-with-reason (or a proven-shared-root fold);
  no silent scope expansion (DIR-013).
- `ruff` + `mypy` zero new issues (NFR-002).
- Assign #3149/#3107/#3102 (and #2831/#2992 as investigate) to the HiC on claim (DIR-012) + fill each
  issue-matrix row verdict in the coord worktree (C-005); tracker comments name this mission.

## Risks / Reviewer guidance

- FR-013 half-fix trap: repointing only ONE fixture leaves the gate SKIPPED — insist the test RAN GREEN. FR-015
  must NOT pull the #2831 P0 into scope without a proven shared root cause. Verify #3102 closeout is in the PR
  body, not just claimed.
