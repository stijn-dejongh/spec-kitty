---
work_package_id: WP08
title: Route meta.json fail-closed callers — batch A (coordination/migration/audit/status)
dependencies:
- WP07
requirement_refs:
- FR-007
- NFR-003
- NFR-002
planning_base_branch: feat/doctrine-charter-split-unification
merge_target_branch: feat/doctrine-charter-split-unification
branch_strategy: Planning artifacts for this mission were generated on feat/doctrine-charter-split-unification. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/doctrine-charter-split-unification unless the human explicitly redirects the landing branch.
subtasks:
- T001
- T002
history:
- Generated by /spec-kitty.tasks 2026-08-02
agent_profile: python-pedro
authoritative_surface: src/specify_cli/status/aggregate.py
create_intent:
- tests/specify_cli/test_meta_fail_closed_batch_a.py
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- src/specify_cli/coordination/commit_router.py
- src/specify_cli/coordination/legacy_resolution.py
- src/specify_cli/coordination/status_transition.py
- src/specify_cli/coordination/surface_resolver.py
- src/specify_cli/migration/backfill_identity.py
- src/specify_cli/migration/backfill_runtime_state.py
- src/specify_cli/migration/backfill_topology.py
- src/specify_cli/migration/mission_state.py
- src/specify_cli/migration/normalize_mission_lifecycle.py
- src/specify_cli/migration/runtime_state_cutover.py
- src/specify_cli/audit/classifiers/meta.py
- src/specify_cli/status/aggregate.py
- src/specify_cli/status/cutover_eligibility.py
- src/specify_cli/status/emit.py
- src/specify_cli/status/identity_audit.py
- src/specify_cli/status/lifecycle.py
- src/specify_cli/status/store.py
- tests/specify_cli/test_meta_fail_closed_batch_a.py
role: implementer
tags: []
tracker_refs:
- '3140'
---

## ⚡ Do This First: Load Agent Profile

Load your assigned profile: run `/ad-hoc-profile-load python-pedro` and adopt its identity/boundaries before reading further.

## Objective

Route the WP07-census's unwrapped/divergent `load_meta` callers in **batch A** — the **coordination + migration
+ audit + status** subsystems — through the single `load_meta_fail_closed` authority (FR-007), so a corrupt /
non-dict `meta.json` fails closed (typed `MissionMetaReadError` or `None`) across this batch. Depends on **WP07**
(the public reader + the classified census). File-disjoint from WP09's batch B by subsystem (research.md **D2**).

Read first (authoritative — do not re-derive): `../spec.md` FR-007 + User Story 3 + Edge Cases; `../research.md`
**D4/D5** (the census disambiguates the TWO `load_meta` defs and which callers are silent-by-contract — consume
it, do not re-derive). **The WP07 `notes/meta-load-census.md` is authoritative for exact per-file inclusion:**
route only the raise-default-unwrapped / divergent-wrapper sites in these subsystems; leave
deliberately-silent callers (`load_meta_or_empty`, `on_malformed="none"`) untouched.

## Guidance

#### T001 — ATDD red (charter C-011)

Author `tests/specify_cli/test_meta_fail_closed_batch_a.py` exercising a representative corrupt + non-dict
`meta.json` through the batch-A entry points (e.g. `status/aggregate.py`, `coordination/surface_resolver.py`) and
asserting a typed exception / `None`, never a raw `ValueError`. It fails today because these callers are unwrapped.

#### T002 — Route the batch-A callers per the census

For each census-flagged batch-A site in the owned files, replace the unwrapped/divergent `load_meta` call with
`load_meta_fail_closed`, honouring the census's def-disambiguation (`mission_metadata` feature_dir vs
`task_utils` meta_path — do not mis-wire). Preserve silent-contract callers. Keep changes mechanical and
reviewable — one subsystem's routing per reviewable hunk.

## Branch Strategy

Planning branch: `feat/doctrine-charter-split-unification`. Final merge target: same. The execution worktree is
allocated per computed lane from `lanes.json`.

## Definition of Done

- ATDD red (T001) GREEN: batch-A callers fail closed (typed / `None`), zero raw `ValueError`.
- Every routed site matches the WP07 census classification; silent-contract callers preserved.
- `ruff` + `mypy` zero new issues (NFR-002).

## Risks / Reviewer guidance

- Do NOT route a deliberately-silent caller (regression) — cross-check each against the census. Confirm the def
  disambiguation (feature_dir vs meta_path) is respected. NFR-003's full-census contract test lands with WP09;
  this WP must not leave a batch-A site leaking.
