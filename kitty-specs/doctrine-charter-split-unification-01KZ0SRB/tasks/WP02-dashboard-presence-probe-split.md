---
work_package_id: WP02
title: Split dashboard resolve_project_charter_path — presence(yaml)/body(md)
dependencies: []
requirement_refs:
- FR-003
- NFR-001
- NFR-002
planning_base_branch: feat/doctrine-charter-split-unification
merge_target_branch: feat/doctrine-charter-split-unification
branch_strategy: Planning artifacts for this mission were generated on feat/doctrine-charter-split-unification. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/doctrine-charter-split-unification unless the human explicitly redirects the landing branch.
subtasks:
- T001
- T002
- T003
history:
- Generated by /spec-kitty.tasks 2026-08-02
agent_profile: python-pedro
authoritative_surface: src/specify_cli/dashboard/charter_path.py
create_intent:
- tests/specify_cli/dashboard/test_charter_path_presence_probe.py
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- src/specify_cli/dashboard/charter_path.py
- tests/specify_cli/dashboard/test_charter_path_presence_probe.py
role: implementer
tags: []
tracker_refs:
- '3150'
---

## ⚡ Do This First: Load Agent Profile

Load your assigned profile: run `/ad-hoc-profile-load python-pedro` and adopt its identity/boundaries before reading further.

## Objective

Fix **#3150**: split `dashboard/charter_path.py::resolve_project_charter_path` so the **presence** probe (the one
driving the dashboard "no charter" UI / `api.py`) keys on `charter.yaml` and survives `charter.md` deletion,
while the prose **body** served (`api.py` `read_text`) still reads `charter.md` when it exists (FR-003, C-001).

Read first (authoritative — do not re-derive): `../spec.md` FR-003 + User Story 1 + C-001; `../research.md`
**D2** (this file is file-disjoint from every other lane). Ground in the live function at
`src/specify_cli/dashboard/charter_path.py:11` (`resolve_project_charter_path`), whose `:52` currently returns
`canonical_root / ".kittify" / "charter" / "charter.md"` and `:22` returns `None` when `charter.md` is absent —
that md-keyed presence is the #3150 bug.

## Guidance

#### T001 — ATDD red (charter C-011)

Author `tests/specify_cli/dashboard/test_charter_path_presence_probe.py` seeding `charter.yaml` and **deleting**
`charter.md` (NFR-001), asserting the presence probe reports **present**. It fails today because `:52`/`:22` key
on `charter.md`. Commit it red first. Assert at the probe-function level — the dashboard UI stays covered by the
existing UI e2e guard, not claimed from this unit (per FR-003 Acceptance Scenario 1).

#### T002 — Split presence vs body

Make the presence resolution key on `charter.yaml` (survives `charter.md` deletion); keep a body path that still
reads `charter.md` when present. Prefer consuming the charter-layer constant `charter.bundle.CHARTER_YAML` rather
than re-deriving the `.kittify/charter/...` literal (FR-001 single-door discipline; the FR-016 gate in WP11 will
police residual inline literals here).

#### T003 — Both-files no-regression pin

Add a case where both files exist: presence still resolves and the served body still reads `charter.md`
(no regression, C-001). Keep it distinct from the presence proof (NFR-001).

## Branch Strategy

Planning branch: `feat/doctrine-charter-split-unification`. Final merge target: same. The execution worktree is
allocated per computed lane from `lanes.json`.

## Definition of Done

- ATDD red (T001) GREEN: presence probe reports present with `charter.md` deleted (#3150 fixed).
- Body path still serves `charter.md` when present (C-001); both-files no-regression pin passes.
- `ruff` + `mypy` zero new issues (NFR-002).
- Assign #3150 to the HiC on claim (DIR-012) + fill the #3150 issue-matrix row verdict in the coord worktree
  (C-005); tracker comment names this mission.

## Risks / Reviewer guidance

- Do NOT retarget the prose body to `charter.yaml` (C-001) — only the presence probe moves. Verify the fixture
  deletes `charter.md` (NFR-001). Confirm no bundle-contents inspection crept in — presence = file exists.
