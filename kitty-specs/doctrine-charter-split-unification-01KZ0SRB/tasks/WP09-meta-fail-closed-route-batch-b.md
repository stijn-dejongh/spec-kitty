---
work_package_id: WP09
title: Route meta.json fail-closed callers — batch B (merge/dashboard/cli/doc_analysis/tracker/acceptance) + NFR-003 census contract
dependencies:
- WP07
requirement_refs:
- FR-007
- NFR-003
- NFR-002
planning_base_branch: feat/doctrine-charter-split-unification
merge_target_branch: feat/doctrine-charter-split-unification
branch_strategy: Planning artifacts for this mission were generated on feat/doctrine-charter-split-unification. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/doctrine-charter-split-unification unless the human explicitly redirects the landing branch.
subtasks:
- T001
- T002
- T003
history:
- Generated by /spec-kitty.tasks 2026-08-02
agent_profile: python-pedro
authoritative_surface: src/specify_cli/merge/executor.py
create_intent:
- tests/specify_cli/test_meta_fail_closed_full_census_contract.py
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- src/specify_cli/merge/baseline.py
- src/specify_cli/merge/executor.py
- src/specify_cli/merge/ordering.py
- src/specify_cli/dashboard/diagnostics.py
- src/specify_cli/dashboard/scanner.py
- src/specify_cli/cli/commands/agent/mission_check_prerequisites.py
- src/specify_cli/cli/commands/agent/mission_feature_resolution.py
- src/specify_cli/cli/commands/agent/mission_repair.py
- src/specify_cli/cli/commands/agent/mission_setup_plan.py
- src/specify_cli/cli/commands/agent/workflow.py
- src/specify_cli/cli/commands/_coordination_doctor.py
- src/specify_cli/cli/commands/_identity_audit.py
- src/specify_cli/cli/commands/implement.py
- src/specify_cli/cli/commands/merge.py
- src/specify_cli/cli/commands/mission_type.py
- src/specify_cli/cli/commands/tracker.py
- src/specify_cli/doc_analysis/doc_state.py
- src/specify_cli/tracker/origin.py
- src/specify_cli/acceptance/__init__.py
- tests/specify_cli/test_meta_fail_closed_full_census_contract.py
role: implementer
tags: []
tracker_refs:
- '3140'
---

## ⚡ Do This First: Load Agent Profile

Load your assigned profile: run `/ad-hoc-profile-load python-pedro` and adopt its identity/boundaries before reading further.

## Objective

Route the WP07-census's unwrapped/divergent `load_meta` callers in **batch B** — the **merge + dashboard + cli +
doc_analysis + tracker + acceptance** subsystems — through `load_meta_fail_closed` (FR-007), and land the
**NFR-003 full-census contract test** that pins the typed-exception / `None` outcome across the **entire**
enumerated caller set (not a sample). Depends on **WP07**. File-disjoint from WP08's batch A by subsystem
(research.md **D2**); the dashboard `load_meta` callers here are `diagnostics.py`/`scanner.py` — **not**
`dashboard/charter_path.py` (WP02) — and the cli callers exclude `cli/commands/charter/_status_collectors.py`
(WP04).

Read first (authoritative — do not re-derive): `../spec.md` FR-007 + User Story 3 + NFR-003 + Edge Cases;
`../research.md` **D4/D5**. **The WP07 `notes/meta-load-census.md` is authoritative for exact per-file
inclusion**; leave deliberately-silent callers (`load_meta_or_empty`, `on_malformed="none"`) untouched.

## Guidance

#### T001 — ATDD red (charter C-011)

Author `tests/specify_cli/test_meta_fail_closed_full_census_contract.py`: iterate the **full** FR-007 census set
and assert every raise-default reader yields a typed `MissionMetaReadError` / `None` on a corrupt and a non-dict
(`list`) `meta.json` — **zero** raw `ValueError` across the enumerated set (NFR-003, "not a sample"). It fails
today (batch-B callers still leak).

#### T002 — Route the batch-B callers per the census

Replace unwrapped/divergent `load_meta` calls in the owned batch-B files with `load_meta_fail_closed`, honouring
the census def-disambiguation and preserving silent-contract callers. Keep each subsystem's routing a reviewable
hunk.

#### T003 — Full-census contract (NFR-003)

Make T001 exhaustive over the census enumeration (import the caller list / drive each product reader) so the
contract regresses if any future site is added unwrapped. This is the anti-scope-creep guard for the whole IC-03.

## Branch Strategy

Planning branch: `feat/doctrine-charter-split-unification`. Final merge target: same. The execution worktree is
allocated per computed lane from `lanes.json`.

## Definition of Done

- ATDD red (T001) GREEN: the **full** census set fails closed (typed / `None`); zero raw `ValueError` (NFR-003).
- Every routed site matches the WP07 census; silent-contract callers preserved; dashboard/cli exclusions
  (charter_path.py / _status_collectors.py) respected — no collision with WP02/WP04.
- **NFR-003 full-census contract is driven from the reconciled WP07 census** (the grep-count-reconciled row set,
  not a hand-picked sample) and MUST **FAIL if a new unclassified `load_meta(` site appears** — a future
  unwrapped reader added outside the census regresses the gate (research.md D10).
- **Finalize the #3140 issue-matrix verdict + close DIR-012 here** — WP09 is where fail-closed completes, so the
  #3140 verdict is filled in the coord worktree (C-005) and DIR-012 closed at WP09, **not** at WP07 (which held
  the row `(pending)`); tracker comment names this mission.
- `ruff` + `mypy` zero new issues (NFR-002).

## Risks / Reviewer guidance

- The NFR-003 contract must iterate the **whole** census, not a sample — a sample lets a leaking site survive.
  Verify batch-B does not touch `dashboard/charter_path.py` (WP02) or `_status_collectors.py` (WP04).
