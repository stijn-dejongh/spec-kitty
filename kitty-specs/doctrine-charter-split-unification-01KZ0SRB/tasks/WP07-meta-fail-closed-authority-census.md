---
work_package_id: WP07
title: One public load_meta_fail_closed authority + caller census + red tests green
dependencies: []
requirement_refs:
- FR-007
- NFR-003
- NFR-002
- C-004
planning_base_branch: feat/doctrine-charter-split-unification
merge_target_branch: feat/doctrine-charter-split-unification
branch_strategy: Planning artifacts for this mission were generated on feat/doctrine-charter-split-unification. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/doctrine-charter-split-unification unless the human explicitly redirects the landing branch.
subtasks:
- T001
- T002
- T003
- T004
history:
- Generated by /spec-kitty.tasks 2026-08-02
agent_profile: python-pedro
authoritative_surface: src/specify_cli/core/paths.py
create_intent:
- tests/specify_cli/core/test_load_meta_fail_closed_authority.py
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- src/specify_cli/core/paths.py
- src/specify_cli/mission_metadata.py
- src/mission_runtime/lifecycle_phase.py
- tests/specify_cli/core/test_load_meta_fail_closed_authority.py
- tests/unit/status/test_mission_status_aggregate.py
role: implementer
tags: []
tracker_refs:
- '3140'
---

## ⚡ Do This First: Load Agent Profile

Load your assigned profile: run `/ad-hoc-profile-load python-pedro` and adopt its identity/boundaries before reading further.

## Objective

Fix **#3140** foundation: establish **one** public fail-closed `meta.json` reader with a typed contract by
**reusing the existing `core/paths` authority** (no second home, FR-007), emit the reviewable **caller census**
artifact, and route the `mission_runtime`/`runtime` callers so the two red
`test_mission_status_aggregate::TestLoadCoordUnavailableFailsClosed` tests go green (C-004 — these reds are
ours). WP08/WP09 (depend on this WP) route the remaining census callers.

Read first (authoritative — do not re-derive): `../spec.md` FR-007 + User Story 3 + NFR-003 + C-004 + Edge Cases
("silent callers keep `load_meta_or_empty`"); `../research.md` **D4** (reuse `core/paths`, keep import
function-local; the TWO `load_meta` defs hazard) and **D5** (census → publish reader → parallel routing lanes).
Ground in the live anchors: `MissionMetaReadError` at `src/specify_cli/core/paths.py:506`; the private
`_load_meta_fail_closed` at `paths.py:638` (which lazily imports `load_meta` **inside** the function at `:655`);
the two distinct defs — `load_meta(feature_dir)` at `mission_metadata.py:275` **vs**
`load_meta(meta_path)` at `task_utils/support.py:599`; the leak path `mission_runtime/lifecycle_phase.py:120`.

## Guidance

#### T001 — ATDD red (charter C-011)

The two `TestLoadCoordUnavailableFailsClosed` cases in `tests/unit/status/test_mission_status_aggregate.py` are
already red on base (C-004 owns them) — this WP takes them GREEN. Also author
`tests/specify_cli/core/test_load_meta_fail_closed_authority.py` pinning: corrupt + non-dict `meta.json` → typed
`MissionMetaReadError` or `None`, **never** a raw `ValueError`, through the one public reader.

#### T002 — Publish the one public reader (FR-007, D4)

Promote `_load_meta_fail_closed` (`paths.py:638`) to a public `load_meta_fail_closed` in `core/paths` (or have
`mission_metadata` delegate to it) — a single home, **no** competing second authority. **Guard:** the `load_meta`
import MUST stay **function-local** (`:655`) or the `core/paths ↔ mission_metadata` cycle re-forms (D4).

#### T003 — Emit the caller census artifact (D4, gating)

Write `notes/meta-load-census.md` enumerating all ~108/110 `load_meta(` sites across ~55 files, each classified
**raise-default-unwrapped / divergent-wrapper / deliberately-silent**, and **disambiguating which of the two
defs each site targets** (`mission_metadata.py:275` feature_dir vs `task_utils/support.py:599` meta_path — a
census acceptance criterion, or the `task_utils` callers mis-wire). This is a reviewable deliverable gating
WP08/WP09.

#### T004 — Route the mission_runtime/runtime callers → red tests green

Route `mission_runtime/lifecycle_phase.py:120` (the leak path to the aggregate) through the public reader so the
two `TestLoadCoordUnavailableFailsClosed` reds go green. Preserve deliberately-silent callers
(`load_meta_or_empty`, `on_malformed="none"`) — do not route them.

## Branch Strategy

Planning branch: `feat/doctrine-charter-split-unification`. Final merge target: same. The execution worktree is
allocated per computed lane from `lanes.json`.

## Definition of Done

- The two `TestLoadCoordUnavailableFailsClosed` reds go GREEN (C-004); the authority test (T001) passes.
- One public `load_meta_fail_closed` in `core/paths`; import stays function-local (D4); no second authority.
- `notes/meta-load-census.md` published with the full classified + def-disambiguated caller set (gates WP08/WP09).
- **Grep-count census reconciliation (anti-omission guard, research.md D10):** every `load_meta(` occurrence in
  the tree (raw grep count) is a row in `notes/meta-load-census.md` — **row-count == occurrence-count**, no
  undercount. Each row is classified `{route-unwrapped | divergent-wrapper | deliberately-silent}` **and** tagged
  with which of the TWO defs it targets (`mission_metadata.py:275` feature_dir vs `task_utils/support.py:599`
  meta_path). This defeats the occurrence-map undercount trap — the NFR-003 full-census is self-referential, so a
  silently-omitted site would leak yet pass.
- `ruff` + `mypy` zero new issues (NFR-002).
- Assign #3140 to the HiC on claim (DIR-012). The #3140 **issue-matrix row stays `(pending)` here** — fail-closed
  completes on WP09, so the verdict **finalizes on WP09**, not WP07 (research.md D10); seed/hold the row in the
  coord worktree (C-005) with a tracker comment naming this mission.

## Risks / Reviewer guidance

- The function-local-import guard is subtle: a top-level `load_meta` import re-forms the cycle. Verify it stays
  in-function. Verify the census disambiguates the TWO defs — a mis-classified `task_utils` site mis-wires WP08/09.
- Do NOT fix only the two reds and leave ~20 leaking — the census + full-set contract (NFR-003, lands WP09) is
  the anti-scope-creep guard.
