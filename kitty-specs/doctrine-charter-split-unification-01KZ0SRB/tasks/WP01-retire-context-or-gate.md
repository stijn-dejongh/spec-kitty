---
work_package_id: WP01
title: Retire charter/context.py OR-charter.md presence gate → charter.yaml-only
dependencies: []
requirement_refs:
- FR-001
- FR-002
- NFR-001
- NFR-002
planning_base_branch: feat/doctrine-charter-split-unification
merge_target_branch: feat/doctrine-charter-split-unification
branch_strategy: Planning artifacts for this mission were generated on feat/doctrine-charter-split-unification. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/doctrine-charter-split-unification unless the human explicitly redirects the landing branch.
subtasks:
- T001
- T002
- T003
history:
- Generated by /spec-kitty.tasks 2026-08-02
agent_profile: python-pedro
authoritative_surface: src/charter/context.py
create_intent:
- tests/charter/test_context_presence_charter_yaml_only.py
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- src/charter/context.py
- tests/charter/test_context_presence_charter_yaml_only.py
role: implementer
tags: []
tracker_refs: []
---

## ⚡ Do This First: Load Agent Profile

Load your assigned profile: run `/ad-hoc-profile-load python-pedro` and adopt its identity/boundaries before reading further.

## Objective

Retire the `charter/context.py` "missing" presence gate's `OR charter.md` test-compat bridge so charter
presence resolves from the schema-guarded authority `charter.yaml` **only** (FR-002), and confirm the shared
constant home already exists in `charter/bundle.py` (FR-001 — no new symbol; research.md **D1** says
`CHARTER_YAML`/`CHARTER_MD` are already exported and `context.py` already imports them). `charter.md` stays a
**secondary** prose/rationale read — the prose/body readers are **not** touched.

Read first (authoritative — do not re-derive): `../spec.md` FR-001/FR-002 + User Story 1 + Edge Cases + C-001;
`../research.md` **D1**. Ground in the live gate at `src/charter/context.py:249`
(`if not charter_yaml_path.exists() and not charter_path.exists():` — the OR-bridge) and the prose/body readers
at `:300-302` and `:366-371` which **must remain unchanged**.

## Guidance

#### T001 — ATDD red (charter C-011)

Author `tests/charter/test_context_presence_charter_yaml_only.py` that seeds `charter.yaml` and **deletes**
`charter.md` (NFR-001: the presence fixture omits `charter.md`, never seeds both), then asserts the
`context.py` "missing" gate reports the charter **present**. It fails today because the current `:249` gate only
passes courtesy of the `OR charter.md` bridge masking the yaml-only path — commit it red first.

#### T002 — Retire the OR-bridge at :249

Change the gate so presence keys on `charter_yaml_path.exists()` alone (drop the `and not charter_path.exists()`
conjunct). Migrate any `charter.md`-only fixtures the bridge protected so they seed `charter.yaml`. Do **NOT**
touch the C-003 prose/body readers at `:300-302` / `:366-371` (C-001 — they legitimately read `charter.md`).

#### T003 — Both-files no-regression case

Add a second case: with **both** files present, presence behaviour is unchanged and `charter.md` remains
readable for prose. This is the no-regression pin, kept separate from the presence proof (NFR-001).

## Branch Strategy

Planning branch: `feat/doctrine-charter-split-unification`. Final merge target: same. The execution worktree is
allocated per computed lane from `lanes.json`.

## Definition of Done

- ATDD red (T001) goes GREEN: presence resolves on `charter.yaml` with `charter.md` deleted.
- The `:249` OR-bridge is gone; prose readers at `:300-302`/`:366-371` untouched (C-001); FR-001 constant home
  confirmed (no new symbol).
- Both-files no-regression case passes. `ruff` + `mypy` zero new issues (NFR-002).
- NFR-005 OR-gate-retirement note recorded in the PR body.

## Risks / Reviewer guidance

- Fake-green trap: verify the fixture DELETES `charter.md` (NFR-001), not seeds both. Confirm the prose/body
  readers were not collapsed into the presence gate (C-001).
