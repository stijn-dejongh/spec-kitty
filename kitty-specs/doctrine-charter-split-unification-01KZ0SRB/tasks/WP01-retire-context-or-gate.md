---
work_package_id: WP01
title: Pin charter/context.py prose-presence gate (charter.yaml precedence, charter.md readable secondary)
dependencies: []
requirement_refs:
- FR-001
- FR-002
- NFR-002
planning_base_branch: feat/doctrine-charter-split-unification
merge_target_branch: feat/doctrine-charter-split-unification
branch_strategy: Planning artifacts for this mission were generated on feat/doctrine-charter-split-unification. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/doctrine-charter-split-unification unless the human explicitly redirects the landing branch.
subtasks:
- T001
- T002
- T003
history:
- Generated by /spec-kitty.tasks 2026-08-02
- Post-tasks squad correction 2026-08-02 — reframed from "retire OR-gate" to "scope+pin" (the OR-gate is a legitimate C-003 prose-presence gate; retiring it would demote charter.md to invisible and regress 26 fixtures)
agent_profile: python-pedro
authoritative_surface: src/charter/context.py
create_intent:
- tests/charter/test_context_prose_presence_pin.py
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- src/charter/context.py
- tests/charter/test_context_prose_presence_pin.py
role: implementer
tags: []
tracker_refs: []
---

## ⚡ Do This First: Load Agent Profile

Load your assigned profile: run `/ad-hoc-profile-load python-pedro` and adopt its identity/boundaries before reading further.

## Objective

**Scope + PIN (do NOT retire) the `charter/context.py:249` prose-presence gate** as a legitimate C-003 prose
surface (FR-002, as corrected by the post-tasks squad). The gate — `if not charter_yaml_path.exists() and not
charter_path.exists()` (present if `charter.yaml` OR `charter.md` exists) — is **already correct**: post-#3146
it renders compiled `charter.yaml` governance when present (authority, precedence), still renders `charter.md`
prose when that is the only surface (charter.md as the operator's readable secondary), graceful-degrades prose
when `charter.md` is absent (`:300`), and is "missing" only when **both** are absent. The inline comment
(`:207-220`) already documents that a strict `charter.yaml`-only gate regresses 26 `charter.md`-only fixtures.

**Do NOT change the gate's boolean.** This WP adds durability: honest characterization tests pinning the four
cells + precedence, and a one-line comment clarification naming the C-003 prose-presence classification and the
charter.yaml-precedence / charter.md-secondary model. Confirm FR-001's constant home already exists
(`charter/bundle.py` `CHARTER_YAML`/`CHARTER_MD` — research.md **D1**; no new symbol).

Read first (authoritative): `../spec.md` FR-002 + User Story 1 (scenarios 3 & 4) + C-001; `../research.md` **D1**;
the live gate + comment at `src/charter/context.py:207-262` and the prose readers at `:300-302`/`:366-371`.

## Guidance

> **Honesty note (post-tasks squad):** there is **no red-first** here — this WP does not change behaviour, so
> its tests are **characterization pins** (green-first), not a fabricated ATDD red. That is the correct,
> non-fakeable framing; do not invent a red. The value is durability: pin the C-003 behaviour so a later
> "tidy" of the OR into a strict gate is caught.

#### T001 — Characterization pins for the four presence cells

Author `tests/charter/test_context_prose_presence_pin.py` pinning, at the `charter context` render level:
(a) `charter.yaml` present, `charter.md` **deleted** → renders (compiled governance; no crash — SC-002);
(b) `charter.md`-only (`charter.yaml` **absent**) → renders the `charter.md` prose (readable secondary);
(c) **both** absent → mode `missing`;
(d) **both** present → the compiled `charter.yaml` governance is delivered (precedence) **and** `charter.md`
prose is available — deleting `charter.md` from this state drops only prose, never governance.

#### T002 — Precedence assertion (the one genuine behaviour pin)

Make (d) load-bearing: assert the delivered governance/references come from the compiled `charter.yaml` bundle
(not parsed out of `charter.md`), so charter.yaml **takes precedence**. If — and only if — the current render
path does not already honour this, fix it to yaml-first and record it as a real red→green in the PR body;
otherwise this is a characterization pin. Do **not** touch the C-003 prose readers at `:300-302`/`:366-371`.

#### T003 — Clarify the inline rationale

Extend the `:207-220` comment to state explicitly: this is the **C-003 prose-presence gate** (present if the
`charter.yaml` authority **or** the `charter.md` readable secondary exists); it is intentionally **not** an
authority-presence gate (those are FR-003/004/006 on dashboard/analysis/status) and must **not** be tidied into
a strict `charter.yaml`-only gate (regresses md-only rendering; contradicts the charter.md-secondary model).

## Branch Strategy

Planning branch: `feat/doctrine-charter-split-unification`. Final merge target: same. Execution worktree per
computed lane from `lanes.json`.

## Definition of Done

- The four-cell characterization pins (T001) + the precedence assertion (T002) pass; the gate boolean is
  **unchanged** (or, if precedence needed a real fix, that red→green is recorded in the PR body).
- The `:207-220` comment names the C-003 prose-presence classification + charter.yaml-precedence model.
- FR-001 constant home confirmed (no new symbol). `ruff` + `mypy` zero new issues (NFR-002). No `charter.md`
  fixture migration (no behaviour change → no 26-fixture regression).

## Risks / Reviewer guidance

- **Anti-fake-green (inverted here):** reject any attempt to reintroduce a "retire the OR / migrate 26 fixtures"
  behaviour change — that was the corrected misreading. The gate stays an OR.
- Verify (b) truly seeds `charter.md`-only and asserts prose renders (the operator's readable-secondary model);
  verify (d) proves governance comes from `charter.yaml`, not `charter.md`.
- Confirm the prose readers at `:300-302`/`:366-371` are untouched (C-001).
