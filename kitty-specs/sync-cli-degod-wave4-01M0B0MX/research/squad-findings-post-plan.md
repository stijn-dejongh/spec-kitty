# Post-plan adversarial squad — convergent findings

Point-cut: post-plan. Three profile-loaded, read-only lenses (architect-alphonso, python-pedro,
planner-priti). All verdicts: **sound to task against, conditionally** — the decomposition is
FR-complete, acyclic, and proven by the runtime_bridge #2531 precedent; these are translation-time
guards for `/spec-kitty.tasks`, folded into `plan.md` (§ "WP-translation guards"), the contract, and
data-model. No re-planning required.

| # | Sev | Lens | Finding | Disposition |
|---|-----|------|---------|-------------|
| Pd-1 | HIGH | pedro | The SaaS-enable env var is **misnamed** — the gate reads only `SPEC_KITTY_ENABLE_SAAS_SYNC` (`saas_sync_config.py:20/37`); the docs said `SPEC_KITTY_SAAS_SYNC` (nothing reads it) → golden would freeze the *skip* arm. | **FIXED** — corrected across spec/plan/quickstart/contract/brief. |
| Pr-1 | HIGH | priti | Adapter extractions **cannot run as parallel lanes** — every WP shrinks the one `sync.py`, so `owned_files` collide. | **FOLDED** — serial pipeline on `sync.py` (purge is the one genuinely-disjoint subsystem, pedro-confirmed); WP-translation guard §. |
| Pr-2 / A-4 | HIGH | priti + architect | Husk re-export must **exist before the first private relocates**, not "finalize last"; and `@app.command` decorators bind at decoration time (uncovered by runtime_bridge). | **FOLDED** — husk-scaffold is an early prerequisite edge; `@app.command` thin shells **stay in `cli/commands/sync.py`** (seam anchor); `cmd_*`/cores hold the extracted logic; late-bound `sync_module.<name>` calling convention + an AST guard against early-binding a patched name. |
| A-1 | HIGH | architect | `status` (cc 90) / `doctor` (cc 73) pure cores **do not pre-exist** — I/O is interleaved *between* row emissions; extraction is a **gather-I/O → core → render restructure**, not a relocate. | **FOLDED** — IC-04/IC-05 scoped as restructure; FR-001 full-human-render freeze is the load-bearing net (keep C-003 freeze-before-extract hard). |
| A-2 | HIGH | architect | The read/write authority taxonomy **omits a third authority** — receiver/delivery-target **admission** (`_assert_event_sync_runtime_authority` L620, `_assert_delivery_target_matches_context` L653, `_resolve_gated_receiver` L799); and `_open_project_dispatch_runtime`/`opt_out` are irreducibly read+write *at the flow level*. | **FOLDED** — data-model names three authorities; the invariant is non-unification of **ports/classes, not call flows**; mixed-authority flows are frozen verbatim (C-007), not "purified". |
| A-3 | MED | architect | A new `AuthorityGate` risks a DIRECTIVE_044 **fork** — the package already has ≥5 `*Authority*` surfaces (`target_authority`, `consent`, `project_context`, `layout_generation`, `transport_attempts`); the two-authority arch-test must scope *which* symbols or be brittle/vacuous. | **FOLDED** — `sync_authority.py` adapters **delegate** to `preflight`/`sharing_client`/`target_authority`; the arch-test discriminator is an explicit symbol/module allowlist (distinct from `test_2093_authority_invariant.py`). |
| Pd-2 | MED | pedro + priti | The three shared render helpers (`_render_per_project_store` used by **both** status & doctor, `_render_consent_readability`, `_render_tracker_egress`) each **print AND mutate `issues`** — splitting for I/O-free cores couples status↔doctor. | **FOLDED** — their compute-half lands in the shared `sync_store_report_core` **before** both degods; the shells call the render-half. |
| Pd-3 | MED | pedro | Contract item 3 **mislabels `doctor`'s `--json` branches** — `sync doctor()` takes no args, has no `--json`; those shapes are `diagnose`'s. | **FIXED** — contract item 3 rewritten to doctor's real surface (Rich table; issues vs "No issues detected"; exit-4 recovery arm). |
| Pd-4 | MED | pedro | `sync_workspace`'s substantive arms are non-deterministic (live `git rebase`) — the golden must **stub `get_vcs`/`_detect_workspace_context`** (a monkeypatch-golden, not a black-box snapshot). | **FOLDED** — contract + WP note. |
| A-5 | MED | architect | Grant-writer relocation self-trips `test_sync_writer_census.py` (key = `relpath::qualname::…` → relocation reads as growth). | **FOLDED** — each relocated writer requires a **1:1 census-key swap** (drop old, add equivalent), verified per-writer — not a blanket re-pin. |
| Pr-3 | MED | priti | IC-05 (3 monsters + shell-split) and IC-03 (6 adapters) as literal single WPs blow the size ceiling. | **FOLDED** — WP-translation guard: IC-05 → ≥4 WPs (status/doctor/sync_workspace/shell-split); IC-03 → ~5 sequential WPs. |
| Pr-4 | MED | priti | `sync.py` Tier-1 runs before the golden net exists; gap-freeze snapshots are gated to the extraction WPs, not WP01. | **FOLDED** — only `walker.py` is genuinely pre-golden; sync Tier-1 lands after WP01; gap-freeze subtasks attach to their extraction WPs. |
| Pr-5 | LOW | priti + pedro | FR-007 set-unchanged guard must be an **executable test**, not prose. | **FOLDED** — FR-007/IC-06 requires a regression guard, not just `env-census.md`. |
| A-6 | LOW | architect | SC-001 ≤800 LOC/module vs the husk residual — the `sync.py` husk (app + 22 shells + re-export) plausibly exceeds 800. | **FOLDED** — the husk is measured against a separate, larger ceiling than the extracted `sync_*` modules. |
| Pd-5 | LOW | pedro | `SPEC_KITTY_SYNC_DISABLE`↔`ENABLE_SAAS_SYNC` are **orthogonal** (render ON, network/gate OFF is coherent — the hermetic-golden combo). | **NO CHANGE** — hypothesized conflict does not exist; keep both with the corrected var name. |

Coverage complete, no scope creep (all three confirmed). No contested finding dropped.
