---
work_package_id: WP02
title: apply --compile bridge + truthful output + fourth-producer convergence
dependencies: []
requirement_refs:
- FR-003
- FR-004
- FR-008
planning_base_branch: feat/charter-pack-usage-journey
merge_target_branch: feat/charter-pack-usage-journey
branch_strategy: Planning artifacts for this mission were generated on feat/charter-pack-usage-journey. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/charter-pack-usage-journey unless the human explicitly redirects the landing branch.
subtasks:
- T010
- T011
- T012
- T013
- T014
history:
- Generated by /spec-kitty.tasks 2026-08-02
agent_profile: python-pedro
authoritative_surface: src/specify_cli/cli/commands/charter/pack.py
create_intent:
- tests/specify_cli/cli/commands/charter/test_apply_compile_bridge.py
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- src/specify_cli/cli/commands/charter/pack.py
- tests/specify_cli/cli/commands/charter/test_apply_compile_bridge.py
role: implementer
tags: []
tracker_refs:
- '3105'
- '3107'
---

## ⚡ Do This First: Load Agent Profile

Load your assigned profile: run `/ad-hoc-profile-load python-pedro` and adopt its identity/boundaries before reading further.

## Objective

Give the operator a signalled path from "activated" to "governance delivered": add an **opt-in
`charter pack apply --compile`** that chains the **existing** compile seam after the config merge, and make
the **default `apply` output truthful** (name the exact next command instead of hand-waving). Also assert the
config→bundle transform stays **one authority** (FR-008, folded from IC-05). **Build no new compiler** — the
lowering already exists (`compile_charter`/`write_compiled_charter`, exposed as `charter generate`).

Read first: `../spec.md` FR-003/FR-004/FR-008 + Edge Cases (git-worktree, fourth producer); `../plan.md`
IC-02 + IC-05; `../notes/research-synthesis.md` §"Bridge decision — OPTION (a)" and §"A FOURTH config→bundle
producer". Ground in `src/specify_cli/cli/commands/charter/pack.py` (`apply_cmd`, the `:200-205` "a compile
may still be needed" output, the `list_cmd:78`/`path_cmd:112`/`apply_cmd:146` resolve-or-exit blocks),
`charter/generate.py` (`--no-from-interview` path, `_is_inside_git_worktree:77`), and
`charter/compiler.py` (`compile_charter:326`, `write_compiled_charter:441`).

## Guidance

### T010 — Truthful default `apply` output

Replace the vague "a compile may still be needed" (`pack.py:200-205`) with an explicit line naming
`spec-kitty charter generate` as the exact next command required to deliver governance. Default `apply`
stays a **pure additive merge, git-agnostic** (C-004) — do NOT auto-compile.

### T011 — `apply --compile` chains the existing seam

Add a `--compile` flag that, after the config merge succeeds, invokes `charter generate --no-from-interview`
(or calls `compile_charter`+`write_compiled_charter` directly through the same seam). It **inherits
`generate`'s git-worktree requirement** (`generate.py:_is_inside_git_worktree`) — surface that requirement in
`--compile`'s help text and error path. Introduce zero compiler code.

### T012 — Campsite: hoist the resolve-or-exit duplication

Hoist the **2 identical** `path_cmd` (`:112`) / `apply_cmd` (`:146`) `resolve_builtin_pack_path` resolve-or-exit
try-blocks (both catch `(UnknownPackError, FileNotFoundError)`) into one helper. **Leave `list_cmd` (`:78`)
alone** — it is a narrower list-comprehension variant catching `FileNotFoundError` only; do NOT force it into
the shared helper (would over-catch). It is **2×, not 4×** (the synthesis said 4 — the squad corrected it).

### T013 — Journey 7 truthful-output test

Assert: default `apply` output contains `spec-kitty charter generate`; `apply --compile` reports it compiled
and leaves a `charter.yaml` present.

### T014 — IC-05 convergence assertion (FR-008)

Assert `apply --compile` and the upgrade finalize migration's full-document producer
(`m_unify_charter_activation_finalize._compose_charter_yaml_document`/`_write_new_charter_yaml`, `:240-281`)
produce a **convergent** `charter.yaml`. **They will NOT be byte-identical** (the migration embeds raw
`activated_*` + a store/pointer model; `write_compiled_charter` is a pure derived read-cache) — assert on the
**catalog shape derived from the same activation input** (the "document-as-equivalent" branch), not
field-identity, or you codify a latent divergence. No production edit to the migration. This is a test-only
subtask; it can share this WP rather than a standalone tiny WP.

## Branch Strategy

Planning + merge target: `feat/charter-pack-usage-journey`. Worktree per `lanes.json` lane.

## Definition of Done

- `apply --compile` compiles the bundle in one step (reusing the existing seam, no new compiler); default
  `apply` output names `spec-kitty charter generate`. Default apply stays git-agnostic (C-004).
- 2 resolve-or-exit blocks hoisted; `list_cmd` untouched.
- Journey 7 + convergence test pass. `ruff` + `mypy` zero new issues.

## Risks / Reviewer guidance (reviewer-renata)

- Confirm no compiler logic was reimplemented (only the existing seam is invoked). Confirm default `apply` did
  NOT gain a git-worktree requirement. Verify the convergence test asserts shape-from-same-input, not bytes.
