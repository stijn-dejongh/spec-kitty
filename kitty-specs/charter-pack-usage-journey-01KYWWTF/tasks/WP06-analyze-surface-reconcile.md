---
work_package_id: WP06
title: spec-kitty analyze surface reconciliation
dependencies: []
requirement_refs:
- FR-011
planning_base_branch: feat/charter-pack-usage-journey
merge_target_branch: feat/charter-pack-usage-journey
branch_strategy: Planning artifacts for this mission were generated on feat/charter-pack-usage-journey. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/charter-pack-usage-journey unless the human explicitly redirects the landing branch.
subtasks:
- T050
- T051
history:
- Generated by /spec-kitty.tasks 2026-08-02
agent_profile: python-pedro
authoritative_surface: .agents/skills/spec-kitty.analyze
create_intent:
- tests/specify_cli/cli/commands/test_analyze_surface_agreement.py
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- .agents/skills/spec-kitty.analyze/SKILL.md
- src/doctrine/missions/mission-steps/software-dev/analyze/prompt.md
- tests/specify_cli/cli/commands/test_analyze_surface_agreement.py
role: implementer
tags: []
tracker_refs:
- '3096'
---

## ⚡ Do This First: Load Agent Profile

Load your assigned profile: run `/ad-hoc-profile-load python-pedro` and adopt its identity/boundaries before reading further.

## Objective

Make the documented `spec-kitty analyze` surface and the actual CLI agree. Today the docs/skill reference
`spec-kitty analyze`, but no such subcommand exists — only `spec-kitty agent mission record-analysis`.
**Decision (research.md Decision 2): redirect the documented surface to the supported command** rather than
mint a new top-level alias (a forwarding verb creates a parity surface that drifts). Closes **#3096**.

**Scope guard:** this is surface-*reconciliation* only. Do **NOT** absorb the `analyze`-*expansion* issues
#849/#851/#853 (readiness-review / product-coherence / full-corpus) — those are separate scope.

Read first: `../spec.md` FR-011/SC-007; `../plan.md` IC-07; `../research.md` Decision 2. **First, verify the
real surfaces** (the `owned_files` list below is a best-effort guess from planning — confirm and correct the
exact paths before editing):

```bash
grep -rln "spec-kitty analyze\|spec_kitty.analyze\|/skill:spec-kitty.analyze" .agents/ src/doctrine/ docs/ .kittify/ 2>/dev/null
spec-kitty agent mission record-analysis --help    # confirm the canonical surface
```

If any owned-file path is wrong, adjust the WP's `owned_files` (record a one-line rationale) — do not edit
files outside the corrected set.

## Guidance

### T050 — Redirect the documented surface to the canonical command

Update the `spec-kitty.analyze` skill (`SKILL.md`), the command-skills manifest
(`.kittify/command-skills-manifest.json` if the skill is registered there), the mission-step `analyze`
prompt, and any docs so they direct users **exclusively** to `spec-kitty agent mission record-analysis` (with
its staleness-gate + artifact contracts). Remove/replace every documented-but-absent `spec-kitty analyze`
invocation. Keep the redirect faithful to the canonical flow — missing-CLI-command-is-a-gap → trace the source,
do not reimplement analysis logic.

**Fallback (only if a caller hard-codes `spec-kitty analyze`):** expose a thin `analyze` alias that routes
through the canonical `record-analysis` flow — no reimplementation. Prefer the redirect.

### T051 — Surface-agreement verification + scope guard

Add a test/verification that every advertised `analyze` invocation in the touched docs/skill resolves to a
real CLI surface (parse the doc references, assert the CLI accepts them — or assert the redirect text names
only `agent mission record-analysis`). Explicitly do not add readiness-review/product-coherence scope
(#849/#851/#853). If you touched `src/doctrine/` prose, run
`pytest tests/architectural/test_no_legacy_terminology.py`.

## Branch Strategy

Planning + merge target: `feat/charter-pack-usage-journey`. Worktree per `lanes.json` lane.

## Definition of Done

- No documented-but-absent `spec-kitty analyze` reference remains; all point to `agent mission
  record-analysis` (or a thin faithful alias). Surface-agreement test passes. `ruff`+`mypy` clean if code
  changed. Scope did not expand into #849/#851/#853.

## Risks / Reviewer guidance (reviewer-renata)

- Confirm no analysis logic was reimplemented. Confirm all 19-agent skill surfaces (if the skill is shared)
  are updated consistently, not just `.claude`. Confirm the expansion issues were NOT folded.
