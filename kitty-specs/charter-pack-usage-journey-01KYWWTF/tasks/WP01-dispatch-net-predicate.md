---
work_package_id: WP01
title: Dispatch-net predicate — bundle-presence + org-pack-safe routability
dependencies: []
requirement_refs:
- FR-001
- FR-002
- NFR-001
- NFR-004
planning_base_branch: feat/charter-pack-usage-journey
merge_target_branch: feat/charter-pack-usage-journey
branch_strategy: Planning artifacts for this mission were generated on feat/charter-pack-usage-journey. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/charter-pack-usage-journey unless the human explicitly redirects the landing branch.
subtasks:
- T001
- T002
- T003
- T004
- T005
history:
- Generated by /spec-kitty.tasks 2026-08-02
agent_profile: python-pedro
authoritative_surface: src/specify_cli/invocation/empty_charter.py
create_intent:
- tests/specify_cli/invocation/test_is_charter_empty_bundle_predicate.py
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- src/specify_cli/invocation/empty_charter.py
- tests/specify_cli/invocation/test_is_charter_empty_bundle_predicate.py
role: implementer
tags: []
tracker_refs:
- '3104'
- '3118'
---

## ⚡ Do This First: Load Agent Profile

Load your assigned profile: run `/ad-hoc-profile-load python-pedro` and adopt its identity/boundaries before reading further.

## Objective

Rewrite the dispatch-net predicate `is_charter_empty` so that **applying a pack without compiling keeps the
generic-agent safety net engaged**, while a compiled bundle or a routable org/profile config disengages it.
This is the **MVP** — it fixes the active P1 regression (#3104: today `charter pack apply minimal` makes an
unmatched dispatch hard-fail with `ROUTER_NO_MATCH`, i.e. *worse* than an empty project). Ship it first, alone.

**The file is `src/specify_cli/invocation/empty_charter.py` — NOT `src/charter/`.** (The plan's first draft
mis-placed it; the post-plan squad corrected it. Confirm with `ls`.)

Read first (authoritative — do not re-derive): `../spec.md` FR-001/FR-002/NFR-001/NFR-004 + Edge Cases;
`../plan.md` IC-01; `../notes/research-synthesis.md` §"The dispatch predicate (#3104) — corrected,
org-pack-safe" (the exact predicate) and §"POST-PLAN SQUAD REFRESH". Ground in the current
`empty_charter.py` (`is_charter_empty:48-67`, `_MATCH_REASON:42`, imports `:33`) and
`src/charter/pack_context.py` (`PackContext.from_config`, `org_roots`, `activated_agent_profiles`,
`activated_directives:144`).

## Guidance

### T001 — Rewrite `is_charter_empty` to bundle-presence + routability split

Replace the current URN/dimension-set predicate with the org-pack-safe form (synthesis-validated):

```python
def is_charter_empty(repo_root: Path) -> bool:
    if (repo_root / ".kittify/charter/charter.yaml").exists():
        return False                                  # compiled bundle → configured
    pc = PackContext.from_config(repo_root)
    if pc.org_roots != ():                     return False   # org packs → router-routable
    if pc.activated_agent_profiles is not None: return False  # explicit profiles → routable
    return True
```

Behaviour matrix to honour: empty→True (net fires); apply-no-compile→True (**#3104 fix**); apply+compile→False
(router runs, NO_MATCH honest); org-pack no-compile→False (no regression); agent-profile activation→False.
Reuse the `charter.bundle.CHARTER_YAML` constant if it is importable without pulling `src/charter`→
`specify_cli` the wrong way; otherwise a local `Path(".kittify/charter/charter.yaml")` is acceptable here
(this module already lives under `specify_cli`). **Do NOT inspect bundle contents** — presence only ("empty"
means bundle ABSENT, never "present but activations empty"), or you re-import the #3064 exhaustiveness trap.

### T002 — Update `_MATCH_REASON`; campsite the dead code

Rewrite `_MATCH_REASON` (`:42`) so the warning panel states the *bundle-presence* rationale (not the retired
dimension enumeration). Delete the dead ~20-line dimension-enumeration docstring and the now-dead
`charter_activated_urns` import + its `:56` call. **KEEP `PackContext`** — the rewritten predicate still uses
`PackContext.from_config`/`.org_roots`/`.activated_agent_profiles`. (ruff F401/F811 will catch an over-eager
deletion, but get it right by hand.)

### T003 — Journey tests 1-3 (dispatch net)

Author executable tests: (1) empty project → unmatched dispatch falls back to generic agent; (2) after
`apply minimal` **without** compile → still falls back (NOT `ROUTER_NO_MATCH`) — the #3104 assertion;
(3) after a compiled bundle exists → the net disengages, router runs, `ROUTER_NO_MATCH` is the honest signal.
Drive these through the real dispatch seam (`invocation/executor.py`, `invocation/router.py`,
`resolve_generic_fallback`) with realistic activation fixtures, not by calling `is_charter_empty` in isolation
only.

### T004 — Journey 4 org-pack safety + SC-005 pins

(4a) org-pack project (routable profiles, **no** compiled bundle) → net stays OFF (router reaches org
profiles; no regression). (4b) SC-005: a bootstrapped-empty `charter.yaml` (bundle present, activations empty)
keeps the net OFF — pin it so a future contents-inspecting change can't regress. (4c) `frozenset()` opt-out
pin: an explicit zero-profile activation keeps the net disengaged (matches today's `is not None` semantics) —
so a later reader can't "tidy" `is not None` into truthiness unnoticed.

### T005 — NFR-001 perf spy (advisory)

Assert `is_charter_empty` on an unconfigured repo performs **≤ 1 `PackContext.from_config` + 1 `stat`**, with
**no** `charter_activated_urns` URN load (folds #3118's double config-load). Use a load-counting spy;
advisory, not a hard gate.

## Branch Strategy

Planning branch: `feat/charter-pack-usage-journey`. Final merge target: `feat/charter-pack-usage-journey`.
The execution worktree is allocated per computed lane from `lanes.json`.

## Definition of Done

- `is_charter_empty` matches the behaviour matrix; #3104 test (T003 case 2) goes GREEN.
- Dead docstring + `charter_activated_urns` removed; `PackContext` retained; `_MATCH_REASON` truthful.
- Journeys 1-4 + SC-005 + `frozenset()` pins + NFR-001 spy pass. `ruff` + `mypy` zero new issues.
- NFR-004 note recorded in the PR body (all non-routing dimensions dropped; empty=bundle-absent).

## Risks / Reviewer guidance (reviewer-renata)

- Verify the predicate does NOT fire the net for org-pack/profile projects (regression guard is the point of
  FR-002). Verify no bundle-*contents* inspection crept in. Confirm `PackContext` is still imported/used.
