---
work_package_id: WP05
title: Advertised section selectors resolve (graceful-degrade)
dependencies: []
requirement_refs:
- FR-010
planning_base_branch: feat/charter-pack-usage-journey
merge_target_branch: feat/charter-pack-usage-journey
branch_strategy: Planning artifacts for this mission were generated on feat/charter-pack-usage-journey. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/charter-pack-usage-journey unless the human explicitly redirects the landing branch.
subtasks:
- T040
- T041
- T042
history:
- Generated by /spec-kitty.tasks 2026-08-02
agent_profile: python-pedro
authoritative_surface: src/charter/context_renderers/section_bodies.py
create_intent:
- tests/charter/test_section_selector_graceful_degrade.py
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- src/charter/context_renderers/section_bodies.py
- tests/charter/test_section_selector_graceful_degrade.py
- tests/charter/test_context_section_bodies.py
- tests/charter/test_context_selection_render.py
role: implementer
tags: []
tracker_refs:
- '3095'
- '3094'
- '2552'
---

## ⚡ Do This First: Load Agent Profile

Load your assigned profile: run `/ad-hoc-profile-load python-pedro` and adopt its identity/boundaries before reading further.

## Objective

Make the advertised charter section selectors resolve instead of dead-ending. Generated `implement`/`review`
prompts require `charter context --include section:terminology-canon` and `section:code-review-checklist`;
today, against a freshly-compiled pack (whose seeded `charter.md` lacks those headings) they fail with "No
charter section found for selector". Closes **#3095**, its terminology-canon twin **#3094**, and its
code-review-checklist twin **#2552**.

**Scope discipline:** the fix lives **entirely in `section_bodies.py`** — do **NOT** edit `context.py`
(WP03's exclusive file) and do **NOT** retarget any reader to `charter.yaml` (this stays on the `charter.md`
prose path, C-003).

Read first: `../spec.md` FR-010/C-003/SC-007; `../plan.md` IC-06; `../research.md` Decision 1; the
`section_bodies` re-anchor in `../notes/research-synthesis.md`. Ground in
`src/charter/context_renderers/section_bodies.py` (`render_critical_section_include:282`; the
`if body is None: return None` at `:308-311`; `ACTION_CRITICAL_SECTIONS`/`TERMINOLOGY_CANON`/
`CODE_REVIEW_CHECKLIST` `:34-46`; `_extract_section_body:184`) and — for context only, do not edit —
`context.py:342-354` (the `kind=="section"` branch that raises when the renderer returns `None`).

## Guidance

### T040 — `render_critical_section_include` returns an honest placeholder, never `None`

When `_extract_section_body` finds no matching heading, return a short, honest placeholder string instead of
`None` — e.g. `"_This charter has not yet authored a **Terminology Canon** section. Add one to
`.kittify/charter/charter.md`._"` (parameterise on the requested heading). **Both `return None` sites must be
covered** — `render_critical_section_include` has two: the `if body is None: return None` (heading matched but
extraction empty) **and** the terminal `return None` (no slug match at all). Return the honest placeholder from
**both** paths — the function must never return `None` on any path (do not patch only the terminal return and
leave `body is None` dead-ending at `context.py:354`). Because the renderer never returns `None`, the dead-end
raise at `context.py:354` is **never reached** — so `context.py` needs no edit and stays
WP03's. This is the **#2808-safe** fix (operators still author their own content; we don't fabricate
governance). **Do the primary graceful-degrade only.** Do **not** enrich `generate.py`'s
`_CHARTER_MD_COMPANION_SEED` with stub headings — that secondary path tensions with #2808 and is deferred
unless the operator reconciles it (paula m-7).

### T041 — US4.1 tests

Assert `render_critical_section_include` returns a non-`None` placeholder for both `Terminology Canon` and
`Code Review Checklist` when the section is absent, and returns the real body when present. Add an integration
assertion that `charter context --include section:terminology-canon` (and `:code-review-checklist`) against a
freshly-compiled minimal pack **resolves to a rendered string** (placeholder or body) — never raises "No
charter section found for selector". If you touch any `src/doctrine/` prose at all (you should not need to),
run `pytest tests/architectural/test_no_legacy_terminology.py` before pushing.

### T042 — Reconcile pre-existing tests that pin the RETIRED dead-end behaviour

The graceful-degrade change (`render_critical_section_include` never returns `None` for a recognized critical
slug) is spec-mandated (FR-010/#3095/#3094/#2552: the advertised selector must resolve, not dead-end). Two
**pre-existing** tests pin the OLD dead-end and now fail — WP05's direct fallout, owned by no other WP, so
reconciled HERE (atomic ATDD unit). **Intended-behaviour maintenance, NOT green-washing** — assert the NEW
graceful-degrade behaviour, cite FR-010:

- `tests/charter/test_context_selection_render.py::test_section_selector_fails_closed_on_missing_heading` —
  asserts `pytest.raises(ValueError, match="No charter section found")` when a section is absent. That is
  *exactly* the dead-end FR-010 removes. **Flip** it to assert the selector now RESOLVES to the honest
  placeholder string (no raise). Leave the sibling `test_..."No charter.md found"` case UNTOUCHED (that is the
  charter.md-absent path, a different error fired earlier in context.py, unaffected by WP05).
- `tests/charter/test_context_section_bodies.py::test_section_include_fail_closed_on_unbalanced_fence` — asserts
  `render_critical_section_include(...) is None` for a heading MASKED by a malformed fence. The "never return
  `None`" mandate necessarily removes this. **Update** it to assert a non-`None` resolution (the placeholder).
  **Reviewer note (surface this):** for a *masked* (vs *absent*) heading the "has not yet authored" placeholder
  wording is imprecise — assert only that it resolves non-`None` (do NOT over-assert the message text), and flag
  for the reviewer whether the malformed-fence case warrants a distinct message in a follow-up. Do NOT add fence
  re-parsing logic here (out of scope; keep the fix confined to the placeholder return).

Gate: `uv run pytest` both node files + the WP05 test file green; `ruff` clean. Still ONLY edit these test
files (no new production change beyond T040).

## Branch Strategy

Planning + merge target: `feat/charter-pack-usage-journey`. Worktree per `lanes.json` lane.

## Definition of Done

- `render_critical_section_include` never returns `None`; the selector resolves for both headings.
- `context.py` untouched; no `charter.yaml` retarget (C-003 holds). US4.1 tests pass. `ruff`+`mypy` clean.

## Risks / Reviewer guidance (reviewer-renata)

- Confirm the edit is confined to `section_bodies.py` (+ its test) — no `context.py` edit, no seed enrichment.
  Confirm the placeholder is honest (points the operator at the real authoring surface), not fabricated
  governance content.
