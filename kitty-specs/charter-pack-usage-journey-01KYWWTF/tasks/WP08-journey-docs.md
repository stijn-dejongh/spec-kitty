---
work_package_id: WP08
title: Journey documentation — apply → generate two-step + empty-charter dispatch
dependencies:
- WP01
- WP02
- WP03
requirement_refs:
- FR-009
planning_base_branch: feat/charter-pack-usage-journey
merge_target_branch: feat/charter-pack-usage-journey
branch_strategy: Planning artifacts for this mission were generated on feat/charter-pack-usage-journey. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/charter-pack-usage-journey unless the human explicitly redirects the landing branch.
subtasks:
- T070
- T071
history:
- Generated by /spec-kitty.tasks 2026-08-02
agent_profile: curator-carla
authoritative_surface: docs/
create_intent:
- docs/architecture/charter-pack-usage-journey.md
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- docs/architecture/charter-pack-usage-journey.md
- docs/api/charter-commands.md
- docs/context/charter-overview.md
- docs/context/governance-files.md
- docs/guides/setup-governance.md
- docs/development/3-2-page-inventory.yaml
- docs/development/3-2-docs-retrieval-index.yaml
role: implementer
tags: []
tracker_refs:
- "3107"
---

## ⚡ Do This First: Load Agent Profile

Load your assigned profile: run `/ad-hoc-profile-load curator-carla` and adopt its identity/boundaries before reading further.

## Objective

Document the delivered behaviour so the operator-facing story matches reality: the **`apply` → `generate`
two-step** (why `apply` alone doesn't deliver governance, and the exact next command), and the **empty-charter
dispatch behaviour** (applying a pack without compiling keeps the generic-agent net; a compiled bundle
disengages it). This is the journey-doc portion of **#3107** — the inert CLI-reference parity gate of #3107 is
**out of scope** (docs-infra, C-005).

**Dependency:** land after WP01/WP02/WP03 so you document behaviour that actually ships (the predicate, the
`--compile` bridge, the read-surface authority).

Read first: `../spec.md` FR-009; `../plan.md` IC-09; `../notes/research-synthesis.md` (root cause + bridge +
predicate). Survey the existing charter docs so the new page fits the information architecture:

```bash
grep -rln "charter pack apply\|charter generate\|charter journey" docs/ | head
ls docs/architecture/ | grep -i charter
```

## Guidance

### T070 — Author/extend the charter journey guide

Write `docs/architecture/charter-pack-usage-journey.md` (or extend the existing charter journey guide if one
is the better home — record the choice) covering: (a) the write-store vs compiled-bundle vs display-companion
model; (b) `charter pack apply` merges activations but does not compile — run `spec-kitty charter generate`
(or `apply --compile`) to deliver governance; (c) empty-charter dispatch: before a bundle exists an unmatched
request falls back to the warned generic agent; after compile the router runs and `ROUTER_NO_MATCH` is honest;
(d) the deliberate behaviour change (all non-routing dimensions, incl. glossary, no longer suppress the net —
"empty = bundle absent"). Link the new page from the relevant index/README.

### T071 — Freshen inventory + index; terminology guard

If you add a docs page, freshen the page-inventory (`spec-kitty ... inventory_lockfile --write` per the repo's
docs-inventory workflow) and the docs index (`docs_index --write`), then confirm
`check_docs_freshness --ci` is clean. Fix any relative-link breakage (`relative_link_fixer --check`). Run
`pytest tests/architectural/test_no_legacy_terminology.py` before pushing (prose change).

## Branch Strategy

Planning + merge target: `feat/charter-pack-usage-journey`. Worktree per `lanes.json` lane.

## Definition of Done

- The two-step + empty-charter dispatch behaviour is documented and linked. Inventory + index freshened;
  freshness/link checks clean; terminology guard passes.

## Risks / Reviewer guidance (reviewer-renata)

- Confirm the docs match the *shipped* behaviour (not the pre-mission behaviour). Confirm the #3107 parity
  gate was NOT pulled in. Confirm no forbidden terminology.
