# Tracer: tooling-friction

Append friction hit during implementation of this mission (a CLI that hung, a gate
that false-greened, a resolver that stranded a write). One entry per finding:
`YYYY-MM-DD · WP## · actor · <what broke, where>`.

Seeded at planning 2026-07-29. Once WP10's `tracer-append` command exists, prefer it
(routes to the coord surface without a lane-branch commit) over hand-editing.

---
