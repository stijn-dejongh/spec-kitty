# Occurrence map — Common Docs Convergence (DIRECTIVE_035 bulk-edit)
# The move spine driving scripts/docs/redirect_stub_generator.py + relative_link_fixer.py.
# Per-WP `moves:` fragments are appended at /spec-kitty.tasks time; merged by IC-11 (single writer).
# Corrected per post-plan squad (reviews/post-plan-squad.md).
mission: common-docs-convergence-01KZMTR9
change_mode: bulk_edit

# CUMULATIVE SPINE (OB-1, NFR-010). `regenerate-map` OVERWRITES redirect_map.yaml from
# baseline + THIS moves list (no additive merge; _relocate is single-move). Therefore every entry
# below maps a baseline path to its FINAL destination, AND the prior closed mission
# (common-docs-structural-move-01KW3SBK) 29 moves MUST be carried/collapsed here so regen reproduces
# all ~151 prior redirect entries + the new ones. IC-02 collapses them from scripts/docs/redirect_map.yaml
# (source of the prior spine) and adds a test asserting regen reproduces every prior key.
prior_spine_source: scripts/docs/redirect_map.yaml   # 151 entries from the closed structural-move mission
prior_spine_status: TODO_collapse_in_IC-02           # carry baseline->FINAL for the 52 baseline URLs under 1x/2x/3x/reference/archive/

classification:
  code_symbols:
    action: update
    rationale: >
      redirect_stub_generator.py:75-77 hardcodes MISSION_SLUG="common-docs-structural-move-01KW3SBK"
      + DEFAULT_OCCURRENCE_MAP. Repoint the default to this mission (or a neutral sentinel) and always
      pass --occurrence-map. The CLI flag/subcommands already exist — no new flag.
  import_paths: { action: none, rationale: No import paths change. }
  filesystem_paths:
    action: relocate
    rationale: Primary edit class; every move is a `moves:` entry routed through the redirect tooling.
  serialized_keys:
    action: update
    rationale: >
      Frontmatter gains `audience:` (touched pages only; NOT in frontmatter_required_fields, C-012).
      structural_lint_config.concern_bucket_to_section/guides_boundary updated (audience routing, one sub-WP).
      toc.yml/docfx.json/llms.txt/redirect_map.yaml regenerated by IC-11 only.
  cli_commands: { action: none, rationale: No CLI command names change. }
  user_facing_strings:
    action: rewrite
    rationale: >
      In-prose [text](path) links rewritten via relative_link_fixer; the 13 free-text `audience:` values
      migrated to resolvable catalog paths. NOTE: README.md:2 logo is an absolute HTML <img src=.../media/...>
      that relative_link_fixer will NOT touch — see manual_rewrites below.
  tests_fixtures:
    action: update
    rationale: Enumerated literals (see below) updated; gap_analysis.py subdir-name assumptions pinned first.
  logs_telemetry:
    action: update
    rationale: src/specify_cli/retrospective/deprecation.py runtime message references docs/guides/use-retrospective-learning.md.

# Enumerated code/test/CI literals the FR-018 grep gate must cover (non-vacuous).
enumerated_references:
  - { path: "spec-kitty-mission-workflow.md", refs: ["scripts/lint_canonical_producers.py:5,723,812", "tests/status/test_producer_conformance.py:15", "tests/docs/test_no_retrospect_preview.py:36", ".github/workflows/canonical-producer-lint.yml:4"] }
  - { path: "spec-driven.md", refs: ["tests/docs/test_current_charter_paths.py:16", "docs/development/contributing.md:388,663"] }
  - { path: "docs/output/", refs: ["src/specify_cli/missions/documentation/mission.yaml:41 (deliverables)"] }
  - { path: "docs/doctrine/", refs: ["tests/docs/test_asset_howto.py (create-a-doctrine-artifact.md)", "tests/charter/test_charter_context_spdd_reasons.py:290 (spdd-reasons.md)"] }
  - { path: "docs/reference/cli-cheatsheet.md", refs: ["tests/docs/test_docs_query_cli.py:69", "build_cli_reference.py/check_cli_reference_freshness.py/check_docs_freshness.py default-path comments"] }
  - { path: "media/logo_small.webp", refs: ["README.md:2 (absolute raw <img> URL — resolves only on main)"] }
  - { path: "docs/guides/use-retrospective-learning.md", refs: ["src/specify_cli/retrospective/deprecation.py"] }

manual_rewrites:   # not reachable by relative_link_fixer
  - { file: "README.md", line: 2, change: "absolute <img> src media/logo_small.webp -> docs/assets/logo_small.webp (raw/main URL)" }

# Move spine (baseline path -> FINAL dest). kind: retire | rehome | fold | collapse. Destinations RESOLVED.
moves:
  # IC-04 single-root
  - { from: "research/",               to: "RETIRE",                 kind: retire, note: "13 agent files, no live inbound; distil any survivor into docs/integrations/ (NOT docs/plans, C-001)" }
  - { from: "examples/",               to: "docs/guides/",           kind: rehome, note: "user-facing scenario walkthroughs" }
  - { from: "contracts/batch-api-contract.md", to: "docs/api/",      kind: rehome }
  - { from: "glossary/",               to: "docs/context/",          kind: fold, note: "README-only stub; sequence AFTER IC-09 glossary/contexts authority-path repair (L11)" }
  - { from: "media/",                  to: "docs/assets/",           kind: rehome, note: "+ manual README <img> rewrite" }
  - { from: "spec-driven.md",          to: "docs/context/",          kind: rehome, note: "update test_current_charter_paths.py:16 + contributing.md refs" }
  - { from: "spec-kitty-mission-workflow.md", to: "docs/architecture/", kind: rehome, note: "canonical-authority doc (NOT guides); update lint_canonical_producers + 2 tests + canonical-producer-lint.yml" }
  - { from: "HOW_TO_MAINTAIN.md",      to: "docs/operations/",       kind: rehome }
  # IC-05 non-canonical sections + architecture shadow + api consolidation
  - { from: "docs/reference/",         to: "docs/api/",              kind: fold, note: "reference duplicates api; skills/+agent_profiles/ under api/; update cli-cheatsheet refs" }
  - { from: "docs/doctrine/create-a-doctrine-artifact.md", to: "docs/development/", kind: fold, note: "how-to (contributor audience)" }
  - { from: "docs/doctrine/doctrine-kinds.md", to: "docs/architecture/", kind: fold, note: "explanation; update test_asset_howto/test_charter_context_spdd_reasons refs" }
  - { from: "docs/doctrine/spdd-reasons.md", to: "docs/architecture/", kind: fold }
  - { from: "docs/core-concepts/",     to: "RETIRE",                 kind: retire, note: "index-only nav mirror -> keep as toc.yml nav zone, retire dir" }
  - { from: "docs/updates/",           to: "RETIRE",                 kind: retire, note: "index-only nav mirror -> toc.yml nav zone, retire dir" }
  - { from: "docs/output/",            to: "RETIRE",                 kind: retire, note: "unpublished placeholder; FIRST repoint documentation mission.yaml:41 deliverables" }
  - { from: "docs/release-goals/",     to: "docs/changelog/",        kind: fold, note: "NOT docs/plans (C-001 tension)" }
  - { from: "docs/archive/",           to: "docs/changelog/",        kind: fold, note: "holds 14 baseline redirect targets (1x/2x/3x); IC-11 must re-point those prior redirects to the fold dest" }
  - { from: "docs/architecture/README-1.x.md", to: "docs/adr/1.x/",  kind: collapse }
  - { from: "docs/architecture/README-2.x.md", to: "docs/adr/2.x/",  kind: collapse, note: "repoint src/doctrine/templates/diagrams/README.md #usage-flow/#domain-breakdown anchors FIRST" }
  - { from: "docs/architecture/README-3.x.md", to: "docs/adr/3.x/",  kind: collapse }
  # IC-05 loose docs/ root files -> sections
  - { from: "docs/status-model.md",        to: "docs/architecture/", kind: rehome }
  - { from: "docs/trail-model.md",         to: "docs/architecture/", kind: rehome }
  - { from: "docs/host-surface-parity.md", to: "docs/architecture/", kind: rehome }
  - { from: "docs/contextive-glossaries.md", to: "docs/context/",    kind: rehome }
  - { from: "docs/p0-baseline-refresh.md", to: "docs/operations/",   kind: rehome }
  # IC-01 persona kebab-renames (snake_case -> kebab, migrate audience: values to these FINAL paths)
  - { from: "docs/context/audience/internal/spec-kitty-cli-runtime.md", to: "docs/context/audience/internal/spec-kitty-cli-runtime.md", kind: rehome, note: "already kebab; audit all persona filenames for snake_case and kebab here" }
  # IC-06 how-to audience routing + subdivision, IC-07 kebab/index: per-WP fragments appended at tasks time.
